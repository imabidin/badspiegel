/******/ (() => { // webpackBootstrap
/******/ 	"use strict";
/******/ 	var __webpack_modules__ = ({

/***/ "./assets/js/configurator/carousel.js":
/*!********************************************!*\
  !*** ./assets/js/configurator/carousel.js ***!
  \********************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony import */ var core_js_modules_es_symbol_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! core-js/modules/es.symbol.js */ "./node_modules/core-js/modules/es.symbol.js");
/* harmony import */ var core_js_modules_es_symbol_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_symbol_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var core_js_modules_es_symbol_description_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! core-js/modules/es.symbol.description.js */ "./node_modules/core-js/modules/es.symbol.description.js");
/* harmony import */ var core_js_modules_es_symbol_description_js__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_symbol_description_js__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var core_js_modules_es_symbol_iterator_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! core-js/modules/es.symbol.iterator.js */ "./node_modules/core-js/modules/es.symbol.iterator.js");
/* harmony import */ var core_js_modules_es_symbol_iterator_js__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_symbol_iterator_js__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var core_js_modules_es_symbol_to_primitive_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! core-js/modules/es.symbol.to-primitive.js */ "./node_modules/core-js/modules/es.symbol.to-primitive.js");
/* harmony import */ var core_js_modules_es_symbol_to_primitive_js__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_symbol_to_primitive_js__WEBPACK_IMPORTED_MODULE_3__);
/* harmony import */ var core_js_modules_es_array_iterator_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! core-js/modules/es.array.iterator.js */ "./node_modules/core-js/modules/es.array.iterator.js");
/* harmony import */ var core_js_modules_es_array_iterator_js__WEBPACK_IMPORTED_MODULE_4___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_iterator_js__WEBPACK_IMPORTED_MODULE_4__);
/* harmony import */ var core_js_modules_es_date_to_primitive_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! core-js/modules/es.date.to-primitive.js */ "./node_modules/core-js/modules/es.date.to-primitive.js");
/* harmony import */ var core_js_modules_es_date_to_primitive_js__WEBPACK_IMPORTED_MODULE_5___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_date_to_primitive_js__WEBPACK_IMPORTED_MODULE_5__);
/* harmony import */ var core_js_modules_es_number_constructor_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! core-js/modules/es.number.constructor.js */ "./node_modules/core-js/modules/es.number.constructor.js");
/* harmony import */ var core_js_modules_es_number_constructor_js__WEBPACK_IMPORTED_MODULE_6___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_number_constructor_js__WEBPACK_IMPORTED_MODULE_6__);
/* harmony import */ var core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! core-js/modules/es.object.to-string.js */ "./node_modules/core-js/modules/es.object.to-string.js");
/* harmony import */ var core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_7___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_7__);
/* harmony import */ var core_js_modules_es_regexp_to_string_js__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! core-js/modules/es.regexp.to-string.js */ "./node_modules/core-js/modules/es.regexp.to-string.js");
/* harmony import */ var core_js_modules_es_regexp_to_string_js__WEBPACK_IMPORTED_MODULE_8___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_regexp_to_string_js__WEBPACK_IMPORTED_MODULE_8__);
/* harmony import */ var core_js_modules_es_string_iterator_js__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! core-js/modules/es.string.iterator.js */ "./node_modules/core-js/modules/es.string.iterator.js");
/* harmony import */ var core_js_modules_es_string_iterator_js__WEBPACK_IMPORTED_MODULE_9___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_string_iterator_js__WEBPACK_IMPORTED_MODULE_9__);
/* harmony import */ var core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! core-js/modules/web.dom-collections.for-each.js */ "./node_modules/core-js/modules/web.dom-collections.for-each.js");
/* harmony import */ var core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_10___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_10__);
/* harmony import */ var core_js_modules_web_dom_collections_iterator_js__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! core-js/modules/web.dom-collections.iterator.js */ "./node_modules/core-js/modules/web.dom-collections.iterator.js");
/* harmony import */ var core_js_modules_web_dom_collections_iterator_js__WEBPACK_IMPORTED_MODULE_11___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_web_dom_collections_iterator_js__WEBPACK_IMPORTED_MODULE_11__);
function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }












function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
/**
 * Product Configurator - Carousel Management Module
 *
 * This module handles the complete product configuration flow including carousel
 * navigation, step management, progress tracking, add to cart functionality,
 * and responsive indicator scrolling system.
 *
 * Features:
 * - Bootstrap carousel integration with custom navigation
 * - Dynamic progress tracking and visual feedback
 * - Add to cart validation with loading states
 * - Responsive indicator scrolling with smooth animations
 * - Summary display with completion effects
 * - Modal dialogs for incomplete configurations
 * - Mobile-optimized touch interactions
 *
 * @version 2.3.0
 * @package Configurator
 *
 * @todo Check if the carousel functions need to be refactored for better reusability (carouselheight)
 * @todo Check if it is possible just to scroll to add to cart button after pressing "Fertig", if the button is not properly visible on screen
 */

// ====================== CONFIGURATION CONSTANTS ======================

/**
 * Add to cart behavior configuration
 * - 'steps': Requires completion of all steps
 * - 'primary-button': Requires button state change to primary
 * - 'summary-visible': Requires summary to be visible (handles pre-loaded state)
 */
var ADD_TO_CART_MODE = "summary-visible";

/**
 * Shine effect configuration
 */
var SHINE_EFFECT_DURATION = 1.5; // Seconds (0.4 = fast, 0.8 = normal, 1.2 = slow)

/**
 * Indicator scrolling system configuration
 */
var INDICATOR_SCROLL_AMOUNT = 0.8; // Fraction of container width to scroll
var BUTTONS_VISIBLE_CLASS = "buttons-visible"; // CSS class for button visibility
var VISIBILITY_TIMEOUT = 1500; // Auto-hide timeout for scroll buttons (ms)
var SCROLL_TOLERANCE = 2; // Pixel tolerance for scroll boundary detection

// ====================== MAIN CONFIGURATOR CLASS ======================

/**
 * ProductConfigurator Class
 *
 * Main class that orchestrates the entire product configuration experience.
 * Manages carousel navigation, progress tracking, validation, and user interactions.
 */
var ProductConfigurator = /*#__PURE__*/function () {
  /**
   * Constructor - Initialize all configurator components
   * Sets up DOM references, validates required elements, and initializes subsystems
   */
  function ProductConfigurator() {
    var _this$addtocartBtn, _this$carouselEl, _this$progressEl, _this$indicatorsEl, _this$indicatorsEl2, _this$configuratorEl;
    _classCallCheck(this, ProductConfigurator);
    // Core form elements
    this.addtocartBtn = document.querySelector(".single_add_to_cart_button");
    this.formEl = (_this$addtocartBtn = this.addtocartBtn) === null || _this$addtocartBtn === void 0 ? void 0 : _this$addtocartBtn.closest("form");

    // Main configurator container
    this.configuratorEl = document.getElementById("productConfigurator");

    // Bootstrap carousel elements
    this.carouselEl = document.getElementById("productConfiguratorCarousel");
    this.carouselInner = (_this$carouselEl = this.carouselEl) === null || _this$carouselEl === void 0 ? void 0 : _this$carouselEl.querySelector(".carousel-inner");
    this.carouselPrevBtn = document.getElementById("productConfiguratorPrev");
    this.carouselNextBtn = document.getElementById("productConfiguratorNext");

    // Progress tracking elements
    this.progressEl = document.getElementById("productConfiguratorProgress");
    this.progressBar = (_this$progressEl = this.progressEl) === null || _this$progressEl === void 0 ? void 0 : _this$progressEl.querySelector(".progress-bar");

    // Step indicator elements
    this.indicatorsEl = document.getElementById("productConfiguratorIndicators");
    this.indicatorsRow = (_this$indicatorsEl = this.indicatorsEl) === null || _this$indicatorsEl === void 0 ? void 0 : _this$indicatorsEl.querySelector(".row");
    this.indicators = (_this$indicatorsEl2 = this.indicatorsEl) === null || _this$indicatorsEl2 === void 0 ? void 0 : _this$indicatorsEl2.querySelectorAll(".indicator");
    this.indicatorsRightBtn = document.getElementById("scrollIndicatorsRight");
    this.indicatorsLeftBtn = document.getElementById("scrollIndicatorsLeft");
    this.indicatorsScrollBtnTimeoutHelper = null;

    // Summary display element
    this.summaryEl = document.getElementById("productConfiguratorSummary");

    // Configuration state management
    this.currentStep = 1;
    this.totalSteps = parseInt((_this$configuratorEl = this.configuratorEl) === null || _this$configuratorEl === void 0 ? void 0 : _this$configuratorEl.dataset.totalSteps, 10) || 1;
    this.maxStepReached = 1;
    this.hasEverCompleted = false;
    this.isProgrammaticClick = false;
    this.isComplete = false;
    this.shineEffectTriggered = false; // Prevent multiple shine effects

    // Validate critical elements exist before initialization
    if (!this.carouselEl || !this.addtocartBtn) {
      console.error("ProductConfigurator: Missing required elements");
      return;
    }

    // Initialize all subsystems
    this.initCarousel();
    this.initResize();
    this.initAddtocart();
    this.initUIState();
    this.initIndicatorScrollSystem();
    // this.initIndicatorFocusHighlight(); // <-- Neue Methode hinzufügen
  }

  /**
   * CAROUSEL SYSTEM INITIALIZATION
   * ==============================
   */

  /**
   * Initialize Bootstrap carousel with custom event handlers
   * Sets up slide transition events for dynamic content management
   */
  return _createClass(ProductConfigurator, [{
    key: "initCarousel",
    value: function initCarousel() {
      var _this = this;
      if (!this.carouselEl) return;

      // Note: Bootstrap carousel instance is auto-created when needed
      // this.carouselInstance = bootstrap.Carousel.getOrCreateInstance(this.carouselEl);

      // Handle slide start events (before transition)
      this.carouselEl.addEventListener("slide.bs.carousel", function (event) {
        _this.handleSlideStart(event);
      });

      // Handle slide completion events (after transition)
      this.carouselEl.addEventListener("slid.bs.carousel", function () {
        _this.handleSlideComplete();
      });
    }

    /**
     * Initialize responsive behavior for window resize events
     */
  }, {
    key: "initResize",
    value: function initResize() {
      window.addEventListener("resize", this.handleResize.bind(this));
    }

    /**
     * Initialize add to cart button with validation and loading states
     */
  }, {
    key: "initAddtocart",
    value: function initAddtocart() {
      this.addtocartBtn.addEventListener("click", this.handleAddtocartClick.bind(this));
    }

    /**
     * Set initial UI states and layout
     * Establishes baseline progress, heights, and overflow settings
     */
  }, {
    key: "initUIState",
    value: function initUIState() {
      this.updateProgressBar(1);
      this.setCarouselHeight();
      this.addCarouselOverflow();

      // Check if summary is already visible on load and sync button state
      this.syncButtonStateWithSummary();
    }

    /**
     * INDICATOR SCROLLING SYSTEM
     * ==========================
     */

    /**
     * Initialize complete indicator scrolling system
     * Sets up horizontal scrolling for step indicators on mobile devices
     */
  }, {
    key: "initIndicatorScrollSystem",
    value: function initIndicatorScrollSystem() {
      this.setupButtonVisibilityEvents();
      this.setupScrollButtons();
      this.setupScrollListeners();
    }

    /**
     * Setup mouse and touch events for scroll button visibility
     * Implements auto-hide behavior for cleaner mobile interface
     */
  }, {
    key: "setupButtonVisibilityEvents",
    value: function setupButtonVisibilityEvents() {
      var _this$indicatorsEl3, _this$indicatorsEl4, _this$indicatorsEl5;
      (_this$indicatorsEl3 = this.indicatorsEl) === null || _this$indicatorsEl3 === void 0 || _this$indicatorsEl3.addEventListener("mouseenter", this.showButtonsTemporarily.bind(this));
      (_this$indicatorsEl4 = this.indicatorsEl) === null || _this$indicatorsEl4 === void 0 || _this$indicatorsEl4.addEventListener("touchstart", this.showButtonsTemporarily.bind(this), {
        passive: true
      });
      (_this$indicatorsEl5 = this.indicatorsEl) === null || _this$indicatorsEl5 === void 0 || _this$indicatorsEl5.addEventListener("mouseleave", this.hideButtonsIfNotInteracted.bind(this));

      // Optional: Focus/blur events for keyboard navigation
      // [this.indicatorsLeftBtn, this.indicatorsRightBtn].forEach((button) => {
      //     button?.addEventListener("focus", () => {
      //         this.indicatorsEl.classList.add(BUTTONS_VISIBLE_CLASS);
      //     });
      //     button?.addEventListener("blur", () => {
      //         if (!this.indicatorsEl.matches(":hover")) {
      //             this.indicatorsEl.classList.remove(BUTTONS_VISIBLE_CLASS);
      //         }
      //     });
      // });
    }

    /**
     * Setup left/right scroll button click handlers
     * Implements smooth scrolling with calculated scroll distances
     */
  }, {
    key: "setupScrollButtons",
    value: function setupScrollButtons() {
      var _this2 = this;
      if (this.indicatorsRightBtn) {
        this.indicatorsRightBtn.addEventListener("click", function (e) {
          e.preventDefault();
          _this2.scrollContainer(_this2.indicatorsRow.clientWidth * INDICATOR_SCROLL_AMOUNT);
        });
      }
      if (this.indicatorsLeftBtn) {
        this.indicatorsLeftBtn.addEventListener("click", function (e) {
          e.preventDefault();
          _this2.scrollContainer(-_this2.indicatorsRow.clientWidth * INDICATOR_SCROLL_AMOUNT);
        });
      }
    }

    /**
     * Scroll the indicator container by specified amount
     * Handles boundary detection and smooth scrolling animation
     *
     * @param {number} amount - Pixels to scroll (positive = right, negative = left)
     */
  }, {
    key: "scrollContainer",
    value: function scrollContainer(amount) {
      if (!this.indicatorsRow) return;
      this.showButtonsTemporarily();
      var newPosition = this.indicatorsRow.scrollLeft + amount;
      var maxScroll = this.indicatorsRow.scrollWidth - this.indicatorsRow.clientWidth;
      var finalPosition = Math.max(0, Math.min(newPosition, maxScroll));
      this.indicatorsRow.scrollTo({
        left: finalPosition,
        behavior: "smooth"
      });
    }

    /**
     * Show scroll buttons temporarily with auto-hide timer
     * Provides visual feedback during user interaction
     */
  }, {
    key: "showButtonsTemporarily",
    value: function showButtonsTemporarily() {
      var _this$indicatorsEl6,
        _this3 = this;
      (_this$indicatorsEl6 = this.indicatorsEl) === null || _this$indicatorsEl6 === void 0 || _this$indicatorsEl6.classList.add(BUTTONS_VISIBLE_CLASS);
      clearTimeout(this.indicatorsScrollBtnTimeoutHelper);
      this.indicatorsScrollBtnTimeoutHelper = setTimeout(function () {
        _this3.hideButtonsIfNotInteracted();
      }, VISIBILITY_TIMEOUT);
    }

    /**
     * Hide scroll buttons if user is not actively interacting
     * Checks for hover, focus, and active states before hiding
     */
  }, {
    key: "hideButtonsIfNotInteracted",
    value: function hideButtonsIfNotInteracted() {
      if (!this.isInteracted(this.indicatorsLeftBtn) && !this.isInteracted(this.indicatorsRightBtn)) {
        var _this$indicatorsEl7;
        (_this$indicatorsEl7 = this.indicatorsEl) === null || _this$indicatorsEl7 === void 0 || _this$indicatorsEl7.classList.remove(BUTTONS_VISIBLE_CLASS);
      }
    }

    /**
     * Check if user is currently interacting with scroll buttons
     * Utility function for interaction state detection
     *
     * @param {HTMLElement} element - Button element to check
     * @returns {boolean} True if element or container is being interacted with
     */
  }, {
    key: "isInteracted",
    value: function isInteracted(element) {
      var _this$indicatorsEl8, _this$indicatorsEl9;
      return ((_this$indicatorsEl8 = this.indicatorsEl) === null || _this$indicatorsEl8 === void 0 ? void 0 : _this$indicatorsEl8.matches(":hover")) || (element === null || element === void 0 ? void 0 : element.matches(":focus")) || ((_this$indicatorsEl9 = this.indicatorsEl) === null || _this$indicatorsEl9 === void 0 ? void 0 : _this$indicatorsEl9.matches(":active"));
    }

    /**
     * Setup scroll event listeners and resize handling
     * Manages scroll button state based on scroll position
     */
  }, {
    key: "setupScrollListeners",
    value: function setupScrollListeners() {
      if (this.indicatorsRow) {
        this.checkScrollButtons();
        this.indicatorsRow.addEventListener("scroll", this.checkScrollButtons.bind(this));
      }
      window.addEventListener("resize", this.checkScrollButtons.bind(this));
    }

    /**
     * Update scroll button states based on current scroll position
     * Disables buttons at scroll boundaries and provides visual feedback
     */
  }, {
    key: "checkScrollButtons",
    value: function checkScrollButtons() {
      if (!this.indicatorsRow) return;
      var currentScroll = this.indicatorsRow.scrollLeft;
      var maxScroll = this.indicatorsRow.scrollWidth - this.indicatorsRow.clientWidth;

      // Update right scroll button state
      if (this.indicatorsRightBtn) {
        var rightDisabled = currentScroll >= maxScroll - SCROLL_TOLERANCE;
        this.indicatorsRightBtn.style.opacity = rightDisabled ? "0" : "";
        this.indicatorsRightBtn.style.pointerEvents = rightDisabled ? "none" : "";
        this.indicatorsRightBtn.disabled = rightDisabled;
      }

      // Update left scroll button state
      if (this.indicatorsLeftBtn) {
        var leftDisabled = currentScroll <= SCROLL_TOLERANCE;
        this.indicatorsLeftBtn.style.opacity = leftDisabled ? "0" : "";
        this.indicatorsLeftBtn.style.pointerEvents = leftDisabled ? "none" : "";
        this.indicatorsLeftBtn.disabled = leftDisabled;
      }
    }

    /**
     * CAROUSEL EVENT HANDLING
     * =======================
     */

    /**
     * Handle carousel slide start events
     * Manages height transitions, scrolling, and progress updates
     *
     * @param {Event} event - Bootstrap carousel slide event
     */
  }, {
    key: "handleSlideStart",
    value: function handleSlideStart(event) {
      var _this4 = this;
      // Set carousel height for smooth transitions
      this.setCarouselHeight();
      requestAnimationFrame(function () {
        _this4.setCarouselHeight(event.relatedTarget);
      });

      // Ensure configurator is visible during navigation
      this.scrollToConfiguratorTop();

      // Temporarily remove overflow for transition
      this.removeCarouselOverflow();

      // Update progress tracking
      this.handleSlideProgress(event);
    }

    /**
     * Handle carousel slide completion events
     * Resets layout and re-enables overflow for dropdowns
     */
  }, {
    key: "handleSlideComplete",
    value: function handleSlideComplete() {
      this.resetCarouselHeight();
      this.addCarouselOverflow();
    }

    /**
     * Handle window resize events
     * Maintains proper carousel height and scroll button states
     */
  }, {
    key: "handleResize",
    value: function handleResize() {
      if (this.carouselInner) {
        this.setCarouselHeight();
        this.resetCarouselHeight();
      }
      this.checkScrollButtons();
    }

    /**
     * CAROUSEL LAYOUT HELPERS
     * =======================
     */

    /**
     * Set carousel container height based on active slide
     * Prevents layout jump during slide transitions
     *
     * @param {HTMLElement} [slide] - Target slide element (defaults to active slide)
     */
  }, {
    key: "setCarouselHeight",
    value: function setCarouselHeight(slide) {
      var _this$carouselInner,
        _this5 = this;
      var targetSlide = slide || ((_this$carouselInner = this.carouselInner) === null || _this$carouselInner === void 0 ? void 0 : _this$carouselInner.querySelector(".carousel-item.active"));
      if (targetSlide) {
        // Use requestAnimationFrame to batch DOM reads/writes and prevent forced reflow
        requestAnimationFrame(function () {
          var height = targetSlide.offsetHeight;
          requestAnimationFrame(function () {
            _this5.carouselInner.style.height = "".concat(height, "px");
          });
        });
      }
    }

    /**
     * Reset carousel height to auto for natural content flow
     */
  }, {
    key: "resetCarouselHeight",
    value: function resetCarouselHeight() {
      if (this.carouselInner) {
        this.carouselInner.style.height = "";
      }
    }

    /**
     * Remove overflow visibility during transitions
     * Prevents dropdown/modal clipping during carousel animation
     */
  }, {
    key: "removeCarouselOverflow",
    value: function removeCarouselOverflow() {
      var _this$carouselInner2;
      (_this$carouselInner2 = this.carouselInner) === null || _this$carouselInner2 === void 0 || _this$carouselInner2.classList.remove("overflow-visible");
    }

    /**
     * Add overflow visibility after transitions
     * Allows dropdowns and modals to display properly
     */
  }, {
    key: "addCarouselOverflow",
    value: function addCarouselOverflow() {
      var _this$carouselInner3;
      (_this$carouselInner3 = this.carouselInner) === null || _this$carouselInner3 === void 0 || _this$carouselInner3.classList.add("overflow-visible");
    }

    /**
     * Scroll to configurator top if not fully visible
     * Ensures progress bar and navigation remain accessible
     */
  }, {
    key: "scrollToConfiguratorTop",
    value: function scrollToConfiguratorTop() {
      var _this6 = this;
      if (!this.progressBar) return;

      // Use requestAnimationFrame to batch DOM reads and prevent forced reflow
      requestAnimationFrame(function () {
        var rect = _this6.progressBar.getBoundingClientRect();
        var isFullyVisible = rect.top >= 0 && rect.left >= 0 && rect.bottom <= (window.innerHeight || document.documentElement.clientHeight) && rect.right <= (window.innerWidth || document.documentElement.clientWidth);
        if (!isFullyVisible) {
          var _this6$configuratorEl;
          (_this6$configuratorEl = _this6.configuratorEl) === null || _this6$configuratorEl === void 0 || _this6$configuratorEl.scrollIntoView({
            behavior: "smooth",
            block: "start"
          });
        }
      });
    }

    /**
     * PROGRESS TRACKING SYSTEM
     * ========================
     */

    /**
     * Handle slide progress updates and state management
     * Updates step counters, progress bar, and navigation buttons
     *
     * @param {Event} event - Bootstrap carousel slide event
     */
  }, {
    key: "handleSlideProgress",
    value: function handleSlideProgress(event) {
      this.currentStep = event.to + 1;
      this.maxStepReached = Math.max(this.maxStepReached, this.currentStep);

      // Update progress bar based on maximum reached step
      this.updateProgressBar(this.maxStepReached);
      this.updateActiveIndicator(event.to);

      // Update navigation button states
      if (this.carouselPrevBtn && this.carouselNextBtn) {
        this.updateCarouselButtons();
      }

      // Track completion state
      if (this.currentStep === this.totalSteps) {
        this.hasEverCompleted = true;
      }
    }

    /**
     * Update progress bar visual state
     * Shows completion percentage with 90% max until final completion
     *
     * @param {number} currentStep - Current step number (1-based)
     */
  }, {
    key: "updateProgressBar",
    value: function updateProgressBar(currentStep) {
      if (this.isComplete || !this.progressBar) return;
      var percentage;
      if (currentStep < this.totalSteps) {
        // Cap at 90% until final completion
        percentage = currentStep / this.totalSteps * 90;
      } else {
        percentage = 90;
      }
      this.progressBar.style.width = "".concat(percentage, "%");
      this.progressBar.setAttribute("aria-valuenow", percentage.toString());
    }

    /**
     * Update active step indicator visual state
     * Highlights current step and ensures visibility
     *
     * @param {number} stepIndex - Zero-based step index
     */
  }, {
    key: "updateActiveIndicator",
    value: function updateActiveIndicator(stepIndex) {
      if (!this.indicators || this.indicators.length === 0) return;
      this.indicators.forEach(function (indicator, index) {
        var isActive = index === stepIndex;
        indicator.classList.toggle("bg-secondary-subtle", isActive);
        indicator.classList.toggle("text-muted", !isActive);
        indicator.setAttribute("aria-current", isActive ? "true" : "false");
      });
      this.scrollIndicatorIntoView(stepIndex);
    }

    /**
     * Scroll active indicator into view within container
     * Ensures current step is always visible on mobile
     *
     * @param {number} stepIndex - Zero-based step index
     */
  }, {
    key: "scrollIndicatorIntoView",
    value: function scrollIndicatorIntoView(stepIndex) {
      var _this7 = this;
      if (!this.indicatorsRow || !this.indicators) return;
      var activeIndicator = this.indicators[stepIndex];
      if (!activeIndicator) return;

      // Use requestAnimationFrame to batch DOM reads and prevent forced reflow
      requestAnimationFrame(function () {
        var containerRect = _this7.indicatorsRow.getBoundingClientRect();
        var indicatorRect = activeIndicator.getBoundingClientRect();

        // Scroll left if indicator is cut off on left side
        if (indicatorRect.left < containerRect.left) {
          _this7.indicatorsRow.scrollTo({
            left: _this7.indicatorsRow.scrollLeft + (indicatorRect.left - containerRect.left) - 10,
            behavior: "smooth"
          });
        }
        // Scroll right if indicator is cut off on right side
        else if (indicatorRect.right > containerRect.right) {
          _this7.indicatorsRow.scrollTo({
            left: _this7.indicatorsRow.scrollLeft + (indicatorRect.right - containerRect.right) + 10,
            behavior: "smooth"
          });
        }
      });
    }

    /**
     * NAVIGATION BUTTON MANAGEMENT
     * ============================
     */

    /**
     * Update both previous and next button states
     * Coordinates button visibility and functionality
     */
  }, {
    key: "updateCarouselButtons",
    value: function updateCarouselButtons() {
      this.updatePrevButton();
      this.updateNextButton();
    }

    /**
     * Update previous button state and visibility
     * Hides button on first step with fade animation
     */
  }, {
    key: "updatePrevButton",
    value: function updatePrevButton() {
      var _this8 = this;
      if (!this.carouselPrevBtn) return;
      var isFirstStep = this.currentStep === 1;
      this.carouselPrevBtn.disabled = isFirstStep;
      if (isFirstStep) {
        this.carouselPrevBtn.classList.add("fade");
        this.carouselPrevBtn.classList.remove("show");
        // Set tabindex to -1 to prevent focus on hidden/faded button
        this.carouselPrevBtn.setAttribute("tabindex", "-1");
      } else {
        this.carouselPrevBtn.classList.add("show");
        setTimeout(function () {
          _this8.carouselPrevBtn.classList.remove("fade");
          _this8.carouselPrevBtn.classList.remove("show");
          // Restore tabindex to 0 to make button focusable again
          _this8.carouselPrevBtn.setAttribute("tabindex", "0");
        }, 75);
      }
    }

    /**
     * Update next button state and functionality
     * Changes to "Finished" on last step and handles summary display
     */
  }, {
    key: "updateNextButton",
    value: function updateNextButton() {
      var _this9 = this;
      if (!this.carouselNextBtn) return;
      if (this.currentStep === this.totalSteps) {
        // Final step: Change to "Finished" and setup summary
        this.carouselNextBtn.innerHTML = "Fertig";
        this.handleSummary();
      } else {
        // Regular step: Reset to "Next" and carousel navigation
        this.carouselNextBtn.classList.remove("show");
        this.carouselNextBtn.innerHTML = "Weiter";
        setTimeout(function () {
          _this9.carouselNextBtn.removeAttribute("data-bs-toggle");
          _this9.carouselNextBtn.removeAttribute("aria-expanded");
          _this9.carouselNextBtn.removeAttribute("aria-controls");
          _this9.carouselNextBtn.setAttribute("data-bs-target", "#productConfiguratorCarousel");
        }, 300);
      }
    }

    /**
     * SUMMARY DISPLAY MANAGEMENT
     * ==========================
     */ /**
        * Handle summary display setup and button behavior
        * Configures collapse functionality for final step
        */
  }, {
    key: "handleSummary",
    value: function handleSummary() {
      var _this0 = this;
      console.log("🔧 handleSummary() called");
      if (!this.summaryEl) {
        console.log("❌ No summaryEl found");
        return;
      }
      console.log("📊 Summary element:", this.summaryEl);
      console.log("📊 Summary has 'show' class:", this.summaryEl.classList.contains("show"));

      // Show button if summary is already visible
      if (this.summaryEl.classList.contains("show")) {
        this.carouselNextBtn.classList.add("show");
        console.log("✅ Summary already visible, added 'show' to next button");

        // Only apply completion styling automatically for non-summary-visible modes
        if (ADD_TO_CART_MODE !== "summary-visible") {
          console.log("🎯 Non-summary-visible mode, applying completion styling directly");
          this.applyCompletionStyling();
        } else {
          console.log("🎯 Summary-visible mode, styling will be applied on 'Fertig' click only");
        }
      }

      // Configure button for Bootstrap collapse
      setTimeout(function () {
        _this0.carouselNextBtn.setAttribute("data-bs-toggle", "collapse");
        _this0.carouselNextBtn.setAttribute("data-bs-target", "#productConfiguratorSummary");
        _this0.carouselNextBtn.setAttribute("aria-expanded", "false");
        _this0.carouselNextBtn.setAttribute("aria-controls", "productConfiguratorSummary");
        console.log("✅ Bootstrap collapse attributes set");
      }, 1);

      // Setup summary event listeners (once only)
      if (!this.summaryEl.dataset.listenerAdded) {
        console.log("🎯 Setting up event listeners for first time");
        this.lockSummaryOnShow();
        this.setupFinishedButtonScrollBehavior();
        this.summaryEl.dataset.listenerAdded = "true";
      } else {
        console.log("⚠️ Event listeners already added, skipping setup");
      }
    }

    /**
     * Apply completion styling to button and progress bar
     * Centralized method for consistent completion effects
     */
  }, {
    key: "applyCompletionStyling",
    value: function applyCompletionStyling() {
      var _this1 = this;
      console.log("🎨 applyCompletionStyling() called");

      // Prevent multiple styling applications - check and set flag immediately
      if (this.isComplete) {
        console.log("⚠️ Already completed, skipping styling");
        return;
      }

      // Mark configuration as complete immediately to prevent race conditions
      this.isComplete = true;
      console.log("🔒 Configuration marked as complete (early)");
      console.log("📊 Add to cart button before:", this.addtocartBtn.className);

      // Transform add to cart button to primary state
      this.addtocartBtn.classList.add("btn-primary");
      this.addtocartBtn.classList.remove("text-start", "btn-light");
      console.log("📊 Add to cart button after:", this.addtocartBtn.className);

      // Complete progress bar
      if (this.progressBar) {
        this.progressBar.style.width = "100%";
        this.progressBar.setAttribute("aria-valuenow", "100");
        console.log("✅ Progress bar set to 100%");
      }
      console.log("✅ Configuration styling complete");

      // Trigger shine effect only once
      setTimeout(function () {
        _this1.triggerCompletionEffects();
      }, 300);
    }

    /**
     * Setup summary collapse event handlers
     * Handles completion effects and prevents summary hiding
     */
  }, {
    key: "lockSummaryOnShow",
    value: function lockSummaryOnShow() {
      var _this10 = this;
      console.log("🔒 lockSummaryOnShow() called");
      var onSummaryShow = function onSummaryShow() {
        var _this10$carouselNextB;
        console.log("🎉 SUMMARY SHOW EVENT TRIGGERED!");

        // Update button state
        (_this10$carouselNextB = _this10.carouselNextBtn) === null || _this10$carouselNextB === void 0 || _this10$carouselNextB.classList.add("show");

        // Apply completion styling
        _this10.applyCompletionStyling();

        // Scroll to summary
        setTimeout(function () {
          _this10.scrollToSummary();
        }, 1);
      };

      // Setup event listeners
      console.log("🎯 Adding show.bs.collapse event listener");
      this.summaryEl.addEventListener("show.bs.collapse", onSummaryShow);
      this.summaryEl.addEventListener("hide.bs.collapse", function (e) {
        return e.preventDefault();
      });
    } /**
      * Trigger completion visual effects (shine effect on add-to-cart button)
      * Centralized method to ensure consistent completion feedback
      */
  }, {
    key: "triggerCompletionEffects",
    value: function triggerCompletionEffects() {
      var _this11 = this;
      console.log("✨ triggerCompletionEffects() called");

      // Prevent multiple shine effects
      if (this.shineEffectTriggered) {
        console.log("⚠️ Shine effect already triggered, skipping");
        return;
      }
      this.shineEffectTriggered = true;
      console.log("🔒 Shine effect marked as triggered");

      // Add slight delay to ensure button styling is complete
      setTimeout(function () {
        _this11.addShineEffectToButton();
      }, 300);
    }

    /**
     * Setup "Fertig" button click behavior for summary scrolling
     * Ensures clicking "Fertig" always scrolls to summary if it's already visible
     */
  }, {
    key: "setupFinishedButtonScrollBehavior",
    value: function setupFinishedButtonScrollBehavior() {
      var _this12 = this;
      console.log("🎯 setupFinishedButtonScrollBehavior() called");
      if (!this.carouselNextBtn) {
        console.log("❌ No carouselNextBtn found");
        return;
      }
      this.carouselNextBtn.addEventListener("click", function (event) {
        var _this12$summaryEl;
        console.log("🔘 Fertig button clicked!");
        console.log("📊 Current step:", _this12.currentStep, "Total steps:", _this12.totalSteps);
        console.log("📊 ADD_TO_CART_MODE:", ADD_TO_CART_MODE);

        // Only handle when button is in "Fertig" state (last step)
        if (_this12.currentStep !== _this12.totalSteps) {
          console.log("⚠️ Not on last step, ignoring click");
          return;
        }
        console.log("✅ On last step, checking summary visibility");
        console.log("📊 Summary element:", _this12.summaryEl);
        console.log("📊 Summary has 'show' class:", (_this12$summaryEl = _this12.summaryEl) === null || _this12$summaryEl === void 0 ? void 0 : _this12$summaryEl.classList.contains("show"));

        // For summary-visible mode: Apply styling on every "Fertig" click
        if (ADD_TO_CART_MODE === "summary-visible") {
          console.log("🎯 Summary-visible mode: Applying completion styling on Fertig click");

          // Reset shine effect flag to allow multiple triggers
          _this12.shineEffectTriggered = false;
          console.log("🔄 Shine effect flag reset for new trigger");
          _this12.applyCompletionStyling();
        }

        // Check if summary is already visible
        if (_this12.summaryEl && _this12.summaryEl.classList.contains("show")) {
          console.log("📋 Summary already visible, scrolling to it");
          // Small delay to ensure any collapse animation is complete
          setTimeout(function () {
            _this12.scrollToSummary();

            // Reset shine effect flag for re-trigger
            _this12.shineEffectTriggered = false;
            console.log("🔄 Shine effect flag reset for re-trigger");

            // Trigger shine effect when re-clicking "Fertig" on visible summary
            _this12.triggerCompletionEffects();
          }, 100);
        } else {
          console.log("📋 Summary not visible, Bootstrap collapse should handle it");
        }
      });
    }

    /**
     * Handle add to cart button click events
     * Validates configuration completeness before allowing purchase
     */
  }, {
    key: "handleAddtocartClick",
    value: function handleAddtocartClick(event) {
      // Allow programmatic clicks to proceed (from modal confirmation)
      if (this.isProgrammaticClick) {
        // Reset the flag after use to prevent future clicks from bypassing validation
        this.isProgrammaticClick = false;
        this.addtocartLoadingState();
        return;
      }

      // Allow completed configurations to proceed
      if (this.configuratorCompleted()) {
        this.addtocartLoadingState();
        return;
      }

      // Prevent default and show incomplete configuration modal
      event.preventDefault();
      this.showIncompleteConfigModal();
    }

    /**
     * Apply loading state to add to cart button
     * Shows spinner and loading message during cart processing
     */
  }, {
    key: "addtocartLoadingState",
    value: function addtocartLoadingState() {
      this.addtocartBtn.innerHTML = "\n            <span class=\"spinner-border me-2\"\n                  style=\"--bs-spinner-width:1.25rem; --bs-spinner-height:1.25rem; --bs-spinner-border-width:0.225rem\"\n                  role=\"status\">\n            </span>\n            <span aria-live=\"polite\">Warenkorb wird vorbereitet...</span>\n        ";
      this.addtocartBtn.setAttribute("aria-busy", "true");
    }

    /**
     * Check if configurator has been completed
     * Supports multiple completion detection modes
     *
     * @returns {boolean} True if configuration is complete
     */
  }, {
    key: "configuratorCompleted",
    value: function configuratorCompleted() {
      var _this$summaryEl;
      console.log("🔍 configuratorCompleted() check - Mode:", ADD_TO_CART_MODE);
      switch (ADD_TO_CART_MODE) {
        case "steps":
          var stepsResult = this.hasEverCompleted;
          console.log("📊 Steps mode result:", stepsResult, "(hasEverCompleted:", this.hasEverCompleted, ")");
          return stepsResult;
        case "primary-button":
          var primaryResult = this.addtocartBtn.classList.contains("btn-primary");
          console.log("📊 Primary-button mode result:", primaryResult, "(button classes:", this.addtocartBtn.className, ")");
          return primaryResult;
        case "summary-visible":
          var summaryResult = this.summaryEl && this.summaryEl.classList.contains("show");
          console.log("📊 Summary-visible mode result:", summaryResult, "(summary element exists:", !!this.summaryEl, ", has show class:", (_this$summaryEl = this.summaryEl) === null || _this$summaryEl === void 0 ? void 0 : _this$summaryEl.classList.contains("show"), ")");
          return summaryResult;
        default:
          console.log("❌ Unknown mode, returning false");
          return false;
      }
    }

    /**
     * MODAL DIALOG MANAGEMENT
     * =======================
     */

    /**
     * Show modal dialog for incomplete configurations
     * Offers users choice to continue configuring or proceed to cart
     */
  }, {
    key: "showIncompleteConfigModal",
    value: function showIncompleteConfigModal() {
      var _this13 = this;
      createModal({
        title: '<i class="fa-sharp fa-light fa-exclamation-triangle text-warning me-2"></i>Hinweis',
        body: "\n                <p class=\"alert alert-warning mb-3\">Konfiguration noch nicht abgeschlossen!</p>\n                <p class=\"border border-warning-subtle p-3 text-warning-emphasis mb-0\">\n                    Sie k\xF6nnen zur\xFCck zum Konfigurator, <strong>oder</strong> das Produkt direkt in den Warenkorb legen.\n                </p>\n                ",
        footer: [{
          text: "Zurück zum Konfigurator",
          class: "btn-secondary",
          // Beispielklasse, kann angepasst werden
          dismiss: true
        }, {
          text: "Weiter zum Warenkorb",
          class: "btn-primary",
          // Beispielklasse, kann angepasst werden
          dismiss: true,
          // Schließt das Modal nach dem Klick
          onClick: function onClick() {
            _this13.triggerProgrammaticClick();
          }
        }],
        size: "lg"
        // onConfirm ist nicht mehr nötig, da es im footer-Array gehandhabt wird
      });
    }

    /**
     * Trigger programmatic add to cart click
     * Bypasses validation for user-confirmed incomplete configurations
     */
  }, {
    key: "triggerProgrammaticClick",
    value: function triggerProgrammaticClick() {
      this.isProgrammaticClick = true;
      this.addtocartLoadingState();
      this.addtocartBtn.click();
    }

    /**
     * Scroll to summary section smoothly
     * Ensures summary is visible after configuration completion
     */
  }, {
    key: "scrollToSummary",
    value: function scrollToSummary() {
      if (!this.summaryEl) return;
      this.summaryEl.scrollIntoView({
        behavior: "smooth",
        block: "start"
      });
    }

    /**
     * Sync button state with summary visibility on initialization
     * Handles cases where summary is pre-loaded with "show" class
     */
  }, {
    key: "syncButtonStateWithSummary",
    value: function syncButtonStateWithSummary() {
      var _this14 = this;
      if (!this.summaryEl || !this.addtocartBtn) return;

      // Only auto-sync button styling for "primary-button" mode
      // "summary-visible" mode should only style button on active "Fertig" click
      if (ADD_TO_CART_MODE !== "primary-button") return;

      // If summary is already visible but button is not primary, sync the state
      if (this.summaryEl.classList.contains("show") && !this.addtocartBtn.classList.contains("btn-primary")) {
        // Transform add to cart button to primary state
        this.addtocartBtn.classList.add("btn-primary");
        this.addtocartBtn.classList.remove("text-start", "btn-light");

        // Complete progress bar
        if (this.progressBar) {
          this.progressBar.style.width = "100%";
          this.progressBar.setAttribute("aria-valuenow", "100");
        }

        // Mark configuration as complete
        this.isComplete = true;

        // Optional: Trigger shine effect after a short delay
        setTimeout(function () {
          _this14.triggerCompletionEffects();
        }, 500);
      }
    }

    /**
     * Add shine effect to add-to-cart button
     * Creates visual feedback for configuration completion
     */
  }, {
    key: "addShineEffectToButton",
    value: function addShineEffectToButton() {
      if (!this.addtocartBtn) return;
      console.log("✨ Adding shine effect to button");

      // Remove any existing shine effects to prevent overlap
      var existingShines = this.addtocartBtn.querySelectorAll(".btn-shine-effect");
      existingShines.forEach(function (shine) {
        console.log("🧹 Removing existing shine effect");
        shine.remove();
      });
      var shineElement = document.createElement("div");
      shineElement.className = "btn-shine-effect";
      if (getComputedStyle(this.addtocartBtn).position === "static") {
        this.addtocartBtn.style.position = "relative";
      }
      this.addtocartBtn.appendChild(shineElement);
      requestAnimationFrame(function () {
        shineElement.style.animation = "shine ".concat(SHINE_EFFECT_DURATION, "s ease-out forwards");
      });
      setTimeout(function () {
        if (shineElement.parentNode) {
          shineElement.remove();
        }
      }, SHINE_EFFECT_DURATION * 1000); // Convert to milliseconds
    }

    /**
     * Initialize indicator focus highlighting for carousel headers
     * Adds text-primary class to .carousel-header when corresponding indicator is focused
     */
  }, {
    key: "initIndicatorFocusHighlight",
    value: function initIndicatorFocusHighlight() {
      var _this15 = this;
      if (!this.indicators) return;
      this.indicators.forEach(function (indicator, index) {
        // Add focus event listener
        indicator.addEventListener("focus", function () {
          _this15.highlightCarouselHeader(index, true);
        });

        // Add blur event listener
        indicator.addEventListener("blur", function () {
          _this15.highlightCarouselHeader(index, false);
        });

        // Optional: Also handle mouse hover for consistent UX
        indicator.addEventListener("mouseenter", function () {
          _this15.highlightCarouselHeader(index, true);
        });
        indicator.addEventListener("mouseleave", function () {
          _this15.highlightCarouselHeader(index, false);
        });
      });
    }

    /**
     * Highlight or unhighlight carousel header based on indicator focus
     *
     * @param {number} stepIndex - Zero-based step index
     * @param {boolean} highlight - Whether to add or remove highlight
     */
  }, {
    key: "highlightCarouselHeader",
    value: function highlightCarouselHeader(stepIndex, highlight) {
      var _this$carouselInner4;
      // Find the corresponding carousel item
      var carouselItem = (_this$carouselInner4 = this.carouselInner) === null || _this$carouselInner4 === void 0 ? void 0 : _this$carouselInner4.children[stepIndex];
      if (!carouselItem) return;

      // Find the header within that carousel item
      var carouselHeader = carouselItem.querySelector(".carousel-header");
      if (!carouselHeader) return;

      // Add smooth transition for color changes
      if (!carouselHeader.style.transition) {
        carouselHeader.style.transition = "color 0.3s ease";
      }

      // Toggle the text-primary class
      if (highlight) {
        carouselHeader.classList.add("text-primary");
      } else {
        carouselHeader.classList.remove("text-primary");
      }
    }
  }]);
}(); // ====================== MODULE INITIALIZATION ======================
/**
 * Initialize ProductConfigurator when DOM is ready
 * Creates single instance to manage entire configurator experience
 */
document.addEventListener("DOMContentLoaded", function () {
  var configuratorInstance = new ProductConfigurator();

  // Make carousel height update globally available for validation feedback
  window.updateCarouselHeight = function () {
    if (configuratorInstance && configuratorInstance.setCarouselHeight) {
      configuratorInstance.setCarouselHeight();
    }
  };
});

/**
 * Future Enhancement Ideas:
 *
 * 1. Keyboard navigation support:
 *    - Arrow key navigation between steps
 *    - Enter/Space for step selection
 *    - Tab order management
 *
 * 2. Progress persistence:
 *    - Save configuration state to localStorage
 *    - Restore progress on page reload
 *    - URL-based step navigation
 *
 * 3. Advanced validation:
 *    - Custom validation rules per step
 *    - Real-time validation feedback
 *    - Dependent field validation
 *
 * 4. Analytics integration:
 *    - Step completion tracking
 *    - Abandonment point analysis
 *    - Configuration choice analytics
 *
 * 5. Performance optimizations:
 *    - Lazy loading of step content
 *    - Virtual scrolling for large indicator lists
 *    - Image preloading for smoother transitions
 */

/***/ }),

/***/ "./assets/js/configurator/configcode/save.js":
/*!***************************************************!*\
  !*** ./assets/js/configurator/configcode/save.js ***!
  \***************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony import */ var core_js_modules_es_symbol_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! core-js/modules/es.symbol.js */ "./node_modules/core-js/modules/es.symbol.js");
/* harmony import */ var core_js_modules_es_symbol_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_symbol_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var core_js_modules_es_symbol_description_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! core-js/modules/es.symbol.description.js */ "./node_modules/core-js/modules/es.symbol.description.js");
/* harmony import */ var core_js_modules_es_symbol_description_js__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_symbol_description_js__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var core_js_modules_es_array_concat_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! core-js/modules/es.array.concat.js */ "./node_modules/core-js/modules/es.array.concat.js");
/* harmony import */ var core_js_modules_es_array_concat_js__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_concat_js__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var core_js_modules_es_array_find_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! core-js/modules/es.array.find.js */ "./node_modules/core-js/modules/es.array.find.js");
/* harmony import */ var core_js_modules_es_array_find_js__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_find_js__WEBPACK_IMPORTED_MODULE_3__);
/* harmony import */ var core_js_modules_es_object_get_prototype_of_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! core-js/modules/es.object.get-prototype-of.js */ "./node_modules/core-js/modules/es.object.get-prototype-of.js");
/* harmony import */ var core_js_modules_es_object_get_prototype_of_js__WEBPACK_IMPORTED_MODULE_4___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_object_get_prototype_of_js__WEBPACK_IMPORTED_MODULE_4__);
/* harmony import */ var core_js_modules_es_object_set_prototype_of_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! core-js/modules/es.object.set-prototype-of.js */ "./node_modules/core-js/modules/es.object.set-prototype-of.js");
/* harmony import */ var core_js_modules_es_object_set_prototype_of_js__WEBPACK_IMPORTED_MODULE_5___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_object_set_prototype_of_js__WEBPACK_IMPORTED_MODULE_5__);
/* harmony import */ var core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! core-js/modules/es.object.to-string.js */ "./node_modules/core-js/modules/es.object.to-string.js");
/* harmony import */ var core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_6___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_6__);
/* harmony import */ var core_js_modules_es_promise_js__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! core-js/modules/es.promise.js */ "./node_modules/core-js/modules/es.promise.js");
/* harmony import */ var core_js_modules_es_promise_js__WEBPACK_IMPORTED_MODULE_7___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_promise_js__WEBPACK_IMPORTED_MODULE_7__);
/* harmony import */ var core_js_modules_es_string_trim_js__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! core-js/modules/es.string.trim.js */ "./node_modules/core-js/modules/es.string.trim.js");
/* harmony import */ var core_js_modules_es_string_trim_js__WEBPACK_IMPORTED_MODULE_8___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_string_trim_js__WEBPACK_IMPORTED_MODULE_8__);
/* provided dependency */ var $ = __webpack_require__(/*! jquery */ "jquery");
/* provided dependency */ var jQuery = __webpack_require__(/*! jquery */ "jquery");
function _regenerator() { /*! regenerator-runtime -- Copyright (c) 2014-present, Facebook, Inc. -- license (MIT): https://github.com/babel/babel/blob/main/packages/babel-helpers/LICENSE */ var e, t, r = "function" == typeof Symbol ? Symbol : {}, n = r.iterator || "@@iterator", o = r.toStringTag || "@@toStringTag"; function i(r, n, o, i) { var c = n && n.prototype instanceof Generator ? n : Generator, u = Object.create(c.prototype); return _regeneratorDefine2(u, "_invoke", function (r, n, o) { var i, c, u, f = 0, p = o || [], y = !1, G = { p: 0, n: 0, v: e, a: d, f: d.bind(e, 4), d: function d(t, r) { return i = t, c = 0, u = e, G.n = r, a; } }; function d(r, n) { for (c = r, u = n, t = 0; !y && f && !o && t < p.length; t++) { var o, i = p[t], d = G.p, l = i[2]; r > 3 ? (o = l === n) && (u = i[(c = i[4]) ? 5 : (c = 3, 3)], i[4] = i[5] = e) : i[0] <= d && ((o = r < 2 && d < i[1]) ? (c = 0, G.v = n, G.n = i[1]) : d < l && (o = r < 3 || i[0] > n || n > l) && (i[4] = r, i[5] = n, G.n = l, c = 0)); } if (o || r > 1) return a; throw y = !0, n; } return function (o, p, l) { if (f > 1) throw TypeError("Generator is already running"); for (y && 1 === p && d(p, l), c = p, u = l; (t = c < 2 ? e : u) || !y;) { i || (c ? c < 3 ? (c > 1 && (G.n = -1), d(c, u)) : G.n = u : G.v = u); try { if (f = 2, i) { if (c || (o = "next"), t = i[o]) { if (!(t = t.call(i, u))) throw TypeError("iterator result is not an object"); if (!t.done) return t; u = t.value, c < 2 && (c = 0); } else 1 === c && (t = i.return) && t.call(i), c < 2 && (u = TypeError("The iterator does not provide a '" + o + "' method"), c = 1); i = e; } else if ((t = (y = G.n < 0) ? u : r.call(n, G)) !== a) break; } catch (t) { i = e, c = 1, u = t; } finally { f = 1; } } return { value: t, done: y }; }; }(r, o, i), !0), u; } var a = {}; function Generator() {} function GeneratorFunction() {} function GeneratorFunctionPrototype() {} t = Object.getPrototypeOf; var c = [][n] ? t(t([][n]())) : (_regeneratorDefine2(t = {}, n, function () { return this; }), t), u = GeneratorFunctionPrototype.prototype = Generator.prototype = Object.create(c); function f(e) { return Object.setPrototypeOf ? Object.setPrototypeOf(e, GeneratorFunctionPrototype) : (e.__proto__ = GeneratorFunctionPrototype, _regeneratorDefine2(e, o, "GeneratorFunction")), e.prototype = Object.create(u), e; } return GeneratorFunction.prototype = GeneratorFunctionPrototype, _regeneratorDefine2(u, "constructor", GeneratorFunctionPrototype), _regeneratorDefine2(GeneratorFunctionPrototype, "constructor", GeneratorFunction), GeneratorFunction.displayName = "GeneratorFunction", _regeneratorDefine2(GeneratorFunctionPrototype, o, "GeneratorFunction"), _regeneratorDefine2(u), _regeneratorDefine2(u, o, "Generator"), _regeneratorDefine2(u, n, function () { return this; }), _regeneratorDefine2(u, "toString", function () { return "[object Generator]"; }), (_regenerator = function _regenerator() { return { w: i, m: f }; })(); }
function _regeneratorDefine2(e, r, n, t) { var i = Object.defineProperty; try { i({}, "", {}); } catch (e) { i = 0; } _regeneratorDefine2 = function _regeneratorDefine(e, r, n, t) { function o(r, n) { _regeneratorDefine2(e, r, function (e) { return this._invoke(r, n, e); }); } r ? i ? i(e, r, { value: n, enumerable: !t, configurable: !t, writable: !t }) : e[r] = n : (o("next", 0), o("throw", 1), o("return", 2)); }, _regeneratorDefine2(e, r, n, t); }
function asyncGeneratorStep(n, t, e, r, o, a, c) { try { var i = n[a](c), u = i.value; } catch (n) { return void e(n); } i.done ? t(u) : Promise.resolve(u).then(r, o); }
function _asyncToGenerator(n) { return function () { var t = this, e = arguments; return new Promise(function (r, o) { var a = n.apply(t, e); function _next(n) { asyncGeneratorStep(a, r, o, _next, _throw, "next", n); } function _throw(n) { asyncGeneratorStep(a, r, o, _next, _throw, "throw", n); } _next(void 0); }); }; }









/**
 * Product Configurator - Configuration Saving Module
 *
 * This module handles saving product configurations and generating shareable codes.
 * It provides functionality for collecting form data, generating configuration codes,
 * and sharing configurations via multiple channels.
 *
 * Features:
 * - Complete form data collection and serialization
 * - Configuration code generation with server-side storage
 * - Success modal with sharing options (email, WhatsApp)
 * - Direct link generation for immediate access
 * - Clipboard functionality for easy sharing
 * - Debug logging for development support
 * - Automatic favourites integration
 *
 * @version 2.3.0
 * @package Configurator
 */

document.addEventListener("DOMContentLoaded", function () {
  /**
   * Data Collection Utilities
   *
   * Handles gathering and serialization of form data from various input types
   * including radio buttons, checkboxes, text inputs, and select elements.
   *
   * @namespace DataCollection
   */

  /**
   * Gathers all configuration data from form elements
   * Systematically collects data from all configurator form inputs and
   * structures it for server-side processing and storage.
   *
   * @param {boolean} [debug=false] - Enable debug logging for troubleshooting
   * @returns {Object} Collected configuration data with type information
   *
   * @example
   * const config = gatherConfigData(true); // Debug mode enabled
   * // Returns: { "size": { value: "large", type: "radio" }, "custom_text": { value: "Hello", type: "input" } }
   */
  function gatherConfigData() {
    var debug = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : false;
    var configData = {};

    // Process radio button selections
    $("input.option-radio:checked").each(function () {
      var name = $(this).attr("name");
      var val = $(this).val();
      configData[name] = {
        value: val,
        type: "radio"
      };
    });

    // Process checkbox selections (support multiple values)
    $("input.option-check:checked").each(function () {
      var name = $(this).attr("name");
      if (!configData[name]) {
        configData[name] = {
          value: [],
          type: "checkbox"
        };
      }
      configData[name].value.push($(this).val());
    });

    // Process text and number inputs
    $("input.option-input").each(function () {
      var name = $(this).attr("name");
      var val = $(this).val();
      configData[name] = {
        value: val,
        type: "input"
      };
    });

    // Process select dropdown selections
    $("select.option-select").each(function () {
      var name = $(this).attr("name");
      var val = $(this).val();
      configData[name] = {
        value: val,
        type: "select"
      };
    });
    if (debug) {
      console.log("Configuration data collected:", configData);
    }
    return configData;
  }

  /**
   * Event Handlers
   *
   * Manages all user interactions including button clicks, sharing functionality,
   * and clipboard operations for the configuration saving system.
   *
   * @namespace EventHandlers
   */

  /**
   * Configures direct link functionality and clipboard copying
   * Sets up handlers for direct link input field and copy button interactions
   *
   * @param {string} directLink - The direct configuration access link
   *
   * @example
   * setupDirectLinkHandlers("https://example.com/code/ABC123");
   */
  function setupDirectLinkHandlers(directLink) {
    // Direct link copy button handler
    $(document).off("click", "#copy-direct-link").on("click", "#copy-direct-link", function () {
      var input = document.getElementById("direct-link-input");
      input.select();
      input.setSelectionRange(0, 99999); // For mobile device compatibility

      try {
        document.execCommand("copy");
        console.log("Direct link copied to clipboard");
      } catch (err) {
        console.error("Copy operation failed:", err);
      }
    });
  }

  /**
   * Sets up social sharing handlers with pre-formatted messages
   * Configures email and WhatsApp sharing with professional templates
   *
   * @param {string} code - The generated configuration code
   * @param {Object} productInfo - Product information object
   * @param {string} productInfo.title - Product title
   * @param {string} productInfo.price - Product price
   * @param {string} productInfo.deliveryTime - Delivery time information
   * @param {string} productInfo.directLink - Direct access link
   *
   * @example
   * setupShareHandlers("ABC123", {
   *   title: "Premium Mirror",
   *   price: "299,99 €",
   *   deliveryTime: "2-3 Wochen",
   *   directLink: "https://example.com/code/ABC123"
   * });
   */
  function setupShareHandlers(code, productInfo) {
    // Email sharing handler with professional template
    $(document).off("click", "#configcode-share-mail").on("click", "#configcode-share-mail", function () {
      var mailSubject = encodeURIComponent("Badspiegel.de | Meine Konfiguration".concat(productInfo.title ? " – " + productInfo.title : ""));

      // Comprehensive email body with all product details
      var mailBody = encodeURIComponent("Meine pers\xF6nliche Konfiguration f\xFCr Badspiegel.de:\n\n" + "\uD83E\uDE84 Direkter Link (klicken zum \xD6ffnen):\n" + "".concat(productInfo.directLink, "\n\n") + "\uD83D\uDD11 Oder Code eingeben: ".concat(code, "\n\n") + "\uD83D\uDCCC Produkt: ".concat(productInfo.title || "Individueller Badspiegel oder Badmöbel", "\n\n") + (productInfo.price ? "\uD83D\uDCB6 Preis: ".concat(productInfo.price, "\n") : "") + (productInfo.deliveryTime ? "\u23F1\uFE0F Lieferzeit: ".concat(productInfo.deliveryTime, "\n\n") : "") + "Mit diesem Code k\xF6nnen Sie:\n" + "\u2713 Konfiguration sofort aufrufen\n" + "\u2713 \xC4nderungen vornehmen\n" + "\u2713 Die Zusammenstellung optimieren\n\n" + "Viele Gr\xFC\xDFe\nIhr Badspiegel.de Team\n\n" + "\uD83C\uDF1F Ihr Spezialist f\xFCr Badspiegel & ma\xDFgefertigte Badm\xF6bel\n\n" + "PS: Fragen? \uD83D\uDCDE 0231 / 550 33 204 | \u2709\uFE0F service@badspiegel.de");

      // Open email client with pre-filled content
      window.location.href = "mailto:?subject=".concat(mailSubject, "&body=").concat(mailBody);
    });

    // WhatsApp sharing handler with concise message
    $(document).off("click", "#configcode-share-whatsapp").on("click", "#configcode-share-whatsapp", function () {
      var whatsappText = "Meine Konfiguration f\xFCr Badspiegel.de:\n\n".concat(productInfo.directLink, "\n\n(Code: ").concat(code, ")");

      // Open WhatsApp with pre-filled message
      window.open("https://wa.me/?text=".concat(encodeURIComponent(whatsappText)), "_blank");
    });
  }

  /**
   * Generate configuration code via AJAX
   *
   * @param {Object} configData - Configuration data object
   * @returns {Promise} Promise that resolves with {code, directLink} or rejects with error
   */
  function generateConfigCode(configData) {
    return new Promise(function (resolve, reject) {
      $.ajax({
        url: myAjaxData.ajaxUrl,
        method: "POST",
        data: {
          action: "save_config",
          security: myAjaxData.nonce,
          product_id: myAjaxData.productId,
          config_data: configData
        },
        success: function success(response) {
          if (response.success) {
            var code = response.data.generated_code;
            var directLink = "".concat(window.location.origin, "/code/").concat(code);
            resolve({
              code: code,
              directLink: directLink
            });
          } else {
            var _response$data;
            reject(new Error(((_response$data = response.data) === null || _response$data === void 0 ? void 0 : _response$data.msg) || "Konfiguration konnte nicht gespeichert werden"));
          }
        },
        error: function error(xhr, status, _error) {
          reject(new Error("Netzwerkfehler. Bitte versuchen Sie es erneut."));
        }
      });
    });
  }

  /**
   * Add configuration to favourites automatically
   *
   * @param {string} configCode - Generated configuration code
   * @param {number} productId - Product ID
   * @returns {Promise} Promise that resolves when favourite is added
   */
  function addConfigToFavourites(configCode, productId) {
    return new Promise(function (resolve, reject) {
      $.ajax({
        url: myAjaxData.ajaxUrl,
        method: "POST",
        data: {
          action: "add_favourite_with_config",
          nonce: myAjaxData.favouriteNonce,
          product_id: productId,
          config_code: configCode
        },
        success: function success(response) {
          if (response.success) {
            console.log("Configuration added to favourites:", configCode);

            // Update favourites badge if function exists
            if (window.updateFavouritesBadgeDisplay) {
              window.updateFavouritesBadgeDisplay(response.data.count);
            }
            resolve(response.data);
          } else {
            var _response$data2, _response$data3;
            console.warn("Could not add to favourites:", (_response$data2 = response.data) === null || _response$data2 === void 0 ? void 0 : _response$data2.message);
            // Don't reject - favourites addition is optional
            resolve({
              added: false,
              message: (_response$data3 = response.data) === null || _response$data3 === void 0 ? void 0 : _response$data3.message
            });
          }
        },
        error: function error(xhr, status, _error2) {
          console.warn("Favourites addition failed:", _error2);
          // Don't reject - favourites addition is optional
          resolve({
            added: false,
            error: _error2
          });
        }
      });
    });
  }

  /**
   * Enhanced success modal with favourite confirmation
   *
   * @param {string} msg - Success message text to display
   * @param {string} code - Generated configuration code
   * @param {string} tooltip - Tooltip text for copy buttons
   * @param {boolean} addedToFavourites - Whether config was added to favourites
   */
  function showSuccessModal(msg, code, tooltip) {
    var addedToFavourites = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : false;
    // Collect current product information for sharing
    var productInfo = {
      title: $(".product_title, .product-titel, .entry-title").first().text().trim(),
      price: $("#productConfiguroator .product-price").first().text().trim(),
      deliveryTime: $(".delivery-time-data").first().text().trim(),
      link: window.location.href,
      directLink: "".concat(window.location.origin, "/code/").concat(code)
    };

    // Enhanced success message with favourite confirmation
    var successAlert = addedToFavourites ? "\n      <div class=\"alert alert-success\">\n        <p class=\"mb-0\">\n          <span class=\"row g-4 align-items-center\">\n            <span class=\"col-auto\"><i class=\"fa-sharp fa-light fa-check\"></i></span>\n            <span class=\"col border-start border-success-subtle\">Konfiguration als Code gespeichert und zu Ihren Favoriten hinzugef\xFCgt!</span>\n          </span>\n        </p>\n      </div>\n    " : "\n      <div class=\"alert alert-success\">\n        <p class=\"fw-medium mb-0\">Ihre Konfiguration ist gespeichert!</p>\n        <hr class=\"my-2\">\n        <p class=\"mb-0\">Sichern Sie den Code damit Ihre Konfiguration nicht verloren geht.</p>\n      </div>\n    ";

    // Create comprehensive modal body with all sharing options
    var modalBody = "\n      <!-- Success Alert -->\n      ".concat(successAlert, "\n      \n      <!-- Configuration Code Section -->\n      <div class=\"mb-3\">\n        <label class=\"form-label fw-medium mb-1\">\n          <i class=\"fa-sharp fa-light fa-key me-2\"></i>Code\n        </label>\n        <button type=\"button\"\n                class=\"btn btn-outline-success d-block\"\n                style=\"--bs-btn-hover-bg: var(--bs-success-bg-subtle); --bs-btn-hover-color: var(--bs-success-text-emphasis); --bs-btn-color: var(--bs-success-text-emphasis); --bs-btn-border-color: var(--bs-success-border-subtle); --bs-btn-active-color: var(--bs-white); --bs-btn-active-border-color: var(--bs-success);\"\n                data-copy=\"clipboard\"\n                data-voucher=\"").concat(code, "\"\n                data-bs-tooltip=\"true\"\n                title=\"").concat(tooltip, "\">\n          <code class=\"fs-4\">").concat(code, "</code>\n          <i class=\"fa-sharp fa-light fa-copy fa-fw ms-2\" aria-hidden=\"true\"></i>\n          <span class=\"visually-hidden\">Code kopieren</span>\n        </button>\n      </div>\n      \n      <!-- Direct Link Section -->\n      <div class=\"mb-3\">\n        <label class=\"form-label fw-medium mb-1\">\n          <i class=\"fa-sharp fa-light fa-link me-2\"></i>Link\n        </label>\n        <div class=\"input-group\">\n          <input type=\"text\" \n                 class=\"form-control border-success-subtle focus-ring focus-ring-success\" \n                 value=\"").concat(productInfo.directLink, "\" \n                 readonly \n                 id=\"direct-link-input\">\n            <button type=\"button\"\n                    class=\"btn btn-outline-success\"\n                    style=\"--bs-btn-hover-bg: var(--bs-success-bg-subtle); --bs-btn-hover-color: var(--bs-success-text-emphasis); --bs-btn-color: var(--bs-success-text-emphasis); --bs-btn-border-color: var(--bs-success-border-subtle); --bs-btn-active-color: var(--bs-white); --bs-btn-active-border-color: var(--bs-success);\"\n                    data-copy=\"clipboard\"\n                    data-voucher=\"").concat(productInfo.directLink, "\"\n                    data-bs-tooltip=\"true\"\n                    title=\"").concat(tooltip, "\">\n                <i class=\"fa-sharp fa-light fa-copy fa-fw\" aria-hidden=\"true\"></i>\n                <span class=\"visually-hidden\">Link kopieren</span>\n            </button>\n        </div>\n      </div>\n\n      <!-- Social Sharing Section -->\n      <label class=\"form-label fw-medium mb-1\">\n        <i class=\"fa-sharp fa-light fa-share me-2\"></i>Teilen\n      </label>\n      <div class=\"row g-2 mb-0\" role=\"group\">\n        <div class=\"col col-md-auto\">\n          <button type=\"button\"\n                  class=\"btn btn-outline-success btn-sm py-2 text-md-start w-100\"\n                  id=\"configcode-share-mail\"\n                  style=\"--bs-btn-hover-bg: var(--bs-success-bg-subtle); --bs-btn-hover-color: var(--bs-success-text-emphasis); --bs-btn-color: var(--bs-success-text-emphasis); --bs-btn-border-color: var(--bs-success-border-subtle); --bs-btn-active-color: var(--bs-white); --bs-btn-active-border-color: var(--bs-success);\">\n            <i class=\"fa-sharp fa-light fa-envelope fa-fw me-1\" aria-hidden=\"true\"></i>E-Mail teilen\n          </button>\n        </div>\n        <div class=\"col col-md-auto\">\n          <button type=\"button\"\n                  class=\"btn btn-outline-success btn-sm py-2 text-md-start w-100\"\n                  id=\"configcode-share-whatsapp\"\n                  style=\"--bs-btn-hover-bg: var(--bs-success-bg-subtle); --bs-btn-hover-color: var(--bs-success-text-emphasis); --bs-btn-color: var(--bs-success-text-emphasis); --bs-btn-border-color: var(--bs-success-border-subtle); --bs-btn-active-color: var(--bs-white); --bs-btn-active-border-color: var(--bs-success);\">\n            <i class=\"fab fa-whatsapp fa-fw me-1\"></i>WhatsApp teilen\n          </button>\n        </div>\n      </div>\n    ");

    // Initialize modal with success configuration
    createModal({
      title: "Konfiguration gespeichert",
      body: modalBody,
      size: "md",
      footer: [{
        text: "Schließen",
        class: "btn-dark",
        dismiss: true
      }]
    });

    // Initialize Bootstrap tooltips for copy buttons
    setTimeout(function () {
      $('[data-bs-tooltip="true"]').tooltip({
        trigger: "hover"
      });
    }, 0);

    // Setup interactive functionality
    setupDirectLinkHandlers(productInfo.directLink);
    setupShareHandlers(code, productInfo);
  }

  /**
   * Loading States Utility
   *
   * Provides different loading animations for buttons
   *
   * @namespace LoadingStates
   */

  /**
   * Apply spinner loading state (for main save button)
   *
   * @param {jQuery} $button - The button element
   * @returns {Function} Cleanup function to reset button state
   */
  function applySpinnerLoading($button) {
    var originalContent = $button.html();
    var wasDisabled = $button.prop("disabled");
    $button.prop("disabled", true).append('<span class="spinner-border spinner-border-sm ms-2" role="status" aria-hidden="true" data-id="saveSpinner"></span>');

    // Return cleanup function
    return function cleanup() {
      $button.find('[data-id="saveSpinner"]').remove();
      $button.prop("disabled", wasDisabled);
    };
  }

  /**
   * Apply icon animation loading state (for summary save button)
   *
   * @param {HTMLElement|jQuery} button - The button element (can be native DOM or jQuery)
   * @returns {Function} Cleanup function to reset button state
   */
  function applyIconAnimationLoading(button) {
    // Handle both jQuery and native DOM elements
    var $button = button instanceof jQuery ? button : $(button);
    var nativeButton = $button[0];
    var originalContent = $button.html();
    var wasDisabled = $button.prop("disabled");

    // Apply icon animation loading (same as print button)
    $button.prop("disabled", true).html('<i class="fa-sharp fa-light fa-spinner fa-fw fa-spin"></i><span class="visually-hidden">Wird gespeichert...</span>');

    // Return cleanup function
    return function cleanup() {
      $button.html(originalContent);
      $button.prop("disabled", wasDisabled);
    };
  }

  /**
   * Core save configuration function - reusable for multiple buttons
   *
   * @param {jQuery} $button - The button element that triggered the save
   * @param {string} loadingType - Type of loading animation ('spinner' or 'icon')
   * @returns {Promise} Promise that resolves when save is complete
   */
  function saveConfiguration(_x) {
    return _saveConfiguration.apply(this, arguments);
  }
  /**
   * Main Configuration Save Handler
   * Enhanced to automatically add configuration to favourites
   */
  function _saveConfiguration() {
    _saveConfiguration = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee($button) {
      var loadingType,
        cleanupLoading,
        configData,
        response,
        _response$data4,
        code,
        msg,
        tooltip,
        addedToFavourites,
        favouriteResult,
        _response$data5,
        _response$data6,
        _args = arguments,
        _t,
        _t2;
      return _regenerator().w(function (_context) {
        while (1) switch (_context.p = _context.n) {
          case 0:
            loadingType = _args.length > 1 && _args[1] !== undefined ? _args[1] : "spinner";
            // Apply appropriate loading state based on type
            cleanupLoading = loadingType === "icon" ? applyIconAnimationLoading($button) : applySpinnerLoading($button);
            _context.p = 1;
            // Collect all configuration data from form
            configData = gatherConfigData(false); // Send configuration data to server for processing and storage
            _context.n = 2;
            return new Promise(function (resolve, reject) {
              $.ajax({
                url: myAjaxData.ajaxUrl,
                method: "POST",
                data: {
                  action: "save_config",
                  security: myAjaxData.nonce,
                  product_id: myAjaxData.productId,
                  config_data: configData
                },
                success: resolve,
                error: reject
              });
            });
          case 2:
            response = _context.v;
            if (!response.success) {
              _context.n = 7;
              break;
            }
            _response$data4 = response.data, code = _response$data4.generated_code, msg = _response$data4.msg, tooltip = _response$data4.tooltip; // Automatically add configuration to favourites
            addedToFavourites = false;
            _context.p = 3;
            _context.n = 4;
            return addConfigToFavourites(code, myAjaxData.productId);
          case 4:
            favouriteResult = _context.v;
            addedToFavourites = favouriteResult.added !== false;
            _context.n = 6;
            break;
          case 5:
            _context.p = 5;
            _t = _context.v;
            console.warn("Could not add to favourites:", _t);
          case 6:
            // Display success modal with favourite confirmation
            showSuccessModal(msg, code, tooltip, addedToFavourites);
            return _context.a(2, {
              success: true,
              code: code,
              addedToFavourites: addedToFavourites
            });
          case 7:
            console.error("Save configuration failed:", ((_response$data5 = response.data) === null || _response$data5 === void 0 ? void 0 : _response$data5.msg) || "Unknown error");
            throw new Error(((_response$data6 = response.data) === null || _response$data6 === void 0 ? void 0 : _response$data6.msg) || "Unknown error");
          case 8:
            _context.n = 10;
            break;
          case 9:
            _context.p = 9;
            _t2 = _context.v;
            console.error("Save configuration error:", _t2);
            throw _t2;
          case 10:
            _context.p = 10;
            // Reset button state regardless of success or failure
            cleanupLoading();
            return _context.f(10);
          case 11:
            return _context.a(2);
        }
      }, _callee, null, [[3, 5], [1, 9, 10, 11]]);
    }));
    return _saveConfiguration.apply(this, arguments);
  }
  $("#product-configurator-configcode-save").on("click", function () {
    var $button = $(this);
    saveConfiguration($button, "spinner").catch(function (error) {
      console.error("Save failed:", error);
      // Optionally show error message to user
    });
  });

  // Export functions for use in other modules
  window.ConfiguratorSave = {
    gatherConfigData: gatherConfigData,
    generateConfigCode: generateConfigCode,
    saveConfiguration: saveConfiguration,
    // Export the updated function
    applySpinnerLoading: applySpinnerLoading,
    // Export loading utilities
    applyIconAnimationLoading: applyIconAnimationLoading
  };

  // Make showSuccessModal globally available for summary module
  window.showSuccessModal = showSuccessModal;
});

/**
 * Future Enhancement Ideas:
 *
 * 1. Enhanced error handling:
 *    - User-friendly error modals
 *    - Retry mechanisms for failed saves
 *    - Offline storage for drafts
 *
 * 2. Advanced sharing options:
 *    - Social media integration (Facebook, Twitter)
 *    - QR code generation for mobile sharing
 *    - PDF export functionality
 *
 * 3. Configuration management:
 *    - Save multiple configurations per user
 *    - Configuration versioning
 *    - Configuration comparison tools
 *
 * 4. Analytics integration:
 *    - Track save success rates
 *    - Monitor sharing channel usage
 *    - Configuration popularity metrics
 *
 * 5. Performance optimizations:
 *    - Data compression for large configurations
 *    - Background saving
 *    - Progress indicators for large datasets
 *
 * 6. Security improvements (CRITICAL):
 *    - Escape user input in template literals to prevent XSS
 *    - Sanitize HTML content before modal insertion
 *    - Validate all form field names and values
 *    - Implement Content Security Policy headers
 *    - Add CSRF protection for save operations
 *
 * 7. Modern API adoption (CRITICAL):
 *    - Replace deprecated document.execCommand("copy") with modern Clipboard API
 *    - Add fallback for browsers without Clipboard API support
 *    - Implement proper async/await error handling
 *    - Use AbortController for cancellable requests
 *
 * 8. Dependency management:
 *    - Add existence checks for myAjaxData before usage
 *    - Verify createModal function availability
 *    - Implement graceful degradation for missing jQuery
 *    - Add feature detection for required browser APIs
 *
 * 9. Race condition prevention:
 *    - Prevent multiple simultaneous save operations
 *    - Cancel previous requests before new ones
 *    - Implement request queuing system
 *    - Add debouncing for rapid button clicks
 *
 * 10. Error handling robustness:
 *     - Show user-friendly error messages in modals
 *     - Distinguish between network and server errors
 *     - Add comprehensive error logging
 *     - Implement automatic retry with exponential backoff
 *
 * 11. DOM selector reliability:
 *     - Fix typo in selector: #productConfiguroator -> #productConfigurator
 *     - Cache frequently used selectors
 *     - Use data attributes instead of complex class selectors
 *     - Add fallback strategies for missing elements
 *
 * 12. Input validation enhancement:
 *     - Validate configuration data structure before save
 *     - Add client-side validation for required fields
 *     - Implement schema validation for form data
 *     - Sanitize all user inputs before processing
 *
 * 13. Memory management:
 *     - Properly clean up event listeners
 *     - Remove modal elements after closing
 *     - Clear timeouts and intervals
 *     - Implement proper garbage collection for large objects
 *
 * 14. User experience improvements:
 *     - Add save progress indicators
 *     - Implement auto-save functionality
 *     - Show confirmation before leaving page with unsaved changes
 *     - Add keyboard shortcuts for save operations
 *
 * 15. Code maintainability:
 *     - Extract modal HTML generation into templates
 *     - Reduce code duplication between similar functions
 *     - Add comprehensive JSDoc type definitions
 *     - Implement unit tests for core functions
 *
 * 16. Accessibility improvements:
 *     - Add proper ARIA labels to all interactive elements
 *     - Implement keyboard navigation for modals
 *     - Add screen reader announcements for save status
 *     - Ensure high contrast mode compatibility
 *
 * 17. Network resilience:
 *     - Add request timeout handling
 *     - Implement offline save capability
 *     - Add automatic retry for failed network requests
 *     - Show network status indicators
 */

/***/ }),

/***/ "./assets/js/configurator/dependencies.js":
/*!************************************************!*\
  !*** ./assets/js/configurator/dependencies.js ***!
  \************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   applyFocusEffect: () => (/* binding */ applyFocusEffect),
/* harmony export */   autoSelect: () => (/* binding */ autoSelect),
/* harmony export */   dependenciesValueStartingPriceXcontainer: () => (/* binding */ dependenciesValueStartingPriceXcontainer),
/* harmony export */   dependenciesValuesXcontainer: () => (/* binding */ dependenciesValuesXcontainer),
/* harmony export */   dependenciesValuesXvalues: () => (/* binding */ dependenciesValuesXvalues)
/* harmony export */ });
/* harmony import */ var core_js_modules_es_array_concat_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! core-js/modules/es.array.concat.js */ "./node_modules/core-js/modules/es.array.concat.js");
/* harmony import */ var core_js_modules_es_array_concat_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_concat_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var core_js_modules_es_array_filter_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! core-js/modules/es.array.filter.js */ "./node_modules/core-js/modules/es.array.filter.js");
/* harmony import */ var core_js_modules_es_array_filter_js__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_filter_js__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var core_js_modules_es_array_from_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! core-js/modules/es.array.from.js */ "./node_modules/core-js/modules/es.array.from.js");
/* harmony import */ var core_js_modules_es_array_from_js__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_from_js__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! core-js/modules/es.array.includes.js */ "./node_modules/core-js/modules/es.array.includes.js");
/* harmony import */ var core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_3__);
/* harmony import */ var core_js_modules_es_array_map_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! core-js/modules/es.array.map.js */ "./node_modules/core-js/modules/es.array.map.js");
/* harmony import */ var core_js_modules_es_array_map_js__WEBPACK_IMPORTED_MODULE_4___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_map_js__WEBPACK_IMPORTED_MODULE_4__);
/* harmony import */ var core_js_modules_es_function_name_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! core-js/modules/es.function.name.js */ "./node_modules/core-js/modules/es.function.name.js");
/* harmony import */ var core_js_modules_es_function_name_js__WEBPACK_IMPORTED_MODULE_5___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_function_name_js__WEBPACK_IMPORTED_MODULE_5__);
/* harmony import */ var core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! core-js/modules/es.object.to-string.js */ "./node_modules/core-js/modules/es.object.to-string.js");
/* harmony import */ var core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_6___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_6__);
/* harmony import */ var core_js_modules_es_regexp_exec_js__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! core-js/modules/es.regexp.exec.js */ "./node_modules/core-js/modules/es.regexp.exec.js");
/* harmony import */ var core_js_modules_es_regexp_exec_js__WEBPACK_IMPORTED_MODULE_7___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_regexp_exec_js__WEBPACK_IMPORTED_MODULE_7__);
/* harmony import */ var core_js_modules_es_string_iterator_js__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! core-js/modules/es.string.iterator.js */ "./node_modules/core-js/modules/es.string.iterator.js");
/* harmony import */ var core_js_modules_es_string_iterator_js__WEBPACK_IMPORTED_MODULE_8___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_string_iterator_js__WEBPACK_IMPORTED_MODULE_8__);
/* harmony import */ var core_js_modules_es_string_replace_js__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! core-js/modules/es.string.replace.js */ "./node_modules/core-js/modules/es.string.replace.js");
/* harmony import */ var core_js_modules_es_string_replace_js__WEBPACK_IMPORTED_MODULE_9___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_string_replace_js__WEBPACK_IMPORTED_MODULE_9__);
/* harmony import */ var core_js_modules_es_string_trim_js__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! core-js/modules/es.string.trim.js */ "./node_modules/core-js/modules/es.string.trim.js");
/* harmony import */ var core_js_modules_es_string_trim_js__WEBPACK_IMPORTED_MODULE_10___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_string_trim_js__WEBPACK_IMPORTED_MODULE_10__);
/* harmony import */ var core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! core-js/modules/web.dom-collections.for-each.js */ "./node_modules/core-js/modules/web.dom-collections.for-each.js");
/* harmony import */ var core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_11___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_11__);












/**
 * Product Configurator - Dependencies Management Module
 *
 * This module handles conditional dependencies between form elements in the product
 * configurator. It provides functions for managing visibility, auto-selection, and
 * visual feedback based on user selections and business logic dependencies.
 *
 * Features:
 * - Values to Values dependencies (conditional option visibility)
 * - Values to Container dependencies (conditional container visibility)
 * - Auto-selection of valid options when dependencies change
 * - Visual focus effects for user guidance
 * - Performance-optimized event delegation
 * - Debug logging capabilities
 *
 * @version 2.3.0
 * @package Configurator
 *
 * @todo Check if these functions can be used, for a better DRY concept
 */

/**
 * Applies a visual focus effect to a container element
 * Provides user feedback when automatic changes occur due to dependencies
 *
 * @param {HTMLElement} container - The container element to apply focus effect to
 * @param {string} focusClass - CSS class for the specific focus ring (e.g., 'focus-ring-secondary')
 *
 * @example
 * applyFocusEffect(optionContainer, 'focus-ring-primary');
 */
function applyFocusEffect(container, focusClass) {
  var toggle = container.querySelector("[data-target='offdrop']");

  // Apply focus classes with delay for smooth visual transition
  setTimeout(function () {
    toggle === null || toggle === void 0 || toggle.classList.add("focus", "focus-ring", focusClass);
    toggle && (toggle.style.borderColor = "var(--bs-secondary)");
  }, 300);

  // Remove focus classes after duration for clean UI
  setTimeout(function () {
    toggle === null || toggle === void 0 || toggle.classList.remove("focus", "focus-ring", focusClass);
    toggle && (toggle.style.borderColor = "");
  }, 3300);
}

/**
 * Automatically selects the first visible, valid input from a set of inputs
 * Used when dependencies change to maintain a valid selection state
 *
 * @param {HTMLInputElement[]} inputs - Array of target input elements
 * @param {function(HTMLElement): void} [focusCallback] - Optional callback for focus effect on container
 * @param {boolean} [debug=false] - Optional flag to enable debug logging
 *
 * @example
 * const sizeInputs = document.querySelectorAll('input[name="size"]');
 * autoSelect(sizeInputs, (container) => applyFocusEffect(container, 'focus-ring-info'));
 */
var autoSelect = function autoSelect(inputs, focusCallback) {
  var debug = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : false;
  var log = function log() {
    var _console;
    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }
    return debug && (_console = console).log.apply(_console, ["[autoSelect]"].concat(args));
  };
  var warn = function warn() {
    var _console2;
    for (var _len2 = arguments.length, args = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
      args[_key2] = arguments[_key2];
    }
    return debug && (_console2 = console).warn.apply(_console2, ["[autoSelect]"].concat(args));
  };
  log("autoSelect inputs:", inputs);

  // 1. Filter for visible and non-empty inputs only
  var candidates = inputs.filter(function (input) {
    var group = input.closest(".btn, .btn-group, .values-group");
    var visible = group && !group.classList.contains("d-none");
    var nonEmpty = input.value.trim() !== "";
    return visible && nonEmpty;
  });
  log("Auto-select candidates:", candidates);

  // 2. Skip if a selection already exists
  if (candidates.some(function (input) {
    return input.checked;
  })) {
    log("Already selected, aborting.");
    return;
  }

  // 3. Select first candidate and trigger change event
  var first = candidates[0];
  if (first) {
    first.checked = true;
    first.dispatchEvent(new Event("change", {
      bubbles: true
    }));
    log("Auto-selected:", first.value);

    // Apply focus effect if callback provided
    if (focusCallback) {
      var container = first.closest(".option-group, .values-group");
      if (container) focusCallback(container);
    }
  } else {
    warn("No valid candidates found for auto-select.");
  }
};

/**
 * Dependencies: Values × Values
 *
 * Controls target input visibility and selection based on trigger input values.
 * This function manages conditional relationships between different option groups
 * where the selection in one group determines which options are available in another.
 *
 * @param {string} triggerName - Name attribute of the trigger input group
 * @param {function(string): boolean} condition - Function that evaluates trigger value for dependency
 * @param {string} targetOptionName - Name attribute of the target input group
 * @param {function(string): boolean} targetValueCondition - Filter function for valid target values
 * @param {boolean} [debug=false] - Enable debug logging for troubleshooting
 *
 * @example
 * // Show premium finishes only when premium material is selected
 * dependenciesValuesXvalues(
 *   'material',
 *   (value) => value === 'premium',
 *   'finish',
 *   (value) => ['gold', 'platinum', 'titanium'].includes(value),
 *   false
 * );
 */
function dependenciesValuesXvalues(triggerName, condition, targetOptionName, targetValueCondition) {
  var debug = arguments.length > 4 && arguments[4] !== undefined ? arguments[4] : false;
  var log = function log() {
    var _console3;
    for (var _len3 = arguments.length, args = new Array(_len3), _key3 = 0; _key3 < _len3; _key3++) {
      args[_key3] = arguments[_key3];
    }
    return debug && (_console3 = console).log.apply(_console3, ["[ValuesXvalues]"].concat(args));
  };
  var warn = function warn() {
    var _console4;
    for (var _len4 = arguments.length, args = new Array(_len4), _key4 = 0; _key4 < _len4; _key4++) {
      args[_key4] = arguments[_key4];
    }
    return debug && (_console4 = console).warn.apply(_console4, ["[ValuesXvalues]"].concat(args));
  };

  // 1. Get all target group inputs for auto-selection functionality
  var allGroupInputs = Array.from(document.querySelectorAll('.values-group input[name="' + targetOptionName + '"]'));

  // 2. Pre-cache all target inputs with their container buttons for performance
  var allTargetEntries = allGroupInputs.filter(function (input) {
    return targetValueCondition(input.value);
  }).map(function (input) {
    var btn = input.closest("label.btn");
    if (!btn) {
      warn("No container found for input:", input.value);
    }
    return {
      input: input,
      btn: btn
    };
  });

  // 3. Validate that target inputs exist before setting up dependencies
  if (allTargetEntries.length === 0) {
    warn("No valid target inputs found for:", targetOptionName);
    return;
  }

  // 4. Change handler for trigger input events
  function handleChange() {
    var _allGroupInputs$;
    var value = this.value;
    var match = condition(value);
    log("Trigger value:", value, "-> condition result:", match);

    // Toggle visibility of target options based on condition
    allTargetEntries.forEach(function (_ref) {
      var input = _ref.input,
        btn = _ref.btn;
      if (!btn) return;
      btn.classList.toggle("d-none", !match);

      // Clear selection if option becomes hidden
      if (!match) input.checked = false;
    });

    // Auto-select first valid option after dependency change with focus effect
    var targetContainer = (_allGroupInputs$ = allGroupInputs[0]) === null || _allGroupInputs$ === void 0 ? void 0 : _allGroupInputs$.closest(".option-group, .values-group");
    autoSelect(allGroupInputs, targetContainer ? function () {
      return applyFocusEffect(targetContainer, "focus-ring-info");
    } : null);
  }

  // 5. Event delegation: single listener for all trigger inputs (performance optimization)
  document.body.addEventListener("change", function (event) {
    var target = event.target;
    if (target.name === triggerName) {
      handleChange.call(target, event);
    }
  });

  // 6. Initial run for already checked trigger inputs (page load state)
  // Note: Commented out to prevent conflicts with other initialization logic
  // document
  //     .querySelectorAll(`input[name="${triggerName}"]:checked`)
  //     .forEach(input => handleChange.call(input));
}

/**
 * Dependencies: Values × Container
 *
 * Controls entire container visibility based on trigger input values.
 * This function manages dependencies where selecting certain values shows or hides
 * complete option groups, including automatic value initialization and cleanup.
 *
 * @param {string} triggerName - Name attribute of the trigger input group
 * @param {function(string): boolean} condition - Function to evaluate dependency condition
 * @param {string} targetIdSuffix - Suffix for target container ID (#option_{suffix})
 * @param {boolean} [debug=false] - Enable debug logging for troubleshooting
 *
 * @example
 * // Show custom sizing options only when "custom" size is selected
 * dependenciesValuesXcontainer(
 *   'size_type',
 *   (value) => value === 'custom',
 *   'custom_dimensions',
 *   false
 * );
 */
function dependenciesValuesXcontainer(triggerName, condition, targetIdSuffix) {
  var debug = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : false;
  var log = function log() {
    var _console5;
    for (var _len5 = arguments.length, args = new Array(_len5), _key5 = 0; _key5 < _len5; _key5++) {
      args[_key5] = arguments[_key5];
    }
    return debug && (_console5 = console).log.apply(_console5, ["[ValuesXcontainer]"].concat(args));
  };
  var warn = function warn() {
    var _console6;
    for (var _len6 = arguments.length, args = new Array(_len6), _key6 = 0; _key6 < _len6; _key6++) {
      args[_key6] = arguments[_key6];
    }
    return debug && (_console6 = console).warn.apply(_console6, ["[ValuesXcontainer]"].concat(args));
  };
  log("Trigger name:", triggerName);

  // Locate target container element
  var container = document.getElementById("option_".concat(targetIdSuffix));
  if (!container) {
    warn("Container not found:", "#option_".concat(targetIdSuffix));
    return;
  }

  // Main trigger change handler
  var handleChange = function handleChange(event) {
    var match = condition(this.value);
    log("Trigger value:", this.value, "-> condition result:", match);
    log("Condition function:", condition);
    if (match) {
      // Show container and initialize default values
      container.classList.remove("d-none");

      // Set default values for empty number and text inputs with focus effect
      ["number", "text"].forEach(function (type) {
        var input = container.querySelector("input[type=\"".concat(type, "\"]"));
        if (input && input.value.trim() === "") {
          // Use min attribute for numbers, placeholder for text as fallback
          var fallback = input.getAttribute(type === "number" ? "min" : "placeholder") || "";
          input.value = fallback;
          input.classList.replace("no-input", "yes-input");

          // Apply visual focus effect using consistent method
          applyFocusEffect(container, "focus-ring-warning");
        }
      });

      // Auto-select first valid option within the container (only for child option groups)
      if (container.classList.contains("option-group-child")) {
        var allInputs = Array.from(container.querySelectorAll("input"));
        autoSelect(allInputs, function () {
          return applyFocusEffect(container, "focus-ring-secondary");
        });
      }
    } else {
      // Hide container and reset all values
      container.classList.add("d-none");

      // Clear number and text input values
      container.querySelectorAll("input").forEach(function (input) {
        if (["number", "text"].includes(input.type)) {
          input.value = "";
          input.classList.replace("yes-input", "no-input");
        }
      });

      // Select empty fallback option if available
      var emptyOption = container.querySelector('input[value=""]');
      if (emptyOption) {
        emptyOption.checked = true;
        emptyOption.dispatchEvent(new Event("change", {
          bubbles: true
        }));
      }
    }
  };

  // Event delegation for performance optimization
  document.body.addEventListener("change", function (event) {
    var target = event.target;
    if (target.matches("input[name=\"".concat(triggerName, "\"]"))) {
      handleChange.call(target, event);
    }
  });

  // Initial run for already checked trigger inputs (page load state)
  document.querySelectorAll("input[name=\"".concat(triggerName, "\"]:checked")).forEach(function (input) {
    return handleChange.call(input, null);
  });
}

/**
 * Dependencies: Values Starting Price × Container
 *
 * Sets starting prices ("ab X €") for a trigger option based on the cheapest option
 * in the related container. This shows users the minimum price they can expect
 * when selecting an option that opens up additional choices.
 *
 * @param {string} triggerName - Name attribute of the trigger input group
 * @param {function(string): boolean} condition - Function to evaluate which trigger value gets the starting price
 * @param {string} targetIdSuffix - Suffix for target container ID (#option_{suffix}) to get prices from
 * @param {boolean} [debug=false] - Enable debug logging for troubleshooting
 *
 * @example
 * // Set starting price for "facettenschliff" option based on cheapest facette option
 * dependenciesValueStartingPriceXcontainer(
 *   'kantenpolierung',
 *   (value) => value === 'facettenschliff',
 *   'facette'
 * );
 */
function dependenciesValueStartingPriceXcontainer(triggerName, condition, targetIdSuffix) {
  var debug = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : false;
  var log = function log() {
    var _console7;
    for (var _len7 = arguments.length, args = new Array(_len7), _key7 = 0; _key7 < _len7; _key7++) {
      args[_key7] = arguments[_key7];
    }
    return debug && (_console7 = console).log.apply(_console7, ["[ValueStartingPriceXcontainer]"].concat(args));
  };
  var warn = function warn() {
    var _console8;
    for (var _len8 = arguments.length, args = new Array(_len8), _key8 = 0; _key8 < _len8; _key8++) {
      args[_key8] = arguments[_key8];
    }
    return debug && (_console8 = console).warn.apply(_console8, ["[ValueStartingPriceXcontainer]"].concat(args));
  };
  log("Trigger name:", triggerName, "Target container:", targetIdSuffix);

  // Locate target container element to get prices from
  var targetContainer = document.getElementById("option_".concat(targetIdSuffix));
  if (!targetContainer) {
    warn("Target container not found:", "#option_".concat(targetIdSuffix));
    return;
  }

  /**
   * Gets the cheapest price from all options in the target container
   * @returns {string|null} The cheapest price or null if no valid prices found
   */
  var getCheapestPrice = function getCheapestPrice() {
    var radioInputs = targetContainer.querySelectorAll('input[type="radio"]:not(.option-none)');
    var cheapestPrice = null;
    radioInputs.forEach(function (radio) {
      var price = parseFloat(radio.dataset.price || radio.getAttribute("data-price"));
      if (!isNaN(price) && price > 0) {
        if (cheapestPrice === null || price < cheapestPrice) {
          cheapestPrice = price;
        }
      }
    });
    log("Cheapest price found:", cheapestPrice);
    return cheapestPrice;
  };

  /**
   * Updates the starting price display for the trigger option
   * @param {HTMLInputElement} triggerInput - The trigger radio input element
   * @param {string|null} price - The price to display
   */
  var updateTriggerStartingPrice = function updateTriggerStartingPrice(triggerInput, price) {
    if (!triggerInput) return;
    var label = document.querySelector("label.btn[for=\"".concat(triggerInput.id, "\"]"));
    var priceContainer = label === null || label === void 0 ? void 0 : label.querySelector(".col-auto:last-child");
    if (priceContainer) {
      priceContainer.textContent = price ? "(ab ".concat(price, " \u20AC)") : "";
      log("Updated trigger price display for", triggerInput.value, ":", price);
    }
  };

  /**
   * Main function to update starting prices
   */
  var updateStartingPrices = function updateStartingPrices() {
    var cheapestPrice = getCheapestPrice();

    // Find all trigger inputs that match the condition
    var triggerInputs = document.querySelectorAll("input[name=\"".concat(triggerName, "\"]"));
    triggerInputs.forEach(function (input) {
      if (condition(input.value)) {
        updateTriggerStartingPrice(input, cheapestPrice);
      }
    });
  };

  // Event handler for trigger changes
  var handleTriggerChange = function handleTriggerChange(event) {
    // Update starting prices whenever trigger changes
    setTimeout(updateStartingPrices, 10);
  };

  // Event handler for target container changes (when prices might change)
  var handleTargetChange = function handleTargetChange(event) {
    // Update starting prices when target options change
    updateStartingPrices();
  };

  // Event delegation for trigger inputs
  document.body.addEventListener("change", function (event) {
    var target = event.target;
    if (target.matches("input[name=\"".concat(triggerName, "\"]"))) {
      handleTriggerChange.call(target, event);
    }
  });

  // Event delegation for target container inputs (in case prices change dynamically)
  document.body.addEventListener("change", function (event) {
    var target = event.target;
    if (target.matches("#option_".concat(targetIdSuffix, " input[type=\"radio\"]"))) {
      handleTargetChange.call(target, event);
    }
  });

  // Initial run on page load
  setTimeout(function () {
    updateStartingPrices();
  }, 100);

  // Also run when DOM is fully loaded
  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", updateStartingPrices);
  } else {
    updateStartingPrices();
  }
}

/**
 * Future Enhancement Ideas:
 *
 * 1. Dependency chain validation:
 *    function validateDependencyChain(dependencies) {
 *      // Check for circular dependencies
 *      // Validate dependency order
 *    }
 *
 * 2. Dynamic dependency registration:
 *    const dependencyManager = {
 *      register(config) { ... },
 *      unregister(id) { ... },
 *      getActive() { ... }
 *    };
 *
 * 3. Animation support for show/hide transitions:
 *    function animateVisibility(element, show, options = {}) {
 *      // CSS transition or animation support
 *    }
 *
 * 4. Dependency state persistence:
 *    function saveDependencyState() {
 *      // Save current state to localStorage/sessionStorage
 *    }
 *
 * 5. Advanced condition operators:
 *    const CONDITIONS = {
 *      equals: (value, target) => value === target,
 *      includes: (value, array) => array.includes(value),
 *      range: (value, min, max) => value >= min && value <= max,
 *      regex: (value, pattern) => new RegExp(pattern).test(value)
 *    };
 */

/***/ }),

/***/ "./assets/js/configurator/dependencies/ablage.js":
/*!*******************************************************!*\
  !*** ./assets/js/configurator/dependencies/ablage.js ***!
  \*******************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _dependencies_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./../dependencies.js */ "./assets/js/configurator/dependencies.js");
/* harmony import */ var _variables_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../variables.js */ "./assets/js/configurator/variables.js");
/**
 * @version 2.3.0
 */



document.addEventListener("DOMContentLoaded", function () {
  /**
   * Values X Container
   */
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("ablage", function (value) {
    return value !== "";
  }, "ablage_breite");
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("ablage", function (value) {
    return value === "farbiges_glas";
  }, "ablage_farbe");
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("ablage", function (value) {
    return value === "getoentes_glas";
  }, "ablage_toenung");
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("ablage_farbe", function (value) {
    return value === "individuell";
  }, "ablage_farbton");
});

/***/ }),

/***/ "./assets/js/configurator/dependencies/ablagen.js":
/*!********************************************************!*\
  !*** ./assets/js/configurator/dependencies/ablagen.js ***!
  \********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _dependencies_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./../dependencies.js */ "./assets/js/configurator/dependencies.js");
/* harmony import */ var _variables_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../variables.js */ "./assets/js/configurator/variables.js");
/**
 * @version 2.3.0
 */



document.addEventListener("DOMContentLoaded", function () {
  /**
   * Values X Container
   */
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("ablagen", function (value) {
    return value !== "";
  }, "ablagen_typ");
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("ablagen_typ", function (value) {
    return value === "farbiges_glas";
  }, "ablagen_farbe");
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("ablagen_typ", function (value) {
    return value === "getoentes_glas";
  }, "ablagen_toenung");
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("ablagen_farbe", function (value) {
    return value === "individuell";
  }, "ablagen_farbton");
});

/***/ }),

/***/ "./assets/js/configurator/dependencies/ambientelicht.js":
/*!**************************************************************!*\
  !*** ./assets/js/configurator/dependencies/ambientelicht.js ***!
  \**************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony import */ var core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! core-js/modules/es.array.includes.js */ "./node_modules/core-js/modules/es.array.includes.js");
/* harmony import */ var core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var core_js_modules_es_string_includes_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! core-js/modules/es.string.includes.js */ "./node_modules/core-js/modules/es.string.includes.js");
/* harmony import */ var core_js_modules_es_string_includes_js__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_string_includes_js__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var _dependencies_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./../dependencies.js */ "./assets/js/configurator/dependencies.js");
/* harmony import */ var _variables_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../variables.js */ "./assets/js/configurator/variables.js");


/**
 * @version 2.3.0
 */



document.addEventListener("DOMContentLoaded", function () {
  /**
   * Values X Values
   */
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_2__.dependenciesValuesXvalues)("ambientelicht_lichtfarbe", function (value) {
    return value === "warmweiss" || value === "neutralweiss" || value === "kaltweiss";
  }, "ambientelicht_bedienung", function (value) {
    return value === "ohne_extra_schalter" || value === "extra_schalter" || value.includes("drehdimmer") || value.includes("kippschalter") || value.includes("gestensensor") || value.includes("touch_sensor") || value.includes("gestensteuerung");
  });
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_2__.dependenciesValuesXvalues)("ambientelicht_lichtfarbe", function (value) {
    return value.includes("warm_bis_kalt");
  }, "ambientelicht_bedienung", function (value) {
    return value === "extra_gestensteuerung_plus" || value === "extra_fernbedienung";
  });
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_2__.dependenciesValuesXvalues)("ambientelicht_lichtfarbe", function (value) {
    return value.includes("rgb");
  }, "ambientelicht_bedienung", function (value) {
    return value === "extra_rgb_fernbedienung";
  });
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_2__.dependenciesValuesXvalues)("ambientelicht_lichtfarbe", function (value) {
    return value.includes("hue");
  }, "ambientelicht_bedienung", function (value) {
    return value === "philips_hue_app";
  });
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_2__.dependenciesValuesXvalues)("ambientelicht_bedienung", function (value) {
    return !(value.includes("touch_sensor") || value.includes("gestensteuerung"));
  }, "ambientelicht_bedienung_position", function (value) {
    return value === "linke_seite" || value === "rechte_seite";
  });
  /**
   * Values X Container
   */
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_2__.dependenciesValuesXcontainer)("ambientelicht", function (value) {
    return value !== "";
  }, "ambientelicht_lichtfarbe");
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_2__.dependenciesValuesXcontainer)("ambientelicht_lichtfarbe", function (value) {
    return value !== "" && value !== "philips_hue";
  }, "ambientelicht_bedienung");
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_2__.dependenciesValuesXcontainer)("ambientelicht_bedienung", function (value) {
    return value.includes("kippschalter") || value.includes("gestensensor") || value.includes("touch_sensor") || value.includes("gestensteuerung") || value === "schalter" || value === "drehdimmer";
  }, "ambientelicht_bedienung_position");
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_2__.dependenciesValuesXcontainer)("ambientelicht_lichtfarbe", function (value) {
    return value.includes("hue");
  }, "ambientelicht_philips_hue");
});

/***/ }),

/***/ "./assets/js/configurator/dependencies/anschluesse.js":
/*!************************************************************!*\
  !*** ./assets/js/configurator/dependencies/anschluesse.js ***!
  \************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _dependencies_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./../dependencies.js */ "./assets/js/configurator/dependencies.js");
/* harmony import */ var _variables_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../variables.js */ "./assets/js/configurator/variables.js");
/**
 * @version 2.3.0
 */



document.addEventListener("DOMContentLoaded", function () {
  /**
   * Values X Values
   */
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXvalues)("anschluesse", function (value) {
    return value === "1x_1x_steckdosen" || value === "2x_2x_steckdosen" || value === "1x_usb_1x_steckdose" || value === "1x_usb_2x_steckdosen";
  }, "anschluesse_position", function (value) {
    return value === "links_und_rechts";
  });
  /**
   * Values X Container
   */
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("anschluesse", function (value) {
    return value !== "";
  }, "anschluesse_position");
});

/***/ }),

/***/ "./assets/js/configurator/dependencies/auflage.js":
/*!********************************************************!*\
  !*** ./assets/js/configurator/dependencies/auflage.js ***!
  \********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _dependencies_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./../dependencies.js */ "./assets/js/configurator/dependencies.js");
/* harmony import */ var _variables_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../variables.js */ "./assets/js/configurator/variables.js");
/**
 * @version 2.3.0
 */



document.addEventListener("DOMContentLoaded", function () {
  /**
   * Values X Container
   */
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("auflage", function (value) {
    return value === "farbiges_glas";
  }, "auflage_farbe");
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("auflage", function (value) {
    return value === "getoentes_glas";
  }, "auflage_toenung");
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("auflage", function (value) {
    return value === "keramik";
  }, "auflage_keramik");
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("auflage_farbe", function (value) {
    return value === "individuell";
  }, "auflage_farbton");
});

/***/ }),

/***/ "./assets/js/configurator/dependencies/ausschnitte.js":
/*!************************************************************!*\
  !*** ./assets/js/configurator/dependencies/ausschnitte.js ***!
  \************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _dependencies_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./../dependencies.js */ "./assets/js/configurator/dependencies.js");
/* harmony import */ var _variables_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../variables.js */ "./assets/js/configurator/variables.js");
/**
 * @version 2.3.0
 */



document.addEventListener("DOMContentLoaded", function () {
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("ausschnitte", function (value) {
    return value === "1x";
  }, "ausschnitt_durchmesser");
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("ausschnitte", function (value) {
    return value === "1x";
  }, "ausschnitt_abstand_links");
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("ausschnitte", function (value) {
    return value === "1x";
  }, "ausschnitt_abstand_rechts");
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("ausschnitte", function (value) {
    return value === "2x";
  }, "ausschnitt_1_durchmesser");
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("ausschnitte", function (value) {
    return value === "2x";
  }, "ausschnitt_1_abstand_links");
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("ausschnitte", function (value) {
    return value === "2x";
  }, "ausschnitt_1_abstand_rechts");
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("ausschnitte", function (value) {
    return value === "2x";
  }, "ausschnitt_2_durchmesser");
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("ausschnitte", function (value) {
    return value === "2x";
  }, "ausschnitt_2_abstand_links");
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("ausschnitte", function (value) {
    return value === "2x";
  }, "ausschnitt_2_abstand_rechts");
});

/***/ }),

/***/ "./assets/js/configurator/dependencies/bedienung.js":
/*!**********************************************************!*\
  !*** ./assets/js/configurator/dependencies/bedienung.js ***!
  \**********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony import */ var core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! core-js/modules/es.array.includes.js */ "./node_modules/core-js/modules/es.array.includes.js");
/* harmony import */ var core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var core_js_modules_es_string_includes_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! core-js/modules/es.string.includes.js */ "./node_modules/core-js/modules/es.string.includes.js");
/* harmony import */ var core_js_modules_es_string_includes_js__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_string_includes_js__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var _dependencies_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./../dependencies.js */ "./assets/js/configurator/dependencies.js");
/* harmony import */ var _variables_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./../variables.js */ "./assets/js/configurator/variables.js");


/**
 * @version 2.3.0
 */



document.addEventListener("DOMContentLoaded", function () {
  /**
   * Values X Values
   */
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_2__.dependenciesValuesXvalues)("bedienung", function (value) {
    return !(value.includes("touch_sensor") || value.includes("gestensteuerung"));
  }, "bedienung_position", function (value) {
    return value === "linke_seite" || value === "rechte_seite";
  });
  //
  //  isLOR
  //
  if (_variables_js__WEBPACK_IMPORTED_MODULE_3__.isLOR || _variables_js__WEBPACK_IMPORTED_MODULE_3__.isLR) {
    (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_2__.dependenciesValuesXvalues)("bedienung", function (value) {
      return value.includes("kippschalter") || value.includes("gestensensor");
    }, "bedienung_position", function (value) {
      return value.includes("linke_seite") || value.includes("rechte_seite");
    });
  }
  //
  //  isOU
  //
  if (_variables_js__WEBPACK_IMPORTED_MODULE_3__.isOU) {
    (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_2__.dependenciesValuesXvalues)("bedienung", function (value) {
      return value.includes("kippschalter") || value.includes("gestensensor");
    }, "bedienung_position", function (value) {
      return value.includes("unten-mittig") || value.includes("unten-links") || value.includes("unten-rechts");
    });
  }
  //
  //  isRund
  //
  if (_variables_js__WEBPACK_IMPORTED_MODULE_3__.isRund) {
    (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_2__.dependenciesValuesXvalues)("bedienung", function (value) {
      return value.includes("touch_sensor") || value.includes("gestensteuerung");
    }, "bedienung_position", function (value) {
      return value.includes("rechts_3_uhr") || value.includes("links_9_uhr")
      // || value.includes('oben_12_uhr')
      ;
    });
  }
  /**
   * Values X Container
   */
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_2__.dependenciesValuesXcontainer)("bedienung", function (value) {
    return value.includes("kippschalter") || value.includes("gestensensor") || value.includes("touch_sensor") || value.includes("gestensteuerung") || value === "schalter" || value === "drehdimmer";
  }, "bedienung_position");
});

/***/ }),

/***/ "./assets/js/configurator/dependencies/dachschraege.js":
/*!*************************************************************!*\
  !*** ./assets/js/configurator/dependencies/dachschraege.js ***!
  \*************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   autoDetectDachschraegePosition: () => (/* binding */ autoDetectDachschraegePosition),
/* harmony export */   getDachschraegePosition: () => (/* binding */ getDachschraegePosition),
/* harmony export */   getDachschraegeValidationMessage: () => (/* binding */ getDachschraegeValidationMessage),
/* harmony export */   getDependentFields: () => (/* binding */ getDependentFields),
/* harmony export */   isDachschraegeField: () => (/* binding */ isDachschraegeField),
/* harmony export */   onDachschraegeFieldChange: () => (/* binding */ onDachschraegeFieldChange),
/* harmony export */   onDachschraegeFieldInput: () => (/* binding */ onDachschraegeFieldInput),
/* harmony export */   setDachschraegePosition: () => (/* binding */ setDachschraegePosition),
/* harmony export */   suggestOptimalPosition: () => (/* binding */ suggestOptimalPosition),
/* harmony export */   triggerCarouselHeightUpdate: () => (/* binding */ triggerCarouselHeightUpdate),
/* harmony export */   updateDachschraegeConstraints: () => (/* binding */ updateDachschraegeConstraints),
/* harmony export */   validateAndUpdateAllPositionFields: () => (/* binding */ validateAndUpdateAllPositionFields),
/* harmony export */   validateBreiteOben: () => (/* binding */ validateBreiteOben),
/* harmony export */   validateDachschraegePosition: () => (/* binding */ validateDachschraegePosition),
/* harmony export */   validateHoeheRelation: () => (/* binding */ validateHoeheRelation)
/* harmony export */ });
/* harmony import */ var core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! core-js/modules/es.array.includes.js */ "./node_modules/core-js/modules/es.array.includes.js");
/* harmony import */ var core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var core_js_modules_es_function_name_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! core-js/modules/es.function.name.js */ "./node_modules/core-js/modules/es.function.name.js");
/* harmony import */ var core_js_modules_es_function_name_js__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_function_name_js__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! core-js/modules/es.object.to-string.js */ "./node_modules/core-js/modules/es.object.to-string.js");
/* harmony import */ var core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! core-js/modules/web.dom-collections.for-each.js */ "./node_modules/core-js/modules/web.dom-collections.for-each.js");
/* harmony import */ var core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_3__);
/* harmony import */ var _dependencies_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./../dependencies.js */ "./assets/js/configurator/dependencies.js");
/* harmony import */ var _variables_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./../variables.js */ "./assets/js/configurator/variables.js");




/**
 * @version 2.3.0
 *
 * @todo Optimize edge cases, i.e. refresh page, dachschraege position is default on "links", set "hoehe links" to 200 and then "hoehe rechts" to 100, but of them needs to be always above 400, actually specifically dependent on "dachschraege position"
 */




/**
 * Dachschräge position input reference
 */
var dachschraegePositionInputs = document.querySelectorAll('input[name="dachschraege_position"]');

/**
 * Dachschräge Validation Module
 *
 * This module provides specialized validation logic for sloped roof configurations.
 * It works as a plugin for the central input validation system (input.js).
 *
 * ARCHITECTURE:
 * - Pure business logic module (no direct DOM event listeners)
 * - Provides callback functions for input.js event system
 * - Exports validation functions used by central validation engine
 * - Only handles position radio button events (not covered by input.js)
 *
 * Handles validation for:
 * - breite_unten: bottom width (base reference)
 * - breite_oben: top width (must not exceed bottom width)
 * - hoehe_links: left height
 * - hoehe_rechts: right height
 * - dachschraege_position: controls which height can be reduced to 0
 */

/**
 * Gets the currently selected Dachschräge position
 * @returns {string|null} 'links', 'rechts', or null if none selected
 */
function getDachschraegePosition() {
  var checkedInput = document.querySelector('input[name="dachschraege_position"]:checked');
  return checkedInput ? checkedInput.value : null;
}

/**
 * Updates min/max constraints based on Dachschräge position
 * - Position "links": hoehe_links can be 0, hoehe_rechts keeps original min
 * - Position "rechts": hoehe_rechts can be 0, hoehe_links keeps original min
 * - breite_oben: always min=0 regardless of position
 */
function updateDachschraegeConstraints() {
  var position = getDachschraegePosition();

  // Store original min values if not already stored
  if (_variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput && !_variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput.dataset.originalMin) {
    _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput.dataset.originalMin = _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput.getAttribute("min") || "400";
  }
  if (_variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheRechtsInput && !_variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheRechtsInput.dataset.originalMin) {
    _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheRechtsInput.dataset.originalMin = _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheRechtsInput.getAttribute("min") || "400";
  }

  // Always set breite_oben min to 0
  if (_variables_js__WEBPACK_IMPORTED_MODULE_5__.breiteObenInput) {
    _variables_js__WEBPACK_IMPORTED_MODULE_5__.breiteObenInput.setAttribute("min", "0");
  }

  // Update height constraints based on position
  if (position === "links") {
    // Links: hoehe_links can be 0, hoehe_rechts keeps original min
    if (_variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput) {
      _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput.setAttribute("min", "0");
    }
    if (_variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheRechtsInput) {
      _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheRechtsInput.setAttribute("min", _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheRechtsInput.dataset.originalMin);
    }
  } else if (position === "rechts") {
    // Rechts: hoehe_rechts can be 0, hoehe_links keeps original min
    if (_variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput) {
      _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput.setAttribute("min", _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput.dataset.originalMin);
    }
    if (_variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheRechtsInput) {
      _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheRechtsInput.setAttribute("min", "0");
    }
  } else {
    // No position selected: restore original min values for both heights
    if (_variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput) {
      _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput.setAttribute("min", _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput.dataset.originalMin);
    }
    if (_variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheRechtsInput) {
      _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheRechtsInput.setAttribute("min", _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheRechtsInput.dataset.originalMin);
    }
  }
}

/**
 * Validates Dachschräge position constraints
 * Ensures that at least one height is above minimum when a position is selected
 * @returns {string|null} Error message or null if valid
 */
function validateDachschraegePosition() {
  var position = getDachschraegePosition();
  if (!position) return null;
  var hoeheLinks = parseFloat(_variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput === null || _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput === void 0 ? void 0 : _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput.value) || 0;
  var hoeheRechts = parseFloat(_variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheRechtsInput === null || _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheRechtsInput === void 0 ? void 0 : _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheRechtsInput.value) || 0;
  var originalMinHeight = parseFloat(_variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput === null || _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput === void 0 ? void 0 : _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput.dataset.originalMin) || 400;
  if (position === "links") {
    // Wenn links gewählt, muss rechte Höhe mindestens originalMin sein
    if (hoeheRechts < originalMinHeight) {
      return "Bei Dachschr\xE4ge Position \"links\" muss die rechte H\xF6he mindestens ".concat(originalMinHeight, " mm betragen.");
    }
  } else if (position === "rechts") {
    // Wenn rechts gewählt, muss linke Höhe mindestens originalMin sein
    if (hoeheLinks < originalMinHeight) {
      return "Bei Dachschr\xE4ge Position \"rechts\" muss die linke H\xF6he mindestens ".concat(originalMinHeight, " mm betragen.");
    }
  }
  return null;
}

/**
 * Auto-adjusts Dachschräge position based on height values
 * This provides intelligent suggestions when user enters asymmetric heights
 * @deprecated Use suggestOptimalPosition() instead for better UX
 */
function autoDetectDachschraegePosition() {
  suggestOptimalPosition();
}

/**
 * Sets the Dachschräge position programmatically
 * @param {string} position - 'links' or 'rechts'
 */
function setDachschraegePosition(position) {
  var targetInput = document.querySelector("input[name=\"dachschraege_position\"][value=\"".concat(position, "\"]"));
  if (targetInput) {
    targetInput.checked = true;
    targetInput.dispatchEvent(new Event("change", {
      bubbles: true
    }));
  }
}

/**
 * Validates that top width does not exceed bottom width
 * @param {HTMLElement} breiteObenField - Top width input field
 * @param {HTMLElement} breiteUntenField - Bottom width input field
 * @returns {string|null} Error message or null if valid
 */
function validateBreiteOben(breiteObenField, breiteUntenField) {
  var breiteOben = parseFloat(breiteObenField === null || breiteObenField === void 0 ? void 0 : breiteObenField.value) || 0;
  var breiteUnten = parseFloat(breiteUntenField === null || breiteUntenField === void 0 ? void 0 : breiteUntenField.value) || 0;
  if (breiteOben > breiteUnten) {
    return 'Die "Breite oben" darf nicht größer sein als die "Breite unten".';
  }
  return null;
}

/**
 * Validates height relationship with Dachschräge position constraints
 * @param {HTMLElement} hoeheLinksField - Left height input field
 * @param {HTMLElement} hoeheRechtsField - Right height input field
 * @returns {string|null} Error message or null if valid
 */
function validateHoeheRelation(hoeheLinksField, hoeheRechtsField) {
  // First check Dachschräge position constraints
  var positionError = validateDachschraegePosition();
  if (positionError) {
    return positionError;
  }

  // Additional height relationship validation can be added here
  return null;
}

/**
 * Gets validation message for a specific field based on Dachschräge rules
 * Only validates input fields, not radio buttons (position validation happens internally)
 * @param {HTMLElement} field - The field to validate
 * @returns {string|null} Error message or null if valid
 */
function getDachschraegeValidationMessage(field) {
  if (!field) return null;
  switch (field.name) {
    case "breite_oben":
      // Only validate breite_oben against breite_unten
      return validateBreiteOben(field, _variables_js__WEBPACK_IMPORTED_MODULE_5__.breiteUntenInput);
    case "hoehe_links":
      // Only validate left height constraints - don't trigger right height validation here
      var position = getDachschraegePosition();
      if (position === "links") {
        // Bei Position "links" kann das linke Höhe-Feld unter dem ursprünglichen Minimum sein
        // da hier die Dachschräge ist - keine zusätzliche Validierung nötig
      } else if (position === "rechts") {
        // Bei Position "rechts" muss das linke Höhe-Feld mindestens das ursprüngliche Minimum haben
        var hoeheLinks = parseFloat(field.value) || 0;
        var originalMinHeight = parseFloat(field.dataset.originalMin) || 400;
        if (hoeheLinks < originalMinHeight) {
          return "Bei Dachschr\xE4ge Position \"rechts\" muss die linke H\xF6he mindestens ".concat(originalMinHeight, " mm betragen.");
        }
      }

      // Basic min/max validation (this is handled by input.js, but we include it for completeness)
      var minValue = parseFloat(field.getAttribute("min"));
      var currentValue = parseFloat(field.value) || 0;
      if (!isNaN(minValue) && currentValue < minValue) {
        return "Der Wert muss mindestens ".concat(minValue, " mm betragen.");
      }
      return null;
    case "hoehe_rechts":
      // Only validate right height constraints - don't trigger left height validation here
      var positionRight = getDachschraegePosition();
      if (positionRight === "rechts") {
        // Bei Position "rechts" kann das rechte Höhe-Feld unter dem ursprünglichen Minimum sein
        // da hier die Dachschräge ist - keine zusätzliche Validierung nötig
      } else if (positionRight === "links") {
        // Bei Position "links" muss das rechte Höhe-Feld mindestens das ursprüngliche Minimum haben
        var hoeheRechts = parseFloat(field.value) || 0;
        var _originalMinHeight = parseFloat(_variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheRechtsInput === null || _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheRechtsInput === void 0 ? void 0 : _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheRechtsInput.dataset.originalMin) || 400;
        if (hoeheRechts < _originalMinHeight) {
          return "Bei Dachschr\xE4ge Position \"links\" muss die rechte H\xF6he mindestens ".concat(_originalMinHeight, " mm betragen.");
        }
      }

      // Basic min/max validation (this is handled by input.js, but we include it for completeness)
      var minValueRight = parseFloat(field.getAttribute("min"));
      var currentValueRight = parseFloat(field.value) || 0;
      if (!isNaN(minValueRight) && currentValueRight < minValueRight) {
        return "Der Wert muss mindestens ".concat(minValueRight, " mm betragen.");
      }
      return null;

    // Radio button validation removed - position validation happens internally
    // and doesn't need visual feedback containers
    default:
      return null;
  }
}

/**
 * Gets all fields that should be re-validated when a specific field changes
 * This is primarily used for cross-field validation after user completes input
 * @param {HTMLElement} field - The field that changed
 * @returns {HTMLElement[]} Array of fields to re-validate
 */
function getDependentFields(field) {
  if (!field) return [];
  var dependentFields = [];
  switch (field.name) {
    case "breite_unten":
      // When bottom width changes, re-validate top width (for breite_oben > breite_unten check)
      if (_variables_js__WEBPACK_IMPORTED_MODULE_5__.breiteObenInput) dependentFields.push(_variables_js__WEBPACK_IMPORTED_MODULE_5__.breiteObenInput);
      break;
    case "breite_oben":
      // Top width validation is self-contained - no dependent fields needed
      break;
    case "hoehe_links":
      // For height fields, we don't immediately validate the other height field
      // This prevents double validation messages during input
      // Cross-validation happens in change event via onDachschraegeFieldChange
      break;
    case "hoehe_rechts":
      // For height fields, we don't immediately validate the other height field
      // This prevents double validation messages during input
      // Cross-validation happens in change event via onDachschraegeFieldChange
      break;
    case "dachschraege_position":
      // Position radio buttons are handled internally - no dependent fields for validation UI
      // Constraint updates and validation happen through position change event listeners
      updateDachschraegeConstraints();
      if (_variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput) dependentFields.push(_variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput);
      if (_variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheRechtsInput) dependentFields.push(_variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheRechtsInput);

      // Trigger comprehensive update for position changes
      setTimeout(function () {
        validateAndUpdateAllPositionFields();
      }, 10);
      break;
  }

  // Trigger carousel height update after dependent field validation
  if (dependentFields.length > 0) {
    triggerCarouselHeightUpdate();
  }
  return dependentFields;
}

/**
 * Checks if a field is part of Dachschräge configuration
 * @param {HTMLElement} field - The field to check
 * @returns {boolean} True if field is part of Dachschräge
 */
function isDachschraegeField(field) {
  if (!field) return false;

  // Only handle input fields, not radio buttons (radio buttons are handled separately)
  var dachschraegeInputFields = ["breite_unten", "breite_oben", "hoehe_links", "hoehe_rechts"];
  return dachschraegeInputFields.includes(field.name);
}

/**
 * Triggers carousel height update after validation changes
 * Should be called whenever invalid feedback is shown/hidden
 */
function triggerCarouselHeightUpdate() {
  // Small delay to ensure DOM changes (validation feedback) are rendered
  setTimeout(function () {
    if (window.updateCarouselHeight) {
      window.updateCarouselHeight();
    }
  }, 100);
}

/**
 * Validates and updates all Dachschräge-related fields
 * This ensures complete synchronization between all fields
 */
function validateAndUpdateAllPositionFields() {
  // Update constraints first
  updateDachschraegeConstraints();

  // Validate all fields if validation function is available
  if (window.validateField) {
    // Validate height fields (these trigger position-specific validation)
    if (_variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput) {
      window.validateField(_variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput);
    }
    if (_variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheRechtsInput) {
      window.validateField(_variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheRechtsInput);
    }

    // Validate width fields
    if (_variables_js__WEBPACK_IMPORTED_MODULE_5__.breiteObenInput) {
      window.validateField(_variables_js__WEBPACK_IMPORTED_MODULE_5__.breiteObenInput);
    }
    if (_variables_js__WEBPACK_IMPORTED_MODULE_5__.breiteUntenInput) {
      window.validateField(_variables_js__WEBPACK_IMPORTED_MODULE_5__.breiteUntenInput);
    }

    // Note: Position radio buttons are not validated via validateField
    // to prevent invalid-feedback containers from being created
  }

  // Update carousel height after all validations
  triggerCarouselHeightUpdate();
}

/**
 * Provides smart suggestions for position based on current height values
 * More sophisticated than simple auto-detection
 */
function suggestOptimalPosition() {
  var hoeheLinks = parseFloat(_variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput === null || _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput === void 0 ? void 0 : _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput.value) || 0;
  var hoeheRechts = parseFloat(_variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheRechtsInput === null || _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheRechtsInput === void 0 ? void 0 : _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheRechtsInput.value) || 0;
  var originalMinHeight = parseFloat(_variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput === null || _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput === void 0 ? void 0 : _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput.dataset.originalMin) || 400;
  var currentPosition = getDachschraegePosition();

  // Determine optimal position based on values
  var suggestedPosition = null;

  // Case 1: Left height is below minimum, right height is acceptable
  if (hoeheLinks < originalMinHeight && hoeheRechts >= originalMinHeight) {
    suggestedPosition = "links";
  }
  // Case 2: Right height is below minimum, left height is acceptable
  else if (hoeheRechts < originalMinHeight && hoeheLinks >= originalMinHeight) {
    suggestedPosition = "rechts";
  }
  // Case 3: Both heights are acceptable - validate current position
  else if (hoeheLinks >= originalMinHeight && hoeheRechts >= originalMinHeight) {
    // Keep current position if it's valid, otherwise suggest based on smaller value
    if (currentPosition) {
      // Current position is fine, no change needed
      return;
    } else {
      // No position selected, suggest based on which side is smaller
      suggestedPosition = hoeheLinks <= hoeheRechts ? "links" : "rechts";
    }
  }
  // Case 4: Both heights are below minimum - this is an invalid state
  else if (hoeheLinks < originalMinHeight && hoeheRechts < originalMinHeight) {
    // Suggest position based on which height is closer to minimum
    var linksDistance = originalMinHeight - hoeheLinks;
    var rechtsDistance = originalMinHeight - hoeheRechts;
    suggestedPosition = linksDistance <= rechtsDistance ? "links" : "rechts";
  }

  // Only change if suggestion is different from current position
  if (suggestedPosition && suggestedPosition !== currentPosition) {
    setDachschraegePosition(suggestedPosition);

    // Visual feedback that position was auto-adjusted
    setTimeout(function () {
      var positionGroup = document.querySelector('[data-key="dachschraege_position"]');
      if (positionGroup) {
        positionGroup.classList.add("focus-ring-success");
        setTimeout(function () {
          positionGroup.classList.remove("focus-ring-success");
        }, 2000);
      }
    }, 100);
  }
}

/**
 * Callback function for input events on Dachschräge fields
 * Called by input.js when a Dachschräge field receives input
 * @param {HTMLElement} field - The field that received input
 */
function onDachschraegeFieldInput(field) {
  // Handle dependent field validation for real-time feedback
  var dependentFields = getDependentFields(field);
  dependentFields.forEach(function (dependentField) {
    setTimeout(function () {
      if (window.validateField) {
        window.validateField(dependentField);
      }
    }, 0);
  });
}

/**
 * Callback function for change events on Dachschräge fields
 * Called by input.js when a Dachschräge field value changes (user completes input)
 * @param {HTMLElement} field - The field that changed
 */
function onDachschraegeFieldChange(field) {
  // Handle position-specific logic for height fields
  if (field.name === "hoehe_links" || field.name === "hoehe_rechts") {
    // Smart position suggestion based on height values
    suggestOptimalPosition();

    // Re-validate the other height field after position might have changed
    var otherHeightField = field.name === "hoehe_links" ? _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheRechtsInput : _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput;
    if (otherHeightField && window.validateField) {
      setTimeout(function () {
        window.validateField(otherHeightField);
      }, 100); // Small delay to ensure position change is processed
    }
  }

  // Validate dependent fields for all field types
  var dependentFields = getDependentFields(field);
  dependentFields.forEach(function (dependentField) {
    setTimeout(function () {
      if (window.validateField) {
        window.validateField(dependentField);
      }
    }, 0);
  });

  // Trigger position validation and suggestions for height fields
  if (field.name === "hoehe_links" || field.name === "hoehe_rechts") {
    setTimeout(function () {
      validateAndUpdateAllPositionFields();
    }, 50);
  }
}

/**
 * Initialize Dachschräge dependencies on DOM ready
 */
document.addEventListener("DOMContentLoaded", function () {
  // Initialize constraints based on current position
  updateDachschraegeConstraints();

  // Add event listeners ONLY for position radio buttons (not handled by input.js)
  dachschraegePositionInputs.forEach(function (input) {
    input.addEventListener("change", function () {
      // Immediate constraint update for better responsiveness
      updateDachschraegeConstraints();

      // Comprehensive validation of all related fields
      setTimeout(function () {
        validateAndUpdateAllPositionFields();
      }, 20);
    });
  });

  // Initial auto-detection with improved logic
  setTimeout(function () {
    if (_variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput !== null && _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput !== void 0 && _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput.value && _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput.value !== "0" || _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheRechtsInput !== null && _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheRechtsInput !== void 0 && _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheRechtsInput.value && _variables_js__WEBPACK_IMPORTED_MODULE_5__.hoeheRechtsInput.value !== "0") {
      suggestOptimalPosition();
      validateAndUpdateAllPositionFields();
    }
  }, 200); // Increased delay for complete DOM readiness
});

/***/ }),

/***/ "./assets/js/configurator/dependencies/digital-uhr.js":
/*!************************************************************!*\
  !*** ./assets/js/configurator/dependencies/digital-uhr.js ***!
  \************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _dependencies_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./../dependencies.js */ "./assets/js/configurator/dependencies.js");
/* harmony import */ var _variables_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./../variables.js */ "./assets/js/configurator/variables.js");
/**
 * @version 2.3.0
 */



document.addEventListener("DOMContentLoaded", function () {
  /**
   * Values X Container
   */
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("digital_uhr", function (value) {
    return value === "ja";
  }, "digital_uhr_position");
});

/***/ }),

/***/ "./assets/js/configurator/dependencies/lichtfarbe.js":
/*!***********************************************************!*\
  !*** ./assets/js/configurator/dependencies/lichtfarbe.js ***!
  \***********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony import */ var core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! core-js/modules/es.array.includes.js */ "./node_modules/core-js/modules/es.array.includes.js");
/* harmony import */ var core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var core_js_modules_es_string_includes_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! core-js/modules/es.string.includes.js */ "./node_modules/core-js/modules/es.string.includes.js");
/* harmony import */ var core_js_modules_es_string_includes_js__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_string_includes_js__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var _dependencies_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./../dependencies.js */ "./assets/js/configurator/dependencies.js");
/* harmony import */ var _variables_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./../variables.js */ "./assets/js/configurator/variables.js");


/**
 * @version 2.3.0
 */



document.addEventListener("DOMContentLoaded", function () {
  /**
   * Values X Values
   */
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_2__.dependenciesValuesXvalues)("lichtfarbe", function (value) {
    return value === "warmweiss" || value === "neutralweiss" || value === "kaltweiss";
  }, "bedienung", function (value) {
    return value === "ueber_eigenen_wandschalter" || value === "schalter" || value === "drehdimmer" || value === "kippschalter" || value === "gestensensor" || value === "touch_sensor" || value === "touch_sensor_inkl_dimmfunktion" || value === "gestensteuerung_inkl_dimmfunktion";
  });
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_2__.dependenciesValuesXvalues)("lichtfarbe", function (value) {
    return value.includes("warm_und_kalt");
  }, "bedienung", function (value) {
    return value.includes("doppel");
  });
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_2__.dependenciesValuesXvalues)("lichtfarbe", function (value) {
    return value.includes("warm_bis_kalt");
  }, "bedienung", function (value) {
    return value === "gestensteuerung_plus" || value === "fernbedienung";
  });
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_2__.dependenciesValuesXvalues)("lichtfarbe", function (value) {
    return value.includes("rgb");
  }, "bedienung", function (value) {
    return value === "rgb_fernbedienung";
  });
  // dependenciesValuesXvalues(
  //     "bedienung",
  //     value => {
  //         return !(
  //             value.includes("touch_sensor") ||
  //             value.includes("gestensteuerung")
  //         );
  //     },
  //     "bedienung_position",
  //     value => {
  //         return (
  //             value === "linke_seite" ||
  //             value === "rechte_seite");
  //     }
  // );
  /**
   * Values X Container
   */
  // dependenciesValuesXcontainer(
  //     'lichtfarbe', value => {
  //         return (
  //             value === "warmweiss" ||
  //             value === "neutralweiss" ||
  //             value === "kaltweiss"
  //         );
  //     },
  //     'lichtstaerke'
  // );
});

/***/ }),

/***/ "./assets/js/configurator/dependencies/philips-hue.js":
/*!************************************************************!*\
  !*** ./assets/js/configurator/dependencies/philips-hue.js ***!
  \************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony import */ var core_js_modules_es_symbol_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! core-js/modules/es.symbol.js */ "./node_modules/core-js/modules/es.symbol.js");
/* harmony import */ var core_js_modules_es_symbol_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_symbol_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var core_js_modules_es_symbol_description_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! core-js/modules/es.symbol.description.js */ "./node_modules/core-js/modules/es.symbol.description.js");
/* harmony import */ var core_js_modules_es_symbol_description_js__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_symbol_description_js__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var core_js_modules_es_symbol_iterator_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! core-js/modules/es.symbol.iterator.js */ "./node_modules/core-js/modules/es.symbol.iterator.js");
/* harmony import */ var core_js_modules_es_symbol_iterator_js__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_symbol_iterator_js__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var core_js_modules_es_array_concat_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! core-js/modules/es.array.concat.js */ "./node_modules/core-js/modules/es.array.concat.js");
/* harmony import */ var core_js_modules_es_array_concat_js__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_concat_js__WEBPACK_IMPORTED_MODULE_3__);
/* harmony import */ var core_js_modules_es_array_filter_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! core-js/modules/es.array.filter.js */ "./node_modules/core-js/modules/es.array.filter.js");
/* harmony import */ var core_js_modules_es_array_filter_js__WEBPACK_IMPORTED_MODULE_4___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_filter_js__WEBPACK_IMPORTED_MODULE_4__);
/* harmony import */ var core_js_modules_es_array_find_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! core-js/modules/es.array.find.js */ "./node_modules/core-js/modules/es.array.find.js");
/* harmony import */ var core_js_modules_es_array_find_js__WEBPACK_IMPORTED_MODULE_5___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_find_js__WEBPACK_IMPORTED_MODULE_5__);
/* harmony import */ var core_js_modules_es_array_from_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! core-js/modules/es.array.from.js */ "./node_modules/core-js/modules/es.array.from.js");
/* harmony import */ var core_js_modules_es_array_from_js__WEBPACK_IMPORTED_MODULE_6___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_from_js__WEBPACK_IMPORTED_MODULE_6__);
/* harmony import */ var core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! core-js/modules/es.array.includes.js */ "./node_modules/core-js/modules/es.array.includes.js");
/* harmony import */ var core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_7___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_7__);
/* harmony import */ var core_js_modules_es_array_iterator_js__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! core-js/modules/es.array.iterator.js */ "./node_modules/core-js/modules/es.array.iterator.js");
/* harmony import */ var core_js_modules_es_array_iterator_js__WEBPACK_IMPORTED_MODULE_8___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_iterator_js__WEBPACK_IMPORTED_MODULE_8__);
/* harmony import */ var core_js_modules_es_array_slice_js__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! core-js/modules/es.array.slice.js */ "./node_modules/core-js/modules/es.array.slice.js");
/* harmony import */ var core_js_modules_es_array_slice_js__WEBPACK_IMPORTED_MODULE_9___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_slice_js__WEBPACK_IMPORTED_MODULE_9__);
/* harmony import */ var core_js_modules_es_function_name_js__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! core-js/modules/es.function.name.js */ "./node_modules/core-js/modules/es.function.name.js");
/* harmony import */ var core_js_modules_es_function_name_js__WEBPACK_IMPORTED_MODULE_10___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_function_name_js__WEBPACK_IMPORTED_MODULE_10__);
/* harmony import */ var core_js_modules_es_object_entries_js__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! core-js/modules/es.object.entries.js */ "./node_modules/core-js/modules/es.object.entries.js");
/* harmony import */ var core_js_modules_es_object_entries_js__WEBPACK_IMPORTED_MODULE_11___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_object_entries_js__WEBPACK_IMPORTED_MODULE_11__);
/* harmony import */ var core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! core-js/modules/es.object.to-string.js */ "./node_modules/core-js/modules/es.object.to-string.js");
/* harmony import */ var core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_12___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_12__);
/* harmony import */ var core_js_modules_es_regexp_exec_js__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! core-js/modules/es.regexp.exec.js */ "./node_modules/core-js/modules/es.regexp.exec.js");
/* harmony import */ var core_js_modules_es_regexp_exec_js__WEBPACK_IMPORTED_MODULE_13___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_regexp_exec_js__WEBPACK_IMPORTED_MODULE_13__);
/* harmony import */ var core_js_modules_es_regexp_to_string_js__WEBPACK_IMPORTED_MODULE_14__ = __webpack_require__(/*! core-js/modules/es.regexp.to-string.js */ "./node_modules/core-js/modules/es.regexp.to-string.js");
/* harmony import */ var core_js_modules_es_regexp_to_string_js__WEBPACK_IMPORTED_MODULE_14___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_regexp_to_string_js__WEBPACK_IMPORTED_MODULE_14__);
/* harmony import */ var core_js_modules_es_string_includes_js__WEBPACK_IMPORTED_MODULE_15__ = __webpack_require__(/*! core-js/modules/es.string.includes.js */ "./node_modules/core-js/modules/es.string.includes.js");
/* harmony import */ var core_js_modules_es_string_includes_js__WEBPACK_IMPORTED_MODULE_15___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_string_includes_js__WEBPACK_IMPORTED_MODULE_15__);
/* harmony import */ var core_js_modules_es_string_iterator_js__WEBPACK_IMPORTED_MODULE_16__ = __webpack_require__(/*! core-js/modules/es.string.iterator.js */ "./node_modules/core-js/modules/es.string.iterator.js");
/* harmony import */ var core_js_modules_es_string_iterator_js__WEBPACK_IMPORTED_MODULE_16___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_string_iterator_js__WEBPACK_IMPORTED_MODULE_16__);
/* harmony import */ var core_js_modules_es_string_trim_js__WEBPACK_IMPORTED_MODULE_17__ = __webpack_require__(/*! core-js/modules/es.string.trim.js */ "./node_modules/core-js/modules/es.string.trim.js");
/* harmony import */ var core_js_modules_es_string_trim_js__WEBPACK_IMPORTED_MODULE_17___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_string_trim_js__WEBPACK_IMPORTED_MODULE_17__);
/* harmony import */ var core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_18__ = __webpack_require__(/*! core-js/modules/web.dom-collections.for-each.js */ "./node_modules/core-js/modules/web.dom-collections.for-each.js");
/* harmony import */ var core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_18___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_18__);
/* harmony import */ var core_js_modules_web_dom_collections_iterator_js__WEBPACK_IMPORTED_MODULE_19__ = __webpack_require__(/*! core-js/modules/web.dom-collections.iterator.js */ "./node_modules/core-js/modules/web.dom-collections.iterator.js");
/* harmony import */ var core_js_modules_web_dom_collections_iterator_js__WEBPACK_IMPORTED_MODULE_19___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_web_dom_collections_iterator_js__WEBPACK_IMPORTED_MODULE_19__);
/* harmony import */ var _variables_js__WEBPACK_IMPORTED_MODULE_20__ = __webpack_require__(/*! ../variables.js */ "./assets/js/configurator/variables.js");
/* harmony import */ var _dependencies_js__WEBPACK_IMPORTED_MODULE_21__ = __webpack_require__(/*! ../dependencies.js */ "./assets/js/configurator/dependencies.js");
function _slicedToArray(r, e) { return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray(r, e) || _nonIterableRest(); }
function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _iterableToArrayLimit(r, l) { var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (null != t) { var e, n, i, u, a = [], f = !0, o = !1; try { if (i = (t = t.call(r)).next, 0 === l) { if (Object(t) !== t) return; f = !1; } else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0); } catch (r) { o = !0, n = r; } finally { try { if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return; } finally { if (o) throw n; } } return a; } }
function _arrayWithHoles(r) { if (Array.isArray(r)) return r; }




















/**
 * Philips Hue Lighting Configuration Module
 *
 * This module handles the dynamic pricing and configuration logic for Philips Hue
 * lighting options in the product configurator. It manages both main lighting
 * and ambient lighting configurations, calculating appropriate pricing based on
 * the product category, dimensions, and selected lighting type.
 *
 * Features:
 * - Product category-specific lighting calculations
 * - Dynamic price calculation based on perimeter and lighting positions
 * - Automatic category selection for different perimeter ranges
 * - Price display updates for lightstrip variants (Plus vs Gradient)
 * - Reset functionality when switching away from Philips Hue
 * - Support for multiple product types (Badspiegel, Spiegelschrank, Cabinets)
 * - Incompatible option management (auto-hide Lichtstärke and Smart Home)
 * - Container visibility management for Philips Hue specific options
 *
 * Product Categories:
 * - Badspiegel: Main + Ambient lighting (uses position flags for main lighting)
 * - Spiegelschrank: Main + Ambient lighting (forced LORU for main lighting)
 * - Unterschrank/Hochschrank/Side-Lowboards: Ambient lighting only
 *
 * Main Lighting Logic:
 * - Badspiegel: Respects isLORU, isLOR, isLR, isOU, isO flags
 * - Spiegelschrank: Always calculates as LORU (all sides)
 * - Cabinets: No main lighting
 *
 * Ambient Lighting Logic:
 * - All categories: Based on selected ambient position options
 * - Calculates perimeter based on lighting position selections
 * - Supports various positions (waschbecken, decke, seiten, rundherum, etc.)
 *
 * Dependencies:
 * - variables.js: Dimension inputs, lighting position flags, and product category flags
 *
 * @version 2.3.0
 * @package Configurator
 */




// ========================================
// CONFIGURATION & CONSTANTS
// ========================================

/**
 * DEBUG MODE CONFIGURATION
 * Set to true to enable debug logging, false to disable all debug output
 */
var DEBUG_MODE = false;

/**
 * Perimeter categories for pricing lookup
 */
var PERIMETER_CATEGORIES = {
  2: "bis_2m",
  3: "bis_3m",
  4: "bis_4m",
  5: "bis_5m",
  6: "bis_6m",
  8: "bis_8m",
  9: "bis_9m",
  10: "bis_10m"
};

/**
 * Lighting type constants
 */
var LIGHTING_TYPES = {
  MAIN: "lichtfarbe",
  AMBIENT: "ambientelicht"
};

/**
 * Lightstrip variant constants
 */
var LIGHTSTRIP_VARIANTS = {
  PLUS: "lightstrip_plus",
  GRADIENT: "lightstrip_gradient"
};

// ========================================
// GLOBAL VARIABLES & STATE
// ========================================

var mainLightingElements = {};
var ambientLightingElements = {};
var allLightingInputs = {};

// ========================================
// INITIALIZATION & EVENT LISTENERS
// ========================================

/**
 * Initialize Philips Hue configuration on DOM ready
 */
document.addEventListener("DOMContentLoaded", function () {
  // Prevent multiple initializations
  if (window.philipsHueInitialized) {
    debugLog("Philips Hue module already initialized, skipping");
    return;
  }
  debugLog("Initializing Philips Hue module");
  window.philipsHueInitialized = true;
  initializeDOMElements();
  setupEventListeners();
  performInitialUpdates();
  debugLog("Philips Hue module initialization completed");
});

/**
 * Initialize DOM element references
 */
var initializeDOMElements = function initializeDOMElements() {
  // Main lighting elements
  mainLightingElements = {
    radioHue: document.querySelector("input[name='lichtfarbe'][value='philips_hue']"),
    radioOptions: document.querySelectorAll("input[name='lichtfarbe_philips_hue']"),
    selectPlus: document.querySelector("select[name='lichtfarbe_philips_hue_lightstrip_aufpreis']"),
    selectGradient: document.querySelector("select[name='lichtfarbe_philips_hue_gradient_aufpreis']")
  };

  // Ambient lighting elements
  ambientLightingElements = {
    radioHue: document.querySelector("input[value='philips_hue'][name='ambientelicht_lichtfarbe']"),
    radioOptions: document.querySelectorAll("input[name='ambientelicht_philips_hue']"),
    selectPlus: document.querySelector("select[name='ambientelicht_philips_hue_lightstrip_aufpreis']"),
    selectGradient: document.querySelector("select[name='ambientelicht_philips_hue_gradient_aufpreis']")
  };

  // All lighting color inputs for reset functionality
  allLightingInputs = {
    main: document.querySelectorAll("input[name='lichtfarbe']"),
    ambient: document.querySelectorAll("input[name='ambientelicht_lichtfarbe']")
  };
};

/**
 * Set up all event listeners
 */
var setupEventListeners = function setupEventListeners() {
  setupDimensionInputListeners();
  setupMainLightingListeners();
  setupAmbientLightingListeners();
  setupResetListeners();
  setupPhilipsHueDependencies();
};

/**
 * Set up dimension input listeners for real-time price updates
 */
var setupDimensionInputListeners = function setupDimensionInputListeners() {
  var dimensionInputs = [_variables_js__WEBPACK_IMPORTED_MODULE_20__.durchmesserInput, _variables_js__WEBPACK_IMPORTED_MODULE_20__.breiteInput, _variables_js__WEBPACK_IMPORTED_MODULE_20__.hoeheInput].filter(Boolean);
  dimensionInputs.forEach(function (input) {
    if (!input) return;
    ["input", "change", "blur"].forEach(function (eventType) {
      input.addEventListener(eventType, function () {
        if (isValidInput(input)) {
          debugLog("Dimension ".concat(eventType, " event triggered, updating Philips Hue configurations"));
          updateHueConfigurations();
        } else {
          debugLog("Invalid input for ".concat(eventType, " event:"), input.value);
        }
      });
    });
  });
};

/**
 * Set up main lighting Philips Hue option listeners
 */
var setupMainLightingListeners = function setupMainLightingListeners() {
  var _mainLightingElements;
  if (!hasMainLighting() || !((_mainLightingElements = mainLightingElements.radioOptions) !== null && _mainLightingElements !== void 0 && _mainLightingElements.length)) {
    debugLog("Main lighting not available or no radio options found for this product category");
    return;
  }
  debugLog("Setting up main lighting Philips Hue listeners");
  mainLightingElements.radioOptions.forEach(function (radio) {
    if (radio) {
      radio.addEventListener("change", function () {
        debugLog("Main lighting Philips Hue option changed:", radio.value);
        updateMainLightingHueConfiguration();
      });
    }
  });
};

/**
 * Set up ambient lighting Philips Hue option listeners
 */
var setupAmbientLightingListeners = function setupAmbientLightingListeners() {
  var _ambientLightingEleme;
  if (!hasAmbientLighting() || !((_ambientLightingEleme = ambientLightingElements.radioOptions) !== null && _ambientLightingEleme !== void 0 && _ambientLightingEleme.length)) {
    debugLog("Ambient lighting not available or no radio options found for this product category");
    return;
  }
  debugLog("Setting up ambient lighting Philips Hue listeners");
  ambientLightingElements.radioOptions.forEach(function (radio) {
    if (radio) {
      radio.addEventListener("change", function () {
        debugLog("Ambient lighting Philips Hue option changed:", radio.value);
        updateAmbientLightingHueConfiguration();
      });
    }
  });
};

/**
 * Set up reset listeners for lighting color changes
 */
var setupResetListeners = function setupResetListeners() {
  var _allLightingInputs$ma, _allLightingInputs$am;
  // Main lighting reset listeners
  if (hasMainLighting() && (_allLightingInputs$ma = allLightingInputs.main) !== null && _allLightingInputs$ma !== void 0 && _allLightingInputs$ma.length) {
    allLightingInputs.main.forEach(function (radio) {
      if (!radio) return;
      radio.addEventListener("change", function (event) {
        debugLog("Main lighting color changed:", event.target.value);
        if (event.target.value !== "philips_hue") {
          resetHueSelects(LIGHTING_TYPES.MAIN);
        }
      });
    });
  }

  // Ambient lighting reset listeners
  if (hasAmbientLighting() && (_allLightingInputs$am = allLightingInputs.ambient) !== null && _allLightingInputs$am !== void 0 && _allLightingInputs$am.length) {
    allLightingInputs.ambient.forEach(function (radio) {
      if (!radio) return;
      radio.addEventListener("change", function (event) {
        debugLog("Ambient lighting color changed:", event.target.value);
        if (event.target.value !== "philips_hue") {
          resetHueSelects(LIGHTING_TYPES.AMBIENT);
        }
      });
    });
  }
};

/**
 * Set up Philips Hue dependencies using the existing dependenciesValuesXcontainer function
 * This is much cleaner than duplicating the logic
 */
var setupPhilipsHueDependencies = function setupPhilipsHueDependencies() {
  // Hide incompatible containers when Philips Hue is selected (main lighting)
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_21__.dependenciesValuesXcontainer)("lichtfarbe", function (value) {
    return value !== "philips_hue";
  },
  // Show when NOT Philips Hue
  "lichtstaerke", DEBUG_MODE);
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_21__.dependenciesValuesXcontainer)("lichtfarbe", function (value) {
    return value !== "philips_hue";
  },
  // Show when NOT Philips Hue
  "smart_home", DEBUG_MODE);

  // Hide incompatible containers when Philips Hue is selected (ambient lighting)
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_21__.dependenciesValuesXcontainer)("ambientelicht_lichtfarbe", function (value) {
    return value !== "philips_hue";
  },
  // Show when NOT Philips Hue
  "lichtstaerke", DEBUG_MODE);
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_21__.dependenciesValuesXcontainer)("ambientelicht_lichtfarbe", function (value) {
    return value !== "philips_hue";
  },
  // Show when NOT Philips Hue
  "smart_home", DEBUG_MODE);

  // Show Philips Hue containers when Philips Hue is selected
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_21__.dependenciesValuesXcontainer)("lichtfarbe", function (value) {
    return value === "philips_hue";
  },
  // Show when Philips Hue
  "lichtfarbe_philips_hue", DEBUG_MODE);
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_21__.dependenciesValuesXcontainer)("ambientelicht_lichtfarbe", function (value) {
    return value === "philips_hue";
  },
  // Show when Philips Hue
  "ambientelicht_philips_hue", DEBUG_MODE);

  // Show Philips Hue App option only when lichtfarbe contains "hue"
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_21__.dependenciesValuesXvalues)("lichtfarbe", function (value) {
    return value.includes("hue");
  }, "bedienung", function (value) {
    return value === "philips_hue_app";
  }, DEBUG_MODE);
  debugLog("Philips Hue dependencies set up using dependenciesValuesXcontainer");
};

/**
 * Perform initial updates on page load
 */
var performInitialUpdates = function performInitialUpdates() {
  setTimeout(function () {
    debugLog("Performing initial updates");
    updateHueConfigurations();
  }, 100);
};

// ========================================
// MAIN UPDATE FUNCTIONS
// ========================================

/**
 * Updates both main and ambient lighting Hue configurations
 */
var updateHueConfigurations = function updateHueConfigurations() {
  updateMainLightingHueConfiguration();
  updateAmbientLightingHueConfiguration();
};

/**
 * Updates pricing and UI for main lighting Philips Hue options
 */
var updateMainLightingHueConfiguration = function updateMainLightingHueConfiguration() {
  if (!hasMainLighting()) {
    debugLog("Main lighting not available for this product category, skipping update");
    return;
  }
  if (!areElementsValid(mainLightingElements)) {
    debugLog("Main lighting Hue elements not available, skipping update");
    return;
  }
  updateLightingConfiguration(LIGHTING_TYPES.MAIN, mainLightingElements);
};

/**
 * Updates pricing and UI for ambient lighting Philips Hue options
 */
var updateAmbientLightingHueConfiguration = function updateAmbientLightingHueConfiguration() {
  if (!hasAmbientLighting()) {
    debugLog("Ambient lighting not available for this product category, skipping update");
    return;
  }
  if (!areElementsValid(ambientLightingElements)) {
    debugLog("Ambient lighting Hue elements not available, skipping update");
    return;
  }
  updateLightingConfiguration(LIGHTING_TYPES.AMBIENT, ambientLightingElements);
};

/**
 * Generic function to update lighting configuration for both main and ambient lighting
 */
var updateLightingConfiguration = function updateLightingConfiguration(lightingType, elements) {
  try {
    var perimeter = calculatePerimeter(lightingType);
    if (perimeter === 0) {
      debugLog("No valid perimeter calculated for ".concat(lightingType));
      return;
    }
    var category = mapPerimeterToCategory(perimeter);
    if (!category) {
      debugLog("No category found for perimeter:", perimeter);
      return;
    }
    var selectedType = getSelectedLightingType(lightingType);
    var _getPricesForCategory = getPricesForCategory(elements, category),
      plusPrice = _getPricesForCategory.plusPrice,
      gradientPrice = _getPricesForCategory.gradientPrice;
    debugLog("".concat(lightingType, " update:"), {
      perimeter: perimeter,
      category: category,
      selectedType: selectedType,
      plusPrice: plusPrice,
      gradientPrice: gradientPrice
    });

    // Update UI elements
    updateBasePriceDisplay(elements.radioHue, plusPrice);
    updatePriceDisplay(elements.radioOptions, LIGHTSTRIP_VARIANTS.PLUS, plusPrice);
    updatePriceDisplay(elements.radioOptions, LIGHTSTRIP_VARIANTS.GRADIENT, gradientPrice);
    updateSelectValues(elements, category, selectedType, plusPrice, gradientPrice);
  } catch (error) {
    debugWarn("Error updating ".concat(lightingType, " Hue configuration:"), error);
  }
};

// ========================================
// CALCULATION FUNCTIONS
// ========================================

/**
 * Calculates the perimeter based on mirror dimensions and lighting type
 */
var calculatePerimeter = function calculatePerimeter() {
  var lightingType = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : LIGHTING_TYPES.AMBIENT;
  try {
    // Handle circular mirrors (diameter input)
    if (_variables_js__WEBPACK_IMPORTED_MODULE_20__.durchmesserInput !== null && _variables_js__WEBPACK_IMPORTED_MODULE_20__.durchmesserInput !== void 0 && _variables_js__WEBPACK_IMPORTED_MODULE_20__.durchmesserInput.value && parseFloat(_variables_js__WEBPACK_IMPORTED_MODULE_20__.durchmesserInput.value) > 0) {
      var diameter = parseInputValue(_variables_js__WEBPACK_IMPORTED_MODULE_20__.durchmesserInput.value);
      if (diameter <= 0) {
        debugLog("Invalid diameter value:", _variables_js__WEBPACK_IMPORTED_MODULE_20__.durchmesserInput.value);
        return 0;
      }
      return Math.ceil(Math.PI * diameter * 100) / 100;
    }

    // Handle rectangular mirrors (width and height inputs)
    var hasWidth = (_variables_js__WEBPACK_IMPORTED_MODULE_20__.breiteInput === null || _variables_js__WEBPACK_IMPORTED_MODULE_20__.breiteInput === void 0 ? void 0 : _variables_js__WEBPACK_IMPORTED_MODULE_20__.breiteInput.value) && parseFloat(_variables_js__WEBPACK_IMPORTED_MODULE_20__.breiteInput.value) > 0;
    var hasHeight = (_variables_js__WEBPACK_IMPORTED_MODULE_20__.hoeheInput === null || _variables_js__WEBPACK_IMPORTED_MODULE_20__.hoeheInput === void 0 ? void 0 : _variables_js__WEBPACK_IMPORTED_MODULE_20__.hoeheInput.value) && parseFloat(_variables_js__WEBPACK_IMPORTED_MODULE_20__.hoeheInput.value) > 0;
    if (hasWidth || hasHeight) {
      var width = parseInputValue((_variables_js__WEBPACK_IMPORTED_MODULE_20__.breiteInput === null || _variables_js__WEBPACK_IMPORTED_MODULE_20__.breiteInput === void 0 ? void 0 : _variables_js__WEBPACK_IMPORTED_MODULE_20__.breiteInput.value) || "0");
      var height = parseInputValue((_variables_js__WEBPACK_IMPORTED_MODULE_20__.hoeheInput === null || _variables_js__WEBPACK_IMPORTED_MODULE_20__.hoeheInput === void 0 ? void 0 : _variables_js__WEBPACK_IMPORTED_MODULE_20__.hoeheInput.value) || "0");
      if (width <= 0 || height <= 0) {
        debugLog("Invalid rectangular dimensions:", {
          width: width,
          height: height,
          hasWidth: hasWidth,
          hasHeight: hasHeight
        });
        return 0;
      }
      return lightingType === LIGHTING_TYPES.AMBIENT ? calculateAmbientPerimeter(width, height) : calculateMainLightingPerimeter(width, height);
    }
    debugLog("No valid dimensions found for perimeter calculation");
    return 0;
  } catch (error) {
    debugWarn("Error calculating perimeter:", error);
    return 0;
  }
};

/**
 * Calculates perimeter for ambient lighting based on product category and selected position
 */
var calculateAmbientPerimeter = function calculateAmbientPerimeter(width, height) {
  if (!validateDimensions(width, height)) return 0;
  var selectedOption = getSelectedAmbientOption();
  if (!selectedOption) {
    debugLog("No ambient lighting option selected");
    return 0;
  }
  debugLog("Product categories for ambient lighting:", {
    isBadspiegel: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isBadspiegel,
    isSpiegelschrank: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isSpiegelschrank,
    isUnterschrank: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isUnterschrank,
    isHochschrank: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isHochschrank,
    isBoard: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isBoard
  });
  debugLog("Selected ambient option:", selectedOption);

  // Product category specific logic
  if (_variables_js__WEBPACK_IMPORTED_MODULE_20__.isBadspiegel || _variables_js__WEBPACK_IMPORTED_MODULE_20__.isSpiegelschrank) {
    return calculateMirrorAmbientLighting(width, height, selectedOption);
  }
  if (_variables_js__WEBPACK_IMPORTED_MODULE_20__.isUnterschrank || _variables_js__WEBPACK_IMPORTED_MODULE_20__.isHochschrank || _variables_js__WEBPACK_IMPORTED_MODULE_20__.isBoard) {
    return calculateCabinetAmbientLighting(width, height, selectedOption);
  }

  // Fallback: Use mirror logic for unknown categories
  debugWarn("Unknown product category for ambient lighting, using mirror logic");
  return calculateMirrorAmbientLighting(width, height, selectedOption);
};

/**
 * Calculates perimeter for main lighting based on product category and lighting position flags
 */
var calculateMainLightingPerimeter = function calculateMainLightingPerimeter(width, height) {
  if (!validateDimensions(width, height)) return 0;
  debugLog("Product categories:", {
    isBadspiegel: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isBadspiegel,
    isSpiegelschrank: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isSpiegelschrank,
    isUnterschrank: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isUnterschrank,
    isHochschrank: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isHochschrank,
    isBoard: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isBoard
  });
  debugLog("Main lighting flags:", {
    isLORU: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isLORU,
    isLOR: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isLOR,
    isLR: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isLR,
    isOU: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isOU,
    isO: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isO
  });

  // Product category specific logic
  if (_variables_js__WEBPACK_IMPORTED_MODULE_20__.isBadspiegel) {
    return calculateBadspiegelMainLighting(width, height);
  }
  if (_variables_js__WEBPACK_IMPORTED_MODULE_20__.isSpiegelschrank) {
    // Spiegelschrank: Always LORU (all sides), regardless of detected flags
    var perimeter = Math.ceil(2 * (width + height) * 100) / 100;
    debugLog("Spiegelschrank main lighting (forced LORU):", {
      width: width,
      height: height,
      perimeter: perimeter
    });
    return perimeter;
  }

  // Cabinets don't have main lighting
  if (_variables_js__WEBPACK_IMPORTED_MODULE_20__.isUnterschrank || _variables_js__WEBPACK_IMPORTED_MODULE_20__.isHochschrank || _variables_js__WEBPACK_IMPORTED_MODULE_20__.isBoard) {
    debugLog("Product category has no main lighting:", {
      isUnterschrank: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isUnterschrank,
      isHochschrank: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isHochschrank,
      isBoard: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isBoard
    });
    return 0;
  }

  // Fallback: Use Badspiegel logic for unknown product categories
  debugWarn("Unknown product category, using fallback logic");
  return calculateBadspiegelMainLighting(width, height);
};

/**
 * Calculates ambient lighting perimeter for mirrors (Badspiegel and Spiegelschrank)
 */
var calculateMirrorAmbientLighting = function calculateMirrorAmbientLighting(width, height, selectedOption) {
  var lightingCalculations = {
    waschbeckenbeleuchtung: function waschbeckenbeleuchtung() {
      return Math.ceil(width * 100) / 100;
    },
    deckenbeleuchtung: function deckenbeleuchtung() {
      return Math.ceil(width * 100) / 100;
    },
    unten: function unten() {
      return Math.ceil(width * 100) / 100;
    },
    oben: function oben() {
      return Math.ceil(width * 100) / 100;
    },
    seitenbeleuchtung: function seitenbeleuchtung() {
      return Math.ceil(2 * height * 100) / 100;
    },
    wasch_und_deckenbeleuchtung: function wasch_und_deckenbeleuchtung() {
      return Math.ceil(2 * width * 100) / 100;
    },
    oben_und_unten: function oben_und_unten() {
      return Math.ceil(2 * width * 100) / 100;
    },
    wasch_und_seitenbeleuchtung: function wasch_und_seitenbeleuchtung() {
      return Math.ceil((width + 2 * height) * 100) / 100;
    },
    decken_und_seitenbeleuchtung: function decken_und_seitenbeleuchtung() {
      return Math.ceil((width + 2 * height) * 100) / 100;
    },
    rundherumbeleuchtung: function rundherumbeleuchtung() {
      return Math.ceil(2 * (width + height) * 100) / 100;
    }
  };
  var calculation = lightingCalculations[selectedOption];
  if (calculation) {
    var perimeter = calculation();
    debugLog("Mirror ambient lighting calculation:", {
      selectedOption: selectedOption,
      width: width,
      height: height,
      perimeter: perimeter
    });
    return perimeter;
  }
  debugWarn("Unknown mirror ambient lighting option:", selectedOption);
  return 0;
};

/**
 * Calculates ambient lighting perimeter for cabinets
 */
var calculateCabinetAmbientLighting = function calculateCabinetAmbientLighting(width, height, selectedOption) {
  var productType = getProductTypeName();
  var lightingCalculations = {
    waschbeckenbeleuchtung: function waschbeckenbeleuchtung() {
      return Math.ceil(width * 100) / 100;
    },
    deckenbeleuchtung: function deckenbeleuchtung() {
      return Math.ceil(width * 100) / 100;
    },
    unten: function unten() {
      return Math.ceil(width * 100) / 100;
    },
    oben: function oben() {
      return Math.ceil(width * 100) / 100;
    },
    seitenbeleuchtung: function seitenbeleuchtung() {
      return Math.ceil(2 * height * 100) / 100;
    },
    wasch_und_deckenbeleuchtung: function wasch_und_deckenbeleuchtung() {
      return Math.ceil(2 * width * 100) / 100;
    },
    oben_und_unten: function oben_und_unten() {
      return Math.ceil(2 * width * 100) / 100;
    },
    wasch_und_seitenbeleuchtung: function wasch_und_seitenbeleuchtung() {
      return Math.ceil((width + 2 * height) * 100) / 100;
    },
    decken_und_seitenbeleuchtung: function decken_und_seitenbeleuchtung() {
      return Math.ceil((width + 2 * height) * 100) / 100;
    },
    rundherumbeleuchtung: function rundherumbeleuchtung() {
      return Math.ceil(2 * (width + height) * 100) / 100;
    },
    sockelbeleuchtung: function sockelbeleuchtung() {
      return Math.ceil(width * 100) / 100;
    },
    innenbeleuchtung: function innenbeleuchtung() {
      return Math.ceil(2 * (width + height) * 100) / 100;
    }
  };
  var calculation = lightingCalculations[selectedOption];
  if (calculation) {
    var perimeter = calculation();
    debugLog("".concat(productType, " ambient lighting calculation:"), {
      selectedOption: selectedOption,
      width: width,
      height: height,
      perimeter: perimeter
    });
    return perimeter;
  }
  debugWarn("Unknown ".concat(productType, " ambient lighting option:"), selectedOption);
  return 0;
};

/**
 * Calculates main lighting perimeter for Badspiegel based on lighting position flags
 */
var calculateBadspiegelMainLighting = function calculateBadspiegelMainLighting(width, height) {
  var lightingFlags = [{
    flag: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isLORU,
    calculation: function calculation() {
      return Math.ceil(2 * (width + height) * 100) / 100;
    },
    name: "LORU"
  }, {
    flag: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isLOR,
    calculation: function calculation() {
      return Math.ceil((width + 2 * height) * 100) / 100;
    },
    name: "LOR"
  }, {
    flag: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isLR,
    calculation: function calculation() {
      return Math.ceil(2 * height * 100) / 100;
    },
    name: "LR"
  }, {
    flag: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isOU,
    calculation: function calculation() {
      return Math.ceil(2 * width * 100) / 100;
    },
    name: "OU"
  }, {
    flag: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isO,
    calculation: function calculation() {
      return Math.ceil(width * 100) / 100;
    },
    name: "O"
  }];
  for (var _i = 0, _lightingFlags = lightingFlags; _i < _lightingFlags.length; _i++) {
    var _lightingFlags$_i = _lightingFlags[_i],
      flag = _lightingFlags$_i.flag,
      calculation = _lightingFlags$_i.calculation,
      name = _lightingFlags$_i.name;
    if (flag) {
      var perimeter = calculation();
      debugLog("Badspiegel ".concat(name, " calculation:"), {
        width: width,
        height: height,
        perimeter: perimeter
      });
      return perimeter;
    }
  }
  debugWarn("No main lighting position flag is active for Badspiegel!", {
    isLORU: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isLORU,
    isLOR: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isLOR,
    isLR: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isLR,
    isOU: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isOU,
    isO: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isO
  });
  return 0;
};

// ========================================
// RESET FUNCTIONS
// ========================================

/**
 * Resets Philips Hue select fields and pricing
 */
var resetHueSelects = function resetHueSelects(lightingType) {
  try {
    var elements = lightingType === LIGHTING_TYPES.MAIN ? mainLightingElements : ambientLightingElements;
    debugLog("Resetting ".concat(lightingType, " Hue selects"));

    // Reset select field values
    if (elements.selectPlus) elements.selectPlus.value = "";
    if (elements.selectGradient) elements.selectGradient.value = "";

    // Reset radio button prices and UI displays
    if (elements.radioOptions) {
      elements.radioOptions.forEach(function (radio) {
        if (radio) {
          // Clear data attributes
          delete radio.dataset.price;
          radio.removeAttribute("data-price");

          // Clear price display in UI
          clearPriceDisplay(radio);
        }
      });
    }

    // Reset base price display
    updateBasePriceDisplay(elements.radioHue, "");
    debugLog("".concat(lightingType, " Hue reset completed"));
  } catch (error) {
    debugWarn("Error resetting ".concat(lightingType, " Hue selects:"), error);
  }
};

// ========================================
// UI UPDATE HELPER FUNCTIONS
// ========================================

/**
 * Updates the base price display for Philips Hue main option
 */
var updateBasePriceDisplay = function updateBasePriceDisplay(radioElement, price) {
  if (!radioElement) return;
  var priceContainer = getPriceContainer(radioElement);
  if (priceContainer) {
    priceContainer.textContent = price ? "(ab ".concat(price, " \u20AC)") : "";
  }
};

/**
 * Updates the price display in the UI for a specific radio button
 */
var updatePriceDisplay = function updatePriceDisplay(radioElements, targetValue, price) {
  if (!radioElements || !targetValue) return;
  var radio = Array.from(radioElements).find(function (r) {
    return r && r.value === targetValue;
  });
  if (!radio) return;
  var priceContainer = getPriceContainer(radio);
  if (priceContainer) {
    priceContainer.textContent = price ? "(+".concat(price, " \u20AC)") : "";
  }
};

/**
 * Updates select values and radio prices based on selected type
 */
var updateSelectValues = function updateSelectValues(elements, category, selectedType, plusPrice, gradientPrice) {
  if (selectedType === LIGHTSTRIP_VARIANTS.GRADIENT) {
    elements.selectGradient.value = category;
    elements.selectPlus.value = "";
    setRadioPrice(elements.radioOptions, LIGHTSTRIP_VARIANTS.GRADIENT, gradientPrice);
  } else if (selectedType === LIGHTSTRIP_VARIANTS.PLUS) {
    elements.selectPlus.value = category;
    elements.selectGradient.value = "";
    setRadioPrice(elements.radioOptions, LIGHTSTRIP_VARIANTS.PLUS, plusPrice);
  } else {
    // No specific type selected - show both options
    elements.selectPlus.value = "";
    elements.selectGradient.value = "";
  }
};

/**
 * Sets the data-price attribute for a specific radio button option
 */
var setRadioPrice = function setRadioPrice(radioElements, targetValue, price) {
  if (!radioElements || !targetValue) return;
  radioElements.forEach(function (radio) {
    if (radio && radio.value === targetValue) {
      if (price) {
        radio.dataset.price = price;
        radio.setAttribute("data-price", price);
      } else {
        delete radio.dataset.price;
        radio.removeAttribute("data-price");
      }
    }
  });
};

/**
 * Clears price display for a radio button
 */
var clearPriceDisplay = function clearPriceDisplay(radio) {
  var priceContainer = getPriceContainer(radio);
  if (priceContainer) {
    priceContainer.textContent = "";
  }
};

/**
 * Gets the price container element for a radio button
 */
var getPriceContainer = function getPriceContainer(radioElement) {
  if (!radioElement) return null;
  var label = document.querySelector("label.btn[for=\"".concat(radioElement.id, "\"]"));
  return label === null || label === void 0 ? void 0 : label.querySelector(".value-price");
};

// ========================================
// UTILITY & HELPER FUNCTIONS
// ========================================

/**
 * Safely parses input value and converts from millimeters to meters
 */
var parseInputValue = function parseInputValue(input) {
  if (input === null || input === undefined || input === "") {
    return 0;
  }
  var parsed = parseFloat(input);
  return isNaN(parsed) || parsed < 0 ? 0 : parsed / 1000; // Convert mm to m, ensure non-negative
};

/**
 * Maps perimeter to appropriate pricing category
 */
var mapPerimeterToCategory = function mapPerimeterToCategory(perimeter) {
  if (!perimeter || perimeter <= 0) {
    debugLog("Invalid perimeter for category mapping:", perimeter);
    return "";
  }
  for (var _i2 = 0, _Object$entries = Object.entries(PERIMETER_CATEGORIES); _i2 < _Object$entries.length; _i2++) {
    var _Object$entries$_i = _slicedToArray(_Object$entries[_i2], 2),
      maxPerimeter = _Object$entries$_i[0],
      category = _Object$entries$_i[1];
    if (perimeter <= parseFloat(maxPerimeter)) {
      debugLog("Perimeter category mapping:", {
        perimeter: perimeter,
        category: category
      });
      return category;
    }
  }
  debugWarn("Perimeter exceeds maximum category (10m):", perimeter);
  return "";
};

/**
 * Validates input value against min/max constraints
 */
var isValidInput = function isValidInput(input) {
  try {
    if (!input || !input.value || input.value.trim() === "") {
      return false;
    }
    var value = parseFloat(input.value);
    if (isNaN(value) || value <= 0) {
      return false;
    }
    var min = input.min ? parseFloat(input.min) : -Infinity;
    var max = input.max ? parseFloat(input.max) : Infinity;
    return value >= min && value <= max;
  } catch (error) {
    debugWarn("Error validating input:", error);
    return false;
  }
};

/**
 * Validates dimensions
 */
var validateDimensions = function validateDimensions(width, height) {
  if (!width || !height || width <= 0 || height <= 0) {
    debugWarn("Invalid dimensions:", {
      width: width,
      height: height
    });
    return false;
  }
  return true;
};

/**
 * Checks if the current product category supports main lighting
 */
var hasMainLighting = function hasMainLighting() {
  var hasMain = _variables_js__WEBPACK_IMPORTED_MODULE_20__.isBadspiegel || _variables_js__WEBPACK_IMPORTED_MODULE_20__.isSpiegelschrank;
  debugLog("Main lighting availability:", {
    isBadspiegel: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isBadspiegel,
    isSpiegelschrank: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isSpiegelschrank,
    isUnterschrank: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isUnterschrank,
    isHochschrank: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isHochschrank,
    isBoard: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isBoard,
    hasMain: hasMain
  });
  return hasMain;
};

/**
 * Checks if the current product category supports ambient lighting
 */
var hasAmbientLighting = function hasAmbientLighting() {
  var hasAmbient = _variables_js__WEBPACK_IMPORTED_MODULE_20__.isBadspiegel || _variables_js__WEBPACK_IMPORTED_MODULE_20__.isSpiegelschrank || _variables_js__WEBPACK_IMPORTED_MODULE_20__.isUnterschrank || _variables_js__WEBPACK_IMPORTED_MODULE_20__.isHochschrank || _variables_js__WEBPACK_IMPORTED_MODULE_20__.isBoard;
  debugLog("Ambient lighting availability:", {
    isBadspiegel: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isBadspiegel,
    isSpiegelschrank: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isSpiegelschrank,
    isUnterschrank: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isUnterschrank,
    isHochschrank: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isHochschrank,
    isBoard: _variables_js__WEBPACK_IMPORTED_MODULE_20__.isBoard,
    hasAmbient: hasAmbient
  });
  return hasAmbient;
};

/**
 * Checks if all required elements are valid
 */
var areElementsValid = function areElementsValid(elements) {
  return elements.radioHue && elements.selectPlus && elements.selectGradient;
};

/**
 * Gets the selected ambient lighting option
 */
var getSelectedAmbientOption = function getSelectedAmbientOption() {
  var ambientElement = document.querySelector("input[name='ambientelicht']:checked");
  return ambientElement === null || ambientElement === void 0 ? void 0 : ambientElement.value;
};

/**
 * Gets the selected lighting type for main or ambient lighting
 */
var getSelectedLightingType = function getSelectedLightingType(lightingType) {
  var selectorName = lightingType === LIGHTING_TYPES.MAIN ? "lichtfarbe_philips_hue" : "ambientelicht_philips_hue";
  var selectedTypeElement = document.querySelector("input[name='".concat(selectorName, "']:checked"));
  return selectedTypeElement === null || selectedTypeElement === void 0 ? void 0 : selectedTypeElement.value;
};

/**
 * Gets prices for a category from select options
 */
var getPricesForCategory = function getPricesForCategory(elements, category) {
  var plusOption = elements.selectPlus.querySelector("option[value=\"".concat(category, "\"]"));
  var gradientOption = elements.selectGradient.querySelector("option[value=\"".concat(category, "\"]"));
  if (!plusOption || !gradientOption) {
    debugWarn("Missing select options for category:", {
      category: category,
      plusOption: !!plusOption,
      gradientOption: !!gradientOption
    });
    return {
      plusPrice: "",
      gradientPrice: ""
    };
  }
  return {
    plusPrice: plusOption.dataset.price || "",
    gradientPrice: gradientOption.dataset.price || ""
  };
};

/**
 * Gets the product type name for logging
 */
var getProductTypeName = function getProductTypeName() {
  return _variables_js__WEBPACK_IMPORTED_MODULE_20__.isUnterschrank ? "Unterschrank" : _variables_js__WEBPACK_IMPORTED_MODULE_20__.isHochschrank ? "Hochschrank" : _variables_js__WEBPACK_IMPORTED_MODULE_20__.isBoard ? "Side/Lowboard" : "Cabinet";
};

// ========================================
// DEBUG HELPER FUNCTIONS
// ========================================

/**
 * Debug helper functions that respect DEBUG_MODE setting
 */
var debugLog = function debugLog() {
  if (DEBUG_MODE) {
    var _console;
    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }
    (_console = console).debug.apply(_console, ["[Philips Hue]"].concat(args));
  }
};
var debugWarn = function debugWarn() {
  if (DEBUG_MODE) {
    var _console2;
    for (var _len2 = arguments.length, args = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
      args[_key2] = arguments[_key2];
    }
    (_console2 = console).warn.apply(_console2, ["[Philips Hue]"].concat(args));
  }
};
var debugInfo = function debugInfo() {
  if (DEBUG_MODE) {
    var _console3;
    for (var _len3 = arguments.length, args = new Array(_len3), _key3 = 0; _key3 < _len3; _key3++) {
      args[_key3] = arguments[_key3];
    }
    (_console3 = console).info.apply(_console3, ["[Philips Hue]"].concat(args));
  }
};

/***/ }),

/***/ "./assets/js/configurator/dependencies/schminkspiegel.js":
/*!***************************************************************!*\
  !*** ./assets/js/configurator/dependencies/schminkspiegel.js ***!
  \***************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony import */ var core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! core-js/modules/es.array.includes.js */ "./node_modules/core-js/modules/es.array.includes.js");
/* harmony import */ var core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var core_js_modules_es_string_includes_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! core-js/modules/es.string.includes.js */ "./node_modules/core-js/modules/es.string.includes.js");
/* harmony import */ var core_js_modules_es_string_includes_js__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_string_includes_js__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var _dependencies_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./../dependencies.js */ "./assets/js/configurator/dependencies.js");
/* harmony import */ var _variables_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./../variables.js */ "./assets/js/configurator/variables.js");


/**
 * @version 2.3.0
 */



document.addEventListener("DOMContentLoaded", function () {
  /**
   * Values X Values
   */
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_2__.dependenciesValuesXvalues)("schminkspiegel_lichtfarbe", function (value) {
    return value === "warmweiss" || value === "neutralweiss" || value === "kaltweiss";
  }, "schminkspiegel_bedienung", function (value) {
    return value === "ohne_extra_schalter" || value === "extra_kippschalter" || value === "extra_touch_sensor";
  });
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_2__.dependenciesValuesXvalues)("schminkspiegel_lichtfarbe", function (value) {
    return value === "alle_drei_lichtfarben";
  }, "schminkspiegel_bedienung", function (value) {
    return value === "extra_touch_sensor_plus";
  });
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_2__.dependenciesValuesXvalues)("schminkspiegel_bedienung", function (value) {
    return !value.includes("touch_sensor");
  }, "schminkspiegel_bedienung_position", function (value) {
    return value === "linke_seite" || value === "rechte_seite";
  });
  /**
   * Values X Container
   */
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_2__.dependenciesValuesXcontainer)("schminkspiegel", function (value) {
    return value !== "";
  }, "schminkspiegel_position");
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_2__.dependenciesValuesXcontainer)("schminkspiegel", function (value) {
    return value === "beleuchtet";
  }, "schminkspiegel_lichtfarbe");
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_2__.dependenciesValuesXcontainer)("schminkspiegel", function (value) {
    return value === "beleuchtet";
  }, "schminkspiegel_bedienung");
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_2__.dependenciesValuesXcontainer)("schminkspiegel_bedienung", function (value) {
    return value !== "" && value !== "ohne_extra_schalter";
  }, "schminkspiegel_bedienung_position");
});

/***/ }),

/***/ "./assets/js/configurator/dependencies/schrankart.js":
/*!***********************************************************!*\
  !*** ./assets/js/configurator/dependencies/schrankart.js ***!
  \***********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony import */ var core_js_modules_es_array_concat_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! core-js/modules/es.array.concat.js */ "./node_modules/core-js/modules/es.array.concat.js");
/* harmony import */ var core_js_modules_es_array_concat_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_concat_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _dependencies_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./../dependencies.js */ "./assets/js/configurator/dependencies.js");
/* harmony import */ var _variables_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./../variables.js */ "./assets/js/configurator/variables.js");

/**
 * @version 2.3.0
 */



document.addEventListener("DOMContentLoaded", function () {
  /**
   * Bei Halbeinbau automatisch Einbaurahmen auf "ja" setzen
   * (Halbeinbau ohne Einbaurahmen ist nicht möglich)
   *
   * TODO FIX THIS
   */
  //   dependenciesValuesXvalues(
  //     "schrankart",
  //     (value) => {
  //       return (
  //         value === "halbeinbau-teilunterputz"
  //       );
  //     },
  //     "einbaurahmen",
  //     (value) => {
  //       return (
  //         value === ""
  //       );
  //     }
  //   );

  /**
   * Einbaurahmen Tiefe Container - nur bei Halbeinbau sichtbar
   */
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_1__.dependenciesValuesXcontainer)("einbaurahmen", function (value) {
    return value === "ja";
  }, "einbaurahmen_tiefe");

  /**
   * Einbaurahmen Tiefe Min/Max von Breite-Input ableiten
   */
  function updateEinbaurahmenTiefeConstraints() {
    var breiteInput = document.querySelector('input[name="breite"]');
    var einbaurahmenTiefeInput = document.querySelector('input[name="einbaurahmen_tiefe"]');
    if (!breiteInput || !einbaurahmenTiefeInput) return;
    var breiteValue = parseInt(breiteInput.value) || 0;

    // Einbaurahmen-Tiefe: mindestens 80mm, maximum von der angegebenen Breite
    var einbaurahmenMinimum = 80;
    einbaurahmenTiefeInput.min = einbaurahmenMinimum;
    einbaurahmenTiefeInput.max = breiteValue;

    // Placeholder aktualisieren
    einbaurahmenTiefeInput.placeholder = "Geben Sie einen Wert ein (min: ".concat(einbaurahmenMinimum, ", max: ").concat(breiteValue, ")");

    // Falls der aktuelle Wert außerhalb der neuen Grenzen liegt, anpassen
    var currentValue = parseInt(einbaurahmenTiefeInput.value) || 0;
    if (currentValue > einbaurahmenTiefeInput.max) {
      einbaurahmenTiefeInput.value = einbaurahmenTiefeInput.max;
    } else if (currentValue < einbaurahmenTiefeInput.min) {
      einbaurahmenTiefeInput.value = einbaurahmenTiefeInput.min;
    }
  }

  // Event Listener für Breite-Änderungen
  var breiteInput = document.querySelector('input[name="breite"]');
  if (breiteInput) {
    breiteInput.addEventListener("input", updateEinbaurahmenTiefeConstraints);
    breiteInput.addEventListener("change", updateEinbaurahmenTiefeConstraints);

    // Initiale Anpassung
    updateEinbaurahmenTiefeConstraints();
  }
});

/***/ }),

/***/ "./assets/js/configurator/dependencies/spiegelheizung.js":
/*!***************************************************************!*\
  !*** ./assets/js/configurator/dependencies/spiegelheizung.js ***!
  \***************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony import */ var core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! core-js/modules/es.array.includes.js */ "./node_modules/core-js/modules/es.array.includes.js");
/* harmony import */ var core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var core_js_modules_es_string_includes_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! core-js/modules/es.string.includes.js */ "./node_modules/core-js/modules/es.string.includes.js");
/* harmony import */ var core_js_modules_es_string_includes_js__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_string_includes_js__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var _dependencies_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./../dependencies.js */ "./assets/js/configurator/dependencies.js");
/* harmony import */ var _variables_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./../variables.js */ "./assets/js/configurator/variables.js");


/**
 * @version 2.3.0
 */



document.addEventListener("DOMContentLoaded", function () {
  /**
   * Values X Values
   */
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_2__.dependenciesValuesXvalues)("spiegelheizung_bedienung", function (value) {
    return !value.includes("touch_sensor");
  }, "spiegelheizung_bedienung_position", function (value) {
    return value === "linke_seite" || value === "rechte_seite";
  });
  /**
   * Values X Container
   */
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_2__.dependenciesValuesXcontainer)("spiegelheizung", function (value) {
    return value === "ja";
  }, "spiegelheizung_bedienung");
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_2__.dependenciesValuesXcontainer)("spiegelheizung_bedienung", function (value) {
    return value !== "" && value !== "ohne_extra_schalter";
  }, "spiegelheizung_bedienung_position");
});

/***/ }),

/***/ "./assets/js/configurator/dependencies/steckdose.js":
/*!**********************************************************!*\
  !*** ./assets/js/configurator/dependencies/steckdose.js ***!
  \**********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _dependencies_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./../dependencies.js */ "./assets/js/configurator/dependencies.js");
/* harmony import */ var _variables_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./../variables.js */ "./assets/js/configurator/variables.js");
/**
 * @version 2.3.0
 */



document.addEventListener("DOMContentLoaded", function () {
  /**
   * Values X Values
   */
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXvalues)("steckdose", function (value) {
    return value === "2x";
  }, "steckdose_position", function (value) {
    return value === "links_und_rechts";
  });
  /**
   * Values X Container
   */
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("steckdose", function (value) {
    return value !== "";
  }, "steckdose_farbe");
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("steckdose", function (value) {
    return value !== "";
  }, "steckdose_position");
});

/***/ }),

/***/ "./assets/js/configurator/dependencies/tiefe.js":
/*!******************************************************!*\
  !*** ./assets/js/configurator/dependencies/tiefe.js ***!
  \******************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _dependencies_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./../dependencies.js */ "./assets/js/configurator/dependencies.js");
/* harmony import */ var _variables_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./../variables.js */ "./assets/js/configurator/variables.js");
/**
 * @version 2.3.0
 * 
 * @todo Check if this file is needed
 */




/***/ }),

/***/ "./assets/js/configurator/dependencies/tueren.js":
/*!*******************************************************!*\
  !*** ./assets/js/configurator/dependencies/tueren.js ***!
  \*******************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _dependencies_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./../dependencies.js */ "./assets/js/configurator/dependencies.js");
/* harmony import */ var _variables_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./../variables.js */ "./assets/js/configurator/variables.js");
/**
 * @version 2.3.0
 */



document.addEventListener("DOMContentLoaded", function () {
  /**
   * Values X Container
   */
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("tueren", function (value) {
    return value === "1x";
  }, "tuer_anschlagrichtung");
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("tueren", function (value) {
    return value === "2x" || value === "3x" || value === "4x" || value === "5x" || value === "6x";
  }, "tuer_1_anschlagrichtung");
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("tueren", function (value) {
    return value === "2x" || value === "3x" || value === "4x" || value === "5x" || value === "6x";
  }, "tuer_2_anschlagrichtung");
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("tueren", function (value) {
    return value === "3x" || value === "4x" || value === "5x" || value === "6x";
  }, "tuer_3_anschlagrichtung");
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("tueren", function (value) {
    return value === "4x" || value === "5x" || value === "6x";
  }, "tuer_4_anschlagrichtung");
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("tueren", function (value) {
    return value === "5x" || value === "6x";
  }, "tuer_5_anschlagrichtung");
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("tueren", function (value) {
    return value === "6x";
  }, "tuer_6_anschlagrichtung");

  // // Allgemeine Fälle für "2x" bis "6x" mittels Schleifen
  // for (let i = 2; i <= 6; i++) { // Äußere Schleife für 2x, 3x, 4x, 5x, 6x
  //     const currentValue = `${i}x`; // z.B. "2x", "3x", ...
  //     for (let j = 1; j <= i; j++) { // Innere Schleife für die Suffixe _1, _2, ..., _i
  //         dependenciesValuesXcontainer(
  //             "tueren",
  //             value => value === currentValue,
  //             `tuer_${j}_anschlagrichtung`,
  //             true
  //         );
  //     }
  // }
});

/***/ }),

/***/ "./assets/js/configurator/dependencies/tuergriff.js":
/*!**********************************************************!*\
  !*** ./assets/js/configurator/dependencies/tuergriff.js ***!
  \**********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony import */ var core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! core-js/modules/es.array.includes.js */ "./node_modules/core-js/modules/es.array.includes.js");
/* harmony import */ var core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var core_js_modules_es_string_includes_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! core-js/modules/es.string.includes.js */ "./node_modules/core-js/modules/es.string.includes.js");
/* harmony import */ var core_js_modules_es_string_includes_js__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_string_includes_js__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var _dependencies_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./../dependencies.js */ "./assets/js/configurator/dependencies.js");
/* harmony import */ var _variables_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./../variables.js */ "./assets/js/configurator/variables.js");


/**
 * @version 2.3.0
 */



document.addEventListener("DOMContentLoaded", function () {
  /**
   * Values X Container
   */
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_2__.dependenciesValuesXcontainer)("tuergriff", function (value) {
    return value === "tuerueberstand";
  }, "tuerueberstand_position");
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_2__.dependenciesValuesXcontainer)("tuerueberstand_position", function (value) {
    return value.includes("oben");
  }, "tuerueberstand_oben_in_mm");
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_2__.dependenciesValuesXcontainer)("tuerueberstand_position", function (value) {
    return value.includes("unten");
  }, "tuerueberstand_unten_in_mm");
});

/***/ }),

/***/ "./assets/js/configurator/dependencies/tv-geraet.js":
/*!**********************************************************!*\
  !*** ./assets/js/configurator/dependencies/tv-geraet.js ***!
  \**********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony import */ var core_js_modules_es_array_concat_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! core-js/modules/es.array.concat.js */ "./node_modules/core-js/modules/es.array.concat.js");
/* harmony import */ var core_js_modules_es_array_concat_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_concat_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! core-js/modules/es.object.to-string.js */ "./node_modules/core-js/modules/es.object.to-string.js");
/* harmony import */ var core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var core_js_modules_es_regexp_exec_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! core-js/modules/es.regexp.exec.js */ "./node_modules/core-js/modules/es.regexp.exec.js");
/* harmony import */ var core_js_modules_es_regexp_exec_js__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_regexp_exec_js__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var core_js_modules_es_string_replace_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! core-js/modules/es.string.replace.js */ "./node_modules/core-js/modules/es.string.replace.js");
/* harmony import */ var core_js_modules_es_string_replace_js__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_string_replace_js__WEBPACK_IMPORTED_MODULE_3__);
/* harmony import */ var core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! core-js/modules/web.dom-collections.for-each.js */ "./node_modules/core-js/modules/web.dom-collections.for-each.js");
/* harmony import */ var core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_4___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_4__);
/* harmony import */ var _dependencies_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../dependencies.js */ "./assets/js/configurator/dependencies.js");
/* harmony import */ var _variables_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ../variables.js */ "./assets/js/configurator/variables.js");





/**
 * @version 2.3.0
 */




// Mindestabmessungen für jede TV-Größe (Breite x Höhe in mm)
var TV_SIZE_REQUIREMENTS = {
  led_156_zoll_ca_40cm: {
    minWidth: 600,
    minHeight: 600
  },
  led_19_zoll_ca_48cm: {
    minWidth: 700,
    minHeight: 650
  },
  led_22_zoll_ca_56cm: {
    minWidth: 850,
    minHeight: 650
  },
  qled_32_zoll_ca_81cm: {
    minWidth: 1250,
    minHeight: 650
  },
  qled_43_zoll_ca_109cm: {
    minWidth: 1450,
    minHeight: 800
  }
};

// Benutzerfreundliche Fehlermeldungen pro TV-Größe
var TV_SIZE_MESSAGES = {
  qled_43_zoll_ca_109cm: 'Bei einem 43"-TV muss der Spiegel mind. 145 x 80 cm (Breite x Höhe) groß sein. Bitte ändern Sie Breite x Höhe oder wählen Sie einen kleineren Bildschirm.',
  qled_32_zoll_ca_81cm: 'Bei einem 32"-TV muss der Spiegel mind. 125 x 65 cm (Breite x Höhe) groß sein. Bitte ändern Sie Breite x Höhe oder wählen Sie einen kleineren Bildschirm.',
  led_22_zoll_ca_56cm: 'Bei einem 22"-TV muss der Spiegel mind. 85 x 65 cm (Breite x Höhe) groß sein. Bitte ändern Sie Breite x Höhe oder wählen Sie einen kleineren Bildschirm.',
  led_19_zoll_ca_48cm: 'Bei einem 19"-TV sind die Maße zu klein. Bitte vergrößern Sie Breite x Höhe oder wählen Sie einen kleineren Bildschirm.',
  led_156_zoll_ca_40cm: "Für diese sehr kleine TV-Größe sollten minimale Maße beachtet werden (siehe Konfiguration)."
};

// Feedback mode: 'alert' or 'modal'
// Set to 'modal' to use the site's modal utility `createModal` (if available)
var FEEDBACK_MODE = "modal";

// DEBUG: set to true to enable console tracing
var DEBUG = false;
function debugLog() {
  if (DEBUG && typeof console !== "undefined" && console.log) {
    var _console;
    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }
    (_console = console).log.apply(_console, ["[tv-geraet]"].concat(args));
  }
}

// runtime marker: always set a minimal global flag, verbose log only when DEBUG
try {
  var _window;
  if (typeof window !== "undefined") {
    window.tvGeraet = window.tvGeraet || {};
    window.tvGeraet.loaded = true;
    window.tvGeraet.loadedAt = new Date().toISOString();
  }
  debugLog("script executed (module evaluation)", {
    loadedAt: (_window = window) === null || _window === void 0 || (_window = _window.tvGeraet) === null || _window === void 0 ? void 0 : _window.loadedAt
  });
} catch (e) {
  /* ignore */
}

// Small HTML escape helper
function escapeHtml(str) {
  if (typeof str !== "string") return String(str);
  return str.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&#039;");
}

/**
 * Highlight the most important parts of a message (dimensions) with fw-semibold.
 * @param {string} escapedMessage - message already escaped for HTML
 */
function highlightImportant(escapedMessage) {
  // Match patterns like '1450 x 800 mm' or '1250 x 650 mm' (space-insensitive)
  return escapedMessage.replace(/(\d+\s*[x×]\s*\d+\s*mm)/gi, '<span class="fw-semibold">$1</span>');
}

/**
 * Show feedback to the user. Uses `alert` or `createModal` depending on FEEDBACK_MODE.
 * @param {string} message
 * @param {string} [title]
 */
function showFeedback(message, title) {
  debugLog("showFeedback called", {
    mode: FEEDBACK_MODE,
    title: title
  });
  if (FEEDBACK_MODE === "modal" && typeof window.createModal === "function") {
    try {
      // Build a Bootstrap-friendly body with FontAwesome icon
      var escaped = escapeHtml(message);
      var highlighted = highlightImportant(escaped);
      var bodyHtml = "\n        <div class=\"d-flex align-items-start\">\n          <div class=\"me-3\">\n            <i class=\"fa-thin fa-circle-exclamation fa-2x text-warning\" aria-hidden=\"true\"></i>\n          </div>\n          <div class=\"fs-6\">".concat(highlighted, "</div>\n        </div>\n      ");
      window.createModal({
        title: title || "Hinweis",
        body: bodyHtml,
        // createFooterButton prefixes with 'btn ' so pass only the modifier
        footer: [{
          text: "OK",
          class: "btn-dark",
          dismiss: true
        }]
      });
      debugLog("createModal invoked successfully");
      return;
    } catch (e) {
      console.error("[tv-geraet] createModal threw", e);
      // fallback to alert if modal creation fails
    }
  }

  // If modal mode was requested but createModal is not available, warn in console
  if (FEEDBACK_MODE === "modal" && typeof window.createModal !== "function") {
    console.warn('FEEDBACK_MODE is "modal" but window.createModal is not available. Falling back to alert(). Ensure assets/js/modal.js is loaded before tv-geraet.js.');
  }

  // default fallback
  alert(message);
}

// --- Small DOM + state helpers (DRY) -------------------------------------
function getTvOptions() {
  return document.querySelectorAll('input[name="tv_geraet"]');
}
function getValuesGroup() {
  return document.querySelector(".values-group");
}
function getLabelForOption(option) {
  return option.closest("label");
}
function setLabelAvailable(label) {
  if (!label) return;
  label.classList.remove("disabled");
  // label.style.opacity = "1";
  label.style.pointerEvents = "auto";
  delete label.dataset.disabledReason;
}
function setLabelDisabled(label, reason) {
  if (!label) return;
  label.classList.add("disabled");
  // label.style.opacity = "0.5";
  label.style.pointerEvents = "auto"; // keep info button clickable
  if (reason) label.dataset.disabledReason = reason;
}
function getAvailableOptions() {
  return document.querySelectorAll('input[name="tv_geraet"]:not([disabled])');
}
function showNoAvailableFeedback() {
  showFeedback("Für die angegebenen Maße sind keine TV-Geräte verfügbar. Bitte vergrößern Sie die Abmessungen.", "Keine TV-Geräte verfügbar");
}
function showLabelReason(label) {
  if (!label) return;
  var reason = label.dataset.disabledReason;
  debugLog("showLabelReason", {
    reason: reason,
    label: label
  });
  if (reason) showFeedback(reason, "Warum diese Option deaktiviert ist");else showFeedback("Diese TV-Größe ist für die angegebenen Maße nicht verfügbar.", "Warum diese Option deaktiviert ist");
}
// --------------------------------------------------------------------------

// Selected TV tracking (new behavior)
var selectedTvValue = null;
var selectedTvRequirements = null;
function setSelectedTv(tvValue) {
  selectedTvValue = tvValue || null;
  selectedTvRequirements = TV_SIZE_REQUIREMENTS[tvValue] || null;
  debugLog("setSelectedTv", {
    selectedTvValue: selectedTvValue,
    selectedTvRequirements: selectedTvRequirements
  });
}

/**
 * Check current dimensions against the selected TV (if any) and show a hint
 * only when a TV is selected and the current size is smaller than required.
 */
function checkSelectedTvSizing() {
  if (!selectedTvValue || !selectedTvRequirements) return;
  var _getCurrentDimensions = getCurrentDimensions(),
    width = _getCurrentDimensions.width,
    height = _getCurrentDimensions.height;
  debugLog("checkSelectedTvSizing", {
    width: width,
    height: height,
    selectedTvRequirements: selectedTvRequirements
  });
  var tooNarrow = width < selectedTvRequirements.minWidth;
  var tooShort = height < selectedTvRequirements.minHeight;
  if (tooNarrow || tooShort) {
    // Use the configured message if present, but append a short hint about reduction
    var baseMsg = TV_SIZE_MESSAGES[selectedTvValue] || "Die gewählten Maße sind zu klein für das gewählte TV-Gerät.";
    var hint = " Das ausgewählte TV-Gerät wird in der Darstellung verkleinert.";
    showFeedback(baseMsg + hint, "Hinweis zur TV-Größe");
    return false;
  }
  return true;
}

// --- Event listeners / initialization (placed near top) ------------------
document.addEventListener("DOMContentLoaded", function () {
  /**
   * Values X Container
   */
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_5__.dependenciesValuesXcontainer)("tv_seitenanschluss", function (value) {
    return value !== "";
  }, "tv_seitenanschluss_position");

  // Event Listener für Breite- und Höhenänderungen
  var widthInput = document.querySelector('input[name="breite"]');
  var heightInput = document.querySelector('input[name="hoehe"]');

  // TV radio buttons
  var tvRadios = document.querySelectorAll('input[name="tv_geraet"]');
  if (tvRadios && tvRadios.length) {
    tvRadios.forEach(function (radio) {
      radio.addEventListener("change", function (ev) {
        if (ev.target.checked) {
          setSelectedTv(ev.target.value);
          // immediate sizing check when a tv is selected
          checkSelectedTvSizing();
        }
      });
    });
    // initialize selectedTv if one is checked on load
    var initiallyChecked = document.querySelector('input[name="tv_geraet"]:checked');
    if (initiallyChecked) setSelectedTv(initiallyChecked.value);
  }
  if (widthInput) {
    widthInput.addEventListener("input", function () {
      return checkSelectedTvSizing();
    });
    widthInput.addEventListener("change", function () {
      return checkSelectedTvSizing();
    });
  }
  if (heightInput) {
    heightInput.addEventListener("input", function () {
      return checkSelectedTvSizing();
    });
    heightInput.addEventListener("change", function () {
      return checkSelectedTvSizing();
    });
  }

  // Initiale Prüfung der verfügbaren TV-Größen
  // do not run global validation on load — only act when a tv is selected or radios change
  // handleDimensionChange();

  // Delegierter Klick-Handler: wenn ein deaktiviertes Label angeklickt wird,
  // zeigen wir den Grund an. Info-Buttons (.btn-link mit data-modal-link) bleiben klickbar.
  var valuesGroup = getValuesGroup();
  if (valuesGroup) {
    valuesGroup.addEventListener("click", function (ev) {
      var infoBtn = ev.target.closest("button[data-modal-link]");
      if (infoBtn) return; // allow modal button to work

      var label = ev.target.closest("label");
      if (!label) return;
      if (label.classList.contains("disabled")) {
        showLabelReason(label);
        ev.preventDefault();
        ev.stopPropagation();
      }
    }, true);
  }
});

// --------------------------------------------------------------------------

/**
 * Prüft verfügbare TV-Größen basierend auf Breite und Höhe
 * @param {number} width - Breite in mm
 * @param {number} height - Höhe in mm
 */
function updateAvailableTVSizes(width, height) {
  // If there are no tv radio inputs on this page, don't run validation or show warnings.
  var allTvInputs = document.querySelectorAll('input[name="tv_geraet"]');
  if (!allTvInputs || allTvInputs.length === 0) {
    debugLog("updateAvailableTVSizes: no tv_geraet inputs found, skipping");
    return true;
  }
  var tvOptions = getTvOptions();
  tvOptions.forEach(function (option) {
    var tvValue = option.value;
    var requirements = TV_SIZE_REQUIREMENTS[tvValue];
    var label = getLabelForOption(option);
    if (!requirements || !label) return;
    var isAvailable = width >= requirements.minWidth && height >= requirements.minHeight;
    if (isAvailable) {
      option.disabled = false;
      setLabelAvailable(label);
    } else {
      option.disabled = true;
      option.checked = false;
      setLabelDisabled(label, TV_SIZE_MESSAGES[tvValue]);
    }
  });

  // Prüfen ob mindestens eine Option verfügbar ist
  var availableOptions = getAvailableOptions();
  if (availableOptions.length === 0) {
    showNoAvailableFeedback();
    return false;
  }

  // Erste verfügbare Option auswählen, wenn keine ausgewählt ist
  var checkedOption = document.querySelector('input[name="tv_geraet"]:checked:not([disabled])');
  if (!checkedOption && availableOptions.length > 0) {
    availableOptions[0].checked = true;
    // Trigger change event
    availableOptions[0].dispatchEvent(new Event("change", {
      bubbles: true
    }));
  }
  return true;
}

/**
 * Holt die aktuellen Werte für Breite und Höhe
 */
function getCurrentDimensions() {
  var widthInput = document.querySelector('input[name="breite"]');
  var heightInput = document.querySelector('input[name="hoehe"]');
  return {
    width: widthInput ? parseInt(widthInput.value) || 0 : 0,
    height: heightInput ? parseInt(heightInput.value) || 0 : 0
  };
}

/**
 * Event Handler für Dimensionsänderungen
 */
function handleDimensionChange() {
  var _getCurrentDimensions2 = getCurrentDimensions(),
    width = _getCurrentDimensions2.width,
    height = _getCurrentDimensions2.height;
  if (width > 0 && height > 0) {
    updateAvailableTVSizes(width, height);
  }
}

/***/ }),

/***/ "./assets/js/configurator/dependencies/tv-seitenanschluss.js":
/*!*******************************************************************!*\
  !*** ./assets/js/configurator/dependencies/tv-seitenanschluss.js ***!
  \*******************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _dependencies_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./../dependencies.js */ "./assets/js/configurator/dependencies.js");
/* harmony import */ var _variables_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./../variables.js */ "./assets/js/configurator/variables.js");
/**
 * @version 2.3.0
 *
 * @todo Check functionality on rounded mirrors
 */



document.addEventListener("DOMContentLoaded", function () {
  /**
   * Values X Container
   */
  (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("tv_seitenanschluss", function (value) {
    return value !== "";
  }, "tv_seitenanschluss_position");
});

/***/ }),

/***/ "./assets/js/configurator/dependencies/verblendung.js":
/*!************************************************************!*\
  !*** ./assets/js/configurator/dependencies/verblendung.js ***!
  \************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _dependencies_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./../dependencies.js */ "./assets/js/configurator/dependencies.js");
/* harmony import */ var _variables_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./../variables.js */ "./assets/js/configurator/variables.js");
/**
 * @version 2.3.0
 */



document.addEventListener("DOMContentLoaded", function () {
  //*********************************/
  // Dependencies: Values X Container
  //*********************************/
  if (!_variables_js__WEBPACK_IMPORTED_MODULE_1__.isRund) {
    (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("verblendung", function (value) {
      return value !== "";
    }, "verblendung_ausfuehrung");
    (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("verblendung", function (value) {
      return value === "seitenverblendung";
    }, "verblendung_ausfuehrung_seiten");
    (0,_dependencies_js__WEBPACK_IMPORTED_MODULE_0__.dependenciesValuesXcontainer)("verblendung", function (value) {
      return value === "vollverblendung";
    }, "verblendung_ausfuehrung_voll");
  }
});

/***/ }),

/***/ "./assets/js/configurator/events.js":
/*!******************************************!*\
  !*** ./assets/js/configurator/events.js ***!
  \******************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony import */ var core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! core-js/modules/es.array.includes.js */ "./node_modules/core-js/modules/es.array.includes.js");
/* harmony import */ var core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! core-js/modules/es.object.to-string.js */ "./node_modules/core-js/modules/es.object.to-string.js");
/* harmony import */ var core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var core_js_modules_es_string_includes_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! core-js/modules/es.string.includes.js */ "./node_modules/core-js/modules/es.string.includes.js");
/* harmony import */ var core_js_modules_es_string_includes_js__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_string_includes_js__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var core_js_modules_es_string_trim_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! core-js/modules/es.string.trim.js */ "./node_modules/core-js/modules/es.string.trim.js");
/* harmony import */ var core_js_modules_es_string_trim_js__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_string_trim_js__WEBPACK_IMPORTED_MODULE_3__);
/* harmony import */ var core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! core-js/modules/web.dom-collections.for-each.js */ "./node_modules/core-js/modules/web.dom-collections.for-each.js");
/* harmony import */ var core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_4___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_4__);
/* harmony import */ var _variables__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./variables */ "./assets/js/configurator/variables.js");
/* harmony import */ var _summary__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./summary */ "./assets/js/configurator/summary.js");
/* harmony import */ var _pricecalcs_pricematrices__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./pricecalcs/pricematrices */ "./assets/js/configurator/pricecalcs/pricematrices.js");





/**
 * Product Configurator - Event Management Module
 *
 * This module handles all event listeners and interactions for the product
 * configurator system. It manages form input events, price calculations,
 * summary updates, and initialization of pre-selected options.
 *
 * Features:
 * - Automatic initialization of pre-selected options
 * - Real-time price matrix calculations
 * - Summary updates on input changes
 * - Dynamic event binding for configurator elements
 * - Performance optimizations with caching and debouncing
 *
 * @version 2.3.0
 * @package Configurator
 *
 * @todo Use for "dachschraege" calcPrice2x100, whilst using a "prefunction" to get the higher "breite" or "hoehe" value
 */





/**
 * Initialize configurator events when DOM is ready
 * Triggers change events for pre-selected radio inputs and performs
 * initial price calculations for all configurator elements
 */
document.addEventListener("DOMContentLoaded", function () {
  /**
   * Find and trigger events for pre-selected radio inputs
   * This ensures that any default selections are properly processed
   * and reflected in the summary and price calculations
   */
  var optionRadios = document.querySelectorAll("input.option-radio:checked");
  if (optionRadios.length > 0) {
    optionRadios.forEach(function (input) {
      // Dispatch synthetic change event with bubbling enabled
      // This triggers all associated event handlers and calculations
      input.dispatchEvent(new Event("change", {
        bubbles: true
      }));
    });
  }

  /**
   * Trigger initial input events for all option inputs with values
   * This ensures that price calculations are performed on page load
   * for inputs that have default or pre-filled values
   */
  var optionInputs = document.querySelectorAll(".option-input");
  optionInputs.forEach(function (input) {
    if (input.value && input.value.trim() !== "") {
      // Dispatch synthetic input event to trigger price calculations
      input.dispatchEvent(new Event("input", {
        bubbles: true
      }));
    }
  });

  // Small delay to ensure all calculations are completed before summary update
  setTimeout(_summary__WEBPACK_IMPORTED_MODULE_6__.updateSummary, 10);
});

/**
 * Global event listeners for configurator form elements
 * These listeners handle real-time updates to the summary display
 * when users interact with configuration options
 */

/**
 * Radio button change event handler
 * Updates summary when radio button selections change
 * Small delay ensures price matrix calculations complete first
 */
document.querySelectorAll(".option-radio").forEach(function (radioEl) {
  radioEl.addEventListener("change", function () {
    // 1ms delay allows price matrix calculations to complete
    // before summary update, preventing display inconsistencies
    setTimeout(_summary__WEBPACK_IMPORTED_MODULE_6__.updateSummary, 1);
  });
});

/**
 * Input field change event handler
 * Updates summary when text/number inputs change
 * Provides real-time feedback during user typing
 */
document.querySelectorAll(".option-input").forEach(function (inputEl) {
  inputEl.addEventListener("input", function () {
    // 1ms delay ensures price calculations are completed
    // before summary display is updated
    setTimeout(_summary__WEBPACK_IMPORTED_MODULE_6__.updateSummary, 1);
  });
});

/**
 * Centralized Price Calculation Event System
 *
 * Handles all price calculations in a consistent manner with automatic initialization.
 * This ensures all price matrices are calculated on page load and updated on user input.
 */
document.addEventListener("DOMContentLoaded", function () {
  // ====================== DIAMETER CALCULATIONS ======================
  var diameterPriceMatrix = document.querySelector('.option-price[name*="pxd"]');
  if (_variables__WEBPACK_IMPORTED_MODULE_5__.durchmesserInput && diameterPriceMatrix) {
    // Calculation function
    var calcDiameterPrice = function calcDiameterPrice() {
      (0,_pricecalcs_pricematrices__WEBPACK_IMPORTED_MODULE_7__.calcPrice1x100)(_variables__WEBPACK_IMPORTED_MODULE_5__.durchmesserInput, diameterPriceMatrix, diameterCache, diameterLastValue);
    }; // Event listener
    var diameterCache = {};
    var diameterLastValue = {
      lastRoundedValue: null
    };

    // Build cache
    diameterPriceMatrix.querySelectorAll("option[data-label]").forEach(function (option) {
      var labelValue = parseInt(option.dataset.label, 10);
      if (!isNaN(labelValue)) {
        diameterCache[String(labelValue)] = option;
      }
    });
    _variables__WEBPACK_IMPORTED_MODULE_5__.durchmesserInput.addEventListener("input", calcDiameterPrice);

    // Initial calculation
    calcDiameterPrice();
  }

  // ====================== SK2 MODE CALCULATIONS ======================
  var sk2Input = document.querySelector('.option-input[name="breite_hoehe"]');
  var sk2PriceMatrix = document.querySelector('.option-price[name*="pxd"]');
  if (_variables__WEBPACK_IMPORTED_MODULE_5__.isSK2 && sk2Input && sk2PriceMatrix) {
    // Calculation function
    var calcSK2Price = function calcSK2Price() {
      (0,_pricecalcs_pricematrices__WEBPACK_IMPORTED_MODULE_7__.calcPrice1x100)(sk2Input, sk2PriceMatrix, sk2Cache, sk2LastValue);
    }; // Event listener
    var sk2Cache = {};
    var sk2LastValue = {
      lastRoundedValue: null
    };

    // Build cache
    sk2PriceMatrix.querySelectorAll("option[data-label]").forEach(function (option) {
      var labelValue = parseInt(option.dataset.label, 10);
      if (!isNaN(labelValue)) {
        sk2Cache[String(labelValue)] = option;
      }
    });
    sk2Input.addEventListener("input", calcSK2Price);

    // Initial calculation
    calcSK2Price();
  }

  // ====================== DEPTH CALCULATIONS (10-unit steps) ======================
  var depthPriceMatrix = document.querySelector('.option-price[name*="pxt"]');
  if (_variables__WEBPACK_IMPORTED_MODULE_5__.tiefeInput && depthPriceMatrix) {
    // Calculation function
    var calcDepthPrice = function calcDepthPrice() {
      (0,_pricecalcs_pricematrices__WEBPACK_IMPORTED_MODULE_7__.calcPrice1x10)(_variables__WEBPACK_IMPORTED_MODULE_5__.tiefeInput, depthPriceMatrix, depthCache, depthLastValue);
    }; // Event listener
    var depthCache = {};
    var depthLastValue = {
      lastRoundedValue: null
    };

    // Build cache
    depthPriceMatrix.querySelectorAll("option[data-label]").forEach(function (option) {
      var labelValue = parseInt(option.dataset.label, 10);
      if (!isNaN(labelValue)) {
        depthCache[String(labelValue)] = option;
      }
    });
    _variables__WEBPACK_IMPORTED_MODULE_5__.tiefeInput.addEventListener("input", calcDepthPrice);

    // Initial calculation - force it even if input is empty by setting default value
    if (!_variables__WEBPACK_IMPORTED_MODULE_5__.tiefeInput.value || _variables__WEBPACK_IMPORTED_MODULE_5__.tiefeInput.value.trim() === "") {
      // Set minimum value if no value exists
      var minValue = _variables__WEBPACK_IMPORTED_MODULE_5__.tiefeInput.getAttribute("min") || "150";
      _variables__WEBPACK_IMPORTED_MODULE_5__.tiefeInput.value = minValue;
    }

    // Initial calculation
    setTimeout(function () {
      calcDepthPrice();
    }, 50); // Small delay to ensure DOM is fully ready
  }

  // ====================== WIDTH x HEIGHT CALCULATIONS ======================
  var whPriceMatrix = document.querySelector('.option-price[name*="pxbh"]');
  if (_variables__WEBPACK_IMPORTED_MODULE_5__.breiteInput && _variables__WEBPACK_IMPORTED_MODULE_5__.hoeheInput && whPriceMatrix) {
    // Calculation function
    var calcWidthHeightPrice = function calcWidthHeightPrice() {
      (0,_pricecalcs_pricematrices__WEBPACK_IMPORTED_MODULE_7__.calcPrice2x100)(_variables__WEBPACK_IMPORTED_MODULE_5__.breiteInput, _variables__WEBPACK_IMPORTED_MODULE_5__.hoeheInput, whPriceMatrix, whCache, whLastValue);
    }; // Event listeners
    var whCache = {};
    var whLastValue = {
      lastRoundedValue: null
    };

    // Build cache
    whPriceMatrix.querySelectorAll("option").forEach(function (option) {
      if (option.value.includes("x")) {
        whCache[option.value] = option;
      }
    });
    _variables__WEBPACK_IMPORTED_MODULE_5__.breiteInput.addEventListener("input", calcWidthHeightPrice);
    _variables__WEBPACK_IMPORTED_MODULE_5__.hoeheInput.addEventListener("input", calcWidthHeightPrice);

    // Initial calculation
    calcWidthHeightPrice();
  }

  // ====================== KLAPPELEMENTE CALCULATIONS (50-unit steps) ======================
  var klappelementerBreiteInput = document.querySelector('.option-input[name="klappelemente_breite"]');
  var klappelementePriceMatrix = document.querySelector('.option-price[name*="klappelemente_breite_aufpreis"]');
  if (klappelementerBreiteInput && klappelementePriceMatrix) {
    // Calculation function
    var calcKlappelementerPrice = function calcKlappelementerPrice() {
      (0,_pricecalcs_pricematrices__WEBPACK_IMPORTED_MODULE_7__.calcPrice1x50)(klappelementerBreiteInput, klappelementePriceMatrix, klappelementerCache, klappelementerLastValue);
    }; // Event listener
    var klappelementerCache = {};
    var klappelementerLastValue = {
      lastRoundedValue: null
    };

    // Build cache
    klappelementePriceMatrix.querySelectorAll("option[data-label]").forEach(function (option) {
      var labelValue = parseInt(option.dataset.label, 10);
      if (!isNaN(labelValue)) {
        klappelementerCache[String(labelValue)] = option;
      }
    });
    klappelementerBreiteInput.addEventListener("input", calcKlappelementerPrice);

    // Initial calculation - force it even if input is empty by setting default value
    if (!klappelementerBreiteInput.value || klappelementerBreiteInput.value.trim() === "") {
      // Set minimum value if no value exists
      var _minValue = klappelementerBreiteInput.getAttribute("min") || "150";
      klappelementerBreiteInput.value = _minValue;
    }

    // Initial calculation
    setTimeout(function () {
      calcKlappelementerPrice();
    }, 50); // Small delay to ensure DOM is fully ready
  }

  // ====================== ADDITIONAL INPUT FIELDS ======================
  // Handle additional inputs that should trigger diameter recalculation
  var additionalInputs = [document.querySelector('.option-input[name="hoehe_schnittkante"]'), document.querySelector('.option-input[name="schnittkante"]')];
  additionalInputs.forEach(function (input) {
    if (input && _variables__WEBPACK_IMPORTED_MODULE_5__.durchmesserInput && diameterPriceMatrix) {
      input.addEventListener("input", function () {
        // Trigger diameter recalculation when additional inputs change
        _variables__WEBPACK_IMPORTED_MODULE_5__.durchmesserInput.dispatchEvent(new Event("input", {
          bubbles: true
        }));
      });
    }
  });

  // ====================== DACHSCHRAEGE DIMENSION CALCULATIONS ======================
  var fourDimensionPriceMatrix = document.querySelector('.option-price[name*="pxbh"]');
  if (_variables__WEBPACK_IMPORTED_MODULE_5__.breiteUntenInput && _variables__WEBPACK_IMPORTED_MODULE_5__.breiteObenInput && _variables__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput && _variables__WEBPACK_IMPORTED_MODULE_5__.hoeheRechtsInput && fourDimensionPriceMatrix) {
    // Calculation function
    var calcFourDimensionPrice = function calcFourDimensionPrice() {
      (0,_pricecalcs_pricematrices__WEBPACK_IMPORTED_MODULE_7__.calcPrice4x100)(_variables__WEBPACK_IMPORTED_MODULE_5__.breiteUntenInput, _variables__WEBPACK_IMPORTED_MODULE_5__.breiteObenInput, _variables__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput, _variables__WEBPACK_IMPORTED_MODULE_5__.hoeheRechtsInput, fourDimensionPriceMatrix, fourDimensionCache, fourDimensionLastValue);
    }; // Event listeners for all four inputs
    var fourDimensionCache = {};
    var fourDimensionLastValue = {
      lastRoundedValue: null
    };

    // Build cache for four dimension calculations
    fourDimensionPriceMatrix.querySelectorAll("option").forEach(function (option) {
      if (option.value.includes("x")) {
        fourDimensionCache[option.value] = option;
      }
    });
    _variables__WEBPACK_IMPORTED_MODULE_5__.breiteUntenInput.addEventListener("input", calcFourDimensionPrice);
    _variables__WEBPACK_IMPORTED_MODULE_5__.breiteObenInput.addEventListener("input", calcFourDimensionPrice);
    _variables__WEBPACK_IMPORTED_MODULE_5__.hoeheLinksInput.addEventListener("input", calcFourDimensionPrice);
    _variables__WEBPACK_IMPORTED_MODULE_5__.hoeheRechtsInput.addEventListener("input", calcFourDimensionPrice);

    // Initial calculation
    calcFourDimensionPrice();
  }
});

/**
 * Future Enhancement Ideas:
 *
 * 1. Event delegation for dynamically added elements:
 *    document.addEventListener('change', (e) => {
 *      if (e.target.matches('.option-radio')) {
 *        // Handle radio changes
 *      }
 *    });
 *
 * 2. Custom events for better component communication:
 *    document.dispatchEvent(new CustomEvent('configurator:priceUpdated', {
 *      detail: { newPrice: calculatedPrice }
 *    });
 *
 * 3. Performance monitoring:
 *    const performanceObserver = new PerformanceObserver((list) => {
 *      // Monitor event handler performance
 *    });
 *
 * 4. Error boundary for event handlers:
 *    function safeEventHandler(handler) {
 *      return function(...args) {
 *        try {
 *          return handler.apply(this, args);
 *        } catch (error) {
 *          console.error('Event handler error:', error);
 *        }
 *      };
 *    }
 */

/***/ }),

/***/ "./assets/js/configurator/functions.js":
/*!*********************************************!*\
  !*** ./assets/js/configurator/functions.js ***!
  \*********************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   debounce: () => (/* binding */ debounce),
/* harmony export */   formatPrice: () => (/* binding */ formatPrice),
/* harmony export */   getProductPrice: () => (/* binding */ getProductPrice),
/* harmony export */   getProductTitle: () => (/* binding */ getProductTitle),
/* harmony export */   roundUp: () => (/* binding */ roundUp),
/* harmony export */   toNumber: () => (/* binding */ toNumber),
/* harmony export */   updateSelectAndTrigger: () => (/* binding */ updateSelectAndTrigger),
/* harmony export */   updateValueLabel: () => (/* binding */ updateValueLabel)
/* harmony export */ });
/* harmony import */ var core_js_modules_es_array_concat_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! core-js/modules/es.array.concat.js */ "./node_modules/core-js/modules/es.array.concat.js");
/* harmony import */ var core_js_modules_es_array_concat_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_concat_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var core_js_modules_es_number_to_fixed_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! core-js/modules/es.number.to-fixed.js */ "./node_modules/core-js/modules/es.number.to-fixed.js");
/* harmony import */ var core_js_modules_es_number_to_fixed_js__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_number_to_fixed_js__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var core_js_modules_es_regexp_exec_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! core-js/modules/es.regexp.exec.js */ "./node_modules/core-js/modules/es.regexp.exec.js");
/* harmony import */ var core_js_modules_es_regexp_exec_js__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_regexp_exec_js__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var core_js_modules_es_string_replace_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! core-js/modules/es.string.replace.js */ "./node_modules/core-js/modules/es.string.replace.js");
/* harmony import */ var core_js_modules_es_string_replace_js__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_string_replace_js__WEBPACK_IMPORTED_MODULE_3__);
/* harmony import */ var core_js_modules_es_string_trim_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! core-js/modules/es.string.trim.js */ "./node_modules/core-js/modules/es.string.trim.js");
/* harmony import */ var core_js_modules_es_string_trim_js__WEBPACK_IMPORTED_MODULE_4___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_string_trim_js__WEBPACK_IMPORTED_MODULE_4__);





/**
 * Product Configurator - Utility Functions Module
 *
 * This module provides essential utility functions used throughout the product
 * configurator system. It includes number parsing, price formatting, DOM
 * manipulation helpers, and performance optimization utilities.
 *
 * @version 2.3.0
 * @package Configurator
 *
 * @todo Check if these functions can be used, for a better DRY concept
 */

/**
 * Creates a debounced version of a function that delays execution
 * until after the specified delay has elapsed since the last invocation
 *
 * @param {Function} fn - The function to debounce
 * @param {number} delay - The delay in milliseconds (default: 200)
 * @returns {Function} The debounced function
 *
 * @example
 * const debouncedSearch = debounce(performSearch, 300);
 * input.addEventListener('input', debouncedSearch);
 */
function debounce(fn) {
  var delay = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 200;
  var timeout;
  return function () {
    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }
    clearTimeout(timeout);
    timeout = setTimeout(function () {
      return fn.apply(void 0, args);
    }, delay);
  };
}

/**
 * Safely parses strings into numbers with German locale support
 * Handles German decimal notation (comma) and currency symbols
 *
 * @param {string|number} value - The value to convert to number
 * @returns {number} Parsed number or 0 if invalid/NaN
 *
 * @example
 * toNumber("12,50 €")    // Returns 12.5
 * toNumber("1.234,56")   // Returns 1234.56
 * toNumber("invalid")    // Returns 0
 */
function toNumber(value) {
  // Replace comma with dot and remove all non-digit or non-dot characters
  var sanitized = (value === null || value === void 0 ? void 0 : value.replace(",", ".").replace(/[^\d.]/g, "")) || "";
  var number = parseFloat(sanitized);
  return isNaN(number) ? 0 : number;
}

/**
 * Rounds a number up to the next multiple of the specified step
 * Useful for price calculations and quantity adjustments
 *
 * @param {number} value - The value to round up
 * @param {number} step - The step to round up to (default: 100)
 * @returns {number} The rounded up value
 *
 * @example
 * roundUp(150, 100)  // Returns 200
 * roundUp(99, 50)    // Returns 100
 * roundUp(200, 100)  // Returns 200
 */
function roundUp(value) {
  var step = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 100;
  return Math.ceil(value / step) * step;
}

/**
 * Formats a numeric price value to German currency format
 * Returns empty string for zero values to handle optional pricing
 *
 * @param {string|number} price - The price value to format
 * @returns {string} Formatted price string or empty string for zero values
 *
 * @example
 * formatPrice(12.5)     // Returns "12,50 €"
 * formatPrice("15.99")  // Returns "15,99 €"
 * formatPrice(0)        // Returns ""
 * formatPrice("0,00")   // Returns ""
 */
function formatPrice(price) {
  var numericPrice = toNumber(String(price));
  return numericPrice === 0 ? "" : "".concat(numericPrice.toFixed(2).replace(".", ","), " \u20AC");
}

/**
 * Retrieves the product title from the current page
 * Searches for common product title selectors used in the theme
 *
 * @returns {string} Product title text or empty string if not found
 *
 * @example
 * const title = getProductTitle(); // "Badezimmerspiegel Premium"
 */
function getProductTitle() {
  var titleEl = document.querySelector(".product-titel") || document.querySelector(".product_title");
  return titleEl ? titleEl.textContent.trim() : "";
}

/**
 * Retrieves the base product price from the current page
 * Searches for common price selectors and parses the numeric value
 *
 * @returns {number} Product price as number or 0 if not found
 *
 * @example
 * const price = getProductPrice(); // 199.99
 */
function getProductPrice() {
  var priceEl = document.querySelector(".product-price") || document.querySelector(".price");
  return priceEl ? toNumber(priceEl.textContent.trim()) : 0;
}

/**
 * Updates a select element value and triggers a change event
 * Ensures proper event propagation for form validation and listeners
 *
 * @param {HTMLSelectElement} select - The select element to update
 * @param {HTMLOptionElement} option - The option to select
 *
 * @example
 * const select = document.querySelector('#size-select');
 * const option = select.querySelector('option[value="large"]');
 * updateSelectAndTrigger(select, option);
 */
function updateSelectAndTrigger(select, option) {
  select.value = option.value;

  // Synchronize data-price attribute for display systems
  select.setAttribute("data-price", option.getAttribute("data-price") || "0");

  // Trigger change event with bubbling for dependent systems (summary updates, etc.)
  var event = new Event("change", {
    bubbles: true
  });
  select.dispatchEvent(event);
}

/**
 * Updates the value label and button state based on current selection
 * Handles special cases like "none" selections and price display logic
 *
 * @param {HTMLInputElement} input - The selected input element
 * @param {HTMLElement} valueLabel - The label element to update with text
 * @param {HTMLElement} button - The toggle button to update classes on
 * @param {Object} mgr - Manager object containing component state
 * @param {boolean} mgr.valueChanged - Flag to track if value has changed
 *
 * @example
 * updateValueLabel(radioInput, labelElement, toggleButton, managerObject);
 */
function updateValueLabel(input, valueLabel, button, mgr) {
  var selLabel = input.closest("label");
  var text = input.dataset.label || "";
  var price = input.dataset.price || "0";

  // Determine selection state
  var isNone = input.value === "";
  var isOverride = isNone && selLabel.classList.contains("use-first-image-for-none");
  var treatAsValue = !isNone || isOverride;
  if (treatAsValue) {
    // For none-override cases, hide the price display
    var displayPrice = isOverride ? "" : price;
    valueLabel.textContent = displayPrice && displayPrice !== "0" ? "".concat(text, " (+").concat(displayPrice, " \u20AC)") : text;
    valueLabel.classList.add("fade", "show");
  } else {
    // Clear label for empty selections
    valueLabel.textContent = "";
    valueLabel.classList.remove("show");
  }

  // Update button visual state classes
  button.classList.remove("no-selection", "on-selection", "yes-selection");
  button.classList.add(treatAsValue ? "yes-selection" : "no-selection");

  // Mark that value has been changed for manager tracking
  mgr.valueChanged = true;
}

/**
 * Additional utility functions for future use
 * These functions can be uncommented and used as needed
 */

/**
 * Throttles function execution to improve performance
 * Unlike debounce, throttle ensures function runs at regular intervals
 *
 * @param {Function} fn - Function to throttle
 * @param {number} limit - Time limit in milliseconds
 * @returns {Function} Throttled function
 */
/*
export function throttle(fn, limit = 100) {
  let inThrottle;
  return function(...args) {
    if (!inThrottle) {
      fn.apply(this, args);
      inThrottle = true;
      setTimeout(() => inThrottle = false, limit);
    }
  };
}
*/

/**
 * Deep clones an object (simple implementation)
 *
 * @param {Object} obj - Object to clone
 * @returns {Object} Cloned object
 */
/*
export function deepClone(obj) {
  return JSON.parse(JSON.stringify(obj));
}
*/

/**
 * Validates email format using regex
 *
 * @param {string} email - Email address to validate
 * @returns {boolean} True if valid email format
 */
/*
export function isValidEmail(email) {
  const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
  return emailRegex.test(email);
}
*/

/**
 * Formats file size in human readable format
 *
 * @param {number} bytes - File size in bytes
 * @returns {string} Formatted file size (e.g., "1.2 MB")
 */
/*
export function formatFileSize(bytes) {
  if (bytes === 0) return '0 Bytes';
  const k = 1024;
  const sizes = ['Bytes', 'KB', 'MB', 'GB'];
  const i = Math.floor(Math.log(bytes) / Math.log(k));
  return parseFloat((bytes / Math.pow(k, i)).toFixed(2)) + ' ' + sizes[i];
}
*/

/***/ }),

/***/ "./assets/js/configurator/input.js":
/*!*****************************************!*\
  !*** ./assets/js/configurator/input.js ***!
  \*****************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony import */ var core_js_modules_es_array_join_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! core-js/modules/es.array.join.js */ "./node_modules/core-js/modules/es.array.join.js");
/* harmony import */ var core_js_modules_es_array_join_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_join_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var core_js_modules_es_function_name_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! core-js/modules/es.function.name.js */ "./node_modules/core-js/modules/es.function.name.js");
/* harmony import */ var core_js_modules_es_function_name_js__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_function_name_js__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! core-js/modules/es.object.to-string.js */ "./node_modules/core-js/modules/es.object.to-string.js");
/* harmony import */ var core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var core_js_modules_es_string_trim_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! core-js/modules/es.string.trim.js */ "./node_modules/core-js/modules/es.string.trim.js");
/* harmony import */ var core_js_modules_es_string_trim_js__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_string_trim_js__WEBPACK_IMPORTED_MODULE_3__);
/* harmony import */ var core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! core-js/modules/web.dom-collections.for-each.js */ "./node_modules/core-js/modules/web.dom-collections.for-each.js");
/* harmony import */ var core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_4___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_4__);
/* harmony import */ var _dependencies_dachschraege_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./dependencies/dachschraege.js */ "./assets/js/configurator/dependencies/dachschraege.js");





/**
 * Product Configurator - Input Validation Module
 *
 * This module serves as the central validation engine for the product configurator.
 * It provides real-time validation feedback, custom error messages,
 * relational field validation, and Bootstrap form state management.
 *
 * ARCHITECTURE (DRY Principle):
 * - input.js: Central validation engine with generic event listeners
 * - Specialized modules (e.g., dachschraege.js): Domain-specific validation rules
 * - Callback system: Specialized modules provide callback functions for input.js
 *
 * Features:
 * - Real-time validation with visual feedback
 * - Custom error message positioning
 * - Relational field constraints (e.g., width dependencies)
 * - Bootstrap form state classes (is-valid, is-invalid)
 * - Dynamic min/max attribute updates
 * - Modular validation rules via callback system
 *
 * @version 2.3.0
 * @package Configurator
 */


document.addEventListener("DOMContentLoaded", function () {
  /**
   * Select all relevant input fields for validation
   * Includes number inputs, text inputs, and textareas with .option-input class
   */
  var fields = document.querySelectorAll('input.option-input[type="number"], input.option-input[type="text"], textarea.option-input');

  /**
   * Special field references for relational validation
   */

  /**
   * Ensures that an invalid feedback container exists for the given field
   * Creates the container if it doesn't exist and positions it correctly
   * relative to Bootstrap's form-floating structure
   *
   * @param {HTMLElement} field - The input field requiring validation feedback
   * @returns {HTMLElement} The invalid feedback container element
   */
  function ensureInvalidContainer(field) {
    var invalidFeedback;

    // Check if field is within a Bootstrap form-floating container
    var formFloating = field.closest(".form-floating");
    if (formFloating) {
      // For form-floating: place feedback after the floating container
      var nextSibling = formFloating.nextElementSibling;
      if (nextSibling && nextSibling.classList.contains("invalid-feedback")) {
        // Reuse existing feedback element
        invalidFeedback = nextSibling;
      } else {
        // Create new feedback element
        invalidFeedback = document.createElement("div");
        invalidFeedback.classList.add("invalid-feedback");
        invalidFeedback.style.display = "none"; // Initially hidden
        formFloating.insertAdjacentElement("afterend", invalidFeedback);
      }
    } else {
      // For standard inputs: place feedback within parent element
      invalidFeedback = field.parentNode.querySelector(".invalid-feedback");
      if (!invalidFeedback) {
        invalidFeedback = document.createElement("div");
        invalidFeedback.classList.add("invalid-feedback");
        invalidFeedback.style.display = "none"; // Initially hidden
        field.parentNode.appendChild(invalidFeedback);
      }
    }
    return invalidFeedback;
  }

  /**
   * Sets the validation error message for a field
   * Creates the feedback container if needed and manages visibility
   *
   * @param {HTMLElement} field - The input field to show feedback for
   * @param {string} message - The error message to display (empty string hides feedback)
   */
  function setInvalidFeedback(field, message) {
    var feedbackElement = ensureInvalidContainer(field);
    feedbackElement.textContent = message;

    // Show feedback if message exists, hide otherwise
    if (message) {
      feedbackElement.style.display = "block";
    } else {
      feedbackElement.style.display = "none";
    }

    // Trigger carousel height update when feedback visibility changes
    (0,_dependencies_dachschraege_js__WEBPACK_IMPORTED_MODULE_5__.triggerCarouselHeightUpdate)();
  }

  /**
   * Validates a field based on multiple criteria:
   * - Required field validation
   * - Min/max value validation for number inputs
   * - Custom relational validation rules
   * - Sets Bootstrap validation classes and error messages
   *
   * @param {HTMLElement} field - The input field to validate
   */
  function validateField(field) {
    var messages = [];

    // 1. Required field validation
    if (field.hasAttribute("required") && field.value.trim() === "") {
      messages.push("Dieses Feld ist erforderlich.");
    }

    // 2. Min/max validation for number inputs
    if (field.type === "number" && field.value.trim() !== "") {
      var val = parseFloat(field.value);
      var minAttr = field.getAttribute("min");
      var maxAttr = field.getAttribute("max");
      if (minAttr !== null && val < parseFloat(minAttr)) {
        messages.push("Der Wert muss mindestens ".concat(minAttr, " sein."));
      }
      if (maxAttr !== null && val > parseFloat(maxAttr)) {
        messages.push("Der Wert darf h\xF6chstens ".concat(maxAttr, " sein."));
      }
    }

    // 3. Custom relational validation rules

    // Dachschräge validation (outsourced to dedicated module)
    if ((0,_dependencies_dachschraege_js__WEBPACK_IMPORTED_MODULE_5__.isDachschraegeField)(field)) {
      var dachschraegeMessage = (0,_dependencies_dachschraege_js__WEBPACK_IMPORTED_MODULE_5__.getDachschraegeValidationMessage)(field);
      if (dachschraegeMessage) {
        messages.push(dachschraegeMessage);
      }
    }

    // 4. Apply validation state based on collected messages
    if (messages.length > 0) {
      field.classList.add("is-invalid");
      setInvalidFeedback(field, messages.join(" "));
    } else {
      field.classList.remove("is-invalid");
      setInvalidFeedback(field, "");
    }
  }

  // Make validateField globally available for other modules (e.g., dachschraege.js)
  window.validateField = validateField;

  /**
   * Updates field state classes based on focus and content:
   * - 'on-input': Field is currently focused
   * - 'yes-input': Field has content and is not focused
   * - 'no-input': Field is empty and not focused
   *
   * Also triggers field validation after updating classes
   *
   * @param {HTMLElement} field - The input field to update
   */
  function updateClasses(field) {
    // Handle focus state
    if (document.activeElement === field) {
      field.classList.add("on-input");
    } else {
      field.classList.remove("on-input");

      // Update content-based classes when not focused
      if (field.value.trim() === "") {
        field.classList.add("no-input");
        field.classList.remove("yes-input");
      } else {
        field.classList.add("yes-input");
        field.classList.remove("no-input");
      }
    }

    // Trigger validation after class updates
    validateField(field);
  }

  /**
   * Updates the max attribute of shelf width field based on bottom width
   * Ensures shelf width cannot exceed bottom width and corrects values if needed
   * This provides dynamic constraint updates for related fields
   */
  function updateAblageBreiteMax() {
    var breiteUntenField = document.querySelector('input[name="breite_unten"]');
    var ablageBreiteField = document.querySelector('input[name="ablage_breite"]');
    if (breiteUntenField && ablageBreiteField) {
      var breiteUntenValue = parseFloat(breiteUntenField.value) || 0;
      ablageBreiteField.setAttribute("max", breiteUntenValue);

      // Auto-correct shelf width if it exceeds bottom width
      var ablageBreiteValue = parseFloat(ablageBreiteField.value) || 0;
      if (ablageBreiteValue > breiteUntenValue) {
        ablageBreiteField.value = breiteUntenValue;
      }

      // Update classes and validation after correction
      updateClasses(ablageBreiteField);
    }
  }

  /**
   * Initialize event listeners for all input fields
   * Sets up real-time validation and visual feedback
   */
  fields.forEach(function (field) {
    /**
     * Focus event handler
     * Updates visual state when field gains focus
     */
    field.addEventListener("focus", function () {
      updateClasses(field);
    });

    /**
     * Input event handler (real-time validation)
     * Provides immediate feedback during typing and handles relational updates
     */
    field.addEventListener("input", function () {
      updateClasses(field);

      // Handle width field changes for relational validation
      if (field.name === "breite_unten") {
        updateAblageBreiteMax();
      }

      // Handle Dachschräge field dependencies using centralized logic
      if ((0,_dependencies_dachschraege_js__WEBPACK_IMPORTED_MODULE_5__.isDachschraegeField)(field)) {
        (0,_dependencies_dachschraege_js__WEBPACK_IMPORTED_MODULE_5__.onDachschraegeFieldInput)(field);
      }

      // Show 'is-valid' state during typing when no errors exist
      // This provides positive feedback during data entry
      if (document.activeElement === field && !field.classList.contains("is-invalid")) {
        field.classList.add("is-valid");
      } else {
        field.classList.remove("is-valid");
      }
    });

    /**
     * Change event handler
     * Handles cross-field validation and position suggestions when user completes input
     */
    field.addEventListener("change", function () {
      // Handle Dachschräge field dependencies and position suggestions
      if ((0,_dependencies_dachschraege_js__WEBPACK_IMPORTED_MODULE_5__.isDachschraegeField)(field)) {
        (0,_dependencies_dachschraege_js__WEBPACK_IMPORTED_MODULE_5__.onDachschraegeFieldChange)(field);
      }
    });

    /**
     * Blur event handler
     * Performs final validation when field loses focus
     * Removes 'is-valid' state for cleaner final appearance
     */
    field.addEventListener("blur", function () {
      // Optional: Auto-round values to valid range
      // autoRoundToRange(field);

      // Final validation and class updates
      updateClasses(field);

      // Remove positive validation state on blur for cleaner UI
      field.classList.remove("is-valid");
    });
  });

  /**
   * Initialize shelf width constraints on page load
   * Sets up initial max attribute if main width field has a value
   */
  updateAblageBreiteMax();
});

/**
 * Future Enhancement Ideas:
 *
 * 1. Auto-rounding function for numeric inputs:
 *    function autoRoundToRange(field) {
 *      if (field.type === "number" && field.value.trim() !== "") {
 *        const val = parseFloat(field.value);
 *        const min = parseFloat(field.getAttribute("min")) || -Infinity;
 *        const max = parseFloat(field.getAttribute("max")) || Infinity;
 *        const corrected = Math.min(Math.max(val, min), max);
 *        if (corrected !== val) {
 *          field.value = corrected;
 *        }
 *      }
 *    }
 *
 * 2. Debounced validation for better performance on rapid input
 * 3. Custom validation rules configuration object
 * 4. Internationalization support for error messages
 * 5. Accessibility enhancements with ARIA attributes
 */

/***/ }),

/***/ "./assets/js/configurator/offdrops.js":
/*!********************************************!*\
  !*** ./assets/js/configurator/offdrops.js ***!
  \********************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony import */ var core_js_modules_es_array_concat_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! core-js/modules/es.array.concat.js */ "./node_modules/core-js/modules/es.array.concat.js");
/* harmony import */ var core_js_modules_es_array_concat_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_concat_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var core_js_modules_es_array_filter_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! core-js/modules/es.array.filter.js */ "./node_modules/core-js/modules/es.array.filter.js");
/* harmony import */ var core_js_modules_es_array_filter_js__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_filter_js__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var core_js_modules_es_array_from_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! core-js/modules/es.array.from.js */ "./node_modules/core-js/modules/es.array.from.js");
/* harmony import */ var core_js_modules_es_array_from_js__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_from_js__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! core-js/modules/es.array.includes.js */ "./node_modules/core-js/modules/es.array.includes.js");
/* harmony import */ var core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_3__);
/* harmony import */ var core_js_modules_es_array_iterator_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! core-js/modules/es.array.iterator.js */ "./node_modules/core-js/modules/es.array.iterator.js");
/* harmony import */ var core_js_modules_es_array_iterator_js__WEBPACK_IMPORTED_MODULE_4___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_iterator_js__WEBPACK_IMPORTED_MODULE_4__);
/* harmony import */ var core_js_modules_es_array_map_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! core-js/modules/es.array.map.js */ "./node_modules/core-js/modules/es.array.map.js");
/* harmony import */ var core_js_modules_es_array_map_js__WEBPACK_IMPORTED_MODULE_5___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_map_js__WEBPACK_IMPORTED_MODULE_5__);
/* harmony import */ var core_js_modules_es_array_slice_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! core-js/modules/es.array.slice.js */ "./node_modules/core-js/modules/es.array.slice.js");
/* harmony import */ var core_js_modules_es_array_slice_js__WEBPACK_IMPORTED_MODULE_6___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_slice_js__WEBPACK_IMPORTED_MODULE_6__);
/* harmony import */ var core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! core-js/modules/es.object.to-string.js */ "./node_modules/core-js/modules/es.object.to-string.js");
/* harmony import */ var core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_7___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_7__);
/* harmony import */ var core_js_modules_es_regexp_exec_js__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! core-js/modules/es.regexp.exec.js */ "./node_modules/core-js/modules/es.regexp.exec.js");
/* harmony import */ var core_js_modules_es_regexp_exec_js__WEBPACK_IMPORTED_MODULE_8___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_regexp_exec_js__WEBPACK_IMPORTED_MODULE_8__);
/* harmony import */ var core_js_modules_es_regexp_to_string_js__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! core-js/modules/es.regexp.to-string.js */ "./node_modules/core-js/modules/es.regexp.to-string.js");
/* harmony import */ var core_js_modules_es_regexp_to_string_js__WEBPACK_IMPORTED_MODULE_9___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_regexp_to_string_js__WEBPACK_IMPORTED_MODULE_9__);
/* harmony import */ var core_js_modules_es_string_includes_js__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! core-js/modules/es.string.includes.js */ "./node_modules/core-js/modules/es.string.includes.js");
/* harmony import */ var core_js_modules_es_string_includes_js__WEBPACK_IMPORTED_MODULE_10___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_string_includes_js__WEBPACK_IMPORTED_MODULE_10__);
/* harmony import */ var core_js_modules_es_string_iterator_js__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! core-js/modules/es.string.iterator.js */ "./node_modules/core-js/modules/es.string.iterator.js");
/* harmony import */ var core_js_modules_es_string_iterator_js__WEBPACK_IMPORTED_MODULE_11___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_string_iterator_js__WEBPACK_IMPORTED_MODULE_11__);
/* harmony import */ var core_js_modules_es_string_replace_js__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! core-js/modules/es.string.replace.js */ "./node_modules/core-js/modules/es.string.replace.js");
/* harmony import */ var core_js_modules_es_string_replace_js__WEBPACK_IMPORTED_MODULE_12___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_string_replace_js__WEBPACK_IMPORTED_MODULE_12__);
/* harmony import */ var core_js_modules_es_string_trim_js__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! core-js/modules/es.string.trim.js */ "./node_modules/core-js/modules/es.string.trim.js");
/* harmony import */ var core_js_modules_es_string_trim_js__WEBPACK_IMPORTED_MODULE_13___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_string_trim_js__WEBPACK_IMPORTED_MODULE_13__);
/* harmony import */ var core_js_modules_es_weak_map_js__WEBPACK_IMPORTED_MODULE_14__ = __webpack_require__(/*! core-js/modules/es.weak-map.js */ "./node_modules/core-js/modules/es.weak-map.js");
/* harmony import */ var core_js_modules_es_weak_map_js__WEBPACK_IMPORTED_MODULE_14___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_weak_map_js__WEBPACK_IMPORTED_MODULE_14__);
/* harmony import */ var core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_15__ = __webpack_require__(/*! core-js/modules/web.dom-collections.for-each.js */ "./node_modules/core-js/modules/web.dom-collections.for-each.js");
/* harmony import */ var core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_15___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_15__);
/* harmony import */ var core_js_modules_web_dom_collections_iterator_js__WEBPACK_IMPORTED_MODULE_16__ = __webpack_require__(/*! core-js/modules/web.dom-collections.iterator.js */ "./node_modules/core-js/modules/web.dom-collections.iterator.js");
/* harmony import */ var core_js_modules_web_dom_collections_iterator_js__WEBPACK_IMPORTED_MODULE_16___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_web_dom_collections_iterator_js__WEBPACK_IMPORTED_MODULE_16__);

















/**
 * Offdrop Component - Responsive Toggle between Dropdown and Offcanvas
 *
 * This module provides a responsive solution that toggles between a dropdown
 * (on desktop) and an offcanvas (on mobile) based on Bootstrap 5 components.
 * Includes MutationObserver to track dynamic attribute changes and search
 * functionality for option groups.
 *
 * @version 2.3.0
 * @package Configurator
 *
 * @todo Offdrops desktop, dropdowns are not navigable through keyboard
 */

/**
 * Configuration constants for responsive behavior
 */
var BREAKPOINT = 768; // Bootstrap md breakpoint in pixels
var MD_BREAKPOINT = 768; // Bootstrap md breakpoint for dropdown width calculations
var managers = []; // Stores all component instances for lifecycle management
var skipNextClick = false; // Flag to prevent immediate reopening after modal close

/**
 * Initialize all offdrop components when DOM is ready
 */
document.addEventListener("DOMContentLoaded", function () {
  /**
   * 1. Handle modal hide events to prevent dropdown closing after modal close
   * This prevents unwanted interaction between Bootstrap modals and dropdowns
   */
  document.addEventListener("hide.bs.modal", function () {
    skipNextClick = true;
  });

  /**
   * 2. Initialize and manage all offdrop button components
   * Creates responsive dropdown/offcanvas pairs for each button
   */
  document.querySelectorAll('button[data-target="offdrop"]').forEach(function (button) {
    var offcanvasId = button.dataset.offcanvasId;
    var dropdownId = button.dataset.dropdownId;
    var group = button.closest(".option-offdrop");
    if (!group) return;

    // Get required DOM elements
    var source = group.querySelector(".offdrop-body");
    var offcanvasEl = document.getElementById(offcanvasId);
    var dropdownEl = document.getElementById(dropdownId);
    if (!source || !offcanvasEl || !dropdownEl) return;
    var offcanvasBody = offcanvasEl.querySelector(".offcanvas-body");
    var dropdownBody = dropdownEl.querySelector(".dropdown-body");
    if (!offcanvasBody || !dropdownBody) return;
    var valueLabel = group.querySelector(".option-value-label");

    // Extract and clear source nodes for dynamic placement
    var nodes = Array.from(source.children);
    source.innerHTML = "";

    // Initialize Bootstrap component instances
    var offcanvasInst = bootstrap.Offcanvas.getOrCreateInstance(offcanvasEl);
    var dropdownInst = bootstrap.Dropdown.getOrCreateInstance(button);
    dropdownInst._menu = dropdownEl; // Workaround for separate menu element

    /**
     * Manager instance containing all component state and methods
     */
    var mgr = {
      group: group,
      nodes: nodes,
      offcanvasBody: offcanvasBody,
      dropdownBody: dropdownBody,
      dropdownEl: dropdownEl,
      offcanvasInst: offcanvasInst,
      dropdownInst: dropdownInst,
      isDesktop: null,
      originalValue: null,
      valueChanged: false,
      updateValueLabel: updateValueLabel,
      observer: null,
      setupObserver: setupObserver
    };

    /**
     * Updates the value label and button state based on current selection
     *
     * @param {HTMLInputElement} input - The selected input element
     */
    function updateValueLabel(input) {
      var selLabel = input.closest("label");
      var text = input.dataset.label || "";
      var price = input.dataset.price || "0";
      var isNone = input.value === "";
      var isOverride = isNone && selLabel.classList.contains("use-first-image-for-none");
      var treatAsValue = !isNone || isOverride;
      if (treatAsValue) {
        // For none-override cases, hide the price display
        var displayPrice = isOverride ? "" : price;
        valueLabel.textContent = displayPrice && displayPrice !== "0" ? "".concat(text, " (+").concat(displayPrice, " \u20AC)") : text;
        valueLabel.classList.add("fade", "show");
      } else {
        valueLabel.textContent = "";
        valueLabel.classList.remove("show");
      }

      // Update button visual state
      button.classList.remove("no-selection", "on-selection", "yes-selection");
      button.classList.add(treatAsValue ? "yes-selection" : "no-selection");
      mgr.valueChanged = true;
    }

    /**
     * Sets up MutationObserver to track dynamic changes to data attributes
     * Monitors changes to data-price, data-label, and value attributes
     */
    function setupObserver() {
      var config = {
        attributes: true,
        attributeFilter: ["data-price", "data-label", "value"],
        subtree: true
      };
      var callback = function callback(mutations) {
        mutations.forEach(function (mutation) {
          if (mutation.type === "attributes" && mutation.target.matches('input[type="radio"], input[type="checkbox"]')) {
            var input = mutation.target;
            if (input.checked) {
              mgr.updateValueLabel(input);
            }
          }
        });
      };

      // Cleanup existing observer before creating new one
      if (mgr.observer) mgr.observer.disconnect();
      mgr.observer = new MutationObserver(callback);
      mgr.observer.observe(mgr.dropdownBody, config);
      mgr.observer.observe(mgr.offcanvasBody, config);
    }

    // Initialize the mutation observer
    mgr.setupObserver();

    /**
     * Handle input change events for radio buttons and checkboxes
     */
    mgr.group.addEventListener("change", function (e) {
      if (e.target.matches('input[type="radio"], input[type="checkbox"]')) {
        // Update active state on label buttons
        mgr.group.querySelectorAll("label.btn").forEach(function (lbl) {
          return lbl.classList.remove("active");
        });
        var selLbl = e.target.closest("label");
        if (selLbl) selLbl.classList.add("active");

        // Update value label with slight delay for smooth UX
        setTimeout(function () {
          return mgr.updateValueLabel(e.target);
        }, 150);
      }
    });

    /**
     * Offcanvas event handlers
     */

    // Offcanvas show event
    offcanvasEl.addEventListener("show.bs.offcanvas", function () {
      button.classList.add("on-selection");
      var checked = mgr.offcanvasBody.querySelector("input:checked");
      mgr.originalValue = checked ? checked.value : "";
      mgr.valueChanged = false;
    });

    // Offcanvas hide event
    offcanvasEl.addEventListener("hide.bs.offcanvas", function () {
      var checked = mgr.offcanvasBody.querySelector("input:checked");
      var newValue = checked ? checked.value : "";
      if (!mgr.valueChanged) {
        button.classList.remove("on-selection");
        button.classList.add(newValue !== "" ? "yes-selection" : "no-selection");
      }
      mgr.valueChanged = false;
    });

    /**
     * Dropdown event handlers
     */
    // Dropdown show event
    button.addEventListener("show.bs.dropdown", function () {
      button.classList.add("on-selection");
      var checked = mgr.dropdownBody.querySelector("input:checked");
      mgr.originalValue = checked ? checked.value : "";
      mgr.valueChanged = false;

      // Set responsive max-height and overflow for dropdown
      mgr.dropdownBody.style.maxHeight = "69vh";
      mgr.dropdownBody.style.overflowY = "auto";
      mgr.dropdownBody.style.overflowX = "hidden";

      // Constrain dropdown width to trigger button width
      mgr.updateDropdownWidth();
    });

    /**
     * Update dropdown width to match trigger button width
     */
    mgr.updateDropdownWidth = function () {
      var triggerWidth = button.offsetWidth;
      mgr.dropdownEl.style.minWidth = "".concat(triggerWidth, "px");
      mgr.dropdownEl.style.maxWidth = "".concat(triggerWidth, "px");
      mgr.dropdownEl.style.width = "".concat(triggerWidth, "px");
    };

    // Dropdown hide event
    button.addEventListener("hide.bs.dropdown", function () {
      var checked = mgr.dropdownBody.querySelector("input:checked");
      var newValue = checked ? checked.value : "";
      if (!mgr.valueChanged) {
        button.classList.remove("on-selection");
        button.classList.add(newValue !== "" ? "yes-selection" : "no-selection");
      }
      mgr.valueChanged = false;
    });

    /**
     * Main button click handler for responsive toggle
     */
    button.addEventListener("click", function (e) {
      e.preventDefault();
      if (mgr.isDesktop) {
        mgr.dropdownInst.toggle();
      } else {
        mgr.offcanvasInst.toggle();
      }
    });

    /**
     * Dropdown input change events with auto-close and focus management
     */
    mgr.dropdownBody.addEventListener("change", function (e) {
      if (e.target.matches('input[type="radio"], input[type="checkbox"]')) {
        mgr.dropdownInst.hide();

        // Only for trusted user events (not programmatic)
        if (e.isTrusted) {
          // Set visual feedback and focus after transition
          setTimeout(function () {
            button.classList.add("just-selected");
            button.focus();
          }, 150);

          // Remove visual feedback on blur
          button.addEventListener("blur", function () {
            return button.classList.remove("just-selected");
          }, {
            once: true
          });
        }
      }
    });

    /**
     * Offcanvas input change events with auto-close and focus management
     */
    mgr.offcanvasBody.addEventListener("change", function (e) {
      if (e.target.matches('input[type="radio"], input[type="checkbox"]')) {
        mgr.offcanvasInst.hide();

        // Focus management after offcanvas is completely hidden
        offcanvasEl.addEventListener("hidden.bs.offcanvas", function () {
          button.classList.add("just-selected");
          button.focus();
        }, {
          once: true
        });

        // Remove visual feedback on blur
        button.addEventListener("blur", function () {
          return button.classList.remove("just-selected");
        }, {
          once: true
        });
      }
    });

    // Add manager to global array for lifecycle management
    managers.push(mgr);

    // Remove d-none class after initialization to prevent layout shift
    source.classList.remove("d-none");
  });

  /**
   * 3. Global click handler to close dropdowns (preserves Bootstrap behavior)
   * Handles edge cases with modals and prevents unwanted closures
   */
  document.addEventListener("click", function (e) {
    // Skip if modal was just closed to prevent dropdown interference
    if (skipNextClick) {
      skipNextClick = false;
      return;
    }

    // Don't close dropdowns when clicking inside modal
    if (e.target.closest(".modal.show")) return;

    // Close open dropdowns when clicking outside
    managers.forEach(function (mgr) {
      if (!mgr.isDesktop) return;
      var menu = mgr.dropdownInst._menu;
      var toggleEl = mgr.dropdownInst._element;
      if (menu.classList.contains("show") && !menu.contains(e.target) && !toggleEl.contains(e.target)) {
        mgr.dropdownInst.hide();
      }
    });
  });

  /**
   * 4. Responsive behavior management
   * Handles transitions between desktop/mobile layouts
   */
  var mql = window.matchMedia("(min-width: ".concat(BREAKPOINT, "px)"));

  /**
   * Handle responsive layout changes
   *
   * @param {MediaQueryListEvent} e - Media query change event
   */
  var handleResponsiveBehaviour = function handleResponsiveBehaviour(e) {
    var matchesDesktop = e.matches;
    managers.forEach(function (mgr) {
      // Skip if already in correct state
      if (mgr.isDesktop === matchesDesktop) return;

      // Close active component before switching layouts
      if (mgr.isDesktop) mgr.dropdownInst.hide();else mgr.offcanvasInst.hide();

      // Update responsive state
      mgr.isDesktop = matchesDesktop;

      // Update CSS classes for layout
      mgr.group.classList.toggle("offdrop-dropdown", matchesDesktop);
      mgr.group.classList.toggle("offdrop-offcanvas", !matchesDesktop);

      // Disconnect observer before DOM manipulation
      if (mgr.observer) mgr.observer.disconnect();

      // Move nodes to appropriate container
      var target = matchesDesktop ? mgr.dropdownBody : mgr.offcanvasBody;
      target.innerHTML = "";
      mgr.nodes.forEach(function (node) {
        return target.appendChild(node);
      });

      // Reconnect observer after DOM changes
      mgr.setupObserver();

      // Adjust button styling for desktop layout
      target.querySelectorAll(".btn-group-vertical .btn").forEach(function (btn) {
        btn.classList.toggle("border-0", matchesDesktop);
      });

      // Maintain active states after layout switch
      target.querySelectorAll("input:checked").forEach(function (input) {
        var lbl = input.closest("label");
        if (lbl && !lbl.classList.contains("active")) lbl.classList.add("active");
      });
    });
  };

  /**
   * 5. Register responsive behavior listener
   * Uses modern addEventListener with fallback for older browsers
   */
  var addListener = mql.addEventListener || mql.addListener;
  addListener.call(mql, "change", handleResponsiveBehaviour);

  /**
   * 6. Initialize responsive state and value labels
   * Set initial state based on current viewport and update displays
   */
  handleResponsiveBehaviour(mql);
  // Update value labels for all existing selections
  managers.forEach(function (mgr) {
    var target = mgr.isDesktop ? mgr.dropdownBody : mgr.offcanvasBody;
    var checkedInput = target.querySelector("input:checked");
    if (checkedInput) mgr.updateValueLabel(checkedInput);
  });

  /**
   * 7. Handle viewport changes at Bootstrap breakpoints for dropdown width recalculation
   * Recalculates dropdown widths when crossing lg (992px), xl (1200px), and xxl (1400px) breakpoints
   */
  var breakpoints = [992, 1200, 1400]; // Bootstrap lg, xl, xxl breakpoints
  var currentBreakpoint = null;
  var updateBreakpointState = function updateBreakpointState() {
    var width = window.innerWidth;
    var newBreakpoint = null;
    if (width >= 1400) newBreakpoint = "xxl";else if (width >= 1200) newBreakpoint = "xl";else if (width >= 992) newBreakpoint = "lg";else if (width >= 768) newBreakpoint = "md";else newBreakpoint = "sm";

    // Only recalculate if breakpoint actually changed
    if (currentBreakpoint !== newBreakpoint) {
      currentBreakpoint = newBreakpoint;

      // Recalculate dropdown widths for all desktop managers
      managers.forEach(function (mgr) {
        if (mgr.isDesktop && mgr.dropdownEl.classList.contains("show")) {
          // Small delay to ensure layout is settled
          setTimeout(function () {
            return mgr.updateDropdownWidth();
          }, 1);
        }
      });
    }
  };

  // Initialize current breakpoint state
  updateBreakpointState();

  // Listen for resize events with debouncing
  var resizeTimer;
  window.addEventListener("resize", function () {
    clearTimeout(resizeTimer);
    resizeTimer = setTimeout(updateBreakpointState, 1);
  });
});

/**
 * Option Search Module - Filter functionality for Option Groups
 *
 * Provides search functionality for option groups in the product configurator.
 * Features include debounced input, minimum character requirements, caching,
 * accessibility support, and dynamic content handling.
 *
 * @author BadSpiegel Team
 * @version 1.0.0
 */
document.addEventListener("DOMContentLoaded", function () {
  /**
   * Search configuration object
   */
  var config = {
    useCache: false,
    // Enable/disable text normalization caching
    minChars: 3,
    // Minimum characters required for search
    debounceMs: 300,
    // Input debounce delay in milliseconds
    noResultsText: "Keine Optionen gefunden" // Text displayed when no results found
  };

  /**
   * Normalize text for search comparison
   * Converts to lowercase and handles German umlauts
   *
   * @param {string} str - Text to normalize
   * @returns {string} Normalized text
   */
  var normalize = function normalize(str) {
    return str.toLowerCase().replace(/ä/g, "ae").replace(/ö/g, "oe").replace(/ü/g, "ue").replace(/ß/g, "ss");
  };

  /**
   * Cache system for normalized text (performance optimization)
   */
  var cache = new WeakMap();

  /**
   * Get normalized text with optional caching
   *
   * @param {HTMLElement} label - Label element to normalize
   * @returns {string} Normalized text content
   */
  var getNormalizedText = function getNormalizedText(label) {
    if (!config.useCache) {
      return normalize(label.textContent.trim());
    }
    if (!cache.has(label)) {
      cache.set(label, normalize(label.textContent.trim()));
    }
    return cache.get(label);
  };

  /**
   * MutationObserver for dynamic content (only when caching enabled)
   * Maintains cache consistency when new elements are added
   */
  if (config.useCache) {
    var observer = new MutationObserver(function (mutations) {
      mutations.forEach(function (mutation) {
        mutation.addedNodes.forEach(function (node) {
          if (node.nodeType === 1) {
            var _node$querySelectorAl;
            // Element node
            if (node.matches("label.btn")) {
              cache.set(node, normalize(node.textContent.trim()));
            }
            // Handle nested label elements
            (_node$querySelectorAl = node.querySelectorAll) === null || _node$querySelectorAl === void 0 || _node$querySelectorAl.call(node, "label.btn").forEach(function (label) {
              cache.set(label, normalize(label.textContent.trim()));
            });
          }
        });
      });
    });

    // Observe all values groups for dynamic changes
    document.querySelectorAll(".values-group").forEach(function (container) {
      observer.observe(container, {
        childList: true,
        subtree: true
      });
    });
  }

  /**
   * Initialize search functionality for each search input
   */
  document.querySelectorAll(".option-search-input").forEach(function (input) {
    var _input$dataset$target;
    var debounceTimer;
    var wrapper = input.closest(".option-search-wrapper");
    var resetBtn = wrapper.querySelector(".option-search-input-reset");

    // Parse search targets from data attribute
    var targets = ((_input$dataset$target = input.dataset.target) === null || _input$dataset$target === void 0 ? void 0 : _input$dataset$target.split(",").map(function (selector) {
      return selector.trim();
    }).filter(Boolean)) || [];
    if (targets.length === 0) {
      console.warn("No search targets defined for input:", input);
      return;
    }

    /**
     * Setup accessibility attributes for search input
     */
    input.setAttribute("role", "searchbox");
    input.setAttribute("aria-autocomplete", "list");

    /**
     * Initialize reset button
     */
    resetBtn.setAttribute("aria-label", "Suche zurücksetzen");
    resetBtn.classList.add("invisible");
    resetBtn.setAttribute("aria-hidden", "true");
    resetBtn.addEventListener("click", function () {
      input.value = "";
      input.focus();
      performSearch();
    });

    /**
     * Handle Enter key press for mobile keyboard dismissal
     */
    input.addEventListener("keydown", function (e) {
      if (e.key === "Enter") {
        e.preventDefault();

        // Simple solution: blur input and perform search
        input.blur(); // Dismiss mobile keyboard
        setTimeout(performSearch, 100); // Small delay for smooth UX

        // Alternative advanced solution (commented out):
        /*
        // Visual feedback during search
        input.classList.add('searching');
        
        // Dismiss mobile keyboard
        input.blur();
        
        // Perform search with delay
        setTimeout(() => {
          performSearch();
          input.classList.remove('searching');
          
          // Restore focus for screen readers
          if (document.activeElement === document.body) {
            input.focus({ preventScroll: true });
          }
        }, 150);
        */
      }
    });

    /**
     * Main search function
     * Filters options based on normalized input text
     */
    function performSearch() {
      var rawInput = input.value.trim();
      var normalizedTerm = normalize(rawInput);
      var isSearchActive = rawInput.length >= config.minChars;

      // Prepare no-results message with icon
      var iconHTML = '<i class="fa-sharp fa-light fa-triangle-exclamation me-2" aria-hidden="true"></i>';
      var noResultsHTML = "".concat(iconHTML, " ").concat(config.noResultsText);

      // Toggle reset button visibility
      var hasText = rawInput.length > 0;
      resetBtn.classList.toggle("invisible", !hasText);
      resetBtn.setAttribute("aria-hidden", String(!hasText));

      // Process each search target
      targets.forEach(function (selector) {
        document.querySelectorAll(selector).forEach(function (container) {
          // Setup aria-controls relationship
          if (!container.id) {
            container.id = "search-container-".concat(Math.random().toString(36).slice(2, 8));
          }
          input.setAttribute("aria-controls", container.id);

          // Filter option labels
          var hasVisibleOptions = false;
          container.querySelectorAll("label.btn").forEach(function (label) {
            var normalizedText = getNormalizedText(label);
            var shouldShow = !isSearchActive || normalizedText.includes(normalizedTerm);
            label.classList.toggle("d-none", !shouldShow);
            label.setAttribute("aria-hidden", String(!shouldShow));
            if (shouldShow) hasVisibleOptions = true;
          });

          // Handle no-results message
          var noResultsElement = container.querySelector(".no-results");
          if (!noResultsElement) {
            // Create no-results element
            noResultsElement = document.createElement("div");
            noResultsElement.className = "no-results px-md-3";
            noResultsElement.setAttribute("role", "status");
            noResultsElement.setAttribute("aria-live", "polite");
            var alertParagraph = document.createElement("p");
            alertParagraph.className = "alert alert-danger text-truncate mt-1 mt-md-3 mb-3";
            alertParagraph.innerHTML = noResultsHTML;
            noResultsElement.appendChild(alertParagraph);
            container.appendChild(noResultsElement);
          } else {
            // Update existing no-results message
            var _alertParagraph = noResultsElement.querySelector("p.alert");
            if (_alertParagraph) _alertParagraph.innerHTML = noResultsHTML;
          }

          // Show/hide no-results message
          noResultsElement.style.display = hasVisibleOptions ? "none" : "block";
        });
      });
    }

    /**
     * Debounced input handler
     * Delays search execution to improve performance
     */
    function handleInput() {
      clearTimeout(debounceTimer);
      debounceTimer = setTimeout(performSearch, config.debounceMs);
    }
    input.addEventListener("input", handleInput);

    // Perform initial search to set up state
    performSearch();
  });
});

/**
 * CSS for advanced Enter key handling (if enabled)
 * Add this to your stylesheet when using the advanced solution:
 *
 * .option-search-input.searching {
 *   background-color: rgba(0,0,0,0.05);
 *   transition: background-color 0.2s ease;
 * }
 */

/***/ }),

/***/ "./assets/js/configurator/pricecalcs/pricelabels.js":
/*!**********************************************************!*\
  !*** ./assets/js/configurator/pricecalcs/pricelabels.js ***!
  \**********************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony import */ var core_js_modules_es_function_name_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! core-js/modules/es.function.name.js */ "./node_modules/core-js/modules/es.function.name.js");
/* harmony import */ var core_js_modules_es_function_name_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_function_name_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! core-js/modules/es.object.to-string.js */ "./node_modules/core-js/modules/es.object.to-string.js");
/* harmony import */ var core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var core_js_modules_es_string_starts_with_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! core-js/modules/es.string.starts-with.js */ "./node_modules/core-js/modules/es.string.starts-with.js");
/* harmony import */ var core_js_modules_es_string_starts_with_js__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_string_starts_with_js__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! core-js/modules/web.dom-collections.for-each.js */ "./node_modules/core-js/modules/web.dom-collections.for-each.js");
/* harmony import */ var core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_3__);




/**
 * Product Configurator - Price Labels Module
 *
 * This module handles the dynamic updating of price display labels that appear
 * next to price matrix select elements. It automatically populates and updates
 * label information including dimension types, option labels, and formatted prices
 * based on user selections in price calculation dropdowns.
 *
 * Features:
 * - Automatic price label updates on selection changes
 * - Dynamic dimension type detection and labeling
 * - Real-time price and option label synchronization
 * - Event delegation for performance optimization
 * - Support for multiple price matrix types (dimensions, depth, cuts, frames)
 * - Initial state population on page load
 * - Robust DOM structure validation
 *
 * @version 2.3.0
 * @package Configurator
 * @subpackage PriceCalculations
 */

// ====================== PRICE LABEL UPDATE SYSTEM ======================

/**
 * Updates the price display label element that follows a price matrix select
 * Populates dimension type, option label, and formatted price information
 * based on the currently selected option's data attributes
 *
 * @param {HTMLSelectElement} select - The price matrix select element to update labels for
 *
 * @example
 * // HTML structure expected:
 * // <select class="option-pricematrix" name="pxbh_spiegel">
 * //   <option data-label="80x60 cm" data-price="199,00 €">80x60</option>
 * // </select>
 * // <p>
 * //   <span class="sub_name"></span> -
 * //   <span class="sub_label"></span> -
 * //   <span class="sub_price"></span>
 * // </p>
 */
function updatePriceLabel(select) {
  // Get the currently selected option and its data
  var opt = select.options[select.selectedIndex];

  // Find the adjacent paragraph element that contains the price labels
  var p = select.nextElementSibling;

  // Validate DOM structure - ensure we have a paragraph element
  if (!p || p.tagName !== "P") return;

  // Query for the specific label span elements within the paragraph
  var nameSpan = p.querySelector(".sub_name"); // Dimension type label
  var labelSpan = p.querySelector(".sub_label"); // Option description label
  var priceSpan = p.querySelector(".sub_price"); // Formatted price label

  /**
   * DIMENSION TYPE DETECTION AND LABELING
   * =====================================
   * Determines the appropriate German dimension label based on the select element's
   * name attribute prefix. Each prefix corresponds to a specific calculation type.
   */
  if (nameSpan) {
    nameSpan.textContent = select.name && select.name.startsWith("pxbh") ? "Maße" // Width x Height dimensions
    : select.name && select.name.startsWith("pxd") ? "Durchmesser" // Diameter for round mirrors
    : select.name && select.name.startsWith("pxt") ? "Tiefe" // Depth/thickness measurements
    : ""; // No label for unrecognized prefixes
  }

  /**
   * OPTION LABEL AND PRICE POPULATION
   * =================================
   * Extracts and displays the human-readable label and formatted price
   * from the selected option's data attributes
   */

  // Update the descriptive label (e.g., "80x60 cm", "Ø 50 cm")
  if (labelSpan) labelSpan.textContent = opt.dataset.label || "";

  // Update the formatted price display (e.g., "199,00 €", "+ 50,00 €")
  if (priceSpan) priceSpan.textContent = opt.dataset.price || "";
}

// ====================== INITIALIZATION SYSTEM ======================

/**
 * Initial population of all price labels on page load
 * Ensures that pre-selected options display their labels immediately
 * without requiring user interaction
 */
document.querySelectorAll("select.option-pricematrix, select.option-price").forEach(function (sel) {
  return updatePriceLabel(sel);
});

// ====================== EVENT DELEGATION SYSTEM ======================

/**
 * Global change event listener with event delegation
 * Provides efficient event handling for all price matrix selects
 * without individual event listener attachment
 *
 * Uses event delegation pattern for:
 * - Better performance with many select elements
 * - Automatic handling of dynamically added selects
 * - Centralized event management
 */
document.addEventListener("change", function (event) {
  var sel = event.target;

  // Check if the changed element is a price matrix select
  if (sel.matches("select.option-pricematrix, select.option-price")) {
    updatePriceLabel(sel);
  }
});

/**
 * Future Enhancement Ideas:
 *
 * 1. Animation support for label transitions:
 *    function animateLabel(element, newText) {
 *      element.style.opacity = '0';
 *      setTimeout(() => {
 *        element.textContent = newText;
 *        element.style.opacity = '1';
 *      }, 150);
 *    }
 *
 * 2. Label template system for internationalization:
 *    const labelTemplates = {
 *      'de': { pxbh: 'Maße', pxd: 'Durchmesser' },
 *      'en': { pxbh: 'Dimensions', pxd: 'Diameter' }
 *    };
 *
 * 3. Custom label formatting with currency conversion:
 *    function formatPrice(price, currency = 'EUR') {
 *      return new Intl.NumberFormat('de-DE', {
 *        style: 'currency',
 *        currency: currency
 *      }).format(parseFloat(price));
 *    }
 *
 * 4. Label validation and error handling:
 *    function validateLabelStructure(select) {
 *      const p = select.nextElementSibling;
 *      const requiredSpans = ['.sub_name', '.sub_label', '.sub_price'];
 *      return requiredSpans.every(sel => p?.querySelector(sel));
 *    }
 *
 * 5. Accessibility enhancements:
 *    function announceChange(select, newLabel) {
 *      const announcement = `Price updated: ${newLabel}`;
 *      const sr = document.createElement('div');
 *      sr.setAttribute('aria-live', 'polite');
 *      sr.setAttribute('aria-atomic', 'true');
 *      sr.className = 'sr-only';
 *      sr.textContent = announcement;
 *      document.body.appendChild(sr);
 *      setTimeout(() => sr.remove(), 1000);
 *    }
 */

/***/ }),

/***/ "./assets/js/configurator/pricecalcs/pricematrices.js":
/*!************************************************************!*\
  !*** ./assets/js/configurator/pricecalcs/pricematrices.js ***!
  \************************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   calcPrice1x10: () => (/* binding */ calcPrice1x10),
/* harmony export */   calcPrice1x100: () => (/* binding */ calcPrice1x100),
/* harmony export */   calcPrice1x50: () => (/* binding */ calcPrice1x50),
/* harmony export */   calcPrice2x100: () => (/* binding */ calcPrice2x100),
/* harmony export */   calcPrice4x100: () => (/* binding */ calcPrice4x100)
/* harmony export */ });
/* harmony import */ var core_js_modules_es_symbol_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! core-js/modules/es.symbol.js */ "./node_modules/core-js/modules/es.symbol.js");
/* harmony import */ var core_js_modules_es_symbol_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_symbol_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var core_js_modules_es_symbol_description_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! core-js/modules/es.symbol.description.js */ "./node_modules/core-js/modules/es.symbol.description.js");
/* harmony import */ var core_js_modules_es_symbol_description_js__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_symbol_description_js__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var core_js_modules_es_symbol_iterator_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! core-js/modules/es.symbol.iterator.js */ "./node_modules/core-js/modules/es.symbol.iterator.js");
/* harmony import */ var core_js_modules_es_symbol_iterator_js__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_symbol_iterator_js__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var core_js_modules_es_array_concat_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! core-js/modules/es.array.concat.js */ "./node_modules/core-js/modules/es.array.concat.js");
/* harmony import */ var core_js_modules_es_array_concat_js__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_concat_js__WEBPACK_IMPORTED_MODULE_3__);
/* harmony import */ var core_js_modules_es_array_filter_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! core-js/modules/es.array.filter.js */ "./node_modules/core-js/modules/es.array.filter.js");
/* harmony import */ var core_js_modules_es_array_filter_js__WEBPACK_IMPORTED_MODULE_4___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_filter_js__WEBPACK_IMPORTED_MODULE_4__);
/* harmony import */ var core_js_modules_es_array_find_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! core-js/modules/es.array.find.js */ "./node_modules/core-js/modules/es.array.find.js");
/* harmony import */ var core_js_modules_es_array_find_js__WEBPACK_IMPORTED_MODULE_5___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_find_js__WEBPACK_IMPORTED_MODULE_5__);
/* harmony import */ var core_js_modules_es_array_from_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! core-js/modules/es.array.from.js */ "./node_modules/core-js/modules/es.array.from.js");
/* harmony import */ var core_js_modules_es_array_from_js__WEBPACK_IMPORTED_MODULE_6___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_from_js__WEBPACK_IMPORTED_MODULE_6__);
/* harmony import */ var core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! core-js/modules/es.array.includes.js */ "./node_modules/core-js/modules/es.array.includes.js");
/* harmony import */ var core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_7___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_includes_js__WEBPACK_IMPORTED_MODULE_7__);
/* harmony import */ var core_js_modules_es_array_iterator_js__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! core-js/modules/es.array.iterator.js */ "./node_modules/core-js/modules/es.array.iterator.js");
/* harmony import */ var core_js_modules_es_array_iterator_js__WEBPACK_IMPORTED_MODULE_8___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_iterator_js__WEBPACK_IMPORTED_MODULE_8__);
/* harmony import */ var core_js_modules_es_array_map_js__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! core-js/modules/es.array.map.js */ "./node_modules/core-js/modules/es.array.map.js");
/* harmony import */ var core_js_modules_es_array_map_js__WEBPACK_IMPORTED_MODULE_9___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_map_js__WEBPACK_IMPORTED_MODULE_9__);
/* harmony import */ var core_js_modules_es_array_slice_js__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! core-js/modules/es.array.slice.js */ "./node_modules/core-js/modules/es.array.slice.js");
/* harmony import */ var core_js_modules_es_array_slice_js__WEBPACK_IMPORTED_MODULE_10___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_slice_js__WEBPACK_IMPORTED_MODULE_10__);
/* harmony import */ var core_js_modules_es_array_sort_js__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! core-js/modules/es.array.sort.js */ "./node_modules/core-js/modules/es.array.sort.js");
/* harmony import */ var core_js_modules_es_array_sort_js__WEBPACK_IMPORTED_MODULE_11___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_sort_js__WEBPACK_IMPORTED_MODULE_11__);
/* harmony import */ var core_js_modules_es_function_name_js__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! core-js/modules/es.function.name.js */ "./node_modules/core-js/modules/es.function.name.js");
/* harmony import */ var core_js_modules_es_function_name_js__WEBPACK_IMPORTED_MODULE_12___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_function_name_js__WEBPACK_IMPORTED_MODULE_12__);
/* harmony import */ var core_js_modules_es_object_keys_js__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! core-js/modules/es.object.keys.js */ "./node_modules/core-js/modules/es.object.keys.js");
/* harmony import */ var core_js_modules_es_object_keys_js__WEBPACK_IMPORTED_MODULE_13___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_object_keys_js__WEBPACK_IMPORTED_MODULE_13__);
/* harmony import */ var core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_14__ = __webpack_require__(/*! core-js/modules/es.object.to-string.js */ "./node_modules/core-js/modules/es.object.to-string.js");
/* harmony import */ var core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_14___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_14__);
/* harmony import */ var core_js_modules_es_regexp_exec_js__WEBPACK_IMPORTED_MODULE_15__ = __webpack_require__(/*! core-js/modules/es.regexp.exec.js */ "./node_modules/core-js/modules/es.regexp.exec.js");
/* harmony import */ var core_js_modules_es_regexp_exec_js__WEBPACK_IMPORTED_MODULE_15___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_regexp_exec_js__WEBPACK_IMPORTED_MODULE_15__);
/* harmony import */ var core_js_modules_es_regexp_to_string_js__WEBPACK_IMPORTED_MODULE_16__ = __webpack_require__(/*! core-js/modules/es.regexp.to-string.js */ "./node_modules/core-js/modules/es.regexp.to-string.js");
/* harmony import */ var core_js_modules_es_regexp_to_string_js__WEBPACK_IMPORTED_MODULE_16___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_regexp_to_string_js__WEBPACK_IMPORTED_MODULE_16__);
/* harmony import */ var core_js_modules_es_string_includes_js__WEBPACK_IMPORTED_MODULE_17__ = __webpack_require__(/*! core-js/modules/es.string.includes.js */ "./node_modules/core-js/modules/es.string.includes.js");
/* harmony import */ var core_js_modules_es_string_includes_js__WEBPACK_IMPORTED_MODULE_17___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_string_includes_js__WEBPACK_IMPORTED_MODULE_17__);
/* harmony import */ var core_js_modules_es_string_iterator_js__WEBPACK_IMPORTED_MODULE_18__ = __webpack_require__(/*! core-js/modules/es.string.iterator.js */ "./node_modules/core-js/modules/es.string.iterator.js");
/* harmony import */ var core_js_modules_es_string_iterator_js__WEBPACK_IMPORTED_MODULE_18___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_string_iterator_js__WEBPACK_IMPORTED_MODULE_18__);
/* harmony import */ var core_js_modules_web_dom_collections_iterator_js__WEBPACK_IMPORTED_MODULE_19__ = __webpack_require__(/*! core-js/modules/web.dom-collections.iterator.js */ "./node_modules/core-js/modules/web.dom-collections.iterator.js");
/* harmony import */ var core_js_modules_web_dom_collections_iterator_js__WEBPACK_IMPORTED_MODULE_19___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_web_dom_collections_iterator_js__WEBPACK_IMPORTED_MODULE_19__);
/* harmony import */ var _functions__WEBPACK_IMPORTED_MODULE_20__ = __webpack_require__(/*! ./../functions */ "./assets/js/configurator/functions.js");
function _slicedToArray(r, e) { return _arrayWithHoles(r) || _iterableToArrayLimit(r, e) || _unsupportedIterableToArray(r, e) || _nonIterableRest(); }
function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(r, a) { if (r) { if ("string" == typeof r) return _arrayLikeToArray(r, a); var t = {}.toString.call(r).slice(8, -1); return "Object" === t && r.constructor && (t = r.constructor.name), "Map" === t || "Set" === t ? Array.from(r) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r, a) : void 0; } }
function _arrayLikeToArray(r, a) { (null == a || a > r.length) && (a = r.length); for (var e = 0, n = Array(a); e < a; e++) n[e] = r[e]; return n; }
function _iterableToArrayLimit(r, l) { var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (null != t) { var e, n, i, u, a = [], f = !0, o = !1; try { if (i = (t = t.call(r)).next, 0 === l) { if (Object(t) !== t) return; f = !1; } else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0); } catch (r) { o = !0, n = r; } finally { try { if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return; } finally { if (o) throw n; } } return a; } }
function _arrayWithHoles(r) { if (Array.isArray(r)) return r; }




















/**
 * Product Configurator - Price Matrices Module
 *
 * This module handles dynamic price calculations based on dimension inputs.
 * It provides intelligent rounding algorithms and automatic price matrix selection
 * for different product configurations including single dimensions (diameter) and
 * dual dimensions (width x height) with fallback rounding strategies.
 *
 * Features:
 * - Single dimension price calculation with 100-unit rounding
 * - Single dimension price calculation with 50-unit rounding
 * - Single dimension price calculation with 10-unit rounding
 * - Dual dimension price calculation with progressive rounding (100→50)
 * - Price matrix caching for performance optimization
 * - DOM fallback mechanisms for flexible option matching
 * - Automatic select element updates with event triggering
 * - Data attribute synchronization for price display
 * - Prevention of redundant calculations through value caching
 *
 * @version 2.3.0
 * @package Configurator
 * @subpackage PriceCalculations
 */



// ====================== UTILITY FUNCTIONS ======================

/**
 * Searches for a matching option in the price matrix using cache and DOM fallback
 * Provides a flexible mechanism to find options either from pre-built cache
 * or by searching the DOM when cache misses occur
 *
 * @param {Object} pricematrixCache - Pre-built cache of option elements by key
 * @param {HTMLSelectElement} pricematrixSelect - The select element containing price options
 * @param {string} key - The key to search for (e.g., "400", "600x800")
 * @param {Function} [domFallbackFn] - Optional DOM search function for cache misses
 * @returns {HTMLOptionElement|null} The matching option element or null if not found
 *
 * @example
 * const option = findOption(cache, selectEl, "400", (select, key) => {
 *   return Array.from(select.options).find(opt => opt.value === key);
 * });
 */
function findOption(pricematrixCache, pricematrixSelect, key, domFallbackFn) {
  // First attempt: Check the pre-built cache for performance
  var optionToSelect = pricematrixCache[key];

  // Second attempt: Use DOM fallback function if cache miss occurs
  if (!optionToSelect && typeof domFallbackFn === "function") {
    optionToSelect = domFallbackFn(pricematrixSelect, key);
  }
  return optionToSelect;
}

// ====================== SINGLE DIMENSION CALCULATIONS ======================

/**
 * Calculates price for single dimension inputs (e.g., diameter for round mirrors)
 * Rounds input values to the next multiple of 100 and selects corresponding
 * price matrix option with automatic data attribute updates
 *
 * @param {HTMLInputElement} inputElement - The dimension input field
 * @param {HTMLSelectElement} pricematrixSelect - The price matrix select element
 * @param {Object} pricematrixCache - Cached option elements for performance
 * @param {Object} lastValueCache - Cache object to prevent redundant calculations
 * @param {number} lastValueCache.lastRoundedValue - Previously calculated rounded value
 *
 * @example
 * // Usage for diameter input
 * calcPrice1x100(durchmesserInput, matrixSelect, cache, { lastRoundedValue: null });
 *
 * // Input: 350 → Rounded: 400 → Selects option with value "400"
 */
function calcPrice1x100(inputElement, pricematrixSelect, pricematrixCache, lastValueCache) {
  // Parse and validate input value
  var inputValue = parseInt(inputElement.value, 10);
  if (isNaN(inputValue)) return;

  // Get available cache keys for fallback logic
  var cacheKeys = Object.keys(pricematrixCache).map(function (k) {
    return parseInt(k, 10);
  }).sort(function (a, b) {
    return a - b;
  });
  var valueToUse = inputValue;

  // First: Check if exact input value exists in cache
  if (pricematrixCache[String(inputValue)]) {
    valueToUse = inputValue;
  } else {
    // Second: Try rounded value (next multiple of 100)
    var roundedValue = (0,_functions__WEBPACK_IMPORTED_MODULE_20__.roundUp)(inputValue);
    if (pricematrixCache[String(roundedValue)]) {
      valueToUse = roundedValue;
    } else {
      // Third: Find the next available size that's >= input value
      var nextAvailableSize = cacheKeys.find(function (size) {
        return size >= inputValue;
      });
      if (nextAvailableSize) {
        valueToUse = nextAvailableSize;
      } else {
        return; // No suitable option found
      }
    }
  }

  // Performance optimization: Skip if value hasn't changed
  if (lastValueCache.lastRoundedValue === valueToUse) return;
  lastValueCache.lastRoundedValue = valueToUse;

  /**
   * OPTION SEARCH WITH DOM FALLBACK
   * ===============================
   * Attempts to find matching option using cache first, then DOM search
   * The fallback function searches by data-label attribute for flexibility
   */
  var optionToSelect = findOption(pricematrixCache, pricematrixSelect, String(valueToUse), function (pricematrixSelect, key) {
    // DOM fallback: Search by data-label attribute
    return Array.from(pricematrixSelect.options).find(function (currentOption) {
      var labelValue = parseInt(currentOption.dataset.label, 10);
      return !isNaN(labelValue) && labelValue === parseInt(key, 10);
    });
  });

  // Exit if no matching option found
  if (!optionToSelect) return;

  // Update select element and trigger change events
  (0,_functions__WEBPACK_IMPORTED_MODULE_20__.updateSelectAndTrigger)(pricematrixSelect, optionToSelect);

  // Synchronize price data attribute for display systems
  inputElement.dataset.price = optionToSelect.dataset.price || "0";
}

/**
 * Calculates price for single dimension inputs with 50-unit rounding
 * Similar to calcPrice1x100 but rounds to the next multiple of 50 instead of 100
 * Ideal for configurations with smaller step sizes (e.g., 150, 200, 250, 300, 350, 400)
 *
 * @param {HTMLInputElement} inputElement - The dimension input field
 * @param {HTMLSelectElement} pricematrixSelect - The price matrix select element
 * @param {Object} pricematrixCache - Cached option elements for performance
 * @param {Object} lastValueCache - Cache object to prevent redundant calculations
 * @param {number} lastValueCache.lastRoundedValue - Previously calculated rounded value
 *
 * @example
 * // Usage for width input with 50-unit steps
 * calcPrice1x50(breiteInput, matrixSelect, cache, { lastRoundedValue: null });
 *
 * // Input: 175 → Rounded: 200 → Selects option with value "200"
 * // Input: 320 → Rounded: 350 → Selects option with value "350"
 */
function calcPrice1x50(inputElement, pricematrixSelect, pricematrixCache, lastValueCache) {
  // Parse and validate input value
  var inputValue = parseInt(inputElement.value, 10);
  if (isNaN(inputValue)) return;

  // Get available cache keys for fallback logic
  var cacheKeys = Object.keys(pricematrixCache).map(function (k) {
    return parseInt(k, 10);
  }).sort(function (a, b) {
    return a - b;
  });
  var valueToUse = inputValue;

  // First: Check if exact input value exists in cache
  if (pricematrixCache[String(inputValue)]) {
    valueToUse = inputValue;
  } else {
    // Second: Try rounded value (next multiple of 50)
    var roundedValue = Math.ceil(inputValue / 50) * 50;
    if (pricematrixCache[String(roundedValue)]) {
      valueToUse = roundedValue;
    } else {
      // Third: Find the next available size that's >= input value
      var nextAvailableSize = cacheKeys.find(function (size) {
        return size >= inputValue;
      });
      if (nextAvailableSize) {
        valueToUse = nextAvailableSize;
      } else {
        return; // No suitable option found
      }
    }
  }

  // Performance optimization: Skip if value hasn't changed
  if (lastValueCache.lastRoundedValue === valueToUse) return;
  lastValueCache.lastRoundedValue = valueToUse;

  /**
   * OPTION SEARCH WITH DOM FALLBACK
   * ===============================
   * Attempts to find matching option using cache first, then DOM search
   * The fallback function searches by data-label attribute for flexibility
   */
  var optionToSelect = findOption(pricematrixCache, pricematrixSelect, String(valueToUse), function (pricematrixSelect, key) {
    // DOM fallback: Search by data-label attribute
    return Array.from(pricematrixSelect.options).find(function (currentOption) {
      var labelValue = parseInt(currentOption.dataset.label, 10);
      return !isNaN(labelValue) && labelValue === parseInt(key, 10);
    });
  });

  // Exit if no matching option found
  if (!optionToSelect) return;

  // Update select element and trigger change events
  (0,_functions__WEBPACK_IMPORTED_MODULE_20__.updateSelectAndTrigger)(pricematrixSelect, optionToSelect);

  // Synchronize price data attribute for display systems
  inputElement.dataset.price = optionToSelect.dataset.price || "0";
}

/**
 * Calculates price for single dimension inputs with 10-unit rounding
 * Similar to calcPrice1x100 and calcPrice1x50 but rounds to the next multiple of 10
 * Ideal for configurations with very fine step sizes (e.g., 150, 160, 170, 180, ... 400)
 *
 * @param {HTMLInputElement} inputElement - The dimension input field
 * @param {HTMLSelectElement} pricematrixSelect - The price matrix select element
 * @param {Object} pricematrixCache - Cached option elements for performance
 * @param {Object} lastValueCache - Cache object to prevent redundant calculations
 * @param {number} lastValueCache.lastRoundedValue - Previously calculated rounded value
 *
 * @example
 * // Usage for depth input with 10-unit steps
 * calcPrice1x10(tiefeInput, matrixSelect, cache, { lastRoundedValue: null });
 *
 * // Input: 165 → Rounded: 170 → Selects option with value "170"
 * // Input: 235 → Rounded: 240 → Selects option with value "240"
 */
function calcPrice1x10(inputElement, pricematrixSelect, pricematrixCache, lastValueCache) {
  // Parse and validate input value
  var inputValue = parseInt(inputElement.value, 10);
  if (isNaN(inputValue)) return;

  // Get available cache keys for fallback logic
  var cacheKeys = Object.keys(pricematrixCache).map(function (k) {
    return parseInt(k, 10);
  }).sort(function (a, b) {
    return a - b;
  });
  var valueToUse = inputValue;

  // First: Check if exact input value exists in cache
  if (pricematrixCache[String(inputValue)]) {
    valueToUse = inputValue;
  } else {
    // Second: Try rounded value (next multiple of 10)
    var roundedValue = Math.ceil(inputValue / 10) * 10;
    if (pricematrixCache[String(roundedValue)]) {
      valueToUse = roundedValue;
    } else {
      // Third: Find the next available size that's >= input value
      var nextAvailableSize = cacheKeys.find(function (size) {
        return size >= inputValue;
      });
      if (nextAvailableSize) {
        valueToUse = nextAvailableSize;
      } else {
        return; // No suitable option found
      }
    }
  }

  // Performance optimization: Skip if value hasn't changed
  if (lastValueCache.lastRoundedValue === valueToUse) return;
  lastValueCache.lastRoundedValue = valueToUse;

  /**
   * OPTION SEARCH WITH DOM FALLBACK
   * ===============================
   * Attempts to find matching option using cache first, then DOM search
   * The fallback function searches by data-label attribute for flexibility
   */
  var optionToSelect = findOption(pricematrixCache, pricematrixSelect, String(valueToUse), function (pricematrixSelect, key) {
    // DOM fallback: Search by data-label attribute
    return Array.from(pricematrixSelect.options).find(function (currentOption) {
      var labelValue = parseInt(currentOption.dataset.label, 10);
      return !isNaN(labelValue) && labelValue === parseInt(key, 10);
    });
  });

  // Exit if no matching option found
  if (!optionToSelect) return;

  // Update select element and trigger change events
  (0,_functions__WEBPACK_IMPORTED_MODULE_20__.updateSelectAndTrigger)(pricematrixSelect, optionToSelect);

  // Synchronize price data attribute for display systems
  inputElement.dataset.price = optionToSelect.dataset.price || "0";
}

// ====================== DUAL DIMENSION CALCULATIONS ======================

/**
 * Calculates price for dual dimension inputs (width x height for rectangular mirrors)
 * Uses progressive rounding strategy: attempts 100-unit rounding first, falls back
 * to 50-unit rounding if no exact match found in price matrix
 *
 * @param {HTMLInputElement} inputElement1 - First dimension input (usually width)
 * @param {HTMLInputElement} inputElement2 - Second dimension input (usually height)
 * @param {HTMLSelectElement} pricematrixSelect - The price matrix select element
 * @param {Object} pricematrixCache - Cached option elements (currently unused but preserved for consistency)
 * @param {Object} lastValueCache - Cache object to prevent redundant calculations
 * @param {string} lastValueCache.lastRoundedValue - Previously calculated dimension string
 *
 * @example
 * // Usage for width x height inputs
 * calcPrice2x100(breiteInput, hoeheInput, matrixSelect, cache, { lastRoundedValue: null });
 *
 * // Input: 350x780 → Try: 400x800 → Found: Select option
 * // Input: 120x340 → Try: 200x400 → Not found → Try: 150x350 → Found: Select option
 */
function calcPrice2x100(inputElement1, inputElement2, pricematrixSelect, pricematrixCache, lastValueCache) {
  // Parse and validate both input values
  var inputValue1 = parseInt(inputElement1.value, 10);
  var inputValue2 = parseInt(inputElement2.value, 10);
  if (isNaN(inputValue1) || isNaN(inputValue2)) return;

  /**
   * PROGRESSIVE ROUNDING STRATEGY WITH INTELLIGENT FALLBACK
   * ======================================================
   * Step 1: Round to nearest 100 (primary strategy for common sizes)
   * Step 2: Round to nearest 50 (fallback for intermediate sizes)
   * Step 3: Smart fallback to next available size in matrix
   */

  // Step 1: Round both dimensions to next multiple of 100
  var roundedValue1 = Math.ceil(inputValue1 / 100) * 100;
  var roundedValue2 = Math.ceil(inputValue2 / 100) * 100;
  var potentialValue = "".concat(roundedValue1, "x").concat(roundedValue2);

  // Performance optimization: Skip if dimension string hasn't changed
  if (lastValueCache.lastRoundedValue === potentialValue) return;

  /**
   * PRIMARY OPTION SEARCH (100-unit rounding)
   * ========================================
   * Search for exact match in price matrix using 100-unit rounded dimensions
   */
  var optionToSelect = Array.from(pricematrixSelect.options).find(function (opt) {
    return opt.value === potentialValue;
  });

  /**
   * FALLBACK OPTION SEARCH (50-unit rounding)
   * ========================================
   * If 100-unit rounding doesn't yield results, try 50-unit rounding
   * This covers intermediate sizes not available in the primary matrix
   */
  if (!optionToSelect) {
    roundedValue1 = Math.ceil(inputValue1 / 50) * 50;
    roundedValue2 = Math.ceil(inputValue2 / 50) * 50;
    potentialValue = "".concat(roundedValue1, "x").concat(roundedValue2);
    optionToSelect = Array.from(pricematrixSelect.options).find(function (opt) {
      return opt.value === potentialValue;
    });
  }

  /**
   * INTELLIGENT SIZE FALLBACK (similar to calcPrice1x100)
   * ====================================================
   * If both 100 and 50 unit rounding fail, find next available size combination
   */
  if (!optionToSelect) {
    // Get all available dimension combinations from the select options
    var availableOptions = Array.from(pricematrixSelect.options).map(function (opt) {
      return opt.value;
    }).filter(function (val) {
      return val.includes("x");
    }).map(function (val) {
      var _val$split$map = val.split("x").map(function (v) {
          return parseInt(v, 10);
        }),
        _val$split$map2 = _slicedToArray(_val$split$map, 2),
        w = _val$split$map2[0],
        h = _val$split$map2[1];
      return {
        value: val,
        width: w,
        height: h,
        element: pricematrixSelect.querySelector("option[value=\"".concat(val, "\"]"))
      };
    }).filter(function (opt) {
      return !isNaN(opt.width) && !isNaN(opt.height);
    });

    // Find the smallest option that can accommodate both dimensions
    var suitableOption = availableOptions.find(function (opt) {
      return opt.width >= roundedValue1 && opt.height >= roundedValue2;
    });
    if (suitableOption) {
      optionToSelect = suitableOption.element;
      potentialValue = suitableOption.value;
    }
  }

  // Exit if no option found even with intelligent fallback
  if (!optionToSelect) return;

  /**
   * MATRIX UPDATE AND EVENT PROPAGATION
   * ===================================
   * Update the price matrix selection and propagate changes to dependent systems
   * Now using the same pattern as calcPrice1x100 for consistency
   */

  // Update the cache with successful calculation result
  lastValueCache.lastRoundedValue = potentialValue;

  // Use the same updateSelectAndTrigger function as calcPrice1x100 for consistency
  (0,_functions__WEBPACK_IMPORTED_MODULE_20__.updateSelectAndTrigger)(pricematrixSelect, optionToSelect);

  // Synchronize data-price attributes for both input elements (same as calcPrice1x100)
  var price = optionToSelect.dataset.price || "0";
  inputElement1.dataset.price = price;
  inputElement2.dataset.price = price;
}

/**
 * Calculates price for four dimension inputs (breite_unten, breite_oben, hoehe_links, hoehe_rechts)
 * Takes the maximum values from width inputs and height inputs, then uses progressive rounding
 * strategy like calcPrice2x100: attempts 100-unit rounding first, falls back to 50-unit rounding
 *
 * @param {HTMLInputElement} breiteUntenElement - Bottom width input element
 * @param {HTMLInputElement} breiteObenElement - Top width input element
 * @param {HTMLInputElement} hoeheLinksElement - Left height input element
 * @param {HTMLInputElement} hoeheRechtsElement - Right height input element
 * @param {HTMLSelectElement} pricematrixSelect - The price matrix select element
 * @param {Object} pricematrixCache - Cached option elements (currently unused but preserved for consistency)
 * @param {Object} lastValueCache - Cache object to prevent redundant calculations
 * @param {string} lastValueCache.lastRoundedValue - Previously calculated dimension string
 *
 * @example
 * // Usage for four dimension inputs
 * calcPrice4x100(breiteUntenInput, breiteObenInput, hoeheLinksInput, hoeheRechtsInput, matrixSelect, cache, { lastRoundedValue: null });
 *
 * // Input: breiteUnten=350, breiteOben=400, hoeheLinks=780, hoeheRechts=750
 * // Max width=400, Max height=780 → Try: 400x800 → Found: Select option
 */
function calcPrice4x100(breiteUntenElement, breiteObenElement, hoeheLinksElement, hoeheRechtsElement, pricematrixSelect, pricematrixCache, lastValueCache) {
  // Parse and validate all four input values
  var breiteUntenValue = parseInt(breiteUntenElement.value, 10);
  var breiteObenValue = parseInt(breiteObenElement.value, 10);
  var hoeheLinksValue = parseInt(hoeheLinksElement.value, 10);
  var hoeheRechtsValue = parseInt(hoeheRechtsElement.value, 10);

  // Exit if any value is invalid
  if (isNaN(breiteUntenValue) || isNaN(breiteObenValue) || isNaN(hoeheLinksValue) || isNaN(hoeheRechtsValue)) return;

  // Calculate maximum values for width and height
  var maxBreite = Math.max(breiteUntenValue, breiteObenValue);
  var maxHoehe = Math.max(hoeheLinksValue, hoeheRechtsValue);

  /**
   * PROGRESSIVE ROUNDING STRATEGY WITH INTELLIGENT FALLBACK
   * ======================================================
   * Step 1: Round to nearest 100 (primary strategy for common sizes)
   * Step 2: Round to nearest 50 (fallback for intermediate sizes)
   * Step 3: Smart fallback to next available size in matrix
   */

  // Step 1: Round both max dimensions to next multiple of 100
  var roundedValue1 = Math.ceil(maxBreite / 100) * 100;
  var roundedValue2 = Math.ceil(maxHoehe / 100) * 100;
  var potentialValue = "".concat(roundedValue1, "x").concat(roundedValue2);

  // Performance optimization: Skip if dimension string hasn't changed
  if (lastValueCache.lastRoundedValue === potentialValue) return;

  /**
   * PRIMARY OPTION SEARCH (100-unit rounding)
   * ========================================
   * Search for exact match in price matrix using 100-unit rounded dimensions
   */
  var optionToSelect = Array.from(pricematrixSelect.options).find(function (opt) {
    return opt.value === potentialValue;
  });

  /**
   * FALLBACK OPTION SEARCH (50-unit rounding)
   * ========================================
   * If 100-unit rounding doesn't yield results, try 50-unit rounding
   * This covers intermediate sizes not available in the primary matrix
   */
  if (!optionToSelect) {
    roundedValue1 = Math.ceil(maxBreite / 50) * 50;
    roundedValue2 = Math.ceil(maxHoehe / 50) * 50;
    potentialValue = "".concat(roundedValue1, "x").concat(roundedValue2);
    optionToSelect = Array.from(pricematrixSelect.options).find(function (opt) {
      return opt.value === potentialValue;
    });
  }

  /**
   * INTELLIGENT SIZE FALLBACK (similar to calcPrice2x100)
   * ====================================================
   * If both 100 and 50 unit rounding fail, find next available size combination
   */
  if (!optionToSelect) {
    // Get all available dimension combinations from the select options
    var availableOptions = Array.from(pricematrixSelect.options).map(function (opt) {
      return opt.value;
    }).filter(function (val) {
      return val.includes("x");
    }).map(function (val) {
      var _val$split$map3 = val.split("x").map(function (v) {
          return parseInt(v, 10);
        }),
        _val$split$map4 = _slicedToArray(_val$split$map3, 2),
        w = _val$split$map4[0],
        h = _val$split$map4[1];
      return {
        value: val,
        width: w,
        height: h,
        element: pricematrixSelect.querySelector("option[value=\"".concat(val, "\"]"))
      };
    }).filter(function (opt) {
      return !isNaN(opt.width) && !isNaN(opt.height);
    });

    // Find the smallest option that can accommodate both max dimensions
    var suitableOption = availableOptions.find(function (opt) {
      return opt.width >= roundedValue1 && opt.height >= roundedValue2;
    });
    if (suitableOption) {
      optionToSelect = suitableOption.element;
      potentialValue = suitableOption.value;
    }
  }

  // Exit if no option found even with intelligent fallback
  if (!optionToSelect) return;

  /**
   * MATRIX UPDATE AND EVENT PROPAGATION
   * ===================================
   * Update the price matrix selection and propagate changes to dependent systems
   * Using the same pattern as calcPrice2x100 for consistency
   */

  // Update the cache with successful calculation result
  lastValueCache.lastRoundedValue = potentialValue;

  // Use the same updateSelectAndTrigger function for consistency
  (0,_functions__WEBPACK_IMPORTED_MODULE_20__.updateSelectAndTrigger)(pricematrixSelect, optionToSelect);

  // Synchronize data-price attributes for all four input elements
  var price = optionToSelect.dataset.price || "0";
  breiteUntenElement.dataset.price = price;
  breiteObenElement.dataset.price = price;
  hoeheLinksElement.dataset.price = price;
  hoeheRechtsElement.dataset.price = price;
}

/***/ }),

/***/ "./assets/js/configurator/pricecalcs/sk1.js":
/*!**************************************************!*\
  !*** ./assets/js/configurator/pricecalcs/sk1.js ***!
  \**************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony import */ var core_js_modules_es_array_filter_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! core-js/modules/es.array.filter.js */ "./node_modules/core-js/modules/es.array.filter.js");
/* harmony import */ var core_js_modules_es_array_filter_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_filter_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var core_js_modules_es_function_name_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! core-js/modules/es.function.name.js */ "./node_modules/core-js/modules/es.function.name.js");
/* harmony import */ var core_js_modules_es_function_name_js__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_function_name_js__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var core_js_modules_es_number_is_nan_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! core-js/modules/es.number.is-nan.js */ "./node_modules/core-js/modules/es.number.is-nan.js");
/* harmony import */ var core_js_modules_es_number_is_nan_js__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_number_is_nan_js__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! core-js/modules/es.object.to-string.js */ "./node_modules/core-js/modules/es.object.to-string.js");
/* harmony import */ var core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_3__);
/* harmony import */ var core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! core-js/modules/web.dom-collections.for-each.js */ "./node_modules/core-js/modules/web.dom-collections.for-each.js");
/* harmony import */ var core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_4___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_4__);
/* harmony import */ var _variables__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../variables */ "./assets/js/configurator/variables.js");





/**
 * Product Configurator - SK1 Special Cut Calculations Module
 *
 * This module handles specialized geometric calculations for SK1 (single cut edge)
 * mirror configurations. It provides real-time mathematical relationships between
 * diameter, segment height, and cut width for circular mirrors with one straight
 * cut edge, creating a segment shape.
 *
 * Features:
 * - Real-time geometric calculations for circular segment mirrors
 * - Bidirectional input dependencies (change one dimension, auto-calculate others)
 * - Mathematical validation and error handling for impossible configurations
 * - Event-driven updates with input change detection
 * - Flexible input field naming conventions support
 * - Performance optimization through early validation checks
 * - Precise rounding to whole numbers for manufacturing requirements
 *
 * Mathematical Relationships:
 * - Segment Height: h = 2r - (r - √(r² - (t²/4)))
 * - Diameter from Height: d = (h² + t²/4) / h
 * - All calculations based on circle geometry and segment formulas
 *
 * @version 2.3.0
 * @package Configurator
 * @subpackage PriceCalculations
 */



// ====================== MODULE INITIALIZATION ======================

/**
 * Initialize SK1 calculations when DOM is ready
 * Validates product type and sets up input event listeners for geometric calculations
 */
document.addEventListener("DOMContentLoaded", function () {
  // ====================== PRODUCT TYPE VALIDATION ======================

  /**
   * Product compatibility check
   * Ensures this module only runs on SK1 (single cut edge) products
   * Early return prevents unnecessary processing on incompatible products
   */
  if (!_variables__WEBPACK_IMPORTED_MODULE_5__.isSK || !_variables__WEBPACK_IMPORTED_MODULE_5__.isSK1) {
    return;
  }

  // ====================== DOM ELEMENT DISCOVERY ======================

  /**
   * Input field references with flexible naming convention support
   * Accommodates different naming patterns used across product variants
   */

  // Diameter input (imported from variables module)
  // const durchmesserInput = document.querySelector('.option-input[name="durchmesser"]');

  /**
   * Segment height input with fallback naming support
   * Can be named either "hoehe_schnittkante" or "breite_schnittkante"
   * depending on product orientation and configuration system setup
   * @type {HTMLInputElement|null}
   */
  var hoeheInput = document.querySelector('.option-input[name="hoehe_schnittkante"]') || document.querySelector('.option-input[name="breite_schnittkante"]');

  /**
   * Cut width input field
   * Represents the width of the straight cut edge
   * @type {HTMLInputElement|null}
   */
  var schnittkanteInput = document.querySelector('.option-input[name="schnittkante"]');

  /**
   * INPUT VALIDATION AND FILTERING
   * ==============================
   * Create array of available inputs, filtering out null references
   * Enables graceful handling of partial input configurations
   */
  var inputs = [_variables__WEBPACK_IMPORTED_MODULE_5__.durchmesserInput, hoeheInput, schnittkanteInput].filter(Boolean);

  // Early exit if no valid inputs found
  if (inputs.length === 0) {
    return;
  }

  // ====================== DEBOUNCING SETUP ======================

  /**
   * Debouncing timeout for input handling
   * Prevents calculation spam while user is typing
   */
  var debounceTimeout = null;

  /**
   * Creates a debounced version of the calculation function
   * Delays execution until user stops typing for specified delay
   *
   * @param {Function} func - Function to debounce
   * @param {number} delay - Delay in milliseconds
   * @returns {Function} Debounced function
   */
  function debounce(func, delay) {
    return function () {
      var _this = this;
      for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
        args[_key] = arguments[_key];
      }
      // Clear existing timeout
      if (debounceTimeout) {
        clearTimeout(debounceTimeout);
      }

      // Set new timeout
      debounceTimeout = setTimeout(function () {
        func.apply(_this, args);
      }, delay);
    };
  }

  // ====================== EVENT LISTENER SETUP ======================

  /**
   * Attach input event listeners to all available input fields
   * Uses debounced handler to prevent calculation interference while typing
   */

  // Create debounced version of the input handler (500ms delay)
  var debouncedInputHandler = debounce(handleInputChange, 500);
  inputs.forEach(function (el) {
    // Use debounced handler for 'input' events (while typing)
    el.addEventListener("input", debouncedInputHandler);

    // Use immediate handler for 'blur' events (when leaving field)
    el.addEventListener("blur", handleInputChange);
  });

  // ====================== INITIAL CALCULATION ======================

  /**
   * Perform initial calculation if values are already present
   * This handles cases where the form is pre-populated or page is refreshed
   * For SK1: Always calculate height from diameter and cut width when page loads
   */
  var performInitialCalculation = function performInitialCalculation() {
    var dVal = parseFloat(_variables__WEBPACK_IMPORTED_MODULE_5__.durchmesserInput === null || _variables__WEBPACK_IMPORTED_MODULE_5__.durchmesserInput === void 0 ? void 0 : _variables__WEBPACK_IMPORTED_MODULE_5__.durchmesserInput.value);
    var hVal = parseFloat(hoeheInput === null || hoeheInput === void 0 ? void 0 : hoeheInput.value);
    var tVal = parseFloat(schnittkanteInput === null || schnittkanteInput === void 0 ? void 0 : schnittkanteInput.value);

    // For SK1: If we have diameter and cut width, always calculate height
    // This is the primary relationship for SK1 mirrors
    if (!Number.isNaN(dVal) && !Number.isNaN(tVal)) {
      updateHeightFromDT(dVal, tVal);
    }
    // Fallback: If we only have height and cut width, calculate diameter
    else if (!Number.isNaN(hVal) && !Number.isNaN(tVal) && Number.isNaN(dVal)) {
      updateDiameterFromHT(hVal, tVal);
    }
  };

  // Run initial calculation after a short delay to ensure DOM is fully ready
  setTimeout(performInitialCalculation, 100);

  // ====================== CORE CALCULATION LOGIC ======================

  /**
   * Flag to prevent calculation loops when updating fields programmatically
   */
  var isCalculating = false;

  /**
   * Central input change event handler
   * Determines which input changed and triggers appropriate calculations
   * based on geometric relationships between diameter, height, and cut width
   *
   * @param {Event} e - The input change event
   */
  function handleInputChange(e) {
    // Prevent recursive calculations
    if (isCalculating) {
      return;
    }
    var changed = e.target.name; // "durchmesser", "hoehe_schnittkante" oder "schnittkante"

    /**
     * VALUE EXTRACTION AND VALIDATION
     * ===============================
     * Parse current values from all input fields with safety checks
     */
    var dVal = parseFloat(_variables__WEBPACK_IMPORTED_MODULE_5__.durchmesserInput === null || _variables__WEBPACK_IMPORTED_MODULE_5__.durchmesserInput === void 0 ? void 0 : _variables__WEBPACK_IMPORTED_MODULE_5__.durchmesserInput.value); // Durchmesser (diameter)
    var hVal = parseFloat(hoeheInput === null || hoeheInput === void 0 ? void 0 : hoeheInput.value); // Höhe (segment height)
    var tVal = parseFloat(schnittkanteInput === null || schnittkanteInput === void 0 ? void 0 : schnittkanteInput.value); // Schnittkante (cut width)

    // Early exit if all values are invalid (no calculations possible)
    if (Number.isNaN(dVal) && Number.isNaN(hVal) && Number.isNaN(tVal)) {
      return;
    }

    // Set calculation flag
    isCalculating = true;

    /**
     * CALCULATION DISPATCH LOGIC
     * =========================
     * Determine which calculation to perform based on the changed input
     * Each case maintains two known values and calculates the third
     */
    switch (changed) {
      case "durchmesser":
        /**
         * DIAMETER CHANGED SCENARIO
         * Diameter (d) changed → Keep cut width (t) → Calculate new height (h)
         * Mathematical relationship: h = f(d, t)
         */
        if (!Number.isNaN(dVal) && !Number.isNaN(tVal)) {
          updateHeightFromDT(dVal, tVal);
        }
        break;
      case "hoehe_schnittkante":
      case "breite_schnittkante":
        /**
         * HEIGHT CHANGED SCENARIO
         * Height (h) changed → Keep cut width (t) → Calculate new diameter (d)
         * Mathematical relationship: d = f(h, t)
         */
        if (!Number.isNaN(hVal) && !Number.isNaN(tVal)) {
          updateDiameterFromHT(hVal, tVal);
        }
        break;
      case "schnittkante":
        /**
         * CUT WIDTH CHANGED SCENARIO
         * Cut width (t) changed → Keep diameter (d) → Calculate new height (h)
         * Mathematical relationship: h = f(d, t)
         */
        if (!Number.isNaN(dVal) && !Number.isNaN(tVal)) {
          updateHeightFromDT(dVal, tVal);
        }
        break;
    }

    // Clear calculation flag after processing
    setTimeout(function () {
      isCalculating = false;
    }, 100);
  }

  // ====================== CALCULATION UPDATE FUNCTIONS ======================

  /**
   * Updates the segment height input based on diameter and cut width
   * Performs geometric calculation and updates DOM element with validation
   *
   * @param {number} d - Circle diameter
   * @param {number} t - Cut width (chord length)
   */
  function updateHeightFromDT(d, t) {
    var newH = calcHeightGivenDT(d, t);
    if (isFinite(newH) && hoeheInput) {
      var oldValue = hoeheInput.value;
      var newValue = round(newH);

      // Only update if value actually changed to prevent infinite loops
      if (String(newValue) !== oldValue) {
        // Set flag to prevent triggering input events during programmatic update
        var wasCalculating = isCalculating;
        isCalculating = true;
        hoeheInput.value = newValue;

        // Trigger input event to notify other systems (price calculations, summary updates)
        // But do it after a delay to ensure our flag is respected
        setTimeout(function () {
          hoeheInput.dispatchEvent(new Event("input", {
            bubbles: true
          }));
          isCalculating = wasCalculating; // Restore previous state
        }, 50);
      }
    }
  }

  /**
   * Updates the diameter input based on segment height and cut width
   * Performs geometric calculation and updates DOM element with validation
   *
   * @param {number} h - Segment height
   * @param {number} t - Cut width (chord length)
   */
  function updateDiameterFromHT(h, t) {
    var newD = calcDiameterGivenHT(h, t);
    if (isFinite(newD) && _variables__WEBPACK_IMPORTED_MODULE_5__.durchmesserInput) {
      var oldValue = _variables__WEBPACK_IMPORTED_MODULE_5__.durchmesserInput.value;
      var newValue = round(newD);

      // Only update if value actually changed to prevent infinite loops
      if (String(newValue) !== oldValue) {
        // Set flag to prevent triggering input events during programmatic update
        var wasCalculating = isCalculating;
        isCalculating = true;
        _variables__WEBPACK_IMPORTED_MODULE_5__.durchmesserInput.value = newValue;

        // Trigger input event to notify other systems (price calculations, summary updates)
        // But do it after a delay to ensure our flag is respected
        setTimeout(function () {
          _variables__WEBPACK_IMPORTED_MODULE_5__.durchmesserInput.dispatchEvent(new Event("input", {
            bubbles: true
          }));
          isCalculating = wasCalculating; // Restore previous state
        }, 50);
      }
    }
  }

  // ====================== GEOMETRIC CALCULATION FUNCTIONS ======================

  /**
   * Calculates segment height from circle diameter and chord width
   * Uses circle geometry: segment height = radius - distance from center to chord
   *
   * Mathematical Formula:
   * h = 2r - (r - √(r² - (t²/4)))
   *
   * Where:
   * - h = segment height
   * - r = radius (d/2)
   * - t = chord width (cut width)
   *
   * @param {number} d - Circle diameter
   * @param {number} t - Chord width (cut width)
   * @returns {number} Calculated segment height or NaN if impossible geometry
   *
   * @example
   * calcHeightGivenDT(100, 60) // Returns segment height for 100mm diameter, 60mm cut
   */
  function calcHeightGivenDT(d, t) {
    var r = d / 2; // Convert diameter to radius

    /**
     * GEOMETRIC VALIDATION
     * ===================
     * Check if the chord width is geometrically possible for given diameter
     * Discriminant must be non-negative: r² - (t²/4) ≥ 0
     * This ensures the chord doesn't exceed the circle diameter
     */
    var disc = r * r - t * t / 4; // r² - (t²/4)
    if (disc < 0) return NaN; // Impossible geometry

    /**
     * SEGMENT HEIGHT CALCULATION
     * =========================
     * Calculate distance from center to chord, then segment height
     */
    var distanceToChord = r - Math.sqrt(disc); // Distance from center to chord
    var segmentHeight = 2 * r - distanceToChord; // Full height minus distance

    return segmentHeight;
  }

  /**
   * Calculates circle diameter from segment height and chord width
   * Derived from the inverse of the segment height formula
   *
   * Mathematical Formula:
   * d = (h² + t²/4) / h
   *
   * Where:
   * - d = circle diameter
   * - h = segment height
   * - t = chord width (cut width)
   *
   * @param {number} h - Segment height
   * @param {number} t - Chord width (cut width)
   * @returns {number} Calculated diameter or NaN if invalid input
   *
   * @example
   * calcDiameterGivenHT(30, 60) // Returns diameter for 30mm height, 60mm cut
   */
  function calcDiameterGivenHT(h, t) {
    // Prevent division by zero
    if (h === 0) return NaN;

    /**
     * DIAMETER CALCULATION
     * ===================
     * Uses quadratic relationship between height, chord width, and diameter
     * Formula derived from circle geometry and segment relationships
     */
    var numerator = t * t / 4 + h * h; // t²/4 + h²
    return numerator / h; // (t²/4 + h²) / h
  }

  // ====================== UTILITY FUNCTIONS ======================

  /**
   * Rounds numeric values to whole numbers for manufacturing precision
   * Eliminates decimal places that are not practical for physical production
   *
   * @param {number} val - The value to round
   * @returns {number} Rounded integer value
   */
  function round(val) {
    return Math.round(val);
  }

  // ====================== TESTING/VERIFICATION ======================

  /**
   * Test calculation with expected values for verification
   * Expected: diameter=400, cutWidth=150 → height≈385
   */
  if (typeof window !== "undefined" && window.console) {
    // Make calculation functions available for debugging
    window.SK1_calcHeightGivenDT = calcHeightGivenDT;
    window.SK1_calcDiameterGivenHT = calcDiameterGivenHT;
    window.SK1_round = round;
  }
});

/**
 * Future Enhancement Ideas:
 *
 * 1. Visual feedback for impossible geometries:
 *    function showGeometryWarning(input, message) {
 *      input.classList.add('geometry-error');
 *      showTooltip(input, message);
 *    }
 *
 * 2. Advanced validation with tolerance ranges:
 *    const geometryLimits = {
 *      minDiameter: 100,
 *      maxDiameter: 2000,
 *      minCutRatio: 0.1,  // Minimum cut width as ratio of diameter
 *      maxCutRatio: 0.9   // Maximum cut width as ratio of diameter
 *    };
 *
 * 3. Real-time geometry preview:
 *    function drawSegmentPreview(diameter, height, cutWidth) {
 *      // Canvas or SVG visualization of the cut mirror shape
 *    }
 *
 * 4. Manufacturing constraints validation:
 *    function validateManufacturingLimits(dimensions) {
 *      // Check against production capabilities and material limits
 *    }
 *
 * 5. Unit conversion support:
 *    const unitConverter = {
 *      mmToCm: (val) => val / 10,
 *      cmToMm: (val) => val * 10,
 *      inchToMm: (val) => val * 25.4
 *    };
 */

/***/ }),

/***/ "./assets/js/configurator/pricecalcs/sk2.js":
/*!**************************************************!*\
  !*** ./assets/js/configurator/pricecalcs/sk2.js ***!
  \**************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony import */ var core_js_modules_es_string_trim_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! core-js/modules/es.string.trim.js */ "./node_modules/core-js/modules/es.string.trim.js");
/* harmony import */ var core_js_modules_es_string_trim_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_string_trim_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _variables__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../variables */ "./assets/js/configurator/variables.js");

/**
 * Product Configurator - SK2 Special Cut Calculations Module
 *
 * This module handles specialized mathematical calculations for SK2 (double cut edge)
 * mirror configurations. It provides bidirectional conversion between combined
 * width/height values and equivalent diameter measurements using empirically derived
 * linear transformation formulas for complex geometric relationships.
 *
 * Features:
 * - Bidirectional mathematical conversion (width/height ↔ diameter)
 * - Empirically calibrated transformation coefficients (a=1.077, b=0.08)
 * - Real-time input synchronization with debounced updates
 * - Immediate updates on field blur for instant feedback
 * - Performance optimization through change detection
 * - Event propagation for dependent calculation systems
 * - Robust input validation and error handling
 *
 * Mathematical Model:
 * - Diameter = a × (width/height) + b
 * - Width/Height = (diameter - b) ÷ a
 * - Coefficients derived from manufacturing specifications
 *
 * @version 2.3.0
 * @package Configurator
 * @subpackage PriceCalculations
 */



// ====================== MODULE INITIALIZATION ======================

/**
 * Initialize SK2 calculations when DOM is ready
 * Validates product type and establishes bidirectional input relationships
 */
document.addEventListener("DOMContentLoaded", function () {
  // ====================== PRODUCT TYPE VALIDATION ======================

  /**
   * Product compatibility check
   * Ensures this module only runs on SK2 (double cut edge) products
   * Early return prevents unnecessary processing on incompatible products
   */
  if (!_variables__WEBPACK_IMPORTED_MODULE_1__.isSK || !_variables__WEBPACK_IMPORTED_MODULE_1__.isSK2) {
    // console.warn('[SK2] Nicht die richtige Produktseite.');
    return;
  }

  // ====================== MATHEMATICAL CONSTANTS ======================

  /**
   * Empirically derived transformation coefficients
   * These values are calibrated based on manufacturing specifications
   * and geometric relationships specific to SK2 cut patterns
   */

  /**
   * Linear coefficient (slope) for diameter calculation
   * Represents the proportional relationship between dimensions
   * @type {number}
   */
  var a = 1.077;

  /**
   * Offset coefficient (y-intercept) for diameter calculation
   * Accounts for geometric constants in the cutting process
   * @type {number}
   */
  var b = 0.08;

  // ====================== MATHEMATICAL TRANSFORMATION FUNCTIONS ======================

  /**
   * Calculates equivalent diameter from combined width/height value
   * Uses linear transformation: d = a × bh + b
   *
   * @param {number} bh - Combined width/height measurement
   * @returns {number} Calculated equivalent diameter
   *
   * @example
   * computeDiameterFromBH(100) // Returns ~107.8 for 100mm width/height
   */
  function computeDiameterFromBH(bh) {
    return a * bh + b;
  }

  /**
   * Calculates combined width/height from diameter value
   * Uses inverse linear transformation: bh = (d - b) ÷ a
   *
   * @param {number} d - Diameter measurement
   * @returns {number} Calculated combined width/height value
   *
   * @example
   * computeBHFromDiameter(107.8) // Returns ~100 for 107.8mm diameter
   */
  function computeBHFromDiameter(d) {
    return (d - b) / a;
  }

  // ====================== DOM ELEMENT DISCOVERY ======================

  /**
   * Input field references for bidirectional calculations
   * Both fields must be present for proper functionality
   */

  /**
   * Combined width/height input field
   * Represents the rectangular dimension equivalent
   * @type {HTMLInputElement|null}
   */
  var bhInput = document.querySelector('.option-input[name="breite_hoehe"]');

  /**
   * Diameter input field
   * Represents the circular dimension equivalent
   * @type {HTMLInputElement|null}
   */
  var dInput = document.querySelector('.option-input[name="durchmesser"]');

  /**
   * INPUT VALIDATION
   * ===============
   * Ensure both required input fields are available in DOM
   */
  if (!bhInput || !dInput) {
    return;
  }

  // ====================== PERFORMANCE OPTIMIZATION UTILITIES ======================

  /**
   * Creates a debounced version of a function for performance optimization
   * Delays execution until a specified period of inactivity has passed
   *
   * @param {Function} func - The function to debounce
   * @param {number} delay - Delay in milliseconds before execution
   * @returns {Function} Debounced function wrapper
   *
   * @example
   * const debouncedCalc = debounce(calculateValues, 500);
   * input.addEventListener('input', debouncedCalc);
   */
  function debounce(func, delay) {
    var timeoutId;
    return function () {
      var _this = this;
      for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
        args[_key] = arguments[_key];
      }
      // Clear any existing timeout to reset the delay
      if (timeoutId) clearTimeout(timeoutId);

      // Set new timeout for delayed execution
      timeoutId = setTimeout(function () {
        func.apply(_this, args);
      }, delay);
    };
  }

  // ====================== BIDIRECTIONAL UPDATE FUNCTIONS ======================

  /**
   * Updates diameter field based on width/height input changes
   * Performs validation, calculation, and change detection for efficiency
   */
  function updateDiameterFromBH() {
    // Parse and validate input value
    var valBH = parseFloat(bhInput.value);
    if (isNaN(valBH)) return; // Skip invalid inputs

    // Calculate new diameter and round to whole number
    var newD = Math.round(computeDiameterFromBH(valBH));

    /**
     * CHANGE DETECTION OPTIMIZATION
     * ============================
     * Only update if value actually changed to prevent unnecessary events
     */
    if (String(newD) !== dInput.value) {
      dInput.value = newD;

      // Dispatch input event for dependent systems (price calculations, etc.)
      dInput.dispatchEvent(new Event("input", {
        bubbles: true
      }));
    }
  }

  /**
   * Updates width/height field based on diameter input changes
   * Performs validation, calculation, and change detection for efficiency
   */
  function updateBHFromDiameter() {
    // Parse and validate input value
    var valD = parseFloat(dInput.value);
    if (isNaN(valD)) return; // Skip invalid inputs

    // Calculate new width/height and round to whole number
    var newBH = Math.round(computeBHFromDiameter(valD));

    /**
     * CHANGE DETECTION OPTIMIZATION
     * ============================
     * Only update if value actually changed to prevent unnecessary events
     */
    if (String(newBH) !== bhInput.value) {
      bhInput.value = newBH;

      // Dispatch input event for dependent systems
      bhInput.dispatchEvent(new Event("input", {
        bubbles: true
      }));
    }
  }

  // ====================== EVENT HANDLER OPTIMIZATION ======================

  /**
   * Debounced event handlers for input fields
   * Reduces calculation frequency during rapid typing while maintaining responsiveness
   */

  /**
   * Debounced handler for width/height input changes
   * 500ms delay prevents excessive calculations during user typing
   */
  var debouncedBHHandler = debounce(updateDiameterFromBH, 500);

  /**
   * Debounced handler for diameter input changes
   * 500ms delay prevents excessive calculations during user typing
   */
  var debouncedDHandler = debounce(updateBHFromDiameter, 500);

  // ====================== EVENT LISTENER REGISTRATION ======================

  /**
   * PRIMARY INPUT EVENT LISTENERS
   * ============================
   * Debounced handlers for real-time updates during typing
   * Balances responsiveness with performance
   */

  // Width/height input changes trigger diameter calculation
  bhInput.addEventListener("input", debouncedBHHandler);

  // Diameter input changes trigger width/height calculation
  dInput.addEventListener("input", debouncedDHandler);

  /**
   * IMMEDIATE BLUR EVENT LISTENERS
   * =============================
   * Instant updates when user leaves field for immediate feedback
   * Ensures calculations complete when user moves to next field
   */

  // Immediate diameter update when leaving width/height field
  bhInput.addEventListener("blur", updateDiameterFromBH);

  // Immediate width/height update when leaving diameter field
  dInput.addEventListener("blur", updateBHFromDiameter);

  /**
   * INITIAL SYNCHRONIZATION ON PAGE LOAD
   * ====================================
   * Automatically calculate diameter from breite_hoehe value on page start
   * This ensures consistent values when the page loads with pre-filled data
   */

  // Check if breite_hoehe has a value and calculate diameter
  if (bhInput.value && bhInput.value.trim() !== "") {
    updateDiameterFromBH();
  }
  // Alternatively, if diameter has a value but breite_hoehe doesn't, calculate breite_hoehe
  else if (dInput.value && dInput.value.trim() !== "" && (!bhInput.value || bhInput.value.trim() === "")) {
    updateBHFromDiameter();
  }
});

/**
 * Future Enhancement Ideas:
 *
 * 1. Configurable transformation coefficients:
 *    const transformationProfiles = {
 *      standard: { a: 1.077, b: 0.08 },
 *      premium: { a: 1.085, b: 0.05 },
 *      custom: { a: 1.070, b: 0.10 }
 *    };
 *
 * 2. Non-linear transformation support:
 *    function polynomialTransform(bh, coefficients) {
 *      return coefficients.reduce((sum, coeff, index) => {
 *        return sum + coeff * Math.pow(bh, index);
 *      }, 0);
 *    }
 *
 * 3. Validation ranges and manufacturing limits:
 *    const validationRules = {
 *      minDimension: 50,
 *      maxDimension: 2000,
 *      maxDiameterRatio: 1.2,
 *      warningThreshold: 1500
 *    };
 *
 * 4. Unit conversion support:
 *    const unitConverters = {
 *      mmToCm: (val) => val / 10,
 *      cmToInch: (val) => val / 2.54,
 *      inchToMm: (val) => val * 25.4
 *    };
 *
 * 5. Precision control for different manufacturing tolerances:
 *    function roundToPrecision(value, precision = 1) {
 *      const factor = Math.pow(10, precision);
 *      return Math.round(value * factor) / factor;
 *    }
 */

/***/ }),

/***/ "./assets/js/configurator/summary.js":
/*!*******************************************!*\
  !*** ./assets/js/configurator/summary.js ***!
  \*******************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   buildSummary: () => (/* binding */ buildSummary),
/* harmony export */   updateSummary: () => (/* binding */ updateSummary)
/* harmony export */ });
/* harmony import */ var core_js_modules_es_symbol_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! core-js/modules/es.symbol.js */ "./node_modules/core-js/modules/es.symbol.js");
/* harmony import */ var core_js_modules_es_symbol_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_symbol_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var core_js_modules_es_symbol_description_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! core-js/modules/es.symbol.description.js */ "./node_modules/core-js/modules/es.symbol.description.js");
/* harmony import */ var core_js_modules_es_symbol_description_js__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_symbol_description_js__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var core_js_modules_es_array_concat_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! core-js/modules/es.array.concat.js */ "./node_modules/core-js/modules/es.array.concat.js");
/* harmony import */ var core_js_modules_es_array_concat_js__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_concat_js__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var core_js_modules_es_array_from_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! core-js/modules/es.array.from.js */ "./node_modules/core-js/modules/es.array.from.js");
/* harmony import */ var core_js_modules_es_array_from_js__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_from_js__WEBPACK_IMPORTED_MODULE_3__);
/* harmony import */ var core_js_modules_es_array_slice_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! core-js/modules/es.array.slice.js */ "./node_modules/core-js/modules/es.array.slice.js");
/* harmony import */ var core_js_modules_es_array_slice_js__WEBPACK_IMPORTED_MODULE_4___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_slice_js__WEBPACK_IMPORTED_MODULE_4__);
/* harmony import */ var core_js_modules_es_array_sort_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! core-js/modules/es.array.sort.js */ "./node_modules/core-js/modules/es.array.sort.js");
/* harmony import */ var core_js_modules_es_array_sort_js__WEBPACK_IMPORTED_MODULE_5___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_array_sort_js__WEBPACK_IMPORTED_MODULE_5__);
/* harmony import */ var core_js_modules_es_number_to_fixed_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! core-js/modules/es.number.to-fixed.js */ "./node_modules/core-js/modules/es.number.to-fixed.js");
/* harmony import */ var core_js_modules_es_number_to_fixed_js__WEBPACK_IMPORTED_MODULE_6___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_number_to_fixed_js__WEBPACK_IMPORTED_MODULE_6__);
/* harmony import */ var core_js_modules_es_object_get_prototype_of_js__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! core-js/modules/es.object.get-prototype-of.js */ "./node_modules/core-js/modules/es.object.get-prototype-of.js");
/* harmony import */ var core_js_modules_es_object_get_prototype_of_js__WEBPACK_IMPORTED_MODULE_7___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_object_get_prototype_of_js__WEBPACK_IMPORTED_MODULE_7__);
/* harmony import */ var core_js_modules_es_object_set_prototype_of_js__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! core-js/modules/es.object.set-prototype-of.js */ "./node_modules/core-js/modules/es.object.set-prototype-of.js");
/* harmony import */ var core_js_modules_es_object_set_prototype_of_js__WEBPACK_IMPORTED_MODULE_8___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_object_set_prototype_of_js__WEBPACK_IMPORTED_MODULE_8__);
/* harmony import */ var core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! core-js/modules/es.object.to-string.js */ "./node_modules/core-js/modules/es.object.to-string.js");
/* harmony import */ var core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_9___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_9__);
/* harmony import */ var core_js_modules_es_object_values_js__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! core-js/modules/es.object.values.js */ "./node_modules/core-js/modules/es.object.values.js");
/* harmony import */ var core_js_modules_es_object_values_js__WEBPACK_IMPORTED_MODULE_10___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_object_values_js__WEBPACK_IMPORTED_MODULE_10__);
/* harmony import */ var core_js_modules_es_promise_js__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! core-js/modules/es.promise.js */ "./node_modules/core-js/modules/es.promise.js");
/* harmony import */ var core_js_modules_es_promise_js__WEBPACK_IMPORTED_MODULE_11___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_promise_js__WEBPACK_IMPORTED_MODULE_11__);
/* harmony import */ var core_js_modules_es_string_iterator_js__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! core-js/modules/es.string.iterator.js */ "./node_modules/core-js/modules/es.string.iterator.js");
/* harmony import */ var core_js_modules_es_string_iterator_js__WEBPACK_IMPORTED_MODULE_12___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_string_iterator_js__WEBPACK_IMPORTED_MODULE_12__);
/* harmony import */ var core_js_modules_es_string_trim_js__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! core-js/modules/es.string.trim.js */ "./node_modules/core-js/modules/es.string.trim.js");
/* harmony import */ var core_js_modules_es_string_trim_js__WEBPACK_IMPORTED_MODULE_13___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_string_trim_js__WEBPACK_IMPORTED_MODULE_13__);
/* harmony import */ var core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_14__ = __webpack_require__(/*! core-js/modules/web.dom-collections.for-each.js */ "./node_modules/core-js/modules/web.dom-collections.for-each.js");
/* harmony import */ var core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_14___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_14__);
/* harmony import */ var _variables__WEBPACK_IMPORTED_MODULE_15__ = __webpack_require__(/*! ./variables */ "./assets/js/configurator/variables.js");
/* harmony import */ var _functions__WEBPACK_IMPORTED_MODULE_16__ = __webpack_require__(/*! ./functions */ "./assets/js/configurator/functions.js");
/* provided dependency */ var $ = __webpack_require__(/*! jquery */ "jquery");
function _regenerator() { /*! regenerator-runtime -- Copyright (c) 2014-present, Facebook, Inc. -- license (MIT): https://github.com/babel/babel/blob/main/packages/babel-helpers/LICENSE */ var e, t, r = "function" == typeof Symbol ? Symbol : {}, n = r.iterator || "@@iterator", o = r.toStringTag || "@@toStringTag"; function i(r, n, o, i) { var c = n && n.prototype instanceof Generator ? n : Generator, u = Object.create(c.prototype); return _regeneratorDefine2(u, "_invoke", function (r, n, o) { var i, c, u, f = 0, p = o || [], y = !1, G = { p: 0, n: 0, v: e, a: d, f: d.bind(e, 4), d: function d(t, r) { return i = t, c = 0, u = e, G.n = r, a; } }; function d(r, n) { for (c = r, u = n, t = 0; !y && f && !o && t < p.length; t++) { var o, i = p[t], d = G.p, l = i[2]; r > 3 ? (o = l === n) && (u = i[(c = i[4]) ? 5 : (c = 3, 3)], i[4] = i[5] = e) : i[0] <= d && ((o = r < 2 && d < i[1]) ? (c = 0, G.v = n, G.n = i[1]) : d < l && (o = r < 3 || i[0] > n || n > l) && (i[4] = r, i[5] = n, G.n = l, c = 0)); } if (o || r > 1) return a; throw y = !0, n; } return function (o, p, l) { if (f > 1) throw TypeError("Generator is already running"); for (y && 1 === p && d(p, l), c = p, u = l; (t = c < 2 ? e : u) || !y;) { i || (c ? c < 3 ? (c > 1 && (G.n = -1), d(c, u)) : G.n = u : G.v = u); try { if (f = 2, i) { if (c || (o = "next"), t = i[o]) { if (!(t = t.call(i, u))) throw TypeError("iterator result is not an object"); if (!t.done) return t; u = t.value, c < 2 && (c = 0); } else 1 === c && (t = i.return) && t.call(i), c < 2 && (u = TypeError("The iterator does not provide a '" + o + "' method"), c = 1); i = e; } else if ((t = (y = G.n < 0) ? u : r.call(n, G)) !== a) break; } catch (t) { i = e, c = 1, u = t; } finally { f = 1; } } return { value: t, done: y }; }; }(r, o, i), !0), u; } var a = {}; function Generator() {} function GeneratorFunction() {} function GeneratorFunctionPrototype() {} t = Object.getPrototypeOf; var c = [][n] ? t(t([][n]())) : (_regeneratorDefine2(t = {}, n, function () { return this; }), t), u = GeneratorFunctionPrototype.prototype = Generator.prototype = Object.create(c); function f(e) { return Object.setPrototypeOf ? Object.setPrototypeOf(e, GeneratorFunctionPrototype) : (e.__proto__ = GeneratorFunctionPrototype, _regeneratorDefine2(e, o, "GeneratorFunction")), e.prototype = Object.create(u), e; } return GeneratorFunction.prototype = GeneratorFunctionPrototype, _regeneratorDefine2(u, "constructor", GeneratorFunctionPrototype), _regeneratorDefine2(GeneratorFunctionPrototype, "constructor", GeneratorFunction), GeneratorFunction.displayName = "GeneratorFunction", _regeneratorDefine2(GeneratorFunctionPrototype, o, "GeneratorFunction"), _regeneratorDefine2(u), _regeneratorDefine2(u, o, "Generator"), _regeneratorDefine2(u, n, function () { return this; }), _regeneratorDefine2(u, "toString", function () { return "[object Generator]"; }), (_regenerator = function _regenerator() { return { w: i, m: f }; })(); }
function _regeneratorDefine2(e, r, n, t) { var i = Object.defineProperty; try { i({}, "", {}); } catch (e) { i = 0; } _regeneratorDefine2 = function _regeneratorDefine(e, r, n, t) { function o(r, n) { _regeneratorDefine2(e, r, function (e) { return this._invoke(r, n, e); }); } r ? i ? i(e, r, { value: n, enumerable: !t, configurable: !t, writable: !t }) : e[r] = n : (o("next", 0), o("throw", 1), o("return", 2)); }, _regeneratorDefine2(e, r, n, t); }
function asyncGeneratorStep(n, t, e, r, o, a, c) { try { var i = n[a](c), u = i.value; } catch (n) { return void e(n); } i.done ? t(u) : Promise.resolve(u).then(r, o); }
function _asyncToGenerator(n) { return function () { var t = this, e = arguments; return new Promise(function (r, o) { var a = n.apply(t, e); function _next(n) { asyncGeneratorStep(a, r, o, _next, _throw, "next", n); } function _throw(n) { asyncGeneratorStep(a, r, o, _next, _throw, "throw", n); } _next(void 0); }); }; }















/**
 * Product Configurator - Summary Module
 *
 * This module handles the dynamic summary display for the product configurator.
 * It collects selected options from various input types (text inputs, radio buttons,
 * price matrices), calculates total prices, and provides interactive navigation
 * back to specific configuration steps.
 *
 * Features:
 * - Real-time summary updates with DOM update optimization (caching)
 * - Grouped and sorted option display by logical groups and steps
 * - Interactive navigation: click summary items to jump to configurator steps
 * - Print and save configuration as code or direct link
 * - Price calculation and formatted display for all options and total
 * - Support for nested/child option groups and price matrices
 * - Accessible focus and keyboard navigation for summary and controls
 * - Tooltip initialization for summary actions
 * - Graceful error handling for print/save actions
 *
 * @version 2.3.0
 * @package Configurator
 */




/**
 * Configuration flags for summary module behavior
 * @type {Object}
 */
var summaryConfig = {
  enableClickNavigation: false // Set to true to enable clicking summary items to navigate to steps
};

/**
 * Performance cache to prevent unnecessary DOM updates
 * @type {Object}
 */
var summaryCache = {
  lastHash: null,
  lastHtml: null
};

/**
 * Hide tooltip for a specific element
 * @param {HTMLElement} element - The element with tooltip to hide
 */
function hideTooltip(element) {
  var _window$bootstrap;
  if ((_window$bootstrap = window.bootstrap) !== null && _window$bootstrap !== void 0 && _window$bootstrap.Tooltip) {
    var tooltip = bootstrap.Tooltip.getInstance(element);
    if (tooltip) {
      tooltip.hide();
    }
  }
}

/**
 * Set up click interactions for summary items
 */
function setupSummaryInteractions() {
  var summary = document.getElementById("productConfiguratorSummary");
  if (!summary) return;

  // Only add click navigation if enabled in config
  if (summaryConfig.enableClickNavigation) {
    summary.addEventListener("click", function (e) {
      var item = e.target.closest(".list-group-item[data-step]");
      if (!item) return;
      var step = item.dataset.step;
      var key = item.dataset.key;
      scrollToCarouselStep(step, key);
    });
  }

  // Set up print button
  var printBtn = summary.querySelector(".btn-print-summary");
  if (printBtn) {
    printBtn.addEventListener("click", function (e) {
      e.preventDefault();
      e.stopPropagation();
      hideTooltip(printBtn);
      printSummary();
    });
  }
  // Set up save button
  var saveBtn = summary.querySelector(".btn-save-summary");
  if (saveBtn) {
    saveBtn.addEventListener("click", function (e) {
      e.preventDefault();
      e.stopPropagation();
      hideTooltip(saveBtn);
      saveSummaryConfig();
    });
  }

  // Set up edit buttons
  var editBtns = summary.querySelectorAll(".btn-edit-group");
  editBtns.forEach(function (editBtn) {
    editBtn.addEventListener("click", function (e) {
      e.preventDefault();
      e.stopPropagation();
      hideTooltip(editBtn);
      var step = editBtn.dataset.step;
      navigateToCarouselStep(step);
    });
  });

  // Initialize tooltips using the global function
  if (window.initTooltips) {
    window.initTooltips();
  }
}

/**
 * Apply focus and tabindex handling to form control elements
 *
 * @param {HTMLElement} element - The element to focus
 * @param {string} cleanupEventName - Unique identifier for cleanup function
 */
function applyFocusWithTabindex(element) {
  var cleanupEventName = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : "cleanup";
  element.setAttribute("tabindex", "0");
  var cleanup = function cleanup() {
    element.removeAttribute("tabindex");
  };
  element.addEventListener("blur", cleanup, {
    once: true
  });
  element.focus();
}

/**
 * Navigate to a specific carousel step and focus the related form control
 *
 * @param {string} step - The carousel step number to navigate to
 * @param {string} key - The option group key to focus within the step
 */
function scrollToCarouselStep(step, key) {
  if (!step) return;
  var carouselEl = document.getElementById("productConfiguratorCarousel");
  var carouselItem = carouselEl === null || carouselEl === void 0 ? void 0 : carouselEl.querySelector(".carousel-item[data-step=\"".concat(step, "\"]"));
  if (!carouselEl || !carouselItem) return;
  var carousel = bootstrap.Carousel.getOrCreateInstance(carouselEl);
  var items = Array.from(carouselEl.querySelectorAll(".carousel-item"));
  var targetIdx = items.indexOf(carouselItem);
  if (targetIdx === -1) return;

  /**
   * Focus the appropriate form control within the target carousel item
   */
  var focusControlElement = function focusControlElement() {
    var targetGroup = carouselItem.querySelector(".option-group[data-key=\"".concat(key, "\"]"));
    if (!targetGroup) return;
    var controlEl = null;
    var controlType = null;

    // Priority-based control element detection
    var detectionRules = [{
      type: "input",
      selector: "input.form-control",
      getElement: function getElement(input) {
        return input.closest(".form-floating");
      }
    }, {
      type: "button",
      selector: "button.form-select",
      getElement: function getElement(button) {
        return button.closest(".form-floating");
      }
    }, {
      type: "btngroup",
      selector: ".btn-group[data-key]",
      getElement: function getElement(btnGroup) {
        return btnGroup;
      }
    }];

    // Find first matching control element
    for (var _i = 0, _detectionRules = detectionRules; _i < _detectionRules.length; _i++) {
      var rule = _detectionRules[_i];
      var foundElement = targetGroup.querySelector(rule.selector);
      if (foundElement) {
        controlEl = rule.getElement(foundElement);
        if (controlEl) {
          controlType = rule.type;
          break;
        }
      }
    }

    // Apply focus based on control type
    if (controlEl && controlType) {
      applyFocusWithTabindex(controlEl, "cleanup".concat(controlType.charAt(0).toUpperCase() + controlType.slice(1)));
    } else {
      console.warn("[Summary] No focusable control element found for key:", key);
    }
  };

  // Handle carousel navigation
  var activeItem = carouselEl.querySelector(".carousel-item.active");
  var activeIdx = items.indexOf(activeItem);
  if (activeIdx === targetIdx) {
    focusControlElement();
  } else {
    carouselEl.addEventListener("slid.bs.carousel", focusControlElement, {
      once: true
    });
    carousel.to(targetIdx);
  }
}

/**
 * Navigate to a specific carousel step for editing (used by edit buttons)
 * Adds focus-ring styling and focuses the corresponding indicator button after scrolling
 *
 * @param {string} step - The carousel step number to navigate to
 */
function navigateToCarouselStep(step) {
  if (!step) return;
  var carouselEl = document.getElementById("productConfiguratorCarousel");
  var carouselItem = carouselEl === null || carouselEl === void 0 ? void 0 : carouselEl.querySelector(".carousel-item[data-step=\"".concat(step, "\"]"));
  if (!carouselEl || !carouselItem) return;
  var carousel = bootstrap.Carousel.getOrCreateInstance(carouselEl);
  var items = Array.from(carouselEl.querySelectorAll(".carousel-item"));
  var targetIdx = items.indexOf(carouselItem);
  if (targetIdx === -1) return;

  /**
   * Add focus with ring styling to the corresponding indicator button
   */
  var highlightIndicatorButton = function highlightIndicatorButton() {
    // Try multiple selectors to find the indicator button
    var indicatorButton = document.querySelector("button.indicator[data-bs-slide-to=\"".concat(targetIdx, "\"]"));
    if (!indicatorButton) {
      indicatorButton = document.querySelector("button[data-bs-slide-to=\"".concat(targetIdx, "\"]"));
    }
    if (!indicatorButton) {
      indicatorButton = document.querySelector("button[data-bs-target=\"#productConfiguratorCarousel\"][data-bs-slide-to=\"".concat(targetIdx, "\"]"));
    }
    if (indicatorButton) {
      // Check if the indicator is visible and scroll to it if needed
      var rect = indicatorButton.getBoundingClientRect();
      var isVisible = rect.left >= 0 && rect.right <= window.innerWidth && rect.top >= 0 && rect.bottom <= window.innerHeight;
      if (!isVisible) {
        indicatorButton.scrollIntoView({
          behavior: "smooth",
          block: "nearest",
          inline: "center"
        });
      }

      // Add focus ring classes and focus after scrolling
      setTimeout(function () {
        indicatorButton.classList.remove("border-secondary-subtle");
        indicatorButton.classList.add("border-primary-subtle", "focus-ring", "focus-ring-primary");
        indicatorButton.focus();

        // Remove classes after blur
        indicatorButton.addEventListener("blur", function () {
          indicatorButton.classList.remove("border-primary", "focus-ring", "focus-ring-primary");
          indicatorButton.classList.add("border-secondary-subtle");
        }, {
          once: true
        });
      }, isVisible ? 100 : 800);
    }
  };

  // Handle carousel navigation
  var activeItem = carouselEl.querySelector(".carousel-item.active");
  var activeIdx = items.indexOf(activeItem);
  if (activeIdx === targetIdx) {
    highlightIndicatorButton();
  } else {
    carouselEl.addEventListener("slid.bs.carousel", highlightIndicatorButton, {
      once: true
    });
    carousel.to(targetIdx);
  }
}

/**
 * Print the configuration summary with generated code and link
 * Note: The HTML content for the print view, including CSS, is generated on-demand
 * when this function is called (i.e., when the print button is clicked).
 * The data (`summaryHtml`, `totalHtml`) is fetched from the current DOM state.
 * Additionally, a configuration code is generated and included in the print.
 */
function printSummary() {
  return _printSummary.apply(this, arguments);
}
/**
 * Lock the heart button and wait for summary changes
 * @param {HTMLElement} heartButton - The heart button element to lock
 */
function _printSummary() {
  _printSummary = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee() {
    var summaryContent, totalContent, summaryHtml, totalHtml, printBtn, originalBtnContent, codeSection, configData, _yield$window$Configu, code, directLink, printWindow, _t;
    return _regenerator().w(function (_context) {
      while (1) switch (_context.p = _context.n) {
        case 0:
          summaryContent = document.getElementById("productConfiguratorSummary");
          totalContent = document.getElementById("productConfiguratorTotal");
          if (summaryContent) {
            _context.n = 1;
            break;
          }
          return _context.a(2);
        case 1:
          summaryHtml = summaryContent.innerHTML;
          totalHtml = totalContent ? totalContent.innerHTML : ""; // Show loading state
          printBtn = summaryContent.querySelector(".btn-print-summary");
          originalBtnContent = printBtn === null || printBtn === void 0 ? void 0 : printBtn.innerHTML;
          if (printBtn) {
            printBtn.innerHTML = '<i class="fa-sharp fa-light fa-spinner fa-fw fa-spin"></i><span class="visually-hidden">Wird gedruckt...</span>';
            printBtn.disabled = true;
          }
          codeSection = "";
          _context.p = 2;
          if (!(window.ConfiguratorSave && window.ConfiguratorSave.gatherConfigData && window.ConfiguratorSave.generateConfigCode)) {
            _context.n = 4;
            break;
          }
          configData = window.ConfiguratorSave.gatherConfigData(false);
          _context.n = 3;
          return window.ConfiguratorSave.generateConfigCode(configData);
        case 3:
          _yield$window$Configu = _context.v;
          code = _yield$window$Configu.code;
          directLink = _yield$window$Configu.directLink;
          codeSection = "\n        <div class=\"print-code-section\">\n          <h3>Konfiguration als Code</h3>\n          <p>Code: <code style=\"font-size: 1.2em; font-weight: bold; background: #f8f9fa; padding: 4px 8px; border: 1px solid #dee2e6;\">".concat(code, "</code></p>\n          <p>Direkter Link: <a href=\"").concat(directLink, "\" style=\"word-break: break-all; color: #0066cc;\">").concat(directLink, "</a></p>\n          <p class=\"small text-muted\">Mit diesem Code k\xF6nnen Sie Ihre Konfiguration jederzeit wieder aufrufen.</p>\n        </div>\n      ");
        case 4:
          _context.n = 6;
          break;
        case 5:
          _context.p = 5;
          _t = _context.v;
          console.warn("Code generation failed for print:", _t);
          codeSection = "\n      <div class=\"print-code-section\">\n        <h3>Konfiguration als Code</h3>\n        <p class=\"text-muted\">Code konnte nicht generiert werden. Bitte speichern Sie Ihre Konfiguration separat.</p>\n      </div>\n    ";
        case 6:
          _context.p = 6;
          // Reset button state
          if (printBtn && originalBtnContent) {
            printBtn.innerHTML = originalBtnContent;
            printBtn.disabled = false;
          }
          return _context.f(6);
        case 7:
          printWindow = window.open("", "_blank");
          printWindow.document.write("\n    <!DOCTYPE html>\n    <html>\n    <head>\n      <title>Badspiegel.de</title>\n      <style>\n        body { font-family: Arial, sans-serif; margin: 20px; line-height: 1.5; font-size: 14px; }\n        .print-code-section, \n        .print-header { margin-bottom: 16px; border-bottom: 2px solid #dee2e6; }\n        .print-header p { margin: 0; margin-bottom: 16px; }\n        \n        /* Bootstrap-like grid system - modified for print */\n        .row { display: block; margin: 0; }\n        .list-group-item .row { display: flex; align-items: center; margin: 0; }\n        .row.g-1 { display: flex; align-items: center; margin: 0; }\n        .col { flex: 1; padding: 0 4px; }\n        .col-auto { flex: 0 0 auto; padding: 0 4px; }\n        .col-12 { width: 100%; display: block; margin-bottom: 15px; }\n        \n        /* Container layouts */\n        .list-group-body { display: block !important; }\n        .list-group-body > .row { display: block; }\n        \n        /* List styles */\n        .list-group { list-style: none; margin: 0; padding: 0; }\n        .list-group-item { padding: 12px 16px; border: 1px solid #ddd; margin-bottom: 1px; background: #fff; display: block; }\n        .list-group-flush .list-group-item { border-left: 0; border-right: 0; border-top: 0; }\n        .list-group-flush .list-group-item:first-child { border-top: 0; }\n        .list-group-flush .list-group-item:last-child { border-bottom: 0; }\n        \n        /* Typography */\n        .fw-medium { font-weight: 500; }\n        .text-end { text-align: right; }\n        .fs-5 { font-size: 1.25rem; }\n        .small { font-size: 0.875rem; }\n        .text-muted { color: #6c757d; }\n        .text-truncate { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }\n        \n        /* Headers */\n        h5, h6 { font-weight: 500; }\n        h2 { font-size: 1.5rem; margin: 16px 0 16px 0; }\n        h3 { font-size: 1.3rem; margin: 16px 0 8px 0; font-weight: 500; }\n        h5 { font-size: 1.25rem; margin: 16px 0 16px 0; }\n        h6 { font-size: 1.1rem; margin: 16px 0 8px 0; }\n\n        /* Spacing */\n        .mb-0 { margin-bottom: 0; }\n        .px-3 { padding-left: 1rem; padding-right: 1rem; }\n        .py-2 { padding-top: 0.5rem; padding-bottom: 0.5rem; }\n        .pt-3 { padding-top: 1rem; }\n        .p-3 { padding: 1rem; }\n        \n        /* Background and borders */\n        .border-secondary-subtle { border-color: #dee2e6; }\n        .border-top { border-top: 1px solid #dee2e6; }\n        \n        /* Layout improvements */\n        .d-flex { display: flex; }\n        .justify-content-between { justify-content: space-between; }\n        .align-items-center { align-items: center; }\n        \n        /* Hide print button in print */\n        .btn-print-summary,\n        .btn-save-summary,\n        .btn-edit-group,\n        div.vr.border-secondary-subtle { display: none; }\n\n        /* Price styling */\n        .product-price { font-weight: 600; }\n        \n        /* Page breaks */\n        @media print {\n          .print-header { page-break-after: avoid; }\n          .print-code-section { page-break-inside: avoid; }\n          .list-group-item { page-break-inside: avoid; }\n        }\n\n        /* Overwrites */\n        .d-flex.justify-content-between.align-items-center.bg-secondary-subtle.px-3.pt-3.mb-0,\n        .list-group-body.bg-secondary-subtle.row.g-3.p-3.pt-0.mx-0.mt-0 {\n          padding: 0\n        }\n        .list-group-item.border-secondary-subtle {\n          padding: 0.25rem;\n        }\n        .bg-secondary-subtle.border-top.border-secondary-subtle.text-end.px-3.py-2.mb-0.fs-5 {\n          padding: 0.5rem 1rem;\n          border-top: 2px double #dee2e6;\n        }\n\n      </style>\n    </head>\n    <body>\n      <div class=\"print-header\">\n        <h2>Badspiegel.de Konfiguration</h2>\n        <p>Gedruckt am: ".concat(new Date().toLocaleDateString("de-DE"), " um ").concat(new Date().toLocaleTimeString("de-DE"), "</p>\n      </div>\n      ").concat(codeSection, "\n      ").concat(summaryHtml, "\n      ").concat(totalHtml, "\n    </body>\n    </html>\n  "));
          printWindow.document.close();
          printWindow.focus();
          printWindow.print();
          printWindow.close();
        case 8:
          return _context.a(2);
      }
    }, _callee, null, [[2, 5, 6, 7]]);
  }));
  return _printSummary.apply(this, arguments);
}
function lockHeartButton(heartButton) {
  var _window$bootstrap2, _window$bootstrap3;
  if (!heartButton) return;
  var heartIcon = heartButton.querySelector("i");
  if (!heartIcon) return;

  // Store original classes and tooltip title for restoration
  var originalClasses = heartIcon.className;
  var originalTitle = heartButton.getAttribute("title");

  // Lock the button: change icon to solid and disable interaction
  heartIcon.classList.remove("fa-light");
  heartIcon.classList.add("fa-solid");
  heartButton.disabled = true;
  heartButton.setAttribute("aria-pressed", "true");

  // Dispose any existing tooltip on the button FIRST
  if ((_window$bootstrap2 = window.bootstrap) !== null && _window$bootstrap2 !== void 0 && _window$bootstrap2.Tooltip) {
    var existingTooltip = bootstrap.Tooltip.getInstance(heartButton);
    if (existingTooltip) {
      try {
        existingTooltip.dispose();
      } catch (error) {
        console.warn("Error disposing existing tooltip:", error);
      }
    }
  }

  // Create a wrapper span for the tooltip (Bootstrap disabled element workaround)
  var wrapper = document.createElement("span");
  // wrapper.style.cssText = 'display: inline-block;';
  wrapper.setAttribute("title", "Konfiguration in Favoriten gespeichert");

  // Wrap the button
  heartButton.parentNode.insertBefore(wrapper, heartButton);
  wrapper.appendChild(heartButton);

  // Remove tooltip attributes from the disabled button
  heartButton.removeAttribute("title");
  heartButton.removeAttribute("data-bs-tooltip-md");
  heartButton.removeAttribute("data-bs-toggle");
  heartButton.removeAttribute("data-bs-placement");

  // Initialize tooltip on the wrapper with error handling
  var wrapperTooltip = null;
  if ((_window$bootstrap3 = window.bootstrap) !== null && _window$bootstrap3 !== void 0 && _window$bootstrap3.Tooltip) {
    try {
      wrapperTooltip = new bootstrap.Tooltip(wrapper, {
        container: "body",
        title: "Konfiguration wurde schon gespeichert",
        trigger: "hover focus",
        placement: "top"
      });
    } catch (error) {
      console.warn("Error creating wrapper tooltip:", error);
    }
  }

  // Store original hash to detect changes
  var currentHash = summaryCache.lastHash;

  // Create a function to check for changes and unlock
  var checkForChanges = function checkForChanges() {
    // Check if summary has changed
    if (summaryCache.lastHash !== currentHash) {
      // Summary has changed, unlock the button
      heartIcon.className = originalClasses;
      heartButton.disabled = false;
      heartButton.setAttribute("aria-pressed", "false");
      heartButton.setAttribute("title", originalTitle || "Konfiguration speichern");
      heartButton.setAttribute("data-bs-tooltip-md", "true");

      // Remove wrapper and restore original structure
      if (wrapper && wrapper.parentNode) {
        // Dispose wrapper tooltip safely
        if (wrapperTooltip) {
          try {
            wrapperTooltip.dispose();
          } catch (error) {
            console.warn("Error disposing wrapper tooltip:", error);
          }
        }

        // Move button back to original position
        wrapper.parentNode.insertBefore(heartButton, wrapper);
        wrapper.remove();

        // Reinitialize tooltip on the button with delay
        setTimeout(function () {
          var _window$bootstrap4;
          if ((_window$bootstrap4 = window.bootstrap) !== null && _window$bootstrap4 !== void 0 && _window$bootstrap4.Tooltip) {
            try {
              new bootstrap.Tooltip(heartButton, {
                container: "body",
                title: originalTitle || "Konfiguration speichern"
              });
            } catch (error) {
              console.warn("Error recreating button tooltip:", error);
            }
          }
        }, 100);
      }

      // Stop checking
      return true;
    }
    return false;
  };

  // Use a MutationObserver to watch for DOM changes in the summary
  var summaryElement = document.getElementById("productConfiguratorSummary");
  if (summaryElement) {
    var observer = new MutationObserver(function () {
      if (checkForChanges()) {
        observer.disconnect();
      }
    });
    observer.observe(summaryElement, {
      childList: true,
      subtree: true,
      attributes: false,
      characterData: true
    });
  }
}

/**
 * Save the configuration using the same functionality as the main save button
 */
function saveSummaryConfig() {
  return _saveSummaryConfig.apply(this, arguments);
}
/**
 * Group options by their logical groups for better organization
 *
 * @param {Array} optionsSummary - Array of selected option objects
 * @returns {Array} Array of grouped and sorted option objects
 */
function _saveSummaryConfig() {
  _saveSummaryConfig = _asyncToGenerator(/*#__PURE__*/_regenerator().m(function _callee2() {
    var saveBtn, result, errorMessage, _t2;
    return _regenerator().w(function (_context2) {
      while (1) switch (_context2.p = _context2.n) {
        case 0:
          saveBtn = document.querySelector(".btn-save-summary");
          if (saveBtn) {
            _context2.n = 1;
            break;
          }
          return _context2.a(2);
        case 1:
          if (!(!window.ConfiguratorSave || !window.ConfiguratorSave.saveConfiguration)) {
            _context2.n = 2;
            break;
          }
          console.error("ConfiguratorSave module not available");
          alert("Speichern-Funktion nicht verfügbar. Bitte verwenden Sie den Hauptspeichern-Button.");
          return _context2.a(2);
        case 2:
          _context2.p = 2;
          _context2.n = 3;
          return window.ConfiguratorSave.saveConfiguration($(saveBtn), "icon");
        case 3:
          result = _context2.v;
          // Lock immediately after save is successful (modal is opening)
          lockHeartButton(saveBtn);
          _context2.n = 5;
          break;
        case 4:
          _context2.p = 4;
          _t2 = _context2.v;
          console.error("Error saving configuration from summary:", _t2);

          // User-friendly error handling
          errorMessage = "Fehler beim Speichern der Konfiguration.";
          if (_t2.message) {
            errorMessage += " " + _t2.message;
          } else {
            errorMessage += " Bitte versuchen Sie es erneut.";
          }
          alert(errorMessage);
        case 5:
          return _context2.a(2);
      }
    }, _callee2, null, [[2, 4]]);
  }));
  return _saveSummaryConfig.apply(this, arguments);
}
function createGroupedOptions(optionsSummary) {
  var groups = {};
  optionsSummary.forEach(function (item) {
    var _item$groupData = item.groupData,
      groupKey = _item$groupData.groupKey,
      groupLabel = _item$groupData.groupLabel,
      groupStep = _item$groupData.groupStep,
      groupOrder = _item$groupData.groupOrder;
    if (!groups[groupKey]) {
      groups[groupKey] = {
        label: groupLabel,
        step: groupStep,
        order: groupOrder,
        options: []
      };
    }
    groups[groupKey].options.push(item);
  });
  return Object.values(groups).sort(function (a, b) {
    if (a.step !== b.step) return a.step - b.step;
    return a.order - b.order;
  });
}

/**
 * Build the HTML structure for the configuration summary
 *
 * @param {Object} params - Configuration parameters
 * @param {string} params.productTitle - The base product title
 * @param {number} params.productPrice - Base product price in euros
 * @param {Array} params.optionsSummary - Array of selected option objects
 * @returns {string} Complete HTML string for the summary display
 */
function buildSummary(_ref) {
  var productTitle = _ref.productTitle,
    productPrice = _ref.productPrice,
    optionsSummary = _ref.optionsSummary;
  var htmlOutput = "\n    <div class=\"d-flex justify-content-between align-items-center bg-secondary-subtle px-3 pt-3 pe-2 mb-0\">\n      <h5 class=\"lh-base me-auto mb-0\">Zusammenfassung</h5>\n      <div class=\"d-none vr border-secondary-subtle\"></div>\n      <button type=\"button\" class=\"btn-save-summary btn btn-sm btn-link link-body-emphasis\" style=\"--bs-btn-disabled-opacity: 1;--bs-btn-disabled-color: var(--bs-dark)\" title=\"Konfiguration speichern\" data-bs-tooltip-md=\"true\">\n        <i class=\"fa-sharp fa-fw fa-light fa-heart\"></i>\n        <span class=\"visually-hidden\">Speichern</span>\n      </button>\n      <div class=\"vr border-secondary-subtle\"></div>\n      <button type=\"button\" class=\"btn-print-summary btn btn-sm btn-link link-body-emphasis\" title=\"Konfiguration drucken\" data-bs-tooltip-md=\"true\">\n        <i class=\"fa-sharp fa-fw fa-light fa-print\"></i>\n        <span class=\"visually-hidden\">Drucken</span>\n      </button>\n    </div>\n    <div class=\"list-group-body bg-secondary-subtle row g-3 p-3 pt-0 mx-0 mt-0\">\n      <div class=\"col-12 px-0\">\n        <ul class=\"list-group list-group-flush\">\n          <li class=\"list-group-item border-secondary-subtle\">\n            <span class=\"row g-1\">\n              <span class=\"col text-truncate fw-medium\">".concat(productTitle, "</span>\n              <span class=\"col-auto\">").concat((0,_functions__WEBPACK_IMPORTED_MODULE_16__.formatPrice)(productPrice), "</span>\n            </span>\n          </li>\n        </ul>\n      </div>\n  ");
  if (optionsSummary.length === 0) {
    htmlOutput += "\n        <ul class=\"list-group list-group-flush\">\n          <li class=\"list-group-item text-danger\">Keine Optionen ausgew\xE4hlt</li>\n        </ul>\n    ";
  } else {
    var groupedOptions = createGroupedOptions(optionsSummary);
    groupedOptions.forEach(function (group) {
      if (group.options.length === 0) return;
      var showGroupLabel = group.options.some(function (opt) {
        return !opt.isPricematrix;
      });
      htmlOutput += "<div class=\"col-12 px-0\">";
      if (showGroupLabel) {
        htmlOutput += "\n          <div class=\"d-flex justify-content-between align-items-center mb-2\">\n            <h6 class=\"mb-0\">".concat(group.label, "</h6>\n            <button type=\"button\" class=\"btn-edit-group btn btn-sm btn-link link-body-emphasis\" data-step=\"").concat(group.step, "\" title=\"Bearbeiten\" data-bs-tooltip-md=\"true\">\n              <i class=\"fa-sharp fa-fw fa-light fa-edit\"></i>\n              <span class=\"visually-hidden\">Bearbeiten</span>\n            </button>\n          </div>\n        ");
      }
      htmlOutput += "<ul class=\"list-group list-group-flush\">";
      group.options.forEach(function (item) {
        var key = item.key,
          label = item.label,
          pricematrixLabel = item.pricematrixLabel,
          pricematrixPrice = item.pricematrixPrice,
          inputValue = item.inputValue,
          inputPrice = item.inputPrice,
          radioLabel = item.radioLabel,
          radioPrice = item.radioPrice,
          isChild = item.isChild,
          stepNumber = item.stepNumber,
          isPricematrix = item.isPricematrix;
        var value = radioLabel || inputValue || pricematrixLabel || "";
        var price = radioPrice || inputPrice || pricematrixPrice || 0;
        var formattedPrice = (0,_functions__WEBPACK_IMPORTED_MODULE_16__.formatPrice)(price) || "-";
        var childClass = isChild ? " text-muted small" : ""; // Conditional classes and attributes for pricematrix items
        var isClickable = !isPricematrix && summaryConfig.enableClickNavigation;
        var actionClass = isClickable ? " list-group-item-action" : "";
        var interactiveAttrs = isClickable ? "\n            role=\"button\"\n            data-step=\"".concat(stepNumber, "\"\n            data-key=\"").concat(key, "\"\n            tabindex=\"0\"\n            aria-label=\"Zur Option ").concat(label, " springen\"\n        ") : "";
        htmlOutput += "\n          <li\n            class=\"list-group-item".concat(actionClass, " list-group-item-secondary bg-body text-body border-secondary-subtle").concat(childClass, "\"\n            ").concat(interactiveAttrs, "\n          >\n            <span class=\"row g-1\">\n              <span class=\"col-auto fw-medium\">").concat(label, ":</span>\n              <span class=\"col text-truncate\">").concat(value, "</span>\n              <span class=\"col-auto\">").concat(formattedPrice, "</span>\n            </span>\n          </li>\n        ");
      });
      htmlOutput += "</ul></div>";
    });
  }
  htmlOutput += "</div>";
  return htmlOutput;
}

/**
 * Main function to update the product configuration summary
 */
function updateSummary() {
  var productPrice = (0,_functions__WEBPACK_IMPORTED_MODULE_16__.getProductPrice)();
  var productTitle = (0,_functions__WEBPACK_IMPORTED_MODULE_16__.getProductTitle)();
  var optionsSummary = [];
  _variables__WEBPACK_IMPORTED_MODULE_15__.optionGroups.forEach(function (group) {
    var _group$closest;
    var label = group.dataset.label || "Unbekannte Option";
    var isChild = group.classList.contains("option-group-child");
    var key = group.dataset.key;
    var carouselItem = group.closest(".carousel-item");
    var groupDataForSummary = {
      groupKey: group.dataset.group || "Sonstiges",
      groupLabel: (carouselItem === null || carouselItem === void 0 ? void 0 : carouselItem.dataset.label) || group.dataset.group || "Sonstiges",
      groupStep: parseInt(carouselItem === null || carouselItem === void 0 ? void 0 : carouselItem.dataset.step, 10) || 999,
      groupOrder: parseInt(group.dataset.order, 10) || 999
    };

    // Check for pricematrix selections (pxbh = price x by height matrix)
    var pricematrixEl = group.querySelector('.option-price[class*="pxbh"]');
    var pricematrixLabel = null;
    var pricematrixPrice = null;
    var isPricematrix = false;
    if (pricematrixEl && pricematrixEl.value.trim() !== "") {
      var _selected$dataset, _selected$dataset2;
      var selected = pricematrixEl.selectedOptions[0];
      pricematrixLabel = (selected === null || selected === void 0 || (_selected$dataset = selected.dataset) === null || _selected$dataset === void 0 || (_selected$dataset = _selected$dataset.label) === null || _selected$dataset === void 0 ? void 0 : _selected$dataset.trim()) || "";
      pricematrixPrice = (selected === null || selected === void 0 || (_selected$dataset2 = selected.dataset) === null || _selected$dataset2 === void 0 ? void 0 : _selected$dataset2.price) || null;
      isPricematrix = true;
    }

    // Check for text input values
    var inputEl = group.querySelector(".option-input");
    var inputValue = null;
    var inputPrice = null;
    if (inputEl && inputEl.value.trim() !== "") {
      inputValue = inputEl.value.trim();
      inputPrice = inputEl.dataset.price || null;
    }

    // Check for radio button selections
    var radioEl = group.querySelector(".option-radio:checked");
    var radioLabel = null;
    var radioPrice = null;
    if (radioEl && radioEl.value.trim() !== "") {
      radioLabel = radioEl.dataset.label || radioEl.value.trim();
      radioPrice = radioEl.dataset.price || null;
    }
    var stepNumber = ((_group$closest = group.closest(".carousel-item")) === null || _group$closest === void 0 || (_group$closest = _group$closest.dataset) === null || _group$closest === void 0 ? void 0 : _group$closest.step) || null;
    if (inputValue || radioLabel || pricematrixLabel) {
      optionsSummary.push({
        key: key,
        label: label,
        pricematrixLabel: pricematrixLabel,
        pricematrixPrice: pricematrixPrice,
        inputValue: inputValue,
        inputPrice: inputPrice,
        radioLabel: radioLabel,
        radioPrice: radioPrice,
        isChild: isChild,
        stepNumber: stepNumber,
        isPricematrix: isPricematrix,
        groupData: groupDataForSummary
      });
    }
  });

  // Performance optimization: skip update if nothing changed
  var currentHash = JSON.stringify(optionsSummary);
  if (summaryCache.lastHash === currentHash && summaryCache.lastHtml) {
    return;
  }

  // Generate and inject summary HTML
  var summaryHtml = buildSummary({
    productTitle: productTitle,
    productPrice: productPrice,
    optionsSummary: optionsSummary
  });
  var configuratorSummary = document.getElementById("productConfiguratorSummary");
  if (configuratorSummary) {
    configuratorSummary.innerHTML = summaryHtml;
    setupSummaryInteractions(); // Re-establishes click handlers and initializes tooltips
  }

  // Calculate and display total price
  var extraPrice = optionsSummary.reduce(function (acc, item) {
    return acc + (0,_functions__WEBPACK_IMPORTED_MODULE_16__.toNumber)(item.inputPrice) + (0,_functions__WEBPACK_IMPORTED_MODULE_16__.toNumber)(item.radioPrice) + (0,_functions__WEBPACK_IMPORTED_MODULE_16__.toNumber)(item.pricematrixPrice);
  }, 0);
  var totalPrice = productPrice + extraPrice;
  var totalPriceFormatted = (0,_functions__WEBPACK_IMPORTED_MODULE_16__.formatPrice)(totalPrice.toFixed(2));
  var totalHtml = "\n    <p class=\"bg-secondary-subtle border-top border-secondary-subtle text-end px-3 py-2 mb-0 fs-5\">\n      <span class=\"product-price-total-text small\">Gesamtpreis:</span>\n      <span class=\"product-price price fw-medium\">".concat(totalPriceFormatted || "0,00 €", "</span>\n    </p>\n  ");
  var configuratorTotal = document.getElementById("productConfiguratorTotal");
  if (configuratorTotal) {
    configuratorTotal.innerHTML = totalHtml;
  }

  // Update cache
  summaryCache.lastHash = currentHash;
  summaryCache.lastHtml = summaryHtml;
}

/**
 * Initialize the summary module
 */
document.addEventListener("DOMContentLoaded", function () {
  setupSummaryInteractions();
});

/***/ }),

/***/ "./assets/js/configurator/variables.js":
/*!*********************************************!*\
  !*** ./assets/js/configurator/variables.js ***!
  \*********************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   DEBUG_MODE: () => (/* binding */ DEBUG_MODE),
/* harmony export */   addToCartButton: () => (/* binding */ addToCartButton),
/* harmony export */   breiteHoheInput: () => (/* binding */ breiteHoheInput),
/* harmony export */   breiteInput: () => (/* binding */ breiteInput),
/* harmony export */   breiteObenInput: () => (/* binding */ breiteObenInput),
/* harmony export */   breiteUntenInput: () => (/* binding */ breiteUntenInput),
/* harmony export */   checkCheckedInput: () => (/* binding */ checkCheckedInput),
/* harmony export */   checkCheckedInputs: () => (/* binding */ checkCheckedInputs),
/* harmony export */   checkInput: () => (/* binding */ checkInput),
/* harmony export */   checkInputs: () => (/* binding */ checkInputs),
/* harmony export */   checkNoInput: () => (/* binding */ checkNoInput),
/* harmony export */   checkNoInputs: () => (/* binding */ checkNoInputs),
/* harmony export */   configurator: () => (/* binding */ configurator),
/* harmony export */   durchmesserInput: () => (/* binding */ durchmesserInput),
/* harmony export */   hoeheInput: () => (/* binding */ hoeheInput),
/* harmony export */   hoeheLinksInput: () => (/* binding */ hoeheLinksInput),
/* harmony export */   hoeheRechtsInput: () => (/* binding */ hoeheRechtsInput),
/* harmony export */   isBadspiegel: () => (/* binding */ isBadspiegel),
/* harmony export */   isBoard: () => (/* binding */ isBoard),
/* harmony export */   isHochschrank: () => (/* binding */ isHochschrank),
/* harmony export */   isLED: () => (/* binding */ isLED),
/* harmony export */   isLOR: () => (/* binding */ isLOR),
/* harmony export */   isLORU: () => (/* binding */ isLORU),
/* harmony export */   isLR: () => (/* binding */ isLR),
/* harmony export */   isO: () => (/* binding */ isO),
/* harmony export */   isOU: () => (/* binding */ isOU),
/* harmony export */   isRechteckig: () => (/* binding */ isRechteckig),
/* harmony export */   isRund: () => (/* binding */ isRund),
/* harmony export */   isSK: () => (/* binding */ isSK),
/* harmony export */   isSK1: () => (/* binding */ isSK1),
/* harmony export */   isSK2: () => (/* binding */ isSK2),
/* harmony export */   isSpiegelschrank: () => (/* binding */ isSpiegelschrank),
/* harmony export */   isUnterschrank: () => (/* binding */ isUnterschrank),
/* harmony export */   matrixBreiteHoehe: () => (/* binding */ matrixBreiteHoehe),
/* harmony export */   matrixDurchmesser: () => (/* binding */ matrixDurchmesser),
/* harmony export */   numberInput: () => (/* binding */ numberInput),
/* harmony export */   numberInputs: () => (/* binding */ numberInputs),
/* harmony export */   optionGroup: () => (/* binding */ optionGroup),
/* harmony export */   optionGroups: () => (/* binding */ optionGroups),
/* harmony export */   product: () => (/* binding */ product),
/* harmony export */   radioCheckedInput: () => (/* binding */ radioCheckedInput),
/* harmony export */   radioCheckedInputs: () => (/* binding */ radioCheckedInputs),
/* harmony export */   radioInput: () => (/* binding */ radioInput),
/* harmony export */   radioInputs: () => (/* binding */ radioInputs),
/* harmony export */   radioNoInput: () => (/* binding */ radioNoInput),
/* harmony export */   radioNoInputs: () => (/* binding */ radioNoInputs),
/* harmony export */   textInput: () => (/* binding */ textInput),
/* harmony export */   textInputs: () => (/* binding */ textInputs),
/* harmony export */   tiefeInput: () => (/* binding */ tiefeInput)
/* harmony export */ });
/* harmony import */ var core_js_modules_es_function_name_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! core-js/modules/es.function.name.js */ "./node_modules/core-js/modules/es.function.name.js");
/* harmony import */ var core_js_modules_es_function_name_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_function_name_js__WEBPACK_IMPORTED_MODULE_0__);

/**
 * Product Configurator - Variables and Constants Module
 *
 * This module centralizes all DOM element references, product type detection flags,
 * and configurator state variables used throughout the product configuration system.
 * It provides a single source of truth for element queries and product characteristics.
 *
 * Features:
 * - Product type detection (mirrors, cabinets, lighting types)
 * - Form detection (round, rectangular, special cuts)
 * - Lighting configuration detection (LED positions)
 * - DOM element caching for performance optimization
 * - Input field references for all configurator types
 * - Price matrix element identification
 * - Comprehensive product attribute flags
 * - Simple debug system for monitoring
 *
 * @version 2.3.0
 * @package Configurator
 */

// ====================== DEBUG CONFIGURATION ======================

/**
 * Simple debug flag - set to false for production
 * @type {boolean}
 */
var DEBUG_MODE = true;

// ====================== PRODUCT TYPE DETECTION ======================

/**
 * Base product container element
 * @type {HTMLElement|null}
 */
var product = document.querySelector(".product");
if (DEBUG_MODE && product) console.log("✅ const product:", product.className);

// ====================== PRODUCT CATEGORY DETECTION ======================

/**
 * Product category elements
 * @type {boolean}
 */
var isBadspiegel = product === null || product === void 0 ? void 0 : product.classList.contains("product_cat-badspiegel");
var isSpiegelschrank = product === null || product === void 0 ? void 0 : product.classList.contains("product_cat-spiegelschraenke");
var isUnterschrank = product === null || product === void 0 ? void 0 : product.classList.contains("product_cat-unterschraenke");
var isHochschrank = product === null || product === void 0 ? void 0 : product.classList.contains("product_cat-hochschraenke");
var isBoard = (product === null || product === void 0 ? void 0 : product.classList.contains("product_cat-sideboards")) || (product === null || product === void 0 ? void 0 : product.classList.contains("product_cat-lowboards"));
if (DEBUG_MODE) {
  if (isBadspiegel) console.log("✅ Badspiegel detected");
  if (isSpiegelschrank) console.log("✅ Spiegelschrank detected");
  if (isUnterschrank) console.log("✅ Unterschrank detected");
  if (isHochschrank) console.log("✅ Hochschrank detected");
  if (isBoard) console.log("✅ Sideboard/Lowboard detected");
}

// ====================== LIGHTING CONFIGURATION DETECTION ======================

/**
 * General LED lighting presence
 * @type {boolean}
 */
var isLED = product === null || product === void 0 ? void 0 : product.classList.contains("product_attr-beleuchtung_ja");
if (DEBUG_MODE && isLED) console.log("✅ const isLED: ", isLED);

/**
 * LED lighting position
 * @type {boolean}
 */
var isLORU = product === null || product === void 0 ? void 0 : product.classList.contains("product_attr-lichtposition_rundherum");
var isLOR = product === null || product === void 0 ? void 0 : product.classList.contains("product_attr-lichtposition_links-oben-rechts");
var isLR = product === null || product === void 0 ? void 0 : product.classList.contains("product_attr-lichtposition_links-rechts");
var isOU = product === null || product === void 0 ? void 0 : product.classList.contains("product_attr-lichtposition_oben-unten");
var isO = product === null || product === void 0 ? void 0 : product.classList.contains("product_attr-lichtposition_oben");
if (DEBUG_MODE) {
  if (isLORU) console.log("✅ const isLORU: ", isLORU);
  if (isLOR) console.log("✅ const isLOR: ", isLOR);
  if (isLR) console.log("✅ const isLR: ", isLR);
  if (isOU) console.log("✅ const isOU: ", isOU);
  if (isO) console.log("✅ const isO: ", isO);
}

// ====================== PRODUCT SHAPE DETECTION ======================

/**
 * Product shape and form factor detection
 * @type {boolean}
 */
var isRechteckig = (product === null || product === void 0 ? void 0 : product.classList.contains("product_attr-form_rechteckig")) || (product === null || product === void 0 ? void 0 : product.classList.contains("product_attr-form_quadrat"));
var isRund = product === null || product === void 0 ? void 0 : product.classList.contains("product_attr-form_rund");
var isSK = (product === null || product === void 0 ? void 0 : product.classList.contains("product_attr-form_halbmond")) || (product === null || product === void 0 ? void 0 : product.classList.contains("product_attr-form_rund-angeschnitten"));
var isSK1 = (product === null || product === void 0 ? void 0 : product.classList.contains("product_attr-schnittkanten_1")) || (product === null || product === void 0 ? void 0 : product.classList.contains("product_attr-schnittkante_unten")) || (product === null || product === void 0 ? void 0 : product.classList.contains("product_attr-schnittkante_seite"));
var isSK2 = (product === null || product === void 0 ? void 0 : product.classList.contains("product_attr-schnittkanten_2")) || (product === null || product === void 0 ? void 0 : product.classList.contains("product_attr-schnittkante_seite-und-unten"));
if (DEBUG_MODE) {
  if (isRechteckig) console.log("✅ Rechteckig detected");
  if (isRund) console.log("✅ Rund detected");
  if (isSK) console.log("✅ SK detected");
  if (isSK1) console.log("✅ SK1 detected");
  if (isSK2) console.log("✅ SK2 detected");
}

// ====================== SPECIFIC DIMENSION INPUTS ======================

/**
 * Specific dimension input elements
 * @type {HTMLElement|null}
 */
var durchmesserInput = document.querySelector('.option-input[name="durchmesser"]');
var breiteHoheInput = document.querySelector('.option-input[name="breite_hoehe"]');
var breiteInput = document.querySelector('.option-input[name="breite"]');
var breiteUntenInput = document.querySelector('.option-input[name="breite_unten"]');
var breiteObenInput = document.querySelector('.option-input[name="breite_oben"]');
var hoeheInput = document.querySelector('.option-input[name="hoehe"]');
var hoeheLinksInput = document.querySelector('.option-input[name="hoehe_links"]');
var hoeheRechtsInput = document.querySelector('.option-input[name="hoehe_rechts"]');
var tiefeInput = document.querySelector('.option-input[name="tiefe"]');
if (DEBUG_MODE) {
  if (durchmesserInput) console.log("✅ Durchmesser input found");
  if (breiteHoheInput) console.log("✅ BreiteHoehe input found");
  if (breiteInput) console.log("✅ Breite input found");
  if (breiteUntenInput) console.log("✅ BreiteUnten input found");
  if (breiteObenInput) console.log("✅ BreiteOben input found");
  if (hoeheInput) console.log("✅ Hoehe input found");
  if (hoeheLinksInput) console.log("✅ HoeheLinks input found");
  if (hoeheRechtsInput) console.log("✅ HoeheRechts input found");
  if (tiefeInput) console.log("✅ Tiefe input found");
}

/////////////////////////////////////////////////////////////////
/////////////////////////////////////////////////////////////////
/////////////////////////////////////////////////////////////////

// ====================== CART FUNCTIONALITY ======================

/**
 * WooCommerce add to cart button reference
 * Used for cart integration and validation state management
 * @type {HTMLElement|null}
 */
var addToCartButton = document.querySelector(".single_add_to_cart_button");

// ====================== CONFIGURATOR CORE ELEMENTS ======================

/**
 * Main configurator container element
 * Central hub for all configuration interactions
 * @type {HTMLElement|null}
 */
var configurator = document.getElementById("productConfigurator");

/**
 * RESPONSIVE OFFCANVAS SYSTEM
 * ===========================
 */

/**
 * Bootstrap offcanvas element for mobile configuration display
 * @type {HTMLElement|null}
 */
// export const offcanvas = document.getElementById("productConfiguratorOffcanvas"); // not in use

/**
 * All option elements that use offcanvas display mode
 * @type {NodeList}
 */
// export const offcanvasElements = document.querySelectorAll(".option-offcanvas"); // is it in use?

// ====================== OPTION GROUP ELEMENTS ======================

/**
 * GLOBAL OPTION GROUP REFERENCES
 * ==============================
 */

/**
 * Single option group element (first found)
 * @type {HTMLElement|null}
 */
var optionGroup = document.querySelector(".option-group");

/**
 * All option group elements in the configurator
 * Used for iteration and batch operations
 * @type {NodeList}
 */
var optionGroups = document.querySelectorAll(".option-group");

// ====================== INPUT TYPE REFERENCES ======================

/**
 * SINGLE INPUT ELEMENT REFERENCES
 * ===============================
 * These references point to the first found element of each type
 * Useful for singular operations and validation checks
 */

/**
 * First number input field
 */
var numberInput = document.querySelector('.option-input[type="number"]');

/**
 * First text input field
 */
var textInput = document.querySelector('.option-input[type="text"]');

/**
 * First radio button input
 */
var radioInput = document.querySelector('.option-radio[type="radio"]');

/**
 * First checkbox input
 */
var checkInput = document.querySelector('.option-check[type="checkbox"]');

/**
 * MULTIPLE INPUT ELEMENT COLLECTIONS
 * ==================================
 * These NodeLists contain all elements of each type for iteration
 */

/**
 * All number input fields in the configurator
 * @type {NodeList}
 */
var numberInputs = document.querySelectorAll('.option-input[type="number"]');

/**
 * All text input fields in the configurator
 * @type {NodeList}
 */
var textInputs = document.querySelectorAll('.option-input[type="text"]');

/**
 * All radio button inputs in the configurator
 * @type {NodeList}
 */
var radioInputs = document.querySelectorAll('.option-radio[type="radio"]');

/**
 * All checkbox inputs in the configurator
 * @type {NodeList}
 */
var checkInputs = document.querySelectorAll('.option-check[type="checkbox"]');

// ====================== EMPTY VALUE INPUT DETECTION ======================

/**
 * SINGLE "NO SELECTION" INPUT REFERENCES
 * ======================================
 * These inputs represent "none" or "no selection" options
 */

/**
 * First radio button with empty value (represents "no selection")
 */
var radioNoInput = document.querySelector('.option-input[type="radio"][value=""]');

/**
 * First checkbox with empty value (represents "no selection")
 */
var checkNoInput = document.querySelector('.option-input[type="checkbox"][value=""]');

/**
 * MULTIPLE "NO SELECTION" INPUT COLLECTIONS
 * =========================================
 */

/**
 * All radio buttons with empty values
 * @type {NodeList}
 */
var radioNoInputs = document.querySelectorAll('.option-input[type="radio"][value=""]');

/**
 * All checkboxes with empty values
 * @type {NodeList}
 */
var checkNoInputs = document.querySelectorAll('.option-input[type="checkbox"][value=""]');

// ====================== SELECTED INPUT DETECTION ======================

/**
 * SINGLE SELECTED INPUT REFERENCES
 * ================================
 * Currently selected/checked input elements
 */

/**
 * First currently checked radio button
 */
var radioCheckedInput = document.querySelector('.option-radio[type="radio"]:checked');

/**
 * First currently checked checkbox
 */
var checkCheckedInput = document.querySelector('.option-check[type="checkbox"]:checked');

/**
 * MULTIPLE SELECTED INPUT COLLECTIONS
 * ===================================
 */

/**
 * All currently checked radio buttons
 * @type {NodeList}
 */
var radioCheckedInputs = document.querySelectorAll('.option-radio[type="radio"]:checked');

/**
 * All currently checked checkboxes
 * @type {NodeList}
 */
var checkCheckedInputs = document.querySelectorAll('.option-check[type="checkbox"]:checked');

// ====================== PRICE MATRIX ELEMENTS ======================

/**
 * DYNAMIC PRICING CALCULATION ELEMENTS
 * ====================================
 * These select elements contain price matrices for dimension-based pricing
 */

/**
 * Diameter-based price matrix selector
 * Contains options with diameter values and corresponding prices
 * @type {HTMLSelectElement|null}
 */
var matrixDurchmesser = document.querySelector('.option-price[name*="pxd"]');

/**
 * Width x Height price matrix selector
 * Contains options with combined dimension values and corresponding prices
 * @type {HTMLSelectElement|null}
 */
var matrixBreiteHoehe = document.querySelector('.option-price[name*="pxbh"]');

// Debug price matrix elements
if (DEBUG_MODE) {
  if (matrixDurchmesser) console.log("✅ Price matrix Durchmesser found:", matrixDurchmesser.name);
  if (matrixBreiteHoehe) console.log("✅ Price matrix BreiteHoehe found:", matrixBreiteHoehe.name);
}

// ====================== ARCHIVED DETECTION FLAGS ======================

/**
 * LEGACY PRODUCT DETECTION (COMMENTED OUT)
 * ========================================
 * These flags are preserved for potential future use or reference
 * They represent more granular product categorization that may be needed
 */

/*
// Glass type detection flags
export const isGlasESG = document.querySelectorAll('.product-summary.product_cat-esg-glas').length > 0;
export const isGlasVSG = document.querySelectorAll('.product-summary.product_cat-vsg-glas').length > 0;
export const isGlasFloat = document.querySelectorAll('.product-summary.product_cat-floatglas').length > 0;

// Lighting status detection
export const isSpiegelBeleuchtet = document.querySelectorAll('.product-summary.product_cat-spiegel-mit-beleuchtung').length > 0;
export const isSpiegelUnbeleuchtet = document.querySelectorAll('.product-summary.product_cat-spiegel-ohne-beleuchtung').length > 0;

// Shape category detection
export const isRechteckigeSpiegel = document.querySelectorAll('.product-summary.product_cat-rechteckige-spiegel').length > 0;
export const isRundeSpiegel = document.querySelectorAll('.product-summary.product_cat-runde-spiegel').length > 0;
export const isOvaleSpiegel = document.querySelectorAll('.product-summary.product_cat-ovale-spiegel').length > 0;
export const isAbgerundeteSpiegel = document.querySelectorAll('.product-summary.product_cat-abgerundete-spiegel').length > 0;

// Special functionality detection
export const isKlappspiegel = document.querySelectorAll('.product-summary.product_cat-klappspiegel-bad').length > 0;
export const isFacettenspiegel = document.querySelectorAll('.product-summary.product_cat-facettenspiegel').length > 0;
export const isDachschraege = document.querySelectorAll('.product-summary.product_cat-spiegel-fuer-dachschraege').length > 0;

// Corner rounding detection (1-4 corners)
export const isAbgerundeteEcken = document.querySelectorAll('.product-summary.product_cat-spiegel-mit-abgerundeten-ecken').length > 0;
export const isAbgerundeteEcken1 = document.querySelectorAll('.product-summary.product_cat-spiegel-mit-einer-abgerundeten-ecke').length > 0;
export const isAbgerundeteEcken2 = document.querySelectorAll('.product-summary.product_cat-spiegel-mit-zwei-abgerundeten-ecken').length > 0;
export const isAbgerundeteEcken3 = document.querySelectorAll('.product-summary.product_cat-spiegel-mit-drei-abgerundeten-ecken').length > 0;
export const isAbgerundeteEcken4 = document.querySelectorAll('.product-summary.product_cat-spiegel-mit-vier-abgerundeten-ecken').length > 0;

// Detailed LED positioning (alternative detection method)
export const isLEDSpiegelRundherum = document.querySelectorAll('.product-summary.product_cat-led-spiegel-rundherum-beleuchtet').length > 0;
export const isLEDSpiegelLinksObenRechts = document.querySelectorAll('.product-summary.product_cat-led-spiegel-links-oben-rechts-beleuchtet').length > 0;
export const isLEDSpiegelLinksRechts = document.querySelectorAll('.product-summary.product_cat-led-spiegel-links-rechts-beleuchtet').length > 0;
export const isLEDSpiegelObenUnten = document.querySelectorAll('.product-summary.product_cat-led-spiegel-oben-unten-beleuchtet').length > 0;
export const isLEDSpiegelOben = document.querySelectorAll('.product-summary.product_cat-led-spiegel-oben-beleuchtet').length > 0;

// Special lighting effects
export const isAuraEffekt = document.querySelectorAll('.product-summary.product_cat-spiegel-mit-auraeffekt').length > 0;
export const isBlendEffekt = document.querySelectorAll('.product-summary.product_cat-spiegel-mit-blendeffekt').length > 0;
*/

/**
 * Future Enhancement Ideas:
 *
 * 1. Dynamic variable generation:
 *    function createProductFlags(selectors) {
 *      return Object.fromEntries(
 *        selectors.map(sel => [sel.name, document.querySelectorAll(sel.selector).length > 0])
 *      );
 *    }
 *
 * 2. Variable validation system:
 *    function validateVariables() {
 *      const required = ['product', 'configurator', 'addToCartButton'];
 *      return required.every(varName => window[varName] !== null);
 *    }
 *
 * 3. Performance monitoring:
 *    const variablePerformance = {
 *      queryCount: 0,
 *      totalTime: 0,
 *      logQuery: (selector, time) => { ... }
 *    };
 *
 * 4. Variable change detection:
 *    const variableObserver = new MutationObserver(mutations => {
 *      // Re-evaluate variables when DOM changes
 *    });
 *
 * 5. Conditional loading:
 *    const lazyVariables = {
 *      get expensiveVariable() {
 *        return this._cached || (this._cached = computeExpensive());
 *      }
 *    };
 */

// ====================== DEBUG COMPLETE ======================

// Simple debug summary
// if (DEBUG_MODE) {
//   console.log("🔧 Configurator variables loaded");
// }

/***/ }),

/***/ "./assets/scss/configurator.scss":
/*!***************************************!*\
  !*** ./assets/scss/configurator.scss ***!
  \***************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
// extracted by mini-css-extract-plugin


/***/ }),

/***/ "./node_modules/core-js/internals/a-callable.js":
/*!******************************************************!*\
  !*** ./node_modules/core-js/internals/a-callable.js ***!
  \******************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var isCallable = __webpack_require__(/*! ../internals/is-callable */ "./node_modules/core-js/internals/is-callable.js");
var tryToString = __webpack_require__(/*! ../internals/try-to-string */ "./node_modules/core-js/internals/try-to-string.js");

var $TypeError = TypeError;

// `Assert: IsCallable(argument) is true`
module.exports = function (argument) {
  if (isCallable(argument)) return argument;
  throw new $TypeError(tryToString(argument) + ' is not a function');
};


/***/ }),

/***/ "./node_modules/core-js/internals/a-constructor.js":
/*!*********************************************************!*\
  !*** ./node_modules/core-js/internals/a-constructor.js ***!
  \*********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var isConstructor = __webpack_require__(/*! ../internals/is-constructor */ "./node_modules/core-js/internals/is-constructor.js");
var tryToString = __webpack_require__(/*! ../internals/try-to-string */ "./node_modules/core-js/internals/try-to-string.js");

var $TypeError = TypeError;

// `Assert: IsConstructor(argument) is true`
module.exports = function (argument) {
  if (isConstructor(argument)) return argument;
  throw new $TypeError(tryToString(argument) + ' is not a constructor');
};


/***/ }),

/***/ "./node_modules/core-js/internals/a-possible-prototype.js":
/*!****************************************************************!*\
  !*** ./node_modules/core-js/internals/a-possible-prototype.js ***!
  \****************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var isPossiblePrototype = __webpack_require__(/*! ../internals/is-possible-prototype */ "./node_modules/core-js/internals/is-possible-prototype.js");

var $String = String;
var $TypeError = TypeError;

module.exports = function (argument) {
  if (isPossiblePrototype(argument)) return argument;
  throw new $TypeError("Can't set " + $String(argument) + ' as a prototype');
};


/***/ }),

/***/ "./node_modules/core-js/internals/add-to-unscopables.js":
/*!**************************************************************!*\
  !*** ./node_modules/core-js/internals/add-to-unscopables.js ***!
  \**************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var wellKnownSymbol = __webpack_require__(/*! ../internals/well-known-symbol */ "./node_modules/core-js/internals/well-known-symbol.js");
var create = __webpack_require__(/*! ../internals/object-create */ "./node_modules/core-js/internals/object-create.js");
var defineProperty = (__webpack_require__(/*! ../internals/object-define-property */ "./node_modules/core-js/internals/object-define-property.js").f);

var UNSCOPABLES = wellKnownSymbol('unscopables');
var ArrayPrototype = Array.prototype;

// Array.prototype[@@unscopables]
// https://tc39.es/ecma262/#sec-array.prototype-@@unscopables
if (ArrayPrototype[UNSCOPABLES] === undefined) {
  defineProperty(ArrayPrototype, UNSCOPABLES, {
    configurable: true,
    value: create(null)
  });
}

// add a key to Array.prototype[@@unscopables]
module.exports = function (key) {
  ArrayPrototype[UNSCOPABLES][key] = true;
};


/***/ }),

/***/ "./node_modules/core-js/internals/advance-string-index.js":
/*!****************************************************************!*\
  !*** ./node_modules/core-js/internals/advance-string-index.js ***!
  \****************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var charAt = (__webpack_require__(/*! ../internals/string-multibyte */ "./node_modules/core-js/internals/string-multibyte.js").charAt);

// `AdvanceStringIndex` abstract operation
// https://tc39.es/ecma262/#sec-advancestringindex
module.exports = function (S, index, unicode) {
  return index + (unicode ? charAt(S, index).length : 1);
};


/***/ }),

/***/ "./node_modules/core-js/internals/an-instance.js":
/*!*******************************************************!*\
  !*** ./node_modules/core-js/internals/an-instance.js ***!
  \*******************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var isPrototypeOf = __webpack_require__(/*! ../internals/object-is-prototype-of */ "./node_modules/core-js/internals/object-is-prototype-of.js");

var $TypeError = TypeError;

module.exports = function (it, Prototype) {
  if (isPrototypeOf(Prototype, it)) return it;
  throw new $TypeError('Incorrect invocation');
};


/***/ }),

/***/ "./node_modules/core-js/internals/an-object.js":
/*!*****************************************************!*\
  !*** ./node_modules/core-js/internals/an-object.js ***!
  \*****************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var isObject = __webpack_require__(/*! ../internals/is-object */ "./node_modules/core-js/internals/is-object.js");

var $String = String;
var $TypeError = TypeError;

// `Assert: Type(argument) is Object`
module.exports = function (argument) {
  if (isObject(argument)) return argument;
  throw new $TypeError($String(argument) + ' is not an object');
};


/***/ }),

/***/ "./node_modules/core-js/internals/array-buffer-non-extensible.js":
/*!***********************************************************************!*\
  !*** ./node_modules/core-js/internals/array-buffer-non-extensible.js ***!
  \***********************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


// FF26- bug: ArrayBuffers are non-extensible, but Object.isExtensible does not report it
var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");

module.exports = fails(function () {
  if (typeof ArrayBuffer == 'function') {
    var buffer = new ArrayBuffer(8);
    // eslint-disable-next-line es/no-object-isextensible, es/no-object-defineproperty -- safe
    if (Object.isExtensible(buffer)) Object.defineProperty(buffer, 'a', { value: 8 });
  }
});


/***/ }),

/***/ "./node_modules/core-js/internals/array-for-each.js":
/*!**********************************************************!*\
  !*** ./node_modules/core-js/internals/array-for-each.js ***!
  \**********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var $forEach = (__webpack_require__(/*! ../internals/array-iteration */ "./node_modules/core-js/internals/array-iteration.js").forEach);
var arrayMethodIsStrict = __webpack_require__(/*! ../internals/array-method-is-strict */ "./node_modules/core-js/internals/array-method-is-strict.js");

var STRICT_METHOD = arrayMethodIsStrict('forEach');

// `Array.prototype.forEach` method implementation
// https://tc39.es/ecma262/#sec-array.prototype.foreach
module.exports = !STRICT_METHOD ? function forEach(callbackfn /* , thisArg */) {
  return $forEach(this, callbackfn, arguments.length > 1 ? arguments[1] : undefined);
// eslint-disable-next-line es/no-array-prototype-foreach -- safe
} : [].forEach;


/***/ }),

/***/ "./node_modules/core-js/internals/array-from.js":
/*!******************************************************!*\
  !*** ./node_modules/core-js/internals/array-from.js ***!
  \******************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var bind = __webpack_require__(/*! ../internals/function-bind-context */ "./node_modules/core-js/internals/function-bind-context.js");
var call = __webpack_require__(/*! ../internals/function-call */ "./node_modules/core-js/internals/function-call.js");
var toObject = __webpack_require__(/*! ../internals/to-object */ "./node_modules/core-js/internals/to-object.js");
var callWithSafeIterationClosing = __webpack_require__(/*! ../internals/call-with-safe-iteration-closing */ "./node_modules/core-js/internals/call-with-safe-iteration-closing.js");
var isArrayIteratorMethod = __webpack_require__(/*! ../internals/is-array-iterator-method */ "./node_modules/core-js/internals/is-array-iterator-method.js");
var isConstructor = __webpack_require__(/*! ../internals/is-constructor */ "./node_modules/core-js/internals/is-constructor.js");
var lengthOfArrayLike = __webpack_require__(/*! ../internals/length-of-array-like */ "./node_modules/core-js/internals/length-of-array-like.js");
var createProperty = __webpack_require__(/*! ../internals/create-property */ "./node_modules/core-js/internals/create-property.js");
var getIterator = __webpack_require__(/*! ../internals/get-iterator */ "./node_modules/core-js/internals/get-iterator.js");
var getIteratorMethod = __webpack_require__(/*! ../internals/get-iterator-method */ "./node_modules/core-js/internals/get-iterator-method.js");

var $Array = Array;

// `Array.from` method implementation
// https://tc39.es/ecma262/#sec-array.from
module.exports = function from(arrayLike /* , mapfn = undefined, thisArg = undefined */) {
  var O = toObject(arrayLike);
  var IS_CONSTRUCTOR = isConstructor(this);
  var argumentsLength = arguments.length;
  var mapfn = argumentsLength > 1 ? arguments[1] : undefined;
  var mapping = mapfn !== undefined;
  if (mapping) mapfn = bind(mapfn, argumentsLength > 2 ? arguments[2] : undefined);
  var iteratorMethod = getIteratorMethod(O);
  var index = 0;
  var length, result, step, iterator, next, value;
  // if the target is not iterable or it's an array with the default iterator - use a simple case
  if (iteratorMethod && !(this === $Array && isArrayIteratorMethod(iteratorMethod))) {
    result = IS_CONSTRUCTOR ? new this() : [];
    iterator = getIterator(O, iteratorMethod);
    next = iterator.next;
    for (;!(step = call(next, iterator)).done; index++) {
      value = mapping ? callWithSafeIterationClosing(iterator, mapfn, [step.value, index], true) : step.value;
      createProperty(result, index, value);
    }
  } else {
    length = lengthOfArrayLike(O);
    result = IS_CONSTRUCTOR ? new this(length) : $Array(length);
    for (;length > index; index++) {
      value = mapping ? mapfn(O[index], index) : O[index];
      createProperty(result, index, value);
    }
  }
  result.length = index;
  return result;
};


/***/ }),

/***/ "./node_modules/core-js/internals/array-includes.js":
/*!**********************************************************!*\
  !*** ./node_modules/core-js/internals/array-includes.js ***!
  \**********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var toIndexedObject = __webpack_require__(/*! ../internals/to-indexed-object */ "./node_modules/core-js/internals/to-indexed-object.js");
var toAbsoluteIndex = __webpack_require__(/*! ../internals/to-absolute-index */ "./node_modules/core-js/internals/to-absolute-index.js");
var lengthOfArrayLike = __webpack_require__(/*! ../internals/length-of-array-like */ "./node_modules/core-js/internals/length-of-array-like.js");

// `Array.prototype.{ indexOf, includes }` methods implementation
var createMethod = function (IS_INCLUDES) {
  return function ($this, el, fromIndex) {
    var O = toIndexedObject($this);
    var length = lengthOfArrayLike(O);
    if (length === 0) return !IS_INCLUDES && -1;
    var index = toAbsoluteIndex(fromIndex, length);
    var value;
    // Array#includes uses SameValueZero equality algorithm
    // eslint-disable-next-line no-self-compare -- NaN check
    if (IS_INCLUDES && el !== el) while (length > index) {
      value = O[index++];
      // eslint-disable-next-line no-self-compare -- NaN check
      if (value !== value) return true;
    // Array#indexOf ignores holes, Array#includes - not
    } else for (;length > index; index++) {
      if ((IS_INCLUDES || index in O) && O[index] === el) return IS_INCLUDES || index || 0;
    } return !IS_INCLUDES && -1;
  };
};

module.exports = {
  // `Array.prototype.includes` method
  // https://tc39.es/ecma262/#sec-array.prototype.includes
  includes: createMethod(true),
  // `Array.prototype.indexOf` method
  // https://tc39.es/ecma262/#sec-array.prototype.indexof
  indexOf: createMethod(false)
};


/***/ }),

/***/ "./node_modules/core-js/internals/array-iteration.js":
/*!***********************************************************!*\
  !*** ./node_modules/core-js/internals/array-iteration.js ***!
  \***********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var bind = __webpack_require__(/*! ../internals/function-bind-context */ "./node_modules/core-js/internals/function-bind-context.js");
var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this */ "./node_modules/core-js/internals/function-uncurry-this.js");
var IndexedObject = __webpack_require__(/*! ../internals/indexed-object */ "./node_modules/core-js/internals/indexed-object.js");
var toObject = __webpack_require__(/*! ../internals/to-object */ "./node_modules/core-js/internals/to-object.js");
var lengthOfArrayLike = __webpack_require__(/*! ../internals/length-of-array-like */ "./node_modules/core-js/internals/length-of-array-like.js");
var arraySpeciesCreate = __webpack_require__(/*! ../internals/array-species-create */ "./node_modules/core-js/internals/array-species-create.js");

var push = uncurryThis([].push);

// `Array.prototype.{ forEach, map, filter, some, every, find, findIndex, filterReject }` methods implementation
var createMethod = function (TYPE) {
  var IS_MAP = TYPE === 1;
  var IS_FILTER = TYPE === 2;
  var IS_SOME = TYPE === 3;
  var IS_EVERY = TYPE === 4;
  var IS_FIND_INDEX = TYPE === 6;
  var IS_FILTER_REJECT = TYPE === 7;
  var NO_HOLES = TYPE === 5 || IS_FIND_INDEX;
  return function ($this, callbackfn, that, specificCreate) {
    var O = toObject($this);
    var self = IndexedObject(O);
    var length = lengthOfArrayLike(self);
    var boundFunction = bind(callbackfn, that);
    var index = 0;
    var create = specificCreate || arraySpeciesCreate;
    var target = IS_MAP ? create($this, length) : IS_FILTER || IS_FILTER_REJECT ? create($this, 0) : undefined;
    var value, result;
    for (;length > index; index++) if (NO_HOLES || index in self) {
      value = self[index];
      result = boundFunction(value, index, O);
      if (TYPE) {
        if (IS_MAP) target[index] = result; // map
        else if (result) switch (TYPE) {
          case 3: return true;              // some
          case 5: return value;             // find
          case 6: return index;             // findIndex
          case 2: push(target, value);      // filter
        } else switch (TYPE) {
          case 4: return false;             // every
          case 7: push(target, value);      // filterReject
        }
      }
    }
    return IS_FIND_INDEX ? -1 : IS_SOME || IS_EVERY ? IS_EVERY : target;
  };
};

module.exports = {
  // `Array.prototype.forEach` method
  // https://tc39.es/ecma262/#sec-array.prototype.foreach
  forEach: createMethod(0),
  // `Array.prototype.map` method
  // https://tc39.es/ecma262/#sec-array.prototype.map
  map: createMethod(1),
  // `Array.prototype.filter` method
  // https://tc39.es/ecma262/#sec-array.prototype.filter
  filter: createMethod(2),
  // `Array.prototype.some` method
  // https://tc39.es/ecma262/#sec-array.prototype.some
  some: createMethod(3),
  // `Array.prototype.every` method
  // https://tc39.es/ecma262/#sec-array.prototype.every
  every: createMethod(4),
  // `Array.prototype.find` method
  // https://tc39.es/ecma262/#sec-array.prototype.find
  find: createMethod(5),
  // `Array.prototype.findIndex` method
  // https://tc39.es/ecma262/#sec-array.prototype.findIndex
  findIndex: createMethod(6),
  // `Array.prototype.filterReject` method
  // https://github.com/tc39/proposal-array-filtering
  filterReject: createMethod(7)
};


/***/ }),

/***/ "./node_modules/core-js/internals/array-method-has-species-support.js":
/*!****************************************************************************!*\
  !*** ./node_modules/core-js/internals/array-method-has-species-support.js ***!
  \****************************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");
var wellKnownSymbol = __webpack_require__(/*! ../internals/well-known-symbol */ "./node_modules/core-js/internals/well-known-symbol.js");
var V8_VERSION = __webpack_require__(/*! ../internals/environment-v8-version */ "./node_modules/core-js/internals/environment-v8-version.js");

var SPECIES = wellKnownSymbol('species');

module.exports = function (METHOD_NAME) {
  // We can't use this feature detection in V8 since it causes
  // deoptimization and serious performance degradation
  // https://github.com/zloirock/core-js/issues/677
  return V8_VERSION >= 51 || !fails(function () {
    var array = [];
    var constructor = array.constructor = {};
    constructor[SPECIES] = function () {
      return { foo: 1 };
    };
    return array[METHOD_NAME](Boolean).foo !== 1;
  });
};


/***/ }),

/***/ "./node_modules/core-js/internals/array-method-is-strict.js":
/*!******************************************************************!*\
  !*** ./node_modules/core-js/internals/array-method-is-strict.js ***!
  \******************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");

module.exports = function (METHOD_NAME, argument) {
  var method = [][METHOD_NAME];
  return !!method && fails(function () {
    // eslint-disable-next-line no-useless-call -- required for testing
    method.call(null, argument || function () { return 1; }, 1);
  });
};


/***/ }),

/***/ "./node_modules/core-js/internals/array-slice.js":
/*!*******************************************************!*\
  !*** ./node_modules/core-js/internals/array-slice.js ***!
  \*******************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this */ "./node_modules/core-js/internals/function-uncurry-this.js");

module.exports = uncurryThis([].slice);


/***/ }),

/***/ "./node_modules/core-js/internals/array-sort.js":
/*!******************************************************!*\
  !*** ./node_modules/core-js/internals/array-sort.js ***!
  \******************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var arraySlice = __webpack_require__(/*! ../internals/array-slice */ "./node_modules/core-js/internals/array-slice.js");

var floor = Math.floor;

var sort = function (array, comparefn) {
  var length = array.length;

  if (length < 8) {
    // insertion sort
    var i = 1;
    var element, j;

    while (i < length) {
      j = i;
      element = array[i];
      while (j && comparefn(array[j - 1], element) > 0) {
        array[j] = array[--j];
      }
      if (j !== i++) array[j] = element;
    }
  } else {
    // merge sort
    var middle = floor(length / 2);
    var left = sort(arraySlice(array, 0, middle), comparefn);
    var right = sort(arraySlice(array, middle), comparefn);
    var llength = left.length;
    var rlength = right.length;
    var lindex = 0;
    var rindex = 0;

    while (lindex < llength || rindex < rlength) {
      array[lindex + rindex] = (lindex < llength && rindex < rlength)
        ? comparefn(left[lindex], right[rindex]) <= 0 ? left[lindex++] : right[rindex++]
        : lindex < llength ? left[lindex++] : right[rindex++];
    }
  }

  return array;
};

module.exports = sort;


/***/ }),

/***/ "./node_modules/core-js/internals/array-species-constructor.js":
/*!*********************************************************************!*\
  !*** ./node_modules/core-js/internals/array-species-constructor.js ***!
  \*********************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var isArray = __webpack_require__(/*! ../internals/is-array */ "./node_modules/core-js/internals/is-array.js");
var isConstructor = __webpack_require__(/*! ../internals/is-constructor */ "./node_modules/core-js/internals/is-constructor.js");
var isObject = __webpack_require__(/*! ../internals/is-object */ "./node_modules/core-js/internals/is-object.js");
var wellKnownSymbol = __webpack_require__(/*! ../internals/well-known-symbol */ "./node_modules/core-js/internals/well-known-symbol.js");

var SPECIES = wellKnownSymbol('species');
var $Array = Array;

// a part of `ArraySpeciesCreate` abstract operation
// https://tc39.es/ecma262/#sec-arrayspeciescreate
module.exports = function (originalArray) {
  var C;
  if (isArray(originalArray)) {
    C = originalArray.constructor;
    // cross-realm fallback
    if (isConstructor(C) && (C === $Array || isArray(C.prototype))) C = undefined;
    else if (isObject(C)) {
      C = C[SPECIES];
      if (C === null) C = undefined;
    }
  } return C === undefined ? $Array : C;
};


/***/ }),

/***/ "./node_modules/core-js/internals/array-species-create.js":
/*!****************************************************************!*\
  !*** ./node_modules/core-js/internals/array-species-create.js ***!
  \****************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var arraySpeciesConstructor = __webpack_require__(/*! ../internals/array-species-constructor */ "./node_modules/core-js/internals/array-species-constructor.js");

// `ArraySpeciesCreate` abstract operation
// https://tc39.es/ecma262/#sec-arrayspeciescreate
module.exports = function (originalArray, length) {
  return new (arraySpeciesConstructor(originalArray))(length === 0 ? 0 : length);
};


/***/ }),

/***/ "./node_modules/core-js/internals/call-with-safe-iteration-closing.js":
/*!****************************************************************************!*\
  !*** ./node_modules/core-js/internals/call-with-safe-iteration-closing.js ***!
  \****************************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var anObject = __webpack_require__(/*! ../internals/an-object */ "./node_modules/core-js/internals/an-object.js");
var iteratorClose = __webpack_require__(/*! ../internals/iterator-close */ "./node_modules/core-js/internals/iterator-close.js");

// call something on iterator step with safe closing on error
module.exports = function (iterator, fn, value, ENTRIES) {
  try {
    return ENTRIES ? fn(anObject(value)[0], value[1]) : fn(value);
  } catch (error) {
    iteratorClose(iterator, 'throw', error);
  }
};


/***/ }),

/***/ "./node_modules/core-js/internals/check-correctness-of-iteration.js":
/*!**************************************************************************!*\
  !*** ./node_modules/core-js/internals/check-correctness-of-iteration.js ***!
  \**************************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var wellKnownSymbol = __webpack_require__(/*! ../internals/well-known-symbol */ "./node_modules/core-js/internals/well-known-symbol.js");

var ITERATOR = wellKnownSymbol('iterator');
var SAFE_CLOSING = false;

try {
  var called = 0;
  var iteratorWithReturn = {
    next: function () {
      return { done: !!called++ };
    },
    'return': function () {
      SAFE_CLOSING = true;
    }
  };
  iteratorWithReturn[ITERATOR] = function () {
    return this;
  };
  // eslint-disable-next-line es/no-array-from, no-throw-literal -- required for testing
  Array.from(iteratorWithReturn, function () { throw 2; });
} catch (error) { /* empty */ }

module.exports = function (exec, SKIP_CLOSING) {
  try {
    if (!SKIP_CLOSING && !SAFE_CLOSING) return false;
  } catch (error) { return false; } // workaround of old WebKit + `eval` bug
  var ITERATION_SUPPORT = false;
  try {
    var object = {};
    object[ITERATOR] = function () {
      return {
        next: function () {
          return { done: ITERATION_SUPPORT = true };
        }
      };
    };
    exec(object);
  } catch (error) { /* empty */ }
  return ITERATION_SUPPORT;
};


/***/ }),

/***/ "./node_modules/core-js/internals/classof-raw.js":
/*!*******************************************************!*\
  !*** ./node_modules/core-js/internals/classof-raw.js ***!
  \*******************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this */ "./node_modules/core-js/internals/function-uncurry-this.js");

var toString = uncurryThis({}.toString);
var stringSlice = uncurryThis(''.slice);

module.exports = function (it) {
  return stringSlice(toString(it), 8, -1);
};


/***/ }),

/***/ "./node_modules/core-js/internals/classof.js":
/*!***************************************************!*\
  !*** ./node_modules/core-js/internals/classof.js ***!
  \***************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var TO_STRING_TAG_SUPPORT = __webpack_require__(/*! ../internals/to-string-tag-support */ "./node_modules/core-js/internals/to-string-tag-support.js");
var isCallable = __webpack_require__(/*! ../internals/is-callable */ "./node_modules/core-js/internals/is-callable.js");
var classofRaw = __webpack_require__(/*! ../internals/classof-raw */ "./node_modules/core-js/internals/classof-raw.js");
var wellKnownSymbol = __webpack_require__(/*! ../internals/well-known-symbol */ "./node_modules/core-js/internals/well-known-symbol.js");

var TO_STRING_TAG = wellKnownSymbol('toStringTag');
var $Object = Object;

// ES3 wrong here
var CORRECT_ARGUMENTS = classofRaw(function () { return arguments; }()) === 'Arguments';

// fallback for IE11 Script Access Denied error
var tryGet = function (it, key) {
  try {
    return it[key];
  } catch (error) { /* empty */ }
};

// getting tag from ES6+ `Object.prototype.toString`
module.exports = TO_STRING_TAG_SUPPORT ? classofRaw : function (it) {
  var O, tag, result;
  return it === undefined ? 'Undefined' : it === null ? 'Null'
    // @@toStringTag case
    : typeof (tag = tryGet(O = $Object(it), TO_STRING_TAG)) == 'string' ? tag
    // builtinTag case
    : CORRECT_ARGUMENTS ? classofRaw(O)
    // ES3 arguments fallback
    : (result = classofRaw(O)) === 'Object' && isCallable(O.callee) ? 'Arguments' : result;
};


/***/ }),

/***/ "./node_modules/core-js/internals/collection-weak.js":
/*!***********************************************************!*\
  !*** ./node_modules/core-js/internals/collection-weak.js ***!
  \***********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this */ "./node_modules/core-js/internals/function-uncurry-this.js");
var defineBuiltIns = __webpack_require__(/*! ../internals/define-built-ins */ "./node_modules/core-js/internals/define-built-ins.js");
var getWeakData = (__webpack_require__(/*! ../internals/internal-metadata */ "./node_modules/core-js/internals/internal-metadata.js").getWeakData);
var anInstance = __webpack_require__(/*! ../internals/an-instance */ "./node_modules/core-js/internals/an-instance.js");
var anObject = __webpack_require__(/*! ../internals/an-object */ "./node_modules/core-js/internals/an-object.js");
var isNullOrUndefined = __webpack_require__(/*! ../internals/is-null-or-undefined */ "./node_modules/core-js/internals/is-null-or-undefined.js");
var isObject = __webpack_require__(/*! ../internals/is-object */ "./node_modules/core-js/internals/is-object.js");
var iterate = __webpack_require__(/*! ../internals/iterate */ "./node_modules/core-js/internals/iterate.js");
var ArrayIterationModule = __webpack_require__(/*! ../internals/array-iteration */ "./node_modules/core-js/internals/array-iteration.js");
var hasOwn = __webpack_require__(/*! ../internals/has-own-property */ "./node_modules/core-js/internals/has-own-property.js");
var InternalStateModule = __webpack_require__(/*! ../internals/internal-state */ "./node_modules/core-js/internals/internal-state.js");

var setInternalState = InternalStateModule.set;
var internalStateGetterFor = InternalStateModule.getterFor;
var find = ArrayIterationModule.find;
var findIndex = ArrayIterationModule.findIndex;
var splice = uncurryThis([].splice);
var id = 0;

// fallback for uncaught frozen keys
var uncaughtFrozenStore = function (state) {
  return state.frozen || (state.frozen = new UncaughtFrozenStore());
};

var UncaughtFrozenStore = function () {
  this.entries = [];
};

var findUncaughtFrozen = function (store, key) {
  return find(store.entries, function (it) {
    return it[0] === key;
  });
};

UncaughtFrozenStore.prototype = {
  get: function (key) {
    var entry = findUncaughtFrozen(this, key);
    if (entry) return entry[1];
  },
  has: function (key) {
    return !!findUncaughtFrozen(this, key);
  },
  set: function (key, value) {
    var entry = findUncaughtFrozen(this, key);
    if (entry) entry[1] = value;
    else this.entries.push([key, value]);
  },
  'delete': function (key) {
    var index = findIndex(this.entries, function (it) {
      return it[0] === key;
    });
    if (~index) splice(this.entries, index, 1);
    return !!~index;
  }
};

module.exports = {
  getConstructor: function (wrapper, CONSTRUCTOR_NAME, IS_MAP, ADDER) {
    var Constructor = wrapper(function (that, iterable) {
      anInstance(that, Prototype);
      setInternalState(that, {
        type: CONSTRUCTOR_NAME,
        id: id++,
        frozen: null
      });
      if (!isNullOrUndefined(iterable)) iterate(iterable, that[ADDER], { that: that, AS_ENTRIES: IS_MAP });
    });

    var Prototype = Constructor.prototype;

    var getInternalState = internalStateGetterFor(CONSTRUCTOR_NAME);

    var define = function (that, key, value) {
      var state = getInternalState(that);
      var data = getWeakData(anObject(key), true);
      if (data === true) uncaughtFrozenStore(state).set(key, value);
      else data[state.id] = value;
      return that;
    };

    defineBuiltIns(Prototype, {
      // `{ WeakMap, WeakSet }.prototype.delete(key)` methods
      // https://tc39.es/ecma262/#sec-weakmap.prototype.delete
      // https://tc39.es/ecma262/#sec-weakset.prototype.delete
      'delete': function (key) {
        var state = getInternalState(this);
        if (!isObject(key)) return false;
        var data = getWeakData(key);
        if (data === true) return uncaughtFrozenStore(state)['delete'](key);
        return data && hasOwn(data, state.id) && delete data[state.id];
      },
      // `{ WeakMap, WeakSet }.prototype.has(key)` methods
      // https://tc39.es/ecma262/#sec-weakmap.prototype.has
      // https://tc39.es/ecma262/#sec-weakset.prototype.has
      has: function has(key) {
        var state = getInternalState(this);
        if (!isObject(key)) return false;
        var data = getWeakData(key);
        if (data === true) return uncaughtFrozenStore(state).has(key);
        return data && hasOwn(data, state.id);
      }
    });

    defineBuiltIns(Prototype, IS_MAP ? {
      // `WeakMap.prototype.get(key)` method
      // https://tc39.es/ecma262/#sec-weakmap.prototype.get
      get: function get(key) {
        var state = getInternalState(this);
        if (isObject(key)) {
          var data = getWeakData(key);
          if (data === true) return uncaughtFrozenStore(state).get(key);
          if (data) return data[state.id];
        }
      },
      // `WeakMap.prototype.set(key, value)` method
      // https://tc39.es/ecma262/#sec-weakmap.prototype.set
      set: function set(key, value) {
        return define(this, key, value);
      }
    } : {
      // `WeakSet.prototype.add(value)` method
      // https://tc39.es/ecma262/#sec-weakset.prototype.add
      add: function add(value) {
        return define(this, value, true);
      }
    });

    return Constructor;
  }
};


/***/ }),

/***/ "./node_modules/core-js/internals/collection.js":
/*!******************************************************!*\
  !*** ./node_modules/core-js/internals/collection.js ***!
  \******************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");
var globalThis = __webpack_require__(/*! ../internals/global-this */ "./node_modules/core-js/internals/global-this.js");
var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this */ "./node_modules/core-js/internals/function-uncurry-this.js");
var isForced = __webpack_require__(/*! ../internals/is-forced */ "./node_modules/core-js/internals/is-forced.js");
var defineBuiltIn = __webpack_require__(/*! ../internals/define-built-in */ "./node_modules/core-js/internals/define-built-in.js");
var InternalMetadataModule = __webpack_require__(/*! ../internals/internal-metadata */ "./node_modules/core-js/internals/internal-metadata.js");
var iterate = __webpack_require__(/*! ../internals/iterate */ "./node_modules/core-js/internals/iterate.js");
var anInstance = __webpack_require__(/*! ../internals/an-instance */ "./node_modules/core-js/internals/an-instance.js");
var isCallable = __webpack_require__(/*! ../internals/is-callable */ "./node_modules/core-js/internals/is-callable.js");
var isNullOrUndefined = __webpack_require__(/*! ../internals/is-null-or-undefined */ "./node_modules/core-js/internals/is-null-or-undefined.js");
var isObject = __webpack_require__(/*! ../internals/is-object */ "./node_modules/core-js/internals/is-object.js");
var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");
var checkCorrectnessOfIteration = __webpack_require__(/*! ../internals/check-correctness-of-iteration */ "./node_modules/core-js/internals/check-correctness-of-iteration.js");
var setToStringTag = __webpack_require__(/*! ../internals/set-to-string-tag */ "./node_modules/core-js/internals/set-to-string-tag.js");
var inheritIfRequired = __webpack_require__(/*! ../internals/inherit-if-required */ "./node_modules/core-js/internals/inherit-if-required.js");

module.exports = function (CONSTRUCTOR_NAME, wrapper, common) {
  var IS_MAP = CONSTRUCTOR_NAME.indexOf('Map') !== -1;
  var IS_WEAK = CONSTRUCTOR_NAME.indexOf('Weak') !== -1;
  var ADDER = IS_MAP ? 'set' : 'add';
  var NativeConstructor = globalThis[CONSTRUCTOR_NAME];
  var NativePrototype = NativeConstructor && NativeConstructor.prototype;
  var Constructor = NativeConstructor;
  var exported = {};

  var fixMethod = function (KEY) {
    var uncurriedNativeMethod = uncurryThis(NativePrototype[KEY]);
    defineBuiltIn(NativePrototype, KEY,
      KEY === 'add' ? function add(value) {
        uncurriedNativeMethod(this, value === 0 ? 0 : value);
        return this;
      } : KEY === 'delete' ? function (key) {
        return IS_WEAK && !isObject(key) ? false : uncurriedNativeMethod(this, key === 0 ? 0 : key);
      } : KEY === 'get' ? function get(key) {
        return IS_WEAK && !isObject(key) ? undefined : uncurriedNativeMethod(this, key === 0 ? 0 : key);
      } : KEY === 'has' ? function has(key) {
        return IS_WEAK && !isObject(key) ? false : uncurriedNativeMethod(this, key === 0 ? 0 : key);
      } : function set(key, value) {
        uncurriedNativeMethod(this, key === 0 ? 0 : key, value);
        return this;
      }
    );
  };

  var REPLACE = isForced(
    CONSTRUCTOR_NAME,
    !isCallable(NativeConstructor) || !(IS_WEAK || NativePrototype.forEach && !fails(function () {
      new NativeConstructor().entries().next();
    }))
  );

  if (REPLACE) {
    // create collection constructor
    Constructor = common.getConstructor(wrapper, CONSTRUCTOR_NAME, IS_MAP, ADDER);
    InternalMetadataModule.enable();
  } else if (isForced(CONSTRUCTOR_NAME, true)) {
    var instance = new Constructor();
    // early implementations not supports chaining
    var HASNT_CHAINING = instance[ADDER](IS_WEAK ? {} : -0, 1) !== instance;
    // V8 ~ Chromium 40- weak-collections throws on primitives, but should return false
    var THROWS_ON_PRIMITIVES = fails(function () { instance.has(1); });
    // most early implementations doesn't supports iterables, most modern - not close it correctly
    // eslint-disable-next-line no-new -- required for testing
    var ACCEPT_ITERABLES = checkCorrectnessOfIteration(function (iterable) { new NativeConstructor(iterable); });
    // for early implementations -0 and +0 not the same
    var BUGGY_ZERO = !IS_WEAK && fails(function () {
      // V8 ~ Chromium 42- fails only with 5+ elements
      var $instance = new NativeConstructor();
      var index = 5;
      while (index--) $instance[ADDER](index, index);
      return !$instance.has(-0);
    });

    if (!ACCEPT_ITERABLES) {
      Constructor = wrapper(function (dummy, iterable) {
        anInstance(dummy, NativePrototype);
        var that = inheritIfRequired(new NativeConstructor(), dummy, Constructor);
        if (!isNullOrUndefined(iterable)) iterate(iterable, that[ADDER], { that: that, AS_ENTRIES: IS_MAP });
        return that;
      });
      Constructor.prototype = NativePrototype;
      NativePrototype.constructor = Constructor;
    }

    if (THROWS_ON_PRIMITIVES || BUGGY_ZERO) {
      fixMethod('delete');
      fixMethod('has');
      IS_MAP && fixMethod('get');
    }

    if (BUGGY_ZERO || HASNT_CHAINING) fixMethod(ADDER);

    // weak collections should not contains .clear method
    if (IS_WEAK && NativePrototype.clear) delete NativePrototype.clear;
  }

  exported[CONSTRUCTOR_NAME] = Constructor;
  $({ global: true, constructor: true, forced: Constructor !== NativeConstructor }, exported);

  setToStringTag(Constructor, CONSTRUCTOR_NAME);

  if (!IS_WEAK) common.setStrong(Constructor, CONSTRUCTOR_NAME, IS_MAP);

  return Constructor;
};


/***/ }),

/***/ "./node_modules/core-js/internals/copy-constructor-properties.js":
/*!***********************************************************************!*\
  !*** ./node_modules/core-js/internals/copy-constructor-properties.js ***!
  \***********************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var hasOwn = __webpack_require__(/*! ../internals/has-own-property */ "./node_modules/core-js/internals/has-own-property.js");
var ownKeys = __webpack_require__(/*! ../internals/own-keys */ "./node_modules/core-js/internals/own-keys.js");
var getOwnPropertyDescriptorModule = __webpack_require__(/*! ../internals/object-get-own-property-descriptor */ "./node_modules/core-js/internals/object-get-own-property-descriptor.js");
var definePropertyModule = __webpack_require__(/*! ../internals/object-define-property */ "./node_modules/core-js/internals/object-define-property.js");

module.exports = function (target, source, exceptions) {
  var keys = ownKeys(source);
  var defineProperty = definePropertyModule.f;
  var getOwnPropertyDescriptor = getOwnPropertyDescriptorModule.f;
  for (var i = 0; i < keys.length; i++) {
    var key = keys[i];
    if (!hasOwn(target, key) && !(exceptions && hasOwn(exceptions, key))) {
      defineProperty(target, key, getOwnPropertyDescriptor(source, key));
    }
  }
};


/***/ }),

/***/ "./node_modules/core-js/internals/correct-is-regexp-logic.js":
/*!*******************************************************************!*\
  !*** ./node_modules/core-js/internals/correct-is-regexp-logic.js ***!
  \*******************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var wellKnownSymbol = __webpack_require__(/*! ../internals/well-known-symbol */ "./node_modules/core-js/internals/well-known-symbol.js");

var MATCH = wellKnownSymbol('match');

module.exports = function (METHOD_NAME) {
  var regexp = /./;
  try {
    '/./'[METHOD_NAME](regexp);
  } catch (error1) {
    try {
      regexp[MATCH] = false;
      return '/./'[METHOD_NAME](regexp);
    } catch (error2) { /* empty */ }
  } return false;
};


/***/ }),

/***/ "./node_modules/core-js/internals/correct-prototype-getter.js":
/*!********************************************************************!*\
  !*** ./node_modules/core-js/internals/correct-prototype-getter.js ***!
  \********************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");

module.exports = !fails(function () {
  function F() { /* empty */ }
  F.prototype.constructor = null;
  // eslint-disable-next-line es/no-object-getprototypeof -- required for testing
  return Object.getPrototypeOf(new F()) !== F.prototype;
});


/***/ }),

/***/ "./node_modules/core-js/internals/create-iter-result-object.js":
/*!*********************************************************************!*\
  !*** ./node_modules/core-js/internals/create-iter-result-object.js ***!
  \*********************************************************************/
/***/ ((module) => {


// `CreateIterResultObject` abstract operation
// https://tc39.es/ecma262/#sec-createiterresultobject
module.exports = function (value, done) {
  return { value: value, done: done };
};


/***/ }),

/***/ "./node_modules/core-js/internals/create-non-enumerable-property.js":
/*!**************************************************************************!*\
  !*** ./node_modules/core-js/internals/create-non-enumerable-property.js ***!
  \**************************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var DESCRIPTORS = __webpack_require__(/*! ../internals/descriptors */ "./node_modules/core-js/internals/descriptors.js");
var definePropertyModule = __webpack_require__(/*! ../internals/object-define-property */ "./node_modules/core-js/internals/object-define-property.js");
var createPropertyDescriptor = __webpack_require__(/*! ../internals/create-property-descriptor */ "./node_modules/core-js/internals/create-property-descriptor.js");

module.exports = DESCRIPTORS ? function (object, key, value) {
  return definePropertyModule.f(object, key, createPropertyDescriptor(1, value));
} : function (object, key, value) {
  object[key] = value;
  return object;
};


/***/ }),

/***/ "./node_modules/core-js/internals/create-property-descriptor.js":
/*!**********************************************************************!*\
  !*** ./node_modules/core-js/internals/create-property-descriptor.js ***!
  \**********************************************************************/
/***/ ((module) => {


module.exports = function (bitmap, value) {
  return {
    enumerable: !(bitmap & 1),
    configurable: !(bitmap & 2),
    writable: !(bitmap & 4),
    value: value
  };
};


/***/ }),

/***/ "./node_modules/core-js/internals/create-property.js":
/*!***********************************************************!*\
  !*** ./node_modules/core-js/internals/create-property.js ***!
  \***********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var DESCRIPTORS = __webpack_require__(/*! ../internals/descriptors */ "./node_modules/core-js/internals/descriptors.js");
var definePropertyModule = __webpack_require__(/*! ../internals/object-define-property */ "./node_modules/core-js/internals/object-define-property.js");
var createPropertyDescriptor = __webpack_require__(/*! ../internals/create-property-descriptor */ "./node_modules/core-js/internals/create-property-descriptor.js");

module.exports = function (object, key, value) {
  if (DESCRIPTORS) definePropertyModule.f(object, key, createPropertyDescriptor(0, value));
  else object[key] = value;
};


/***/ }),

/***/ "./node_modules/core-js/internals/date-to-primitive.js":
/*!*************************************************************!*\
  !*** ./node_modules/core-js/internals/date-to-primitive.js ***!
  \*************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var anObject = __webpack_require__(/*! ../internals/an-object */ "./node_modules/core-js/internals/an-object.js");
var ordinaryToPrimitive = __webpack_require__(/*! ../internals/ordinary-to-primitive */ "./node_modules/core-js/internals/ordinary-to-primitive.js");

var $TypeError = TypeError;

// `Date.prototype[@@toPrimitive](hint)` method implementation
// https://tc39.es/ecma262/#sec-date.prototype-@@toprimitive
module.exports = function (hint) {
  anObject(this);
  if (hint === 'string' || hint === 'default') hint = 'string';
  else if (hint !== 'number') throw new $TypeError('Incorrect hint');
  return ordinaryToPrimitive(this, hint);
};


/***/ }),

/***/ "./node_modules/core-js/internals/define-built-in-accessor.js":
/*!********************************************************************!*\
  !*** ./node_modules/core-js/internals/define-built-in-accessor.js ***!
  \********************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var makeBuiltIn = __webpack_require__(/*! ../internals/make-built-in */ "./node_modules/core-js/internals/make-built-in.js");
var defineProperty = __webpack_require__(/*! ../internals/object-define-property */ "./node_modules/core-js/internals/object-define-property.js");

module.exports = function (target, name, descriptor) {
  if (descriptor.get) makeBuiltIn(descriptor.get, name, { getter: true });
  if (descriptor.set) makeBuiltIn(descriptor.set, name, { setter: true });
  return defineProperty.f(target, name, descriptor);
};


/***/ }),

/***/ "./node_modules/core-js/internals/define-built-in.js":
/*!***********************************************************!*\
  !*** ./node_modules/core-js/internals/define-built-in.js ***!
  \***********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var isCallable = __webpack_require__(/*! ../internals/is-callable */ "./node_modules/core-js/internals/is-callable.js");
var definePropertyModule = __webpack_require__(/*! ../internals/object-define-property */ "./node_modules/core-js/internals/object-define-property.js");
var makeBuiltIn = __webpack_require__(/*! ../internals/make-built-in */ "./node_modules/core-js/internals/make-built-in.js");
var defineGlobalProperty = __webpack_require__(/*! ../internals/define-global-property */ "./node_modules/core-js/internals/define-global-property.js");

module.exports = function (O, key, value, options) {
  if (!options) options = {};
  var simple = options.enumerable;
  var name = options.name !== undefined ? options.name : key;
  if (isCallable(value)) makeBuiltIn(value, name, options);
  if (options.global) {
    if (simple) O[key] = value;
    else defineGlobalProperty(key, value);
  } else {
    try {
      if (!options.unsafe) delete O[key];
      else if (O[key]) simple = true;
    } catch (error) { /* empty */ }
    if (simple) O[key] = value;
    else definePropertyModule.f(O, key, {
      value: value,
      enumerable: false,
      configurable: !options.nonConfigurable,
      writable: !options.nonWritable
    });
  } return O;
};


/***/ }),

/***/ "./node_modules/core-js/internals/define-built-ins.js":
/*!************************************************************!*\
  !*** ./node_modules/core-js/internals/define-built-ins.js ***!
  \************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var defineBuiltIn = __webpack_require__(/*! ../internals/define-built-in */ "./node_modules/core-js/internals/define-built-in.js");

module.exports = function (target, src, options) {
  for (var key in src) defineBuiltIn(target, key, src[key], options);
  return target;
};


/***/ }),

/***/ "./node_modules/core-js/internals/define-global-property.js":
/*!******************************************************************!*\
  !*** ./node_modules/core-js/internals/define-global-property.js ***!
  \******************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var globalThis = __webpack_require__(/*! ../internals/global-this */ "./node_modules/core-js/internals/global-this.js");

// eslint-disable-next-line es/no-object-defineproperty -- safe
var defineProperty = Object.defineProperty;

module.exports = function (key, value) {
  try {
    defineProperty(globalThis, key, { value: value, configurable: true, writable: true });
  } catch (error) {
    globalThis[key] = value;
  } return value;
};


/***/ }),

/***/ "./node_modules/core-js/internals/delete-property-or-throw.js":
/*!********************************************************************!*\
  !*** ./node_modules/core-js/internals/delete-property-or-throw.js ***!
  \********************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var tryToString = __webpack_require__(/*! ../internals/try-to-string */ "./node_modules/core-js/internals/try-to-string.js");

var $TypeError = TypeError;

module.exports = function (O, P) {
  if (!delete O[P]) throw new $TypeError('Cannot delete property ' + tryToString(P) + ' of ' + tryToString(O));
};


/***/ }),

/***/ "./node_modules/core-js/internals/descriptors.js":
/*!*******************************************************!*\
  !*** ./node_modules/core-js/internals/descriptors.js ***!
  \*******************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");

// Detect IE8's incomplete defineProperty implementation
module.exports = !fails(function () {
  // eslint-disable-next-line es/no-object-defineproperty -- required for testing
  return Object.defineProperty({}, 1, { get: function () { return 7; } })[1] !== 7;
});


/***/ }),

/***/ "./node_modules/core-js/internals/document-create-element.js":
/*!*******************************************************************!*\
  !*** ./node_modules/core-js/internals/document-create-element.js ***!
  \*******************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var globalThis = __webpack_require__(/*! ../internals/global-this */ "./node_modules/core-js/internals/global-this.js");
var isObject = __webpack_require__(/*! ../internals/is-object */ "./node_modules/core-js/internals/is-object.js");

var document = globalThis.document;
// typeof document.createElement is 'object' in old IE
var EXISTS = isObject(document) && isObject(document.createElement);

module.exports = function (it) {
  return EXISTS ? document.createElement(it) : {};
};


/***/ }),

/***/ "./node_modules/core-js/internals/does-not-exceed-safe-integer.js":
/*!************************************************************************!*\
  !*** ./node_modules/core-js/internals/does-not-exceed-safe-integer.js ***!
  \************************************************************************/
/***/ ((module) => {


var $TypeError = TypeError;
var MAX_SAFE_INTEGER = 0x1FFFFFFFFFFFFF; // 2 ** 53 - 1 == 9007199254740991

module.exports = function (it) {
  if (it > MAX_SAFE_INTEGER) throw $TypeError('Maximum allowed index exceeded');
  return it;
};


/***/ }),

/***/ "./node_modules/core-js/internals/dom-iterables.js":
/*!*********************************************************!*\
  !*** ./node_modules/core-js/internals/dom-iterables.js ***!
  \*********************************************************/
/***/ ((module) => {


// iterable DOM collections
// flag - `iterable` interface - 'entries', 'keys', 'values', 'forEach' methods
module.exports = {
  CSSRuleList: 0,
  CSSStyleDeclaration: 0,
  CSSValueList: 0,
  ClientRectList: 0,
  DOMRectList: 0,
  DOMStringList: 0,
  DOMTokenList: 1,
  DataTransferItemList: 0,
  FileList: 0,
  HTMLAllCollection: 0,
  HTMLCollection: 0,
  HTMLFormElement: 0,
  HTMLSelectElement: 0,
  MediaList: 0,
  MimeTypeArray: 0,
  NamedNodeMap: 0,
  NodeList: 1,
  PaintRequestList: 0,
  Plugin: 0,
  PluginArray: 0,
  SVGLengthList: 0,
  SVGNumberList: 0,
  SVGPathSegList: 0,
  SVGPointList: 0,
  SVGStringList: 0,
  SVGTransformList: 0,
  SourceBufferList: 0,
  StyleSheetList: 0,
  TextTrackCueList: 0,
  TextTrackList: 0,
  TouchList: 0
};


/***/ }),

/***/ "./node_modules/core-js/internals/dom-token-list-prototype.js":
/*!********************************************************************!*\
  !*** ./node_modules/core-js/internals/dom-token-list-prototype.js ***!
  \********************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


// in old WebKit versions, `element.classList` is not an instance of global `DOMTokenList`
var documentCreateElement = __webpack_require__(/*! ../internals/document-create-element */ "./node_modules/core-js/internals/document-create-element.js");

var classList = documentCreateElement('span').classList;
var DOMTokenListPrototype = classList && classList.constructor && classList.constructor.prototype;

module.exports = DOMTokenListPrototype === Object.prototype ? undefined : DOMTokenListPrototype;


/***/ }),

/***/ "./node_modules/core-js/internals/enum-bug-keys.js":
/*!*********************************************************!*\
  !*** ./node_modules/core-js/internals/enum-bug-keys.js ***!
  \*********************************************************/
/***/ ((module) => {


// IE8- don't enum bug keys
module.exports = [
  'constructor',
  'hasOwnProperty',
  'isPrototypeOf',
  'propertyIsEnumerable',
  'toLocaleString',
  'toString',
  'valueOf'
];


/***/ }),

/***/ "./node_modules/core-js/internals/environment-ff-version.js":
/*!******************************************************************!*\
  !*** ./node_modules/core-js/internals/environment-ff-version.js ***!
  \******************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var userAgent = __webpack_require__(/*! ../internals/environment-user-agent */ "./node_modules/core-js/internals/environment-user-agent.js");

var firefox = userAgent.match(/firefox\/(\d+)/i);

module.exports = !!firefox && +firefox[1];


/***/ }),

/***/ "./node_modules/core-js/internals/environment-is-ie-or-edge.js":
/*!*********************************************************************!*\
  !*** ./node_modules/core-js/internals/environment-is-ie-or-edge.js ***!
  \*********************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var UA = __webpack_require__(/*! ../internals/environment-user-agent */ "./node_modules/core-js/internals/environment-user-agent.js");

module.exports = /MSIE|Trident/.test(UA);


/***/ }),

/***/ "./node_modules/core-js/internals/environment-is-ios-pebble.js":
/*!*********************************************************************!*\
  !*** ./node_modules/core-js/internals/environment-is-ios-pebble.js ***!
  \*********************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var userAgent = __webpack_require__(/*! ../internals/environment-user-agent */ "./node_modules/core-js/internals/environment-user-agent.js");

module.exports = /ipad|iphone|ipod/i.test(userAgent) && typeof Pebble != 'undefined';


/***/ }),

/***/ "./node_modules/core-js/internals/environment-is-ios.js":
/*!**************************************************************!*\
  !*** ./node_modules/core-js/internals/environment-is-ios.js ***!
  \**************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var userAgent = __webpack_require__(/*! ../internals/environment-user-agent */ "./node_modules/core-js/internals/environment-user-agent.js");

// eslint-disable-next-line redos/no-vulnerable -- safe
module.exports = /(?:ipad|iphone|ipod).*applewebkit/i.test(userAgent);


/***/ }),

/***/ "./node_modules/core-js/internals/environment-is-node.js":
/*!***************************************************************!*\
  !*** ./node_modules/core-js/internals/environment-is-node.js ***!
  \***************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var ENVIRONMENT = __webpack_require__(/*! ../internals/environment */ "./node_modules/core-js/internals/environment.js");

module.exports = ENVIRONMENT === 'NODE';


/***/ }),

/***/ "./node_modules/core-js/internals/environment-is-webos-webkit.js":
/*!***********************************************************************!*\
  !*** ./node_modules/core-js/internals/environment-is-webos-webkit.js ***!
  \***********************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var userAgent = __webpack_require__(/*! ../internals/environment-user-agent */ "./node_modules/core-js/internals/environment-user-agent.js");

module.exports = /web0s(?!.*chrome)/i.test(userAgent);


/***/ }),

/***/ "./node_modules/core-js/internals/environment-user-agent.js":
/*!******************************************************************!*\
  !*** ./node_modules/core-js/internals/environment-user-agent.js ***!
  \******************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var globalThis = __webpack_require__(/*! ../internals/global-this */ "./node_modules/core-js/internals/global-this.js");

var navigator = globalThis.navigator;
var userAgent = navigator && navigator.userAgent;

module.exports = userAgent ? String(userAgent) : '';


/***/ }),

/***/ "./node_modules/core-js/internals/environment-v8-version.js":
/*!******************************************************************!*\
  !*** ./node_modules/core-js/internals/environment-v8-version.js ***!
  \******************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var globalThis = __webpack_require__(/*! ../internals/global-this */ "./node_modules/core-js/internals/global-this.js");
var userAgent = __webpack_require__(/*! ../internals/environment-user-agent */ "./node_modules/core-js/internals/environment-user-agent.js");

var process = globalThis.process;
var Deno = globalThis.Deno;
var versions = process && process.versions || Deno && Deno.version;
var v8 = versions && versions.v8;
var match, version;

if (v8) {
  match = v8.split('.');
  // in old Chrome, versions of V8 isn't V8 = Chrome / 10
  // but their correct versions are not interesting for us
  version = match[0] > 0 && match[0] < 4 ? 1 : +(match[0] + match[1]);
}

// BrowserFS NodeJS `process` polyfill incorrectly set `.v8` to `0.0`
// so check `userAgent` even if `.v8` exists, but 0
if (!version && userAgent) {
  match = userAgent.match(/Edge\/(\d+)/);
  if (!match || match[1] >= 74) {
    match = userAgent.match(/Chrome\/(\d+)/);
    if (match) version = +match[1];
  }
}

module.exports = version;


/***/ }),

/***/ "./node_modules/core-js/internals/environment-webkit-version.js":
/*!**********************************************************************!*\
  !*** ./node_modules/core-js/internals/environment-webkit-version.js ***!
  \**********************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var userAgent = __webpack_require__(/*! ../internals/environment-user-agent */ "./node_modules/core-js/internals/environment-user-agent.js");

var webkit = userAgent.match(/AppleWebKit\/(\d+)\./);

module.exports = !!webkit && +webkit[1];


/***/ }),

/***/ "./node_modules/core-js/internals/environment.js":
/*!*******************************************************!*\
  !*** ./node_modules/core-js/internals/environment.js ***!
  \*******************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


/* global Bun, Deno -- detection */
var globalThis = __webpack_require__(/*! ../internals/global-this */ "./node_modules/core-js/internals/global-this.js");
var userAgent = __webpack_require__(/*! ../internals/environment-user-agent */ "./node_modules/core-js/internals/environment-user-agent.js");
var classof = __webpack_require__(/*! ../internals/classof-raw */ "./node_modules/core-js/internals/classof-raw.js");

var userAgentStartsWith = function (string) {
  return userAgent.slice(0, string.length) === string;
};

module.exports = (function () {
  if (userAgentStartsWith('Bun/')) return 'BUN';
  if (userAgentStartsWith('Cloudflare-Workers')) return 'CLOUDFLARE';
  if (userAgentStartsWith('Deno/')) return 'DENO';
  if (userAgentStartsWith('Node.js/')) return 'NODE';
  if (globalThis.Bun && typeof Bun.version == 'string') return 'BUN';
  if (globalThis.Deno && typeof Deno.version == 'object') return 'DENO';
  if (classof(globalThis.process) === 'process') return 'NODE';
  if (globalThis.window && globalThis.document) return 'BROWSER';
  return 'REST';
})();


/***/ }),

/***/ "./node_modules/core-js/internals/export.js":
/*!**************************************************!*\
  !*** ./node_modules/core-js/internals/export.js ***!
  \**************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var globalThis = __webpack_require__(/*! ../internals/global-this */ "./node_modules/core-js/internals/global-this.js");
var getOwnPropertyDescriptor = (__webpack_require__(/*! ../internals/object-get-own-property-descriptor */ "./node_modules/core-js/internals/object-get-own-property-descriptor.js").f);
var createNonEnumerableProperty = __webpack_require__(/*! ../internals/create-non-enumerable-property */ "./node_modules/core-js/internals/create-non-enumerable-property.js");
var defineBuiltIn = __webpack_require__(/*! ../internals/define-built-in */ "./node_modules/core-js/internals/define-built-in.js");
var defineGlobalProperty = __webpack_require__(/*! ../internals/define-global-property */ "./node_modules/core-js/internals/define-global-property.js");
var copyConstructorProperties = __webpack_require__(/*! ../internals/copy-constructor-properties */ "./node_modules/core-js/internals/copy-constructor-properties.js");
var isForced = __webpack_require__(/*! ../internals/is-forced */ "./node_modules/core-js/internals/is-forced.js");

/*
  options.target         - name of the target object
  options.global         - target is the global object
  options.stat           - export as static methods of target
  options.proto          - export as prototype methods of target
  options.real           - real prototype method for the `pure` version
  options.forced         - export even if the native feature is available
  options.bind           - bind methods to the target, required for the `pure` version
  options.wrap           - wrap constructors to preventing global pollution, required for the `pure` version
  options.unsafe         - use the simple assignment of property instead of delete + defineProperty
  options.sham           - add a flag to not completely full polyfills
  options.enumerable     - export as enumerable property
  options.dontCallGetSet - prevent calling a getter on target
  options.name           - the .name of the function if it does not match the key
*/
module.exports = function (options, source) {
  var TARGET = options.target;
  var GLOBAL = options.global;
  var STATIC = options.stat;
  var FORCED, target, key, targetProperty, sourceProperty, descriptor;
  if (GLOBAL) {
    target = globalThis;
  } else if (STATIC) {
    target = globalThis[TARGET] || defineGlobalProperty(TARGET, {});
  } else {
    target = globalThis[TARGET] && globalThis[TARGET].prototype;
  }
  if (target) for (key in source) {
    sourceProperty = source[key];
    if (options.dontCallGetSet) {
      descriptor = getOwnPropertyDescriptor(target, key);
      targetProperty = descriptor && descriptor.value;
    } else targetProperty = target[key];
    FORCED = isForced(GLOBAL ? key : TARGET + (STATIC ? '.' : '#') + key, options.forced);
    // contained in target
    if (!FORCED && targetProperty !== undefined) {
      if (typeof sourceProperty == typeof targetProperty) continue;
      copyConstructorProperties(sourceProperty, targetProperty);
    }
    // add a flag to not completely full polyfills
    if (options.sham || (targetProperty && targetProperty.sham)) {
      createNonEnumerableProperty(sourceProperty, 'sham', true);
    }
    defineBuiltIn(target, key, sourceProperty, options);
  }
};


/***/ }),

/***/ "./node_modules/core-js/internals/fails.js":
/*!*************************************************!*\
  !*** ./node_modules/core-js/internals/fails.js ***!
  \*************************************************/
/***/ ((module) => {


module.exports = function (exec) {
  try {
    return !!exec();
  } catch (error) {
    return true;
  }
};


/***/ }),

/***/ "./node_modules/core-js/internals/fix-regexp-well-known-symbol-logic.js":
/*!******************************************************************************!*\
  !*** ./node_modules/core-js/internals/fix-regexp-well-known-symbol-logic.js ***!
  \******************************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


// TODO: Remove from `core-js@4` since it's moved to entry points
__webpack_require__(/*! ../modules/es.regexp.exec */ "./node_modules/core-js/modules/es.regexp.exec.js");
var call = __webpack_require__(/*! ../internals/function-call */ "./node_modules/core-js/internals/function-call.js");
var defineBuiltIn = __webpack_require__(/*! ../internals/define-built-in */ "./node_modules/core-js/internals/define-built-in.js");
var regexpExec = __webpack_require__(/*! ../internals/regexp-exec */ "./node_modules/core-js/internals/regexp-exec.js");
var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");
var wellKnownSymbol = __webpack_require__(/*! ../internals/well-known-symbol */ "./node_modules/core-js/internals/well-known-symbol.js");
var createNonEnumerableProperty = __webpack_require__(/*! ../internals/create-non-enumerable-property */ "./node_modules/core-js/internals/create-non-enumerable-property.js");

var SPECIES = wellKnownSymbol('species');
var RegExpPrototype = RegExp.prototype;

module.exports = function (KEY, exec, FORCED, SHAM) {
  var SYMBOL = wellKnownSymbol(KEY);

  var DELEGATES_TO_SYMBOL = !fails(function () {
    // String methods call symbol-named RegExp methods
    var O = {};
    O[SYMBOL] = function () { return 7; };
    return ''[KEY](O) !== 7;
  });

  var DELEGATES_TO_EXEC = DELEGATES_TO_SYMBOL && !fails(function () {
    // Symbol-named RegExp methods call .exec
    var execCalled = false;
    var re = /a/;

    if (KEY === 'split') {
      // We can't use real regex here since it causes deoptimization
      // and serious performance degradation in V8
      // https://github.com/zloirock/core-js/issues/306
      re = {};
      // RegExp[@@split] doesn't call the regex's exec method, but first creates
      // a new one. We need to return the patched regex when creating the new one.
      re.constructor = {};
      re.constructor[SPECIES] = function () { return re; };
      re.flags = '';
      re[SYMBOL] = /./[SYMBOL];
    }

    re.exec = function () {
      execCalled = true;
      return null;
    };

    re[SYMBOL]('');
    return !execCalled;
  });

  if (
    !DELEGATES_TO_SYMBOL ||
    !DELEGATES_TO_EXEC ||
    FORCED
  ) {
    var nativeRegExpMethod = /./[SYMBOL];
    var methods = exec(SYMBOL, ''[KEY], function (nativeMethod, regexp, str, arg2, forceStringMethod) {
      var $exec = regexp.exec;
      if ($exec === regexpExec || $exec === RegExpPrototype.exec) {
        if (DELEGATES_TO_SYMBOL && !forceStringMethod) {
          // The native String method already delegates to @@method (this
          // polyfilled function), leasing to infinite recursion.
          // We avoid it by directly calling the native @@method method.
          return { done: true, value: call(nativeRegExpMethod, regexp, str, arg2) };
        }
        return { done: true, value: call(nativeMethod, str, regexp, arg2) };
      }
      return { done: false };
    });

    defineBuiltIn(String.prototype, KEY, methods[0]);
    defineBuiltIn(RegExpPrototype, SYMBOL, methods[1]);
  }

  if (SHAM) createNonEnumerableProperty(RegExpPrototype[SYMBOL], 'sham', true);
};


/***/ }),

/***/ "./node_modules/core-js/internals/freezing.js":
/*!****************************************************!*\
  !*** ./node_modules/core-js/internals/freezing.js ***!
  \****************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");

module.exports = !fails(function () {
  // eslint-disable-next-line es/no-object-isextensible, es/no-object-preventextensions -- required for testing
  return Object.isExtensible(Object.preventExtensions({}));
});


/***/ }),

/***/ "./node_modules/core-js/internals/function-apply.js":
/*!**********************************************************!*\
  !*** ./node_modules/core-js/internals/function-apply.js ***!
  \**********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var NATIVE_BIND = __webpack_require__(/*! ../internals/function-bind-native */ "./node_modules/core-js/internals/function-bind-native.js");

var FunctionPrototype = Function.prototype;
var apply = FunctionPrototype.apply;
var call = FunctionPrototype.call;

// eslint-disable-next-line es/no-function-prototype-bind, es/no-reflect -- safe
module.exports = typeof Reflect == 'object' && Reflect.apply || (NATIVE_BIND ? call.bind(apply) : function () {
  return call.apply(apply, arguments);
});


/***/ }),

/***/ "./node_modules/core-js/internals/function-bind-context.js":
/*!*****************************************************************!*\
  !*** ./node_modules/core-js/internals/function-bind-context.js ***!
  \*****************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this-clause */ "./node_modules/core-js/internals/function-uncurry-this-clause.js");
var aCallable = __webpack_require__(/*! ../internals/a-callable */ "./node_modules/core-js/internals/a-callable.js");
var NATIVE_BIND = __webpack_require__(/*! ../internals/function-bind-native */ "./node_modules/core-js/internals/function-bind-native.js");

var bind = uncurryThis(uncurryThis.bind);

// optional / simple context binding
module.exports = function (fn, that) {
  aCallable(fn);
  return that === undefined ? fn : NATIVE_BIND ? bind(fn, that) : function (/* ...args */) {
    return fn.apply(that, arguments);
  };
};


/***/ }),

/***/ "./node_modules/core-js/internals/function-bind-native.js":
/*!****************************************************************!*\
  !*** ./node_modules/core-js/internals/function-bind-native.js ***!
  \****************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");

module.exports = !fails(function () {
  // eslint-disable-next-line es/no-function-prototype-bind -- safe
  var test = (function () { /* empty */ }).bind();
  // eslint-disable-next-line no-prototype-builtins -- safe
  return typeof test != 'function' || test.hasOwnProperty('prototype');
});


/***/ }),

/***/ "./node_modules/core-js/internals/function-call.js":
/*!*********************************************************!*\
  !*** ./node_modules/core-js/internals/function-call.js ***!
  \*********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var NATIVE_BIND = __webpack_require__(/*! ../internals/function-bind-native */ "./node_modules/core-js/internals/function-bind-native.js");

var call = Function.prototype.call;
// eslint-disable-next-line es/no-function-prototype-bind -- safe
module.exports = NATIVE_BIND ? call.bind(call) : function () {
  return call.apply(call, arguments);
};


/***/ }),

/***/ "./node_modules/core-js/internals/function-name.js":
/*!*********************************************************!*\
  !*** ./node_modules/core-js/internals/function-name.js ***!
  \*********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var DESCRIPTORS = __webpack_require__(/*! ../internals/descriptors */ "./node_modules/core-js/internals/descriptors.js");
var hasOwn = __webpack_require__(/*! ../internals/has-own-property */ "./node_modules/core-js/internals/has-own-property.js");

var FunctionPrototype = Function.prototype;
// eslint-disable-next-line es/no-object-getownpropertydescriptor -- safe
var getDescriptor = DESCRIPTORS && Object.getOwnPropertyDescriptor;

var EXISTS = hasOwn(FunctionPrototype, 'name');
// additional protection from minified / mangled / dropped function names
var PROPER = EXISTS && (function something() { /* empty */ }).name === 'something';
var CONFIGURABLE = EXISTS && (!DESCRIPTORS || (DESCRIPTORS && getDescriptor(FunctionPrototype, 'name').configurable));

module.exports = {
  EXISTS: EXISTS,
  PROPER: PROPER,
  CONFIGURABLE: CONFIGURABLE
};


/***/ }),

/***/ "./node_modules/core-js/internals/function-uncurry-this-accessor.js":
/*!**************************************************************************!*\
  !*** ./node_modules/core-js/internals/function-uncurry-this-accessor.js ***!
  \**************************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this */ "./node_modules/core-js/internals/function-uncurry-this.js");
var aCallable = __webpack_require__(/*! ../internals/a-callable */ "./node_modules/core-js/internals/a-callable.js");

module.exports = function (object, key, method) {
  try {
    // eslint-disable-next-line es/no-object-getownpropertydescriptor -- safe
    return uncurryThis(aCallable(Object.getOwnPropertyDescriptor(object, key)[method]));
  } catch (error) { /* empty */ }
};


/***/ }),

/***/ "./node_modules/core-js/internals/function-uncurry-this-clause.js":
/*!************************************************************************!*\
  !*** ./node_modules/core-js/internals/function-uncurry-this-clause.js ***!
  \************************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var classofRaw = __webpack_require__(/*! ../internals/classof-raw */ "./node_modules/core-js/internals/classof-raw.js");
var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this */ "./node_modules/core-js/internals/function-uncurry-this.js");

module.exports = function (fn) {
  // Nashorn bug:
  //   https://github.com/zloirock/core-js/issues/1128
  //   https://github.com/zloirock/core-js/issues/1130
  if (classofRaw(fn) === 'Function') return uncurryThis(fn);
};


/***/ }),

/***/ "./node_modules/core-js/internals/function-uncurry-this.js":
/*!*****************************************************************!*\
  !*** ./node_modules/core-js/internals/function-uncurry-this.js ***!
  \*****************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var NATIVE_BIND = __webpack_require__(/*! ../internals/function-bind-native */ "./node_modules/core-js/internals/function-bind-native.js");

var FunctionPrototype = Function.prototype;
var call = FunctionPrototype.call;
// eslint-disable-next-line es/no-function-prototype-bind -- safe
var uncurryThisWithBind = NATIVE_BIND && FunctionPrototype.bind.bind(call, call);

module.exports = NATIVE_BIND ? uncurryThisWithBind : function (fn) {
  return function () {
    return call.apply(fn, arguments);
  };
};


/***/ }),

/***/ "./node_modules/core-js/internals/get-built-in.js":
/*!********************************************************!*\
  !*** ./node_modules/core-js/internals/get-built-in.js ***!
  \********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var globalThis = __webpack_require__(/*! ../internals/global-this */ "./node_modules/core-js/internals/global-this.js");
var isCallable = __webpack_require__(/*! ../internals/is-callable */ "./node_modules/core-js/internals/is-callable.js");

var aFunction = function (argument) {
  return isCallable(argument) ? argument : undefined;
};

module.exports = function (namespace, method) {
  return arguments.length < 2 ? aFunction(globalThis[namespace]) : globalThis[namespace] && globalThis[namespace][method];
};


/***/ }),

/***/ "./node_modules/core-js/internals/get-iterator-method.js":
/*!***************************************************************!*\
  !*** ./node_modules/core-js/internals/get-iterator-method.js ***!
  \***************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var classof = __webpack_require__(/*! ../internals/classof */ "./node_modules/core-js/internals/classof.js");
var getMethod = __webpack_require__(/*! ../internals/get-method */ "./node_modules/core-js/internals/get-method.js");
var isNullOrUndefined = __webpack_require__(/*! ../internals/is-null-or-undefined */ "./node_modules/core-js/internals/is-null-or-undefined.js");
var Iterators = __webpack_require__(/*! ../internals/iterators */ "./node_modules/core-js/internals/iterators.js");
var wellKnownSymbol = __webpack_require__(/*! ../internals/well-known-symbol */ "./node_modules/core-js/internals/well-known-symbol.js");

var ITERATOR = wellKnownSymbol('iterator');

module.exports = function (it) {
  if (!isNullOrUndefined(it)) return getMethod(it, ITERATOR)
    || getMethod(it, '@@iterator')
    || Iterators[classof(it)];
};


/***/ }),

/***/ "./node_modules/core-js/internals/get-iterator.js":
/*!********************************************************!*\
  !*** ./node_modules/core-js/internals/get-iterator.js ***!
  \********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var call = __webpack_require__(/*! ../internals/function-call */ "./node_modules/core-js/internals/function-call.js");
var aCallable = __webpack_require__(/*! ../internals/a-callable */ "./node_modules/core-js/internals/a-callable.js");
var anObject = __webpack_require__(/*! ../internals/an-object */ "./node_modules/core-js/internals/an-object.js");
var tryToString = __webpack_require__(/*! ../internals/try-to-string */ "./node_modules/core-js/internals/try-to-string.js");
var getIteratorMethod = __webpack_require__(/*! ../internals/get-iterator-method */ "./node_modules/core-js/internals/get-iterator-method.js");

var $TypeError = TypeError;

module.exports = function (argument, usingIterator) {
  var iteratorMethod = arguments.length < 2 ? getIteratorMethod(argument) : usingIterator;
  if (aCallable(iteratorMethod)) return anObject(call(iteratorMethod, argument));
  throw new $TypeError(tryToString(argument) + ' is not iterable');
};


/***/ }),

/***/ "./node_modules/core-js/internals/get-json-replacer-function.js":
/*!**********************************************************************!*\
  !*** ./node_modules/core-js/internals/get-json-replacer-function.js ***!
  \**********************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this */ "./node_modules/core-js/internals/function-uncurry-this.js");
var isArray = __webpack_require__(/*! ../internals/is-array */ "./node_modules/core-js/internals/is-array.js");
var isCallable = __webpack_require__(/*! ../internals/is-callable */ "./node_modules/core-js/internals/is-callable.js");
var classof = __webpack_require__(/*! ../internals/classof-raw */ "./node_modules/core-js/internals/classof-raw.js");
var toString = __webpack_require__(/*! ../internals/to-string */ "./node_modules/core-js/internals/to-string.js");

var push = uncurryThis([].push);

module.exports = function (replacer) {
  if (isCallable(replacer)) return replacer;
  if (!isArray(replacer)) return;
  var rawLength = replacer.length;
  var keys = [];
  for (var i = 0; i < rawLength; i++) {
    var element = replacer[i];
    if (typeof element == 'string') push(keys, element);
    else if (typeof element == 'number' || classof(element) === 'Number' || classof(element) === 'String') push(keys, toString(element));
  }
  var keysLength = keys.length;
  var root = true;
  return function (key, value) {
    if (root) {
      root = false;
      return value;
    }
    if (isArray(this)) return value;
    for (var j = 0; j < keysLength; j++) if (keys[j] === key) return value;
  };
};


/***/ }),

/***/ "./node_modules/core-js/internals/get-method.js":
/*!******************************************************!*\
  !*** ./node_modules/core-js/internals/get-method.js ***!
  \******************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var aCallable = __webpack_require__(/*! ../internals/a-callable */ "./node_modules/core-js/internals/a-callable.js");
var isNullOrUndefined = __webpack_require__(/*! ../internals/is-null-or-undefined */ "./node_modules/core-js/internals/is-null-or-undefined.js");

// `GetMethod` abstract operation
// https://tc39.es/ecma262/#sec-getmethod
module.exports = function (V, P) {
  var func = V[P];
  return isNullOrUndefined(func) ? undefined : aCallable(func);
};


/***/ }),

/***/ "./node_modules/core-js/internals/get-substitution.js":
/*!************************************************************!*\
  !*** ./node_modules/core-js/internals/get-substitution.js ***!
  \************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this */ "./node_modules/core-js/internals/function-uncurry-this.js");
var toObject = __webpack_require__(/*! ../internals/to-object */ "./node_modules/core-js/internals/to-object.js");

var floor = Math.floor;
var charAt = uncurryThis(''.charAt);
var replace = uncurryThis(''.replace);
var stringSlice = uncurryThis(''.slice);
// eslint-disable-next-line redos/no-vulnerable -- safe
var SUBSTITUTION_SYMBOLS = /\$([$&'`]|\d{1,2}|<[^>]*>)/g;
var SUBSTITUTION_SYMBOLS_NO_NAMED = /\$([$&'`]|\d{1,2})/g;

// `GetSubstitution` abstract operation
// https://tc39.es/ecma262/#sec-getsubstitution
module.exports = function (matched, str, position, captures, namedCaptures, replacement) {
  var tailPos = position + matched.length;
  var m = captures.length;
  var symbols = SUBSTITUTION_SYMBOLS_NO_NAMED;
  if (namedCaptures !== undefined) {
    namedCaptures = toObject(namedCaptures);
    symbols = SUBSTITUTION_SYMBOLS;
  }
  return replace(replacement, symbols, function (match, ch) {
    var capture;
    switch (charAt(ch, 0)) {
      case '$': return '$';
      case '&': return matched;
      case '`': return stringSlice(str, 0, position);
      case "'": return stringSlice(str, tailPos);
      case '<':
        capture = namedCaptures[stringSlice(ch, 1, -1)];
        break;
      default: // \d\d?
        var n = +ch;
        if (n === 0) return match;
        if (n > m) {
          var f = floor(n / 10);
          if (f === 0) return match;
          if (f <= m) return captures[f - 1] === undefined ? charAt(ch, 1) : captures[f - 1] + charAt(ch, 1);
          return match;
        }
        capture = captures[n - 1];
    }
    return capture === undefined ? '' : capture;
  });
};


/***/ }),

/***/ "./node_modules/core-js/internals/global-this.js":
/*!*******************************************************!*\
  !*** ./node_modules/core-js/internals/global-this.js ***!
  \*******************************************************/
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {


var check = function (it) {
  return it && it.Math === Math && it;
};

// https://github.com/zloirock/core-js/issues/86#issuecomment-115759028
module.exports =
  // eslint-disable-next-line es/no-global-this -- safe
  check(typeof globalThis == 'object' && globalThis) ||
  check(typeof window == 'object' && window) ||
  // eslint-disable-next-line no-restricted-globals -- safe
  check(typeof self == 'object' && self) ||
  check(typeof __webpack_require__.g == 'object' && __webpack_require__.g) ||
  check(typeof this == 'object' && this) ||
  // eslint-disable-next-line no-new-func -- fallback
  (function () { return this; })() || Function('return this')();


/***/ }),

/***/ "./node_modules/core-js/internals/has-own-property.js":
/*!************************************************************!*\
  !*** ./node_modules/core-js/internals/has-own-property.js ***!
  \************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this */ "./node_modules/core-js/internals/function-uncurry-this.js");
var toObject = __webpack_require__(/*! ../internals/to-object */ "./node_modules/core-js/internals/to-object.js");

var hasOwnProperty = uncurryThis({}.hasOwnProperty);

// `HasOwnProperty` abstract operation
// https://tc39.es/ecma262/#sec-hasownproperty
// eslint-disable-next-line es/no-object-hasown -- safe
module.exports = Object.hasOwn || function hasOwn(it, key) {
  return hasOwnProperty(toObject(it), key);
};


/***/ }),

/***/ "./node_modules/core-js/internals/hidden-keys.js":
/*!*******************************************************!*\
  !*** ./node_modules/core-js/internals/hidden-keys.js ***!
  \*******************************************************/
/***/ ((module) => {


module.exports = {};


/***/ }),

/***/ "./node_modules/core-js/internals/host-report-errors.js":
/*!**************************************************************!*\
  !*** ./node_modules/core-js/internals/host-report-errors.js ***!
  \**************************************************************/
/***/ ((module) => {


module.exports = function (a, b) {
  try {
    // eslint-disable-next-line no-console -- safe
    arguments.length === 1 ? console.error(a) : console.error(a, b);
  } catch (error) { /* empty */ }
};


/***/ }),

/***/ "./node_modules/core-js/internals/html.js":
/*!************************************************!*\
  !*** ./node_modules/core-js/internals/html.js ***!
  \************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var getBuiltIn = __webpack_require__(/*! ../internals/get-built-in */ "./node_modules/core-js/internals/get-built-in.js");

module.exports = getBuiltIn('document', 'documentElement');


/***/ }),

/***/ "./node_modules/core-js/internals/ie8-dom-define.js":
/*!**********************************************************!*\
  !*** ./node_modules/core-js/internals/ie8-dom-define.js ***!
  \**********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var DESCRIPTORS = __webpack_require__(/*! ../internals/descriptors */ "./node_modules/core-js/internals/descriptors.js");
var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");
var createElement = __webpack_require__(/*! ../internals/document-create-element */ "./node_modules/core-js/internals/document-create-element.js");

// Thanks to IE8 for its funny defineProperty
module.exports = !DESCRIPTORS && !fails(function () {
  // eslint-disable-next-line es/no-object-defineproperty -- required for testing
  return Object.defineProperty(createElement('div'), 'a', {
    get: function () { return 7; }
  }).a !== 7;
});


/***/ }),

/***/ "./node_modules/core-js/internals/indexed-object.js":
/*!**********************************************************!*\
  !*** ./node_modules/core-js/internals/indexed-object.js ***!
  \**********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this */ "./node_modules/core-js/internals/function-uncurry-this.js");
var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");
var classof = __webpack_require__(/*! ../internals/classof-raw */ "./node_modules/core-js/internals/classof-raw.js");

var $Object = Object;
var split = uncurryThis(''.split);

// fallback for non-array-like ES3 and non-enumerable old V8 strings
module.exports = fails(function () {
  // throws an error in rhino, see https://github.com/mozilla/rhino/issues/346
  // eslint-disable-next-line no-prototype-builtins -- safe
  return !$Object('z').propertyIsEnumerable(0);
}) ? function (it) {
  return classof(it) === 'String' ? split(it, '') : $Object(it);
} : $Object;


/***/ }),

/***/ "./node_modules/core-js/internals/inherit-if-required.js":
/*!***************************************************************!*\
  !*** ./node_modules/core-js/internals/inherit-if-required.js ***!
  \***************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var isCallable = __webpack_require__(/*! ../internals/is-callable */ "./node_modules/core-js/internals/is-callable.js");
var isObject = __webpack_require__(/*! ../internals/is-object */ "./node_modules/core-js/internals/is-object.js");
var setPrototypeOf = __webpack_require__(/*! ../internals/object-set-prototype-of */ "./node_modules/core-js/internals/object-set-prototype-of.js");

// makes subclassing work correct for wrapped built-ins
module.exports = function ($this, dummy, Wrapper) {
  var NewTarget, NewTargetPrototype;
  if (
    // it can work only with native `setPrototypeOf`
    setPrototypeOf &&
    // we haven't completely correct pre-ES6 way for getting `new.target`, so use this
    isCallable(NewTarget = dummy.constructor) &&
    NewTarget !== Wrapper &&
    isObject(NewTargetPrototype = NewTarget.prototype) &&
    NewTargetPrototype !== Wrapper.prototype
  ) setPrototypeOf($this, NewTargetPrototype);
  return $this;
};


/***/ }),

/***/ "./node_modules/core-js/internals/inspect-source.js":
/*!**********************************************************!*\
  !*** ./node_modules/core-js/internals/inspect-source.js ***!
  \**********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this */ "./node_modules/core-js/internals/function-uncurry-this.js");
var isCallable = __webpack_require__(/*! ../internals/is-callable */ "./node_modules/core-js/internals/is-callable.js");
var store = __webpack_require__(/*! ../internals/shared-store */ "./node_modules/core-js/internals/shared-store.js");

var functionToString = uncurryThis(Function.toString);

// this helper broken in `core-js@3.4.1-3.4.4`, so we can't use `shared` helper
if (!isCallable(store.inspectSource)) {
  store.inspectSource = function (it) {
    return functionToString(it);
  };
}

module.exports = store.inspectSource;


/***/ }),

/***/ "./node_modules/core-js/internals/internal-metadata.js":
/*!*************************************************************!*\
  !*** ./node_modules/core-js/internals/internal-metadata.js ***!
  \*************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");
var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this */ "./node_modules/core-js/internals/function-uncurry-this.js");
var hiddenKeys = __webpack_require__(/*! ../internals/hidden-keys */ "./node_modules/core-js/internals/hidden-keys.js");
var isObject = __webpack_require__(/*! ../internals/is-object */ "./node_modules/core-js/internals/is-object.js");
var hasOwn = __webpack_require__(/*! ../internals/has-own-property */ "./node_modules/core-js/internals/has-own-property.js");
var defineProperty = (__webpack_require__(/*! ../internals/object-define-property */ "./node_modules/core-js/internals/object-define-property.js").f);
var getOwnPropertyNamesModule = __webpack_require__(/*! ../internals/object-get-own-property-names */ "./node_modules/core-js/internals/object-get-own-property-names.js");
var getOwnPropertyNamesExternalModule = __webpack_require__(/*! ../internals/object-get-own-property-names-external */ "./node_modules/core-js/internals/object-get-own-property-names-external.js");
var isExtensible = __webpack_require__(/*! ../internals/object-is-extensible */ "./node_modules/core-js/internals/object-is-extensible.js");
var uid = __webpack_require__(/*! ../internals/uid */ "./node_modules/core-js/internals/uid.js");
var FREEZING = __webpack_require__(/*! ../internals/freezing */ "./node_modules/core-js/internals/freezing.js");

var REQUIRED = false;
var METADATA = uid('meta');
var id = 0;

var setMetadata = function (it) {
  defineProperty(it, METADATA, { value: {
    objectID: 'O' + id++, // object ID
    weakData: {}          // weak collections IDs
  } });
};

var fastKey = function (it, create) {
  // return a primitive with prefix
  if (!isObject(it)) return typeof it == 'symbol' ? it : (typeof it == 'string' ? 'S' : 'P') + it;
  if (!hasOwn(it, METADATA)) {
    // can't set metadata to uncaught frozen object
    if (!isExtensible(it)) return 'F';
    // not necessary to add metadata
    if (!create) return 'E';
    // add missing metadata
    setMetadata(it);
  // return object ID
  } return it[METADATA].objectID;
};

var getWeakData = function (it, create) {
  if (!hasOwn(it, METADATA)) {
    // can't set metadata to uncaught frozen object
    if (!isExtensible(it)) return true;
    // not necessary to add metadata
    if (!create) return false;
    // add missing metadata
    setMetadata(it);
  // return the store of weak collections IDs
  } return it[METADATA].weakData;
};

// add metadata on freeze-family methods calling
var onFreeze = function (it) {
  if (FREEZING && REQUIRED && isExtensible(it) && !hasOwn(it, METADATA)) setMetadata(it);
  return it;
};

var enable = function () {
  meta.enable = function () { /* empty */ };
  REQUIRED = true;
  var getOwnPropertyNames = getOwnPropertyNamesModule.f;
  var splice = uncurryThis([].splice);
  var test = {};
  test[METADATA] = 1;

  // prevent exposing of metadata key
  if (getOwnPropertyNames(test).length) {
    getOwnPropertyNamesModule.f = function (it) {
      var result = getOwnPropertyNames(it);
      for (var i = 0, length = result.length; i < length; i++) {
        if (result[i] === METADATA) {
          splice(result, i, 1);
          break;
        }
      } return result;
    };

    $({ target: 'Object', stat: true, forced: true }, {
      getOwnPropertyNames: getOwnPropertyNamesExternalModule.f
    });
  }
};

var meta = module.exports = {
  enable: enable,
  fastKey: fastKey,
  getWeakData: getWeakData,
  onFreeze: onFreeze
};

hiddenKeys[METADATA] = true;


/***/ }),

/***/ "./node_modules/core-js/internals/internal-state.js":
/*!**********************************************************!*\
  !*** ./node_modules/core-js/internals/internal-state.js ***!
  \**********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var NATIVE_WEAK_MAP = __webpack_require__(/*! ../internals/weak-map-basic-detection */ "./node_modules/core-js/internals/weak-map-basic-detection.js");
var globalThis = __webpack_require__(/*! ../internals/global-this */ "./node_modules/core-js/internals/global-this.js");
var isObject = __webpack_require__(/*! ../internals/is-object */ "./node_modules/core-js/internals/is-object.js");
var createNonEnumerableProperty = __webpack_require__(/*! ../internals/create-non-enumerable-property */ "./node_modules/core-js/internals/create-non-enumerable-property.js");
var hasOwn = __webpack_require__(/*! ../internals/has-own-property */ "./node_modules/core-js/internals/has-own-property.js");
var shared = __webpack_require__(/*! ../internals/shared-store */ "./node_modules/core-js/internals/shared-store.js");
var sharedKey = __webpack_require__(/*! ../internals/shared-key */ "./node_modules/core-js/internals/shared-key.js");
var hiddenKeys = __webpack_require__(/*! ../internals/hidden-keys */ "./node_modules/core-js/internals/hidden-keys.js");

var OBJECT_ALREADY_INITIALIZED = 'Object already initialized';
var TypeError = globalThis.TypeError;
var WeakMap = globalThis.WeakMap;
var set, get, has;

var enforce = function (it) {
  return has(it) ? get(it) : set(it, {});
};

var getterFor = function (TYPE) {
  return function (it) {
    var state;
    if (!isObject(it) || (state = get(it)).type !== TYPE) {
      throw new TypeError('Incompatible receiver, ' + TYPE + ' required');
    } return state;
  };
};

if (NATIVE_WEAK_MAP || shared.state) {
  var store = shared.state || (shared.state = new WeakMap());
  /* eslint-disable no-self-assign -- prototype methods protection */
  store.get = store.get;
  store.has = store.has;
  store.set = store.set;
  /* eslint-enable no-self-assign -- prototype methods protection */
  set = function (it, metadata) {
    if (store.has(it)) throw new TypeError(OBJECT_ALREADY_INITIALIZED);
    metadata.facade = it;
    store.set(it, metadata);
    return metadata;
  };
  get = function (it) {
    return store.get(it) || {};
  };
  has = function (it) {
    return store.has(it);
  };
} else {
  var STATE = sharedKey('state');
  hiddenKeys[STATE] = true;
  set = function (it, metadata) {
    if (hasOwn(it, STATE)) throw new TypeError(OBJECT_ALREADY_INITIALIZED);
    metadata.facade = it;
    createNonEnumerableProperty(it, STATE, metadata);
    return metadata;
  };
  get = function (it) {
    return hasOwn(it, STATE) ? it[STATE] : {};
  };
  has = function (it) {
    return hasOwn(it, STATE);
  };
}

module.exports = {
  set: set,
  get: get,
  has: has,
  enforce: enforce,
  getterFor: getterFor
};


/***/ }),

/***/ "./node_modules/core-js/internals/is-array-iterator-method.js":
/*!********************************************************************!*\
  !*** ./node_modules/core-js/internals/is-array-iterator-method.js ***!
  \********************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var wellKnownSymbol = __webpack_require__(/*! ../internals/well-known-symbol */ "./node_modules/core-js/internals/well-known-symbol.js");
var Iterators = __webpack_require__(/*! ../internals/iterators */ "./node_modules/core-js/internals/iterators.js");

var ITERATOR = wellKnownSymbol('iterator');
var ArrayPrototype = Array.prototype;

// check on default Array iterator
module.exports = function (it) {
  return it !== undefined && (Iterators.Array === it || ArrayPrototype[ITERATOR] === it);
};


/***/ }),

/***/ "./node_modules/core-js/internals/is-array.js":
/*!****************************************************!*\
  !*** ./node_modules/core-js/internals/is-array.js ***!
  \****************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var classof = __webpack_require__(/*! ../internals/classof-raw */ "./node_modules/core-js/internals/classof-raw.js");

// `IsArray` abstract operation
// https://tc39.es/ecma262/#sec-isarray
// eslint-disable-next-line es/no-array-isarray -- safe
module.exports = Array.isArray || function isArray(argument) {
  return classof(argument) === 'Array';
};


/***/ }),

/***/ "./node_modules/core-js/internals/is-callable.js":
/*!*******************************************************!*\
  !*** ./node_modules/core-js/internals/is-callable.js ***!
  \*******************************************************/
/***/ ((module) => {


// https://tc39.es/ecma262/#sec-IsHTMLDDA-internal-slot
var documentAll = typeof document == 'object' && document.all;

// `IsCallable` abstract operation
// https://tc39.es/ecma262/#sec-iscallable
// eslint-disable-next-line unicorn/no-typeof-undefined -- required for testing
module.exports = typeof documentAll == 'undefined' && documentAll !== undefined ? function (argument) {
  return typeof argument == 'function' || argument === documentAll;
} : function (argument) {
  return typeof argument == 'function';
};


/***/ }),

/***/ "./node_modules/core-js/internals/is-constructor.js":
/*!**********************************************************!*\
  !*** ./node_modules/core-js/internals/is-constructor.js ***!
  \**********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this */ "./node_modules/core-js/internals/function-uncurry-this.js");
var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");
var isCallable = __webpack_require__(/*! ../internals/is-callable */ "./node_modules/core-js/internals/is-callable.js");
var classof = __webpack_require__(/*! ../internals/classof */ "./node_modules/core-js/internals/classof.js");
var getBuiltIn = __webpack_require__(/*! ../internals/get-built-in */ "./node_modules/core-js/internals/get-built-in.js");
var inspectSource = __webpack_require__(/*! ../internals/inspect-source */ "./node_modules/core-js/internals/inspect-source.js");

var noop = function () { /* empty */ };
var construct = getBuiltIn('Reflect', 'construct');
var constructorRegExp = /^\s*(?:class|function)\b/;
var exec = uncurryThis(constructorRegExp.exec);
var INCORRECT_TO_STRING = !constructorRegExp.test(noop);

var isConstructorModern = function isConstructor(argument) {
  if (!isCallable(argument)) return false;
  try {
    construct(noop, [], argument);
    return true;
  } catch (error) {
    return false;
  }
};

var isConstructorLegacy = function isConstructor(argument) {
  if (!isCallable(argument)) return false;
  switch (classof(argument)) {
    case 'AsyncFunction':
    case 'GeneratorFunction':
    case 'AsyncGeneratorFunction': return false;
  }
  try {
    // we can't check .prototype since constructors produced by .bind haven't it
    // `Function#toString` throws on some built-it function in some legacy engines
    // (for example, `DOMQuad` and similar in FF41-)
    return INCORRECT_TO_STRING || !!exec(constructorRegExp, inspectSource(argument));
  } catch (error) {
    return true;
  }
};

isConstructorLegacy.sham = true;

// `IsConstructor` abstract operation
// https://tc39.es/ecma262/#sec-isconstructor
module.exports = !construct || fails(function () {
  var called;
  return isConstructorModern(isConstructorModern.call)
    || !isConstructorModern(Object)
    || !isConstructorModern(function () { called = true; })
    || called;
}) ? isConstructorLegacy : isConstructorModern;


/***/ }),

/***/ "./node_modules/core-js/internals/is-forced.js":
/*!*****************************************************!*\
  !*** ./node_modules/core-js/internals/is-forced.js ***!
  \*****************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");
var isCallable = __webpack_require__(/*! ../internals/is-callable */ "./node_modules/core-js/internals/is-callable.js");

var replacement = /#|\.prototype\./;

var isForced = function (feature, detection) {
  var value = data[normalize(feature)];
  return value === POLYFILL ? true
    : value === NATIVE ? false
    : isCallable(detection) ? fails(detection)
    : !!detection;
};

var normalize = isForced.normalize = function (string) {
  return String(string).replace(replacement, '.').toLowerCase();
};

var data = isForced.data = {};
var NATIVE = isForced.NATIVE = 'N';
var POLYFILL = isForced.POLYFILL = 'P';

module.exports = isForced;


/***/ }),

/***/ "./node_modules/core-js/internals/is-null-or-undefined.js":
/*!****************************************************************!*\
  !*** ./node_modules/core-js/internals/is-null-or-undefined.js ***!
  \****************************************************************/
/***/ ((module) => {


// we can't use just `it == null` since of `document.all` special case
// https://tc39.es/ecma262/#sec-IsHTMLDDA-internal-slot-aec
module.exports = function (it) {
  return it === null || it === undefined;
};


/***/ }),

/***/ "./node_modules/core-js/internals/is-object.js":
/*!*****************************************************!*\
  !*** ./node_modules/core-js/internals/is-object.js ***!
  \*****************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var isCallable = __webpack_require__(/*! ../internals/is-callable */ "./node_modules/core-js/internals/is-callable.js");

module.exports = function (it) {
  return typeof it == 'object' ? it !== null : isCallable(it);
};


/***/ }),

/***/ "./node_modules/core-js/internals/is-possible-prototype.js":
/*!*****************************************************************!*\
  !*** ./node_modules/core-js/internals/is-possible-prototype.js ***!
  \*****************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var isObject = __webpack_require__(/*! ../internals/is-object */ "./node_modules/core-js/internals/is-object.js");

module.exports = function (argument) {
  return isObject(argument) || argument === null;
};


/***/ }),

/***/ "./node_modules/core-js/internals/is-pure.js":
/*!***************************************************!*\
  !*** ./node_modules/core-js/internals/is-pure.js ***!
  \***************************************************/
/***/ ((module) => {


module.exports = false;


/***/ }),

/***/ "./node_modules/core-js/internals/is-regexp.js":
/*!*****************************************************!*\
  !*** ./node_modules/core-js/internals/is-regexp.js ***!
  \*****************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var isObject = __webpack_require__(/*! ../internals/is-object */ "./node_modules/core-js/internals/is-object.js");
var classof = __webpack_require__(/*! ../internals/classof-raw */ "./node_modules/core-js/internals/classof-raw.js");
var wellKnownSymbol = __webpack_require__(/*! ../internals/well-known-symbol */ "./node_modules/core-js/internals/well-known-symbol.js");

var MATCH = wellKnownSymbol('match');

// `IsRegExp` abstract operation
// https://tc39.es/ecma262/#sec-isregexp
module.exports = function (it) {
  var isRegExp;
  return isObject(it) && ((isRegExp = it[MATCH]) !== undefined ? !!isRegExp : classof(it) === 'RegExp');
};


/***/ }),

/***/ "./node_modules/core-js/internals/is-symbol.js":
/*!*****************************************************!*\
  !*** ./node_modules/core-js/internals/is-symbol.js ***!
  \*****************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var getBuiltIn = __webpack_require__(/*! ../internals/get-built-in */ "./node_modules/core-js/internals/get-built-in.js");
var isCallable = __webpack_require__(/*! ../internals/is-callable */ "./node_modules/core-js/internals/is-callable.js");
var isPrototypeOf = __webpack_require__(/*! ../internals/object-is-prototype-of */ "./node_modules/core-js/internals/object-is-prototype-of.js");
var USE_SYMBOL_AS_UID = __webpack_require__(/*! ../internals/use-symbol-as-uid */ "./node_modules/core-js/internals/use-symbol-as-uid.js");

var $Object = Object;

module.exports = USE_SYMBOL_AS_UID ? function (it) {
  return typeof it == 'symbol';
} : function (it) {
  var $Symbol = getBuiltIn('Symbol');
  return isCallable($Symbol) && isPrototypeOf($Symbol.prototype, $Object(it));
};


/***/ }),

/***/ "./node_modules/core-js/internals/iterate.js":
/*!***************************************************!*\
  !*** ./node_modules/core-js/internals/iterate.js ***!
  \***************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var bind = __webpack_require__(/*! ../internals/function-bind-context */ "./node_modules/core-js/internals/function-bind-context.js");
var call = __webpack_require__(/*! ../internals/function-call */ "./node_modules/core-js/internals/function-call.js");
var anObject = __webpack_require__(/*! ../internals/an-object */ "./node_modules/core-js/internals/an-object.js");
var tryToString = __webpack_require__(/*! ../internals/try-to-string */ "./node_modules/core-js/internals/try-to-string.js");
var isArrayIteratorMethod = __webpack_require__(/*! ../internals/is-array-iterator-method */ "./node_modules/core-js/internals/is-array-iterator-method.js");
var lengthOfArrayLike = __webpack_require__(/*! ../internals/length-of-array-like */ "./node_modules/core-js/internals/length-of-array-like.js");
var isPrototypeOf = __webpack_require__(/*! ../internals/object-is-prototype-of */ "./node_modules/core-js/internals/object-is-prototype-of.js");
var getIterator = __webpack_require__(/*! ../internals/get-iterator */ "./node_modules/core-js/internals/get-iterator.js");
var getIteratorMethod = __webpack_require__(/*! ../internals/get-iterator-method */ "./node_modules/core-js/internals/get-iterator-method.js");
var iteratorClose = __webpack_require__(/*! ../internals/iterator-close */ "./node_modules/core-js/internals/iterator-close.js");

var $TypeError = TypeError;

var Result = function (stopped, result) {
  this.stopped = stopped;
  this.result = result;
};

var ResultPrototype = Result.prototype;

module.exports = function (iterable, unboundFunction, options) {
  var that = options && options.that;
  var AS_ENTRIES = !!(options && options.AS_ENTRIES);
  var IS_RECORD = !!(options && options.IS_RECORD);
  var IS_ITERATOR = !!(options && options.IS_ITERATOR);
  var INTERRUPTED = !!(options && options.INTERRUPTED);
  var fn = bind(unboundFunction, that);
  var iterator, iterFn, index, length, result, next, step;

  var stop = function (condition) {
    if (iterator) iteratorClose(iterator, 'normal');
    return new Result(true, condition);
  };

  var callFn = function (value) {
    if (AS_ENTRIES) {
      anObject(value);
      return INTERRUPTED ? fn(value[0], value[1], stop) : fn(value[0], value[1]);
    } return INTERRUPTED ? fn(value, stop) : fn(value);
  };

  if (IS_RECORD) {
    iterator = iterable.iterator;
  } else if (IS_ITERATOR) {
    iterator = iterable;
  } else {
    iterFn = getIteratorMethod(iterable);
    if (!iterFn) throw new $TypeError(tryToString(iterable) + ' is not iterable');
    // optimisation for array iterators
    if (isArrayIteratorMethod(iterFn)) {
      for (index = 0, length = lengthOfArrayLike(iterable); length > index; index++) {
        result = callFn(iterable[index]);
        if (result && isPrototypeOf(ResultPrototype, result)) return result;
      } return new Result(false);
    }
    iterator = getIterator(iterable, iterFn);
  }

  next = IS_RECORD ? iterable.next : iterator.next;
  while (!(step = call(next, iterator)).done) {
    try {
      result = callFn(step.value);
    } catch (error) {
      iteratorClose(iterator, 'throw', error);
    }
    if (typeof result == 'object' && result && isPrototypeOf(ResultPrototype, result)) return result;
  } return new Result(false);
};


/***/ }),

/***/ "./node_modules/core-js/internals/iterator-close.js":
/*!**********************************************************!*\
  !*** ./node_modules/core-js/internals/iterator-close.js ***!
  \**********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var call = __webpack_require__(/*! ../internals/function-call */ "./node_modules/core-js/internals/function-call.js");
var anObject = __webpack_require__(/*! ../internals/an-object */ "./node_modules/core-js/internals/an-object.js");
var getMethod = __webpack_require__(/*! ../internals/get-method */ "./node_modules/core-js/internals/get-method.js");

module.exports = function (iterator, kind, value) {
  var innerResult, innerError;
  anObject(iterator);
  try {
    innerResult = getMethod(iterator, 'return');
    if (!innerResult) {
      if (kind === 'throw') throw value;
      return value;
    }
    innerResult = call(innerResult, iterator);
  } catch (error) {
    innerError = true;
    innerResult = error;
  }
  if (kind === 'throw') throw value;
  if (innerError) throw innerResult;
  anObject(innerResult);
  return value;
};


/***/ }),

/***/ "./node_modules/core-js/internals/iterator-create-constructor.js":
/*!***********************************************************************!*\
  !*** ./node_modules/core-js/internals/iterator-create-constructor.js ***!
  \***********************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var IteratorPrototype = (__webpack_require__(/*! ../internals/iterators-core */ "./node_modules/core-js/internals/iterators-core.js").IteratorPrototype);
var create = __webpack_require__(/*! ../internals/object-create */ "./node_modules/core-js/internals/object-create.js");
var createPropertyDescriptor = __webpack_require__(/*! ../internals/create-property-descriptor */ "./node_modules/core-js/internals/create-property-descriptor.js");
var setToStringTag = __webpack_require__(/*! ../internals/set-to-string-tag */ "./node_modules/core-js/internals/set-to-string-tag.js");
var Iterators = __webpack_require__(/*! ../internals/iterators */ "./node_modules/core-js/internals/iterators.js");

var returnThis = function () { return this; };

module.exports = function (IteratorConstructor, NAME, next, ENUMERABLE_NEXT) {
  var TO_STRING_TAG = NAME + ' Iterator';
  IteratorConstructor.prototype = create(IteratorPrototype, { next: createPropertyDescriptor(+!ENUMERABLE_NEXT, next) });
  setToStringTag(IteratorConstructor, TO_STRING_TAG, false, true);
  Iterators[TO_STRING_TAG] = returnThis;
  return IteratorConstructor;
};


/***/ }),

/***/ "./node_modules/core-js/internals/iterator-define.js":
/*!***********************************************************!*\
  !*** ./node_modules/core-js/internals/iterator-define.js ***!
  \***********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");
var call = __webpack_require__(/*! ../internals/function-call */ "./node_modules/core-js/internals/function-call.js");
var IS_PURE = __webpack_require__(/*! ../internals/is-pure */ "./node_modules/core-js/internals/is-pure.js");
var FunctionName = __webpack_require__(/*! ../internals/function-name */ "./node_modules/core-js/internals/function-name.js");
var isCallable = __webpack_require__(/*! ../internals/is-callable */ "./node_modules/core-js/internals/is-callable.js");
var createIteratorConstructor = __webpack_require__(/*! ../internals/iterator-create-constructor */ "./node_modules/core-js/internals/iterator-create-constructor.js");
var getPrototypeOf = __webpack_require__(/*! ../internals/object-get-prototype-of */ "./node_modules/core-js/internals/object-get-prototype-of.js");
var setPrototypeOf = __webpack_require__(/*! ../internals/object-set-prototype-of */ "./node_modules/core-js/internals/object-set-prototype-of.js");
var setToStringTag = __webpack_require__(/*! ../internals/set-to-string-tag */ "./node_modules/core-js/internals/set-to-string-tag.js");
var createNonEnumerableProperty = __webpack_require__(/*! ../internals/create-non-enumerable-property */ "./node_modules/core-js/internals/create-non-enumerable-property.js");
var defineBuiltIn = __webpack_require__(/*! ../internals/define-built-in */ "./node_modules/core-js/internals/define-built-in.js");
var wellKnownSymbol = __webpack_require__(/*! ../internals/well-known-symbol */ "./node_modules/core-js/internals/well-known-symbol.js");
var Iterators = __webpack_require__(/*! ../internals/iterators */ "./node_modules/core-js/internals/iterators.js");
var IteratorsCore = __webpack_require__(/*! ../internals/iterators-core */ "./node_modules/core-js/internals/iterators-core.js");

var PROPER_FUNCTION_NAME = FunctionName.PROPER;
var CONFIGURABLE_FUNCTION_NAME = FunctionName.CONFIGURABLE;
var IteratorPrototype = IteratorsCore.IteratorPrototype;
var BUGGY_SAFARI_ITERATORS = IteratorsCore.BUGGY_SAFARI_ITERATORS;
var ITERATOR = wellKnownSymbol('iterator');
var KEYS = 'keys';
var VALUES = 'values';
var ENTRIES = 'entries';

var returnThis = function () { return this; };

module.exports = function (Iterable, NAME, IteratorConstructor, next, DEFAULT, IS_SET, FORCED) {
  createIteratorConstructor(IteratorConstructor, NAME, next);

  var getIterationMethod = function (KIND) {
    if (KIND === DEFAULT && defaultIterator) return defaultIterator;
    if (!BUGGY_SAFARI_ITERATORS && KIND && KIND in IterablePrototype) return IterablePrototype[KIND];

    switch (KIND) {
      case KEYS: return function keys() { return new IteratorConstructor(this, KIND); };
      case VALUES: return function values() { return new IteratorConstructor(this, KIND); };
      case ENTRIES: return function entries() { return new IteratorConstructor(this, KIND); };
    }

    return function () { return new IteratorConstructor(this); };
  };

  var TO_STRING_TAG = NAME + ' Iterator';
  var INCORRECT_VALUES_NAME = false;
  var IterablePrototype = Iterable.prototype;
  var nativeIterator = IterablePrototype[ITERATOR]
    || IterablePrototype['@@iterator']
    || DEFAULT && IterablePrototype[DEFAULT];
  var defaultIterator = !BUGGY_SAFARI_ITERATORS && nativeIterator || getIterationMethod(DEFAULT);
  var anyNativeIterator = NAME === 'Array' ? IterablePrototype.entries || nativeIterator : nativeIterator;
  var CurrentIteratorPrototype, methods, KEY;

  // fix native
  if (anyNativeIterator) {
    CurrentIteratorPrototype = getPrototypeOf(anyNativeIterator.call(new Iterable()));
    if (CurrentIteratorPrototype !== Object.prototype && CurrentIteratorPrototype.next) {
      if (!IS_PURE && getPrototypeOf(CurrentIteratorPrototype) !== IteratorPrototype) {
        if (setPrototypeOf) {
          setPrototypeOf(CurrentIteratorPrototype, IteratorPrototype);
        } else if (!isCallable(CurrentIteratorPrototype[ITERATOR])) {
          defineBuiltIn(CurrentIteratorPrototype, ITERATOR, returnThis);
        }
      }
      // Set @@toStringTag to native iterators
      setToStringTag(CurrentIteratorPrototype, TO_STRING_TAG, true, true);
      if (IS_PURE) Iterators[TO_STRING_TAG] = returnThis;
    }
  }

  // fix Array.prototype.{ values, @@iterator }.name in V8 / FF
  if (PROPER_FUNCTION_NAME && DEFAULT === VALUES && nativeIterator && nativeIterator.name !== VALUES) {
    if (!IS_PURE && CONFIGURABLE_FUNCTION_NAME) {
      createNonEnumerableProperty(IterablePrototype, 'name', VALUES);
    } else {
      INCORRECT_VALUES_NAME = true;
      defaultIterator = function values() { return call(nativeIterator, this); };
    }
  }

  // export additional methods
  if (DEFAULT) {
    methods = {
      values: getIterationMethod(VALUES),
      keys: IS_SET ? defaultIterator : getIterationMethod(KEYS),
      entries: getIterationMethod(ENTRIES)
    };
    if (FORCED) for (KEY in methods) {
      if (BUGGY_SAFARI_ITERATORS || INCORRECT_VALUES_NAME || !(KEY in IterablePrototype)) {
        defineBuiltIn(IterablePrototype, KEY, methods[KEY]);
      }
    } else $({ target: NAME, proto: true, forced: BUGGY_SAFARI_ITERATORS || INCORRECT_VALUES_NAME }, methods);
  }

  // define iterator
  if ((!IS_PURE || FORCED) && IterablePrototype[ITERATOR] !== defaultIterator) {
    defineBuiltIn(IterablePrototype, ITERATOR, defaultIterator, { name: DEFAULT });
  }
  Iterators[NAME] = defaultIterator;

  return methods;
};


/***/ }),

/***/ "./node_modules/core-js/internals/iterators-core.js":
/*!**********************************************************!*\
  !*** ./node_modules/core-js/internals/iterators-core.js ***!
  \**********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");
var isCallable = __webpack_require__(/*! ../internals/is-callable */ "./node_modules/core-js/internals/is-callable.js");
var isObject = __webpack_require__(/*! ../internals/is-object */ "./node_modules/core-js/internals/is-object.js");
var create = __webpack_require__(/*! ../internals/object-create */ "./node_modules/core-js/internals/object-create.js");
var getPrototypeOf = __webpack_require__(/*! ../internals/object-get-prototype-of */ "./node_modules/core-js/internals/object-get-prototype-of.js");
var defineBuiltIn = __webpack_require__(/*! ../internals/define-built-in */ "./node_modules/core-js/internals/define-built-in.js");
var wellKnownSymbol = __webpack_require__(/*! ../internals/well-known-symbol */ "./node_modules/core-js/internals/well-known-symbol.js");
var IS_PURE = __webpack_require__(/*! ../internals/is-pure */ "./node_modules/core-js/internals/is-pure.js");

var ITERATOR = wellKnownSymbol('iterator');
var BUGGY_SAFARI_ITERATORS = false;

// `%IteratorPrototype%` object
// https://tc39.es/ecma262/#sec-%iteratorprototype%-object
var IteratorPrototype, PrototypeOfArrayIteratorPrototype, arrayIterator;

/* eslint-disable es/no-array-prototype-keys -- safe */
if ([].keys) {
  arrayIterator = [].keys();
  // Safari 8 has buggy iterators w/o `next`
  if (!('next' in arrayIterator)) BUGGY_SAFARI_ITERATORS = true;
  else {
    PrototypeOfArrayIteratorPrototype = getPrototypeOf(getPrototypeOf(arrayIterator));
    if (PrototypeOfArrayIteratorPrototype !== Object.prototype) IteratorPrototype = PrototypeOfArrayIteratorPrototype;
  }
}

var NEW_ITERATOR_PROTOTYPE = !isObject(IteratorPrototype) || fails(function () {
  var test = {};
  // FF44- legacy iterators case
  return IteratorPrototype[ITERATOR].call(test) !== test;
});

if (NEW_ITERATOR_PROTOTYPE) IteratorPrototype = {};
else if (IS_PURE) IteratorPrototype = create(IteratorPrototype);

// `%IteratorPrototype%[@@iterator]()` method
// https://tc39.es/ecma262/#sec-%iteratorprototype%-@@iterator
if (!isCallable(IteratorPrototype[ITERATOR])) {
  defineBuiltIn(IteratorPrototype, ITERATOR, function () {
    return this;
  });
}

module.exports = {
  IteratorPrototype: IteratorPrototype,
  BUGGY_SAFARI_ITERATORS: BUGGY_SAFARI_ITERATORS
};


/***/ }),

/***/ "./node_modules/core-js/internals/iterators.js":
/*!*****************************************************!*\
  !*** ./node_modules/core-js/internals/iterators.js ***!
  \*****************************************************/
/***/ ((module) => {


module.exports = {};


/***/ }),

/***/ "./node_modules/core-js/internals/length-of-array-like.js":
/*!****************************************************************!*\
  !*** ./node_modules/core-js/internals/length-of-array-like.js ***!
  \****************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var toLength = __webpack_require__(/*! ../internals/to-length */ "./node_modules/core-js/internals/to-length.js");

// `LengthOfArrayLike` abstract operation
// https://tc39.es/ecma262/#sec-lengthofarraylike
module.exports = function (obj) {
  return toLength(obj.length);
};


/***/ }),

/***/ "./node_modules/core-js/internals/make-built-in.js":
/*!*********************************************************!*\
  !*** ./node_modules/core-js/internals/make-built-in.js ***!
  \*********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this */ "./node_modules/core-js/internals/function-uncurry-this.js");
var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");
var isCallable = __webpack_require__(/*! ../internals/is-callable */ "./node_modules/core-js/internals/is-callable.js");
var hasOwn = __webpack_require__(/*! ../internals/has-own-property */ "./node_modules/core-js/internals/has-own-property.js");
var DESCRIPTORS = __webpack_require__(/*! ../internals/descriptors */ "./node_modules/core-js/internals/descriptors.js");
var CONFIGURABLE_FUNCTION_NAME = (__webpack_require__(/*! ../internals/function-name */ "./node_modules/core-js/internals/function-name.js").CONFIGURABLE);
var inspectSource = __webpack_require__(/*! ../internals/inspect-source */ "./node_modules/core-js/internals/inspect-source.js");
var InternalStateModule = __webpack_require__(/*! ../internals/internal-state */ "./node_modules/core-js/internals/internal-state.js");

var enforceInternalState = InternalStateModule.enforce;
var getInternalState = InternalStateModule.get;
var $String = String;
// eslint-disable-next-line es/no-object-defineproperty -- safe
var defineProperty = Object.defineProperty;
var stringSlice = uncurryThis(''.slice);
var replace = uncurryThis(''.replace);
var join = uncurryThis([].join);

var CONFIGURABLE_LENGTH = DESCRIPTORS && !fails(function () {
  return defineProperty(function () { /* empty */ }, 'length', { value: 8 }).length !== 8;
});

var TEMPLATE = String(String).split('String');

var makeBuiltIn = module.exports = function (value, name, options) {
  if (stringSlice($String(name), 0, 7) === 'Symbol(') {
    name = '[' + replace($String(name), /^Symbol\(([^)]*)\).*$/, '$1') + ']';
  }
  if (options && options.getter) name = 'get ' + name;
  if (options && options.setter) name = 'set ' + name;
  if (!hasOwn(value, 'name') || (CONFIGURABLE_FUNCTION_NAME && value.name !== name)) {
    if (DESCRIPTORS) defineProperty(value, 'name', { value: name, configurable: true });
    else value.name = name;
  }
  if (CONFIGURABLE_LENGTH && options && hasOwn(options, 'arity') && value.length !== options.arity) {
    defineProperty(value, 'length', { value: options.arity });
  }
  try {
    if (options && hasOwn(options, 'constructor') && options.constructor) {
      if (DESCRIPTORS) defineProperty(value, 'prototype', { writable: false });
    // in V8 ~ Chrome 53, prototypes of some methods, like `Array.prototype.values`, are non-writable
    } else if (value.prototype) value.prototype = undefined;
  } catch (error) { /* empty */ }
  var state = enforceInternalState(value);
  if (!hasOwn(state, 'source')) {
    state.source = join(TEMPLATE, typeof name == 'string' ? name : '');
  } return value;
};

// add fake Function#toString for correct work wrapped methods / constructors with methods like LoDash isNative
// eslint-disable-next-line no-extend-native -- required
Function.prototype.toString = makeBuiltIn(function toString() {
  return isCallable(this) && getInternalState(this).source || inspectSource(this);
}, 'toString');


/***/ }),

/***/ "./node_modules/core-js/internals/math-trunc.js":
/*!******************************************************!*\
  !*** ./node_modules/core-js/internals/math-trunc.js ***!
  \******************************************************/
/***/ ((module) => {


var ceil = Math.ceil;
var floor = Math.floor;

// `Math.trunc` method
// https://tc39.es/ecma262/#sec-math.trunc
// eslint-disable-next-line es/no-math-trunc -- safe
module.exports = Math.trunc || function trunc(x) {
  var n = +x;
  return (n > 0 ? floor : ceil)(n);
};


/***/ }),

/***/ "./node_modules/core-js/internals/microtask.js":
/*!*****************************************************!*\
  !*** ./node_modules/core-js/internals/microtask.js ***!
  \*****************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var globalThis = __webpack_require__(/*! ../internals/global-this */ "./node_modules/core-js/internals/global-this.js");
var safeGetBuiltIn = __webpack_require__(/*! ../internals/safe-get-built-in */ "./node_modules/core-js/internals/safe-get-built-in.js");
var bind = __webpack_require__(/*! ../internals/function-bind-context */ "./node_modules/core-js/internals/function-bind-context.js");
var macrotask = (__webpack_require__(/*! ../internals/task */ "./node_modules/core-js/internals/task.js").set);
var Queue = __webpack_require__(/*! ../internals/queue */ "./node_modules/core-js/internals/queue.js");
var IS_IOS = __webpack_require__(/*! ../internals/environment-is-ios */ "./node_modules/core-js/internals/environment-is-ios.js");
var IS_IOS_PEBBLE = __webpack_require__(/*! ../internals/environment-is-ios-pebble */ "./node_modules/core-js/internals/environment-is-ios-pebble.js");
var IS_WEBOS_WEBKIT = __webpack_require__(/*! ../internals/environment-is-webos-webkit */ "./node_modules/core-js/internals/environment-is-webos-webkit.js");
var IS_NODE = __webpack_require__(/*! ../internals/environment-is-node */ "./node_modules/core-js/internals/environment-is-node.js");

var MutationObserver = globalThis.MutationObserver || globalThis.WebKitMutationObserver;
var document = globalThis.document;
var process = globalThis.process;
var Promise = globalThis.Promise;
var microtask = safeGetBuiltIn('queueMicrotask');
var notify, toggle, node, promise, then;

// modern engines have queueMicrotask method
if (!microtask) {
  var queue = new Queue();

  var flush = function () {
    var parent, fn;
    if (IS_NODE && (parent = process.domain)) parent.exit();
    while (fn = queue.get()) try {
      fn();
    } catch (error) {
      if (queue.head) notify();
      throw error;
    }
    if (parent) parent.enter();
  };

  // browsers with MutationObserver, except iOS - https://github.com/zloirock/core-js/issues/339
  // also except WebOS Webkit https://github.com/zloirock/core-js/issues/898
  if (!IS_IOS && !IS_NODE && !IS_WEBOS_WEBKIT && MutationObserver && document) {
    toggle = true;
    node = document.createTextNode('');
    new MutationObserver(flush).observe(node, { characterData: true });
    notify = function () {
      node.data = toggle = !toggle;
    };
  // environments with maybe non-completely correct, but existent Promise
  } else if (!IS_IOS_PEBBLE && Promise && Promise.resolve) {
    // Promise.resolve without an argument throws an error in LG WebOS 2
    promise = Promise.resolve(undefined);
    // workaround of WebKit ~ iOS Safari 10.1 bug
    promise.constructor = Promise;
    then = bind(promise.then, promise);
    notify = function () {
      then(flush);
    };
  // Node.js without promises
  } else if (IS_NODE) {
    notify = function () {
      process.nextTick(flush);
    };
  // for other environments - macrotask based on:
  // - setImmediate
  // - MessageChannel
  // - window.postMessage
  // - onreadystatechange
  // - setTimeout
  } else {
    // `webpack` dev server bug on IE global methods - use bind(fn, global)
    macrotask = bind(macrotask, globalThis);
    notify = function () {
      macrotask(flush);
    };
  }

  microtask = function (fn) {
    if (!queue.head) notify();
    queue.add(fn);
  };
}

module.exports = microtask;


/***/ }),

/***/ "./node_modules/core-js/internals/new-promise-capability.js":
/*!******************************************************************!*\
  !*** ./node_modules/core-js/internals/new-promise-capability.js ***!
  \******************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var aCallable = __webpack_require__(/*! ../internals/a-callable */ "./node_modules/core-js/internals/a-callable.js");

var $TypeError = TypeError;

var PromiseCapability = function (C) {
  var resolve, reject;
  this.promise = new C(function ($$resolve, $$reject) {
    if (resolve !== undefined || reject !== undefined) throw new $TypeError('Bad Promise constructor');
    resolve = $$resolve;
    reject = $$reject;
  });
  this.resolve = aCallable(resolve);
  this.reject = aCallable(reject);
};

// `NewPromiseCapability` abstract operation
// https://tc39.es/ecma262/#sec-newpromisecapability
module.exports.f = function (C) {
  return new PromiseCapability(C);
};


/***/ }),

/***/ "./node_modules/core-js/internals/not-a-regexp.js":
/*!********************************************************!*\
  !*** ./node_modules/core-js/internals/not-a-regexp.js ***!
  \********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var isRegExp = __webpack_require__(/*! ../internals/is-regexp */ "./node_modules/core-js/internals/is-regexp.js");

var $TypeError = TypeError;

module.exports = function (it) {
  if (isRegExp(it)) {
    throw new $TypeError("The method doesn't accept regular expressions");
  } return it;
};


/***/ }),

/***/ "./node_modules/core-js/internals/object-create.js":
/*!*********************************************************!*\
  !*** ./node_modules/core-js/internals/object-create.js ***!
  \*********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


/* global ActiveXObject -- old IE, WSH */
var anObject = __webpack_require__(/*! ../internals/an-object */ "./node_modules/core-js/internals/an-object.js");
var definePropertiesModule = __webpack_require__(/*! ../internals/object-define-properties */ "./node_modules/core-js/internals/object-define-properties.js");
var enumBugKeys = __webpack_require__(/*! ../internals/enum-bug-keys */ "./node_modules/core-js/internals/enum-bug-keys.js");
var hiddenKeys = __webpack_require__(/*! ../internals/hidden-keys */ "./node_modules/core-js/internals/hidden-keys.js");
var html = __webpack_require__(/*! ../internals/html */ "./node_modules/core-js/internals/html.js");
var documentCreateElement = __webpack_require__(/*! ../internals/document-create-element */ "./node_modules/core-js/internals/document-create-element.js");
var sharedKey = __webpack_require__(/*! ../internals/shared-key */ "./node_modules/core-js/internals/shared-key.js");

var GT = '>';
var LT = '<';
var PROTOTYPE = 'prototype';
var SCRIPT = 'script';
var IE_PROTO = sharedKey('IE_PROTO');

var EmptyConstructor = function () { /* empty */ };

var scriptTag = function (content) {
  return LT + SCRIPT + GT + content + LT + '/' + SCRIPT + GT;
};

// Create object with fake `null` prototype: use ActiveX Object with cleared prototype
var NullProtoObjectViaActiveX = function (activeXDocument) {
  activeXDocument.write(scriptTag(''));
  activeXDocument.close();
  var temp = activeXDocument.parentWindow.Object;
  // eslint-disable-next-line no-useless-assignment -- avoid memory leak
  activeXDocument = null;
  return temp;
};

// Create object with fake `null` prototype: use iframe Object with cleared prototype
var NullProtoObjectViaIFrame = function () {
  // Thrash, waste and sodomy: IE GC bug
  var iframe = documentCreateElement('iframe');
  var JS = 'java' + SCRIPT + ':';
  var iframeDocument;
  iframe.style.display = 'none';
  html.appendChild(iframe);
  // https://github.com/zloirock/core-js/issues/475
  iframe.src = String(JS);
  iframeDocument = iframe.contentWindow.document;
  iframeDocument.open();
  iframeDocument.write(scriptTag('document.F=Object'));
  iframeDocument.close();
  return iframeDocument.F;
};

// Check for document.domain and active x support
// No need to use active x approach when document.domain is not set
// see https://github.com/es-shims/es5-shim/issues/150
// variation of https://github.com/kitcambridge/es5-shim/commit/4f738ac066346
// avoid IE GC bug
var activeXDocument;
var NullProtoObject = function () {
  try {
    activeXDocument = new ActiveXObject('htmlfile');
  } catch (error) { /* ignore */ }
  NullProtoObject = typeof document != 'undefined'
    ? document.domain && activeXDocument
      ? NullProtoObjectViaActiveX(activeXDocument) // old IE
      : NullProtoObjectViaIFrame()
    : NullProtoObjectViaActiveX(activeXDocument); // WSH
  var length = enumBugKeys.length;
  while (length--) delete NullProtoObject[PROTOTYPE][enumBugKeys[length]];
  return NullProtoObject();
};

hiddenKeys[IE_PROTO] = true;

// `Object.create` method
// https://tc39.es/ecma262/#sec-object.create
// eslint-disable-next-line es/no-object-create -- safe
module.exports = Object.create || function create(O, Properties) {
  var result;
  if (O !== null) {
    EmptyConstructor[PROTOTYPE] = anObject(O);
    result = new EmptyConstructor();
    EmptyConstructor[PROTOTYPE] = null;
    // add "__proto__" for Object.getPrototypeOf polyfill
    result[IE_PROTO] = O;
  } else result = NullProtoObject();
  return Properties === undefined ? result : definePropertiesModule.f(result, Properties);
};


/***/ }),

/***/ "./node_modules/core-js/internals/object-define-properties.js":
/*!********************************************************************!*\
  !*** ./node_modules/core-js/internals/object-define-properties.js ***!
  \********************************************************************/
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


var DESCRIPTORS = __webpack_require__(/*! ../internals/descriptors */ "./node_modules/core-js/internals/descriptors.js");
var V8_PROTOTYPE_DEFINE_BUG = __webpack_require__(/*! ../internals/v8-prototype-define-bug */ "./node_modules/core-js/internals/v8-prototype-define-bug.js");
var definePropertyModule = __webpack_require__(/*! ../internals/object-define-property */ "./node_modules/core-js/internals/object-define-property.js");
var anObject = __webpack_require__(/*! ../internals/an-object */ "./node_modules/core-js/internals/an-object.js");
var toIndexedObject = __webpack_require__(/*! ../internals/to-indexed-object */ "./node_modules/core-js/internals/to-indexed-object.js");
var objectKeys = __webpack_require__(/*! ../internals/object-keys */ "./node_modules/core-js/internals/object-keys.js");

// `Object.defineProperties` method
// https://tc39.es/ecma262/#sec-object.defineproperties
// eslint-disable-next-line es/no-object-defineproperties -- safe
exports.f = DESCRIPTORS && !V8_PROTOTYPE_DEFINE_BUG ? Object.defineProperties : function defineProperties(O, Properties) {
  anObject(O);
  var props = toIndexedObject(Properties);
  var keys = objectKeys(Properties);
  var length = keys.length;
  var index = 0;
  var key;
  while (length > index) definePropertyModule.f(O, key = keys[index++], props[key]);
  return O;
};


/***/ }),

/***/ "./node_modules/core-js/internals/object-define-property.js":
/*!******************************************************************!*\
  !*** ./node_modules/core-js/internals/object-define-property.js ***!
  \******************************************************************/
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


var DESCRIPTORS = __webpack_require__(/*! ../internals/descriptors */ "./node_modules/core-js/internals/descriptors.js");
var IE8_DOM_DEFINE = __webpack_require__(/*! ../internals/ie8-dom-define */ "./node_modules/core-js/internals/ie8-dom-define.js");
var V8_PROTOTYPE_DEFINE_BUG = __webpack_require__(/*! ../internals/v8-prototype-define-bug */ "./node_modules/core-js/internals/v8-prototype-define-bug.js");
var anObject = __webpack_require__(/*! ../internals/an-object */ "./node_modules/core-js/internals/an-object.js");
var toPropertyKey = __webpack_require__(/*! ../internals/to-property-key */ "./node_modules/core-js/internals/to-property-key.js");

var $TypeError = TypeError;
// eslint-disable-next-line es/no-object-defineproperty -- safe
var $defineProperty = Object.defineProperty;
// eslint-disable-next-line es/no-object-getownpropertydescriptor -- safe
var $getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;
var ENUMERABLE = 'enumerable';
var CONFIGURABLE = 'configurable';
var WRITABLE = 'writable';

// `Object.defineProperty` method
// https://tc39.es/ecma262/#sec-object.defineproperty
exports.f = DESCRIPTORS ? V8_PROTOTYPE_DEFINE_BUG ? function defineProperty(O, P, Attributes) {
  anObject(O);
  P = toPropertyKey(P);
  anObject(Attributes);
  if (typeof O === 'function' && P === 'prototype' && 'value' in Attributes && WRITABLE in Attributes && !Attributes[WRITABLE]) {
    var current = $getOwnPropertyDescriptor(O, P);
    if (current && current[WRITABLE]) {
      O[P] = Attributes.value;
      Attributes = {
        configurable: CONFIGURABLE in Attributes ? Attributes[CONFIGURABLE] : current[CONFIGURABLE],
        enumerable: ENUMERABLE in Attributes ? Attributes[ENUMERABLE] : current[ENUMERABLE],
        writable: false
      };
    }
  } return $defineProperty(O, P, Attributes);
} : $defineProperty : function defineProperty(O, P, Attributes) {
  anObject(O);
  P = toPropertyKey(P);
  anObject(Attributes);
  if (IE8_DOM_DEFINE) try {
    return $defineProperty(O, P, Attributes);
  } catch (error) { /* empty */ }
  if ('get' in Attributes || 'set' in Attributes) throw new $TypeError('Accessors not supported');
  if ('value' in Attributes) O[P] = Attributes.value;
  return O;
};


/***/ }),

/***/ "./node_modules/core-js/internals/object-get-own-property-descriptor.js":
/*!******************************************************************************!*\
  !*** ./node_modules/core-js/internals/object-get-own-property-descriptor.js ***!
  \******************************************************************************/
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


var DESCRIPTORS = __webpack_require__(/*! ../internals/descriptors */ "./node_modules/core-js/internals/descriptors.js");
var call = __webpack_require__(/*! ../internals/function-call */ "./node_modules/core-js/internals/function-call.js");
var propertyIsEnumerableModule = __webpack_require__(/*! ../internals/object-property-is-enumerable */ "./node_modules/core-js/internals/object-property-is-enumerable.js");
var createPropertyDescriptor = __webpack_require__(/*! ../internals/create-property-descriptor */ "./node_modules/core-js/internals/create-property-descriptor.js");
var toIndexedObject = __webpack_require__(/*! ../internals/to-indexed-object */ "./node_modules/core-js/internals/to-indexed-object.js");
var toPropertyKey = __webpack_require__(/*! ../internals/to-property-key */ "./node_modules/core-js/internals/to-property-key.js");
var hasOwn = __webpack_require__(/*! ../internals/has-own-property */ "./node_modules/core-js/internals/has-own-property.js");
var IE8_DOM_DEFINE = __webpack_require__(/*! ../internals/ie8-dom-define */ "./node_modules/core-js/internals/ie8-dom-define.js");

// eslint-disable-next-line es/no-object-getownpropertydescriptor -- safe
var $getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;

// `Object.getOwnPropertyDescriptor` method
// https://tc39.es/ecma262/#sec-object.getownpropertydescriptor
exports.f = DESCRIPTORS ? $getOwnPropertyDescriptor : function getOwnPropertyDescriptor(O, P) {
  O = toIndexedObject(O);
  P = toPropertyKey(P);
  if (IE8_DOM_DEFINE) try {
    return $getOwnPropertyDescriptor(O, P);
  } catch (error) { /* empty */ }
  if (hasOwn(O, P)) return createPropertyDescriptor(!call(propertyIsEnumerableModule.f, O, P), O[P]);
};


/***/ }),

/***/ "./node_modules/core-js/internals/object-get-own-property-names-external.js":
/*!**********************************************************************************!*\
  !*** ./node_modules/core-js/internals/object-get-own-property-names-external.js ***!
  \**********************************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


/* eslint-disable es/no-object-getownpropertynames -- safe */
var classof = __webpack_require__(/*! ../internals/classof-raw */ "./node_modules/core-js/internals/classof-raw.js");
var toIndexedObject = __webpack_require__(/*! ../internals/to-indexed-object */ "./node_modules/core-js/internals/to-indexed-object.js");
var $getOwnPropertyNames = (__webpack_require__(/*! ../internals/object-get-own-property-names */ "./node_modules/core-js/internals/object-get-own-property-names.js").f);
var arraySlice = __webpack_require__(/*! ../internals/array-slice */ "./node_modules/core-js/internals/array-slice.js");

var windowNames = typeof window == 'object' && window && Object.getOwnPropertyNames
  ? Object.getOwnPropertyNames(window) : [];

var getWindowNames = function (it) {
  try {
    return $getOwnPropertyNames(it);
  } catch (error) {
    return arraySlice(windowNames);
  }
};

// fallback for IE11 buggy Object.getOwnPropertyNames with iframe and window
module.exports.f = function getOwnPropertyNames(it) {
  return windowNames && classof(it) === 'Window'
    ? getWindowNames(it)
    : $getOwnPropertyNames(toIndexedObject(it));
};


/***/ }),

/***/ "./node_modules/core-js/internals/object-get-own-property-names.js":
/*!*************************************************************************!*\
  !*** ./node_modules/core-js/internals/object-get-own-property-names.js ***!
  \*************************************************************************/
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


var internalObjectKeys = __webpack_require__(/*! ../internals/object-keys-internal */ "./node_modules/core-js/internals/object-keys-internal.js");
var enumBugKeys = __webpack_require__(/*! ../internals/enum-bug-keys */ "./node_modules/core-js/internals/enum-bug-keys.js");

var hiddenKeys = enumBugKeys.concat('length', 'prototype');

// `Object.getOwnPropertyNames` method
// https://tc39.es/ecma262/#sec-object.getownpropertynames
// eslint-disable-next-line es/no-object-getownpropertynames -- safe
exports.f = Object.getOwnPropertyNames || function getOwnPropertyNames(O) {
  return internalObjectKeys(O, hiddenKeys);
};


/***/ }),

/***/ "./node_modules/core-js/internals/object-get-own-property-symbols.js":
/*!***************************************************************************!*\
  !*** ./node_modules/core-js/internals/object-get-own-property-symbols.js ***!
  \***************************************************************************/
/***/ ((__unused_webpack_module, exports) => {


// eslint-disable-next-line es/no-object-getownpropertysymbols -- safe
exports.f = Object.getOwnPropertySymbols;


/***/ }),

/***/ "./node_modules/core-js/internals/object-get-prototype-of.js":
/*!*******************************************************************!*\
  !*** ./node_modules/core-js/internals/object-get-prototype-of.js ***!
  \*******************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var hasOwn = __webpack_require__(/*! ../internals/has-own-property */ "./node_modules/core-js/internals/has-own-property.js");
var isCallable = __webpack_require__(/*! ../internals/is-callable */ "./node_modules/core-js/internals/is-callable.js");
var toObject = __webpack_require__(/*! ../internals/to-object */ "./node_modules/core-js/internals/to-object.js");
var sharedKey = __webpack_require__(/*! ../internals/shared-key */ "./node_modules/core-js/internals/shared-key.js");
var CORRECT_PROTOTYPE_GETTER = __webpack_require__(/*! ../internals/correct-prototype-getter */ "./node_modules/core-js/internals/correct-prototype-getter.js");

var IE_PROTO = sharedKey('IE_PROTO');
var $Object = Object;
var ObjectPrototype = $Object.prototype;

// `Object.getPrototypeOf` method
// https://tc39.es/ecma262/#sec-object.getprototypeof
// eslint-disable-next-line es/no-object-getprototypeof -- safe
module.exports = CORRECT_PROTOTYPE_GETTER ? $Object.getPrototypeOf : function (O) {
  var object = toObject(O);
  if (hasOwn(object, IE_PROTO)) return object[IE_PROTO];
  var constructor = object.constructor;
  if (isCallable(constructor) && object instanceof constructor) {
    return constructor.prototype;
  } return object instanceof $Object ? ObjectPrototype : null;
};


/***/ }),

/***/ "./node_modules/core-js/internals/object-is-extensible.js":
/*!****************************************************************!*\
  !*** ./node_modules/core-js/internals/object-is-extensible.js ***!
  \****************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");
var isObject = __webpack_require__(/*! ../internals/is-object */ "./node_modules/core-js/internals/is-object.js");
var classof = __webpack_require__(/*! ../internals/classof-raw */ "./node_modules/core-js/internals/classof-raw.js");
var ARRAY_BUFFER_NON_EXTENSIBLE = __webpack_require__(/*! ../internals/array-buffer-non-extensible */ "./node_modules/core-js/internals/array-buffer-non-extensible.js");

// eslint-disable-next-line es/no-object-isextensible -- safe
var $isExtensible = Object.isExtensible;
var FAILS_ON_PRIMITIVES = fails(function () { $isExtensible(1); });

// `Object.isExtensible` method
// https://tc39.es/ecma262/#sec-object.isextensible
module.exports = (FAILS_ON_PRIMITIVES || ARRAY_BUFFER_NON_EXTENSIBLE) ? function isExtensible(it) {
  if (!isObject(it)) return false;
  if (ARRAY_BUFFER_NON_EXTENSIBLE && classof(it) === 'ArrayBuffer') return false;
  return $isExtensible ? $isExtensible(it) : true;
} : $isExtensible;


/***/ }),

/***/ "./node_modules/core-js/internals/object-is-prototype-of.js":
/*!******************************************************************!*\
  !*** ./node_modules/core-js/internals/object-is-prototype-of.js ***!
  \******************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this */ "./node_modules/core-js/internals/function-uncurry-this.js");

module.exports = uncurryThis({}.isPrototypeOf);


/***/ }),

/***/ "./node_modules/core-js/internals/object-keys-internal.js":
/*!****************************************************************!*\
  !*** ./node_modules/core-js/internals/object-keys-internal.js ***!
  \****************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this */ "./node_modules/core-js/internals/function-uncurry-this.js");
var hasOwn = __webpack_require__(/*! ../internals/has-own-property */ "./node_modules/core-js/internals/has-own-property.js");
var toIndexedObject = __webpack_require__(/*! ../internals/to-indexed-object */ "./node_modules/core-js/internals/to-indexed-object.js");
var indexOf = (__webpack_require__(/*! ../internals/array-includes */ "./node_modules/core-js/internals/array-includes.js").indexOf);
var hiddenKeys = __webpack_require__(/*! ../internals/hidden-keys */ "./node_modules/core-js/internals/hidden-keys.js");

var push = uncurryThis([].push);

module.exports = function (object, names) {
  var O = toIndexedObject(object);
  var i = 0;
  var result = [];
  var key;
  for (key in O) !hasOwn(hiddenKeys, key) && hasOwn(O, key) && push(result, key);
  // Don't enum bug & hidden keys
  while (names.length > i) if (hasOwn(O, key = names[i++])) {
    ~indexOf(result, key) || push(result, key);
  }
  return result;
};


/***/ }),

/***/ "./node_modules/core-js/internals/object-keys.js":
/*!*******************************************************!*\
  !*** ./node_modules/core-js/internals/object-keys.js ***!
  \*******************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var internalObjectKeys = __webpack_require__(/*! ../internals/object-keys-internal */ "./node_modules/core-js/internals/object-keys-internal.js");
var enumBugKeys = __webpack_require__(/*! ../internals/enum-bug-keys */ "./node_modules/core-js/internals/enum-bug-keys.js");

// `Object.keys` method
// https://tc39.es/ecma262/#sec-object.keys
// eslint-disable-next-line es/no-object-keys -- safe
module.exports = Object.keys || function keys(O) {
  return internalObjectKeys(O, enumBugKeys);
};


/***/ }),

/***/ "./node_modules/core-js/internals/object-property-is-enumerable.js":
/*!*************************************************************************!*\
  !*** ./node_modules/core-js/internals/object-property-is-enumerable.js ***!
  \*************************************************************************/
/***/ ((__unused_webpack_module, exports) => {


var $propertyIsEnumerable = {}.propertyIsEnumerable;
// eslint-disable-next-line es/no-object-getownpropertydescriptor -- safe
var getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;

// Nashorn ~ JDK8 bug
var NASHORN_BUG = getOwnPropertyDescriptor && !$propertyIsEnumerable.call({ 1: 2 }, 1);

// `Object.prototype.propertyIsEnumerable` method implementation
// https://tc39.es/ecma262/#sec-object.prototype.propertyisenumerable
exports.f = NASHORN_BUG ? function propertyIsEnumerable(V) {
  var descriptor = getOwnPropertyDescriptor(this, V);
  return !!descriptor && descriptor.enumerable;
} : $propertyIsEnumerable;


/***/ }),

/***/ "./node_modules/core-js/internals/object-set-prototype-of.js":
/*!*******************************************************************!*\
  !*** ./node_modules/core-js/internals/object-set-prototype-of.js ***!
  \*******************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


/* eslint-disable no-proto -- safe */
var uncurryThisAccessor = __webpack_require__(/*! ../internals/function-uncurry-this-accessor */ "./node_modules/core-js/internals/function-uncurry-this-accessor.js");
var isObject = __webpack_require__(/*! ../internals/is-object */ "./node_modules/core-js/internals/is-object.js");
var requireObjectCoercible = __webpack_require__(/*! ../internals/require-object-coercible */ "./node_modules/core-js/internals/require-object-coercible.js");
var aPossiblePrototype = __webpack_require__(/*! ../internals/a-possible-prototype */ "./node_modules/core-js/internals/a-possible-prototype.js");

// `Object.setPrototypeOf` method
// https://tc39.es/ecma262/#sec-object.setprototypeof
// Works with __proto__ only. Old v8 can't work with null proto objects.
// eslint-disable-next-line es/no-object-setprototypeof -- safe
module.exports = Object.setPrototypeOf || ('__proto__' in {} ? function () {
  var CORRECT_SETTER = false;
  var test = {};
  var setter;
  try {
    setter = uncurryThisAccessor(Object.prototype, '__proto__', 'set');
    setter(test, []);
    CORRECT_SETTER = test instanceof Array;
  } catch (error) { /* empty */ }
  return function setPrototypeOf(O, proto) {
    requireObjectCoercible(O);
    aPossiblePrototype(proto);
    if (!isObject(O)) return O;
    if (CORRECT_SETTER) setter(O, proto);
    else O.__proto__ = proto;
    return O;
  };
}() : undefined);


/***/ }),

/***/ "./node_modules/core-js/internals/object-to-array.js":
/*!***********************************************************!*\
  !*** ./node_modules/core-js/internals/object-to-array.js ***!
  \***********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var DESCRIPTORS = __webpack_require__(/*! ../internals/descriptors */ "./node_modules/core-js/internals/descriptors.js");
var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");
var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this */ "./node_modules/core-js/internals/function-uncurry-this.js");
var objectGetPrototypeOf = __webpack_require__(/*! ../internals/object-get-prototype-of */ "./node_modules/core-js/internals/object-get-prototype-of.js");
var objectKeys = __webpack_require__(/*! ../internals/object-keys */ "./node_modules/core-js/internals/object-keys.js");
var toIndexedObject = __webpack_require__(/*! ../internals/to-indexed-object */ "./node_modules/core-js/internals/to-indexed-object.js");
var $propertyIsEnumerable = (__webpack_require__(/*! ../internals/object-property-is-enumerable */ "./node_modules/core-js/internals/object-property-is-enumerable.js").f);

var propertyIsEnumerable = uncurryThis($propertyIsEnumerable);
var push = uncurryThis([].push);

// in some IE versions, `propertyIsEnumerable` returns incorrect result on integer keys
// of `null` prototype objects
var IE_BUG = DESCRIPTORS && fails(function () {
  // eslint-disable-next-line es/no-object-create -- safe
  var O = Object.create(null);
  O[2] = 2;
  return !propertyIsEnumerable(O, 2);
});

// `Object.{ entries, values }` methods implementation
var createMethod = function (TO_ENTRIES) {
  return function (it) {
    var O = toIndexedObject(it);
    var keys = objectKeys(O);
    var IE_WORKAROUND = IE_BUG && objectGetPrototypeOf(O) === null;
    var length = keys.length;
    var i = 0;
    var result = [];
    var key;
    while (length > i) {
      key = keys[i++];
      if (!DESCRIPTORS || (IE_WORKAROUND ? key in O : propertyIsEnumerable(O, key))) {
        push(result, TO_ENTRIES ? [key, O[key]] : O[key]);
      }
    }
    return result;
  };
};

module.exports = {
  // `Object.entries` method
  // https://tc39.es/ecma262/#sec-object.entries
  entries: createMethod(true),
  // `Object.values` method
  // https://tc39.es/ecma262/#sec-object.values
  values: createMethod(false)
};


/***/ }),

/***/ "./node_modules/core-js/internals/object-to-string.js":
/*!************************************************************!*\
  !*** ./node_modules/core-js/internals/object-to-string.js ***!
  \************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var TO_STRING_TAG_SUPPORT = __webpack_require__(/*! ../internals/to-string-tag-support */ "./node_modules/core-js/internals/to-string-tag-support.js");
var classof = __webpack_require__(/*! ../internals/classof */ "./node_modules/core-js/internals/classof.js");

// `Object.prototype.toString` method implementation
// https://tc39.es/ecma262/#sec-object.prototype.tostring
module.exports = TO_STRING_TAG_SUPPORT ? {}.toString : function toString() {
  return '[object ' + classof(this) + ']';
};


/***/ }),

/***/ "./node_modules/core-js/internals/ordinary-to-primitive.js":
/*!*****************************************************************!*\
  !*** ./node_modules/core-js/internals/ordinary-to-primitive.js ***!
  \*****************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var call = __webpack_require__(/*! ../internals/function-call */ "./node_modules/core-js/internals/function-call.js");
var isCallable = __webpack_require__(/*! ../internals/is-callable */ "./node_modules/core-js/internals/is-callable.js");
var isObject = __webpack_require__(/*! ../internals/is-object */ "./node_modules/core-js/internals/is-object.js");

var $TypeError = TypeError;

// `OrdinaryToPrimitive` abstract operation
// https://tc39.es/ecma262/#sec-ordinarytoprimitive
module.exports = function (input, pref) {
  var fn, val;
  if (pref === 'string' && isCallable(fn = input.toString) && !isObject(val = call(fn, input))) return val;
  if (isCallable(fn = input.valueOf) && !isObject(val = call(fn, input))) return val;
  if (pref !== 'string' && isCallable(fn = input.toString) && !isObject(val = call(fn, input))) return val;
  throw new $TypeError("Can't convert object to primitive value");
};


/***/ }),

/***/ "./node_modules/core-js/internals/own-keys.js":
/*!****************************************************!*\
  !*** ./node_modules/core-js/internals/own-keys.js ***!
  \****************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var getBuiltIn = __webpack_require__(/*! ../internals/get-built-in */ "./node_modules/core-js/internals/get-built-in.js");
var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this */ "./node_modules/core-js/internals/function-uncurry-this.js");
var getOwnPropertyNamesModule = __webpack_require__(/*! ../internals/object-get-own-property-names */ "./node_modules/core-js/internals/object-get-own-property-names.js");
var getOwnPropertySymbolsModule = __webpack_require__(/*! ../internals/object-get-own-property-symbols */ "./node_modules/core-js/internals/object-get-own-property-symbols.js");
var anObject = __webpack_require__(/*! ../internals/an-object */ "./node_modules/core-js/internals/an-object.js");

var concat = uncurryThis([].concat);

// all object keys, includes non-enumerable and symbols
module.exports = getBuiltIn('Reflect', 'ownKeys') || function ownKeys(it) {
  var keys = getOwnPropertyNamesModule.f(anObject(it));
  var getOwnPropertySymbols = getOwnPropertySymbolsModule.f;
  return getOwnPropertySymbols ? concat(keys, getOwnPropertySymbols(it)) : keys;
};


/***/ }),

/***/ "./node_modules/core-js/internals/path.js":
/*!************************************************!*\
  !*** ./node_modules/core-js/internals/path.js ***!
  \************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var globalThis = __webpack_require__(/*! ../internals/global-this */ "./node_modules/core-js/internals/global-this.js");

module.exports = globalThis;


/***/ }),

/***/ "./node_modules/core-js/internals/perform.js":
/*!***************************************************!*\
  !*** ./node_modules/core-js/internals/perform.js ***!
  \***************************************************/
/***/ ((module) => {


module.exports = function (exec) {
  try {
    return { error: false, value: exec() };
  } catch (error) {
    return { error: true, value: error };
  }
};


/***/ }),

/***/ "./node_modules/core-js/internals/promise-constructor-detection.js":
/*!*************************************************************************!*\
  !*** ./node_modules/core-js/internals/promise-constructor-detection.js ***!
  \*************************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var globalThis = __webpack_require__(/*! ../internals/global-this */ "./node_modules/core-js/internals/global-this.js");
var NativePromiseConstructor = __webpack_require__(/*! ../internals/promise-native-constructor */ "./node_modules/core-js/internals/promise-native-constructor.js");
var isCallable = __webpack_require__(/*! ../internals/is-callable */ "./node_modules/core-js/internals/is-callable.js");
var isForced = __webpack_require__(/*! ../internals/is-forced */ "./node_modules/core-js/internals/is-forced.js");
var inspectSource = __webpack_require__(/*! ../internals/inspect-source */ "./node_modules/core-js/internals/inspect-source.js");
var wellKnownSymbol = __webpack_require__(/*! ../internals/well-known-symbol */ "./node_modules/core-js/internals/well-known-symbol.js");
var ENVIRONMENT = __webpack_require__(/*! ../internals/environment */ "./node_modules/core-js/internals/environment.js");
var IS_PURE = __webpack_require__(/*! ../internals/is-pure */ "./node_modules/core-js/internals/is-pure.js");
var V8_VERSION = __webpack_require__(/*! ../internals/environment-v8-version */ "./node_modules/core-js/internals/environment-v8-version.js");

var NativePromisePrototype = NativePromiseConstructor && NativePromiseConstructor.prototype;
var SPECIES = wellKnownSymbol('species');
var SUBCLASSING = false;
var NATIVE_PROMISE_REJECTION_EVENT = isCallable(globalThis.PromiseRejectionEvent);

var FORCED_PROMISE_CONSTRUCTOR = isForced('Promise', function () {
  var PROMISE_CONSTRUCTOR_SOURCE = inspectSource(NativePromiseConstructor);
  var GLOBAL_CORE_JS_PROMISE = PROMISE_CONSTRUCTOR_SOURCE !== String(NativePromiseConstructor);
  // V8 6.6 (Node 10 and Chrome 66) have a bug with resolving custom thenables
  // https://bugs.chromium.org/p/chromium/issues/detail?id=830565
  // We can't detect it synchronously, so just check versions
  if (!GLOBAL_CORE_JS_PROMISE && V8_VERSION === 66) return true;
  // We need Promise#{ catch, finally } in the pure version for preventing prototype pollution
  if (IS_PURE && !(NativePromisePrototype['catch'] && NativePromisePrototype['finally'])) return true;
  // We can't use @@species feature detection in V8 since it causes
  // deoptimization and performance degradation
  // https://github.com/zloirock/core-js/issues/679
  if (!V8_VERSION || V8_VERSION < 51 || !/native code/.test(PROMISE_CONSTRUCTOR_SOURCE)) {
    // Detect correctness of subclassing with @@species support
    var promise = new NativePromiseConstructor(function (resolve) { resolve(1); });
    var FakePromise = function (exec) {
      exec(function () { /* empty */ }, function () { /* empty */ });
    };
    var constructor = promise.constructor = {};
    constructor[SPECIES] = FakePromise;
    SUBCLASSING = promise.then(function () { /* empty */ }) instanceof FakePromise;
    if (!SUBCLASSING) return true;
  // Unhandled rejections tracking support, NodeJS Promise without it fails @@species test
  } return !GLOBAL_CORE_JS_PROMISE && (ENVIRONMENT === 'BROWSER' || ENVIRONMENT === 'DENO') && !NATIVE_PROMISE_REJECTION_EVENT;
});

module.exports = {
  CONSTRUCTOR: FORCED_PROMISE_CONSTRUCTOR,
  REJECTION_EVENT: NATIVE_PROMISE_REJECTION_EVENT,
  SUBCLASSING: SUBCLASSING
};


/***/ }),

/***/ "./node_modules/core-js/internals/promise-native-constructor.js":
/*!**********************************************************************!*\
  !*** ./node_modules/core-js/internals/promise-native-constructor.js ***!
  \**********************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var globalThis = __webpack_require__(/*! ../internals/global-this */ "./node_modules/core-js/internals/global-this.js");

module.exports = globalThis.Promise;


/***/ }),

/***/ "./node_modules/core-js/internals/promise-resolve.js":
/*!***********************************************************!*\
  !*** ./node_modules/core-js/internals/promise-resolve.js ***!
  \***********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var anObject = __webpack_require__(/*! ../internals/an-object */ "./node_modules/core-js/internals/an-object.js");
var isObject = __webpack_require__(/*! ../internals/is-object */ "./node_modules/core-js/internals/is-object.js");
var newPromiseCapability = __webpack_require__(/*! ../internals/new-promise-capability */ "./node_modules/core-js/internals/new-promise-capability.js");

module.exports = function (C, x) {
  anObject(C);
  if (isObject(x) && x.constructor === C) return x;
  var promiseCapability = newPromiseCapability.f(C);
  var resolve = promiseCapability.resolve;
  resolve(x);
  return promiseCapability.promise;
};


/***/ }),

/***/ "./node_modules/core-js/internals/promise-statics-incorrect-iteration.js":
/*!*******************************************************************************!*\
  !*** ./node_modules/core-js/internals/promise-statics-incorrect-iteration.js ***!
  \*******************************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var NativePromiseConstructor = __webpack_require__(/*! ../internals/promise-native-constructor */ "./node_modules/core-js/internals/promise-native-constructor.js");
var checkCorrectnessOfIteration = __webpack_require__(/*! ../internals/check-correctness-of-iteration */ "./node_modules/core-js/internals/check-correctness-of-iteration.js");
var FORCED_PROMISE_CONSTRUCTOR = (__webpack_require__(/*! ../internals/promise-constructor-detection */ "./node_modules/core-js/internals/promise-constructor-detection.js").CONSTRUCTOR);

module.exports = FORCED_PROMISE_CONSTRUCTOR || !checkCorrectnessOfIteration(function (iterable) {
  NativePromiseConstructor.all(iterable).then(undefined, function () { /* empty */ });
});


/***/ }),

/***/ "./node_modules/core-js/internals/queue.js":
/*!*************************************************!*\
  !*** ./node_modules/core-js/internals/queue.js ***!
  \*************************************************/
/***/ ((module) => {


var Queue = function () {
  this.head = null;
  this.tail = null;
};

Queue.prototype = {
  add: function (item) {
    var entry = { item: item, next: null };
    var tail = this.tail;
    if (tail) tail.next = entry;
    else this.head = entry;
    this.tail = entry;
  },
  get: function () {
    var entry = this.head;
    if (entry) {
      var next = this.head = entry.next;
      if (next === null) this.tail = null;
      return entry.item;
    }
  }
};

module.exports = Queue;


/***/ }),

/***/ "./node_modules/core-js/internals/regexp-exec-abstract.js":
/*!****************************************************************!*\
  !*** ./node_modules/core-js/internals/regexp-exec-abstract.js ***!
  \****************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var call = __webpack_require__(/*! ../internals/function-call */ "./node_modules/core-js/internals/function-call.js");
var anObject = __webpack_require__(/*! ../internals/an-object */ "./node_modules/core-js/internals/an-object.js");
var isCallable = __webpack_require__(/*! ../internals/is-callable */ "./node_modules/core-js/internals/is-callable.js");
var classof = __webpack_require__(/*! ../internals/classof-raw */ "./node_modules/core-js/internals/classof-raw.js");
var regexpExec = __webpack_require__(/*! ../internals/regexp-exec */ "./node_modules/core-js/internals/regexp-exec.js");

var $TypeError = TypeError;

// `RegExpExec` abstract operation
// https://tc39.es/ecma262/#sec-regexpexec
module.exports = function (R, S) {
  var exec = R.exec;
  if (isCallable(exec)) {
    var result = call(exec, R, S);
    if (result !== null) anObject(result);
    return result;
  }
  if (classof(R) === 'RegExp') return call(regexpExec, R, S);
  throw new $TypeError('RegExp#exec called on incompatible receiver');
};


/***/ }),

/***/ "./node_modules/core-js/internals/regexp-exec.js":
/*!*******************************************************!*\
  !*** ./node_modules/core-js/internals/regexp-exec.js ***!
  \*******************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


/* eslint-disable regexp/no-empty-capturing-group, regexp/no-empty-group, regexp/no-lazy-ends -- testing */
/* eslint-disable regexp/no-useless-quantifier -- testing */
var call = __webpack_require__(/*! ../internals/function-call */ "./node_modules/core-js/internals/function-call.js");
var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this */ "./node_modules/core-js/internals/function-uncurry-this.js");
var toString = __webpack_require__(/*! ../internals/to-string */ "./node_modules/core-js/internals/to-string.js");
var regexpFlags = __webpack_require__(/*! ../internals/regexp-flags */ "./node_modules/core-js/internals/regexp-flags.js");
var stickyHelpers = __webpack_require__(/*! ../internals/regexp-sticky-helpers */ "./node_modules/core-js/internals/regexp-sticky-helpers.js");
var shared = __webpack_require__(/*! ../internals/shared */ "./node_modules/core-js/internals/shared.js");
var create = __webpack_require__(/*! ../internals/object-create */ "./node_modules/core-js/internals/object-create.js");
var getInternalState = (__webpack_require__(/*! ../internals/internal-state */ "./node_modules/core-js/internals/internal-state.js").get);
var UNSUPPORTED_DOT_ALL = __webpack_require__(/*! ../internals/regexp-unsupported-dot-all */ "./node_modules/core-js/internals/regexp-unsupported-dot-all.js");
var UNSUPPORTED_NCG = __webpack_require__(/*! ../internals/regexp-unsupported-ncg */ "./node_modules/core-js/internals/regexp-unsupported-ncg.js");

var nativeReplace = shared('native-string-replace', String.prototype.replace);
var nativeExec = RegExp.prototype.exec;
var patchedExec = nativeExec;
var charAt = uncurryThis(''.charAt);
var indexOf = uncurryThis(''.indexOf);
var replace = uncurryThis(''.replace);
var stringSlice = uncurryThis(''.slice);

var UPDATES_LAST_INDEX_WRONG = (function () {
  var re1 = /a/;
  var re2 = /b*/g;
  call(nativeExec, re1, 'a');
  call(nativeExec, re2, 'a');
  return re1.lastIndex !== 0 || re2.lastIndex !== 0;
})();

var UNSUPPORTED_Y = stickyHelpers.BROKEN_CARET;

// nonparticipating capturing group, copied from es5-shim's String#split patch.
var NPCG_INCLUDED = /()??/.exec('')[1] !== undefined;

var PATCH = UPDATES_LAST_INDEX_WRONG || NPCG_INCLUDED || UNSUPPORTED_Y || UNSUPPORTED_DOT_ALL || UNSUPPORTED_NCG;

if (PATCH) {
  patchedExec = function exec(string) {
    var re = this;
    var state = getInternalState(re);
    var str = toString(string);
    var raw = state.raw;
    var result, reCopy, lastIndex, match, i, object, group;

    if (raw) {
      raw.lastIndex = re.lastIndex;
      result = call(patchedExec, raw, str);
      re.lastIndex = raw.lastIndex;
      return result;
    }

    var groups = state.groups;
    var sticky = UNSUPPORTED_Y && re.sticky;
    var flags = call(regexpFlags, re);
    var source = re.source;
    var charsAdded = 0;
    var strCopy = str;

    if (sticky) {
      flags = replace(flags, 'y', '');
      if (indexOf(flags, 'g') === -1) {
        flags += 'g';
      }

      strCopy = stringSlice(str, re.lastIndex);
      // Support anchored sticky behavior.
      if (re.lastIndex > 0 && (!re.multiline || re.multiline && charAt(str, re.lastIndex - 1) !== '\n')) {
        source = '(?: ' + source + ')';
        strCopy = ' ' + strCopy;
        charsAdded++;
      }
      // ^(? + rx + ) is needed, in combination with some str slicing, to
      // simulate the 'y' flag.
      reCopy = new RegExp('^(?:' + source + ')', flags);
    }

    if (NPCG_INCLUDED) {
      reCopy = new RegExp('^' + source + '$(?!\\s)', flags);
    }
    if (UPDATES_LAST_INDEX_WRONG) lastIndex = re.lastIndex;

    match = call(nativeExec, sticky ? reCopy : re, strCopy);

    if (sticky) {
      if (match) {
        match.input = stringSlice(match.input, charsAdded);
        match[0] = stringSlice(match[0], charsAdded);
        match.index = re.lastIndex;
        re.lastIndex += match[0].length;
      } else re.lastIndex = 0;
    } else if (UPDATES_LAST_INDEX_WRONG && match) {
      re.lastIndex = re.global ? match.index + match[0].length : lastIndex;
    }
    if (NPCG_INCLUDED && match && match.length > 1) {
      // Fix browsers whose `exec` methods don't consistently return `undefined`
      // for NPCG, like IE8. NOTE: This doesn't work for /(.?)?/
      call(nativeReplace, match[0], reCopy, function () {
        for (i = 1; i < arguments.length - 2; i++) {
          if (arguments[i] === undefined) match[i] = undefined;
        }
      });
    }

    if (match && groups) {
      match.groups = object = create(null);
      for (i = 0; i < groups.length; i++) {
        group = groups[i];
        object[group[0]] = match[group[1]];
      }
    }

    return match;
  };
}

module.exports = patchedExec;


/***/ }),

/***/ "./node_modules/core-js/internals/regexp-flags-detection.js":
/*!******************************************************************!*\
  !*** ./node_modules/core-js/internals/regexp-flags-detection.js ***!
  \******************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var globalThis = __webpack_require__(/*! ../internals/global-this */ "./node_modules/core-js/internals/global-this.js");
var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");

// babel-minify and Closure Compiler transpiles RegExp('.', 'd') -> /./d and it causes SyntaxError
var RegExp = globalThis.RegExp;

var FLAGS_GETTER_IS_CORRECT = !fails(function () {
  var INDICES_SUPPORT = true;
  try {
    RegExp('.', 'd');
  } catch (error) {
    INDICES_SUPPORT = false;
  }

  var O = {};
  // modern V8 bug
  var calls = '';
  var expected = INDICES_SUPPORT ? 'dgimsy' : 'gimsy';

  var addGetter = function (key, chr) {
    // eslint-disable-next-line es/no-object-defineproperty -- safe
    Object.defineProperty(O, key, { get: function () {
      calls += chr;
      return true;
    } });
  };

  var pairs = {
    dotAll: 's',
    global: 'g',
    ignoreCase: 'i',
    multiline: 'm',
    sticky: 'y'
  };

  if (INDICES_SUPPORT) pairs.hasIndices = 'd';

  for (var key in pairs) addGetter(key, pairs[key]);

  // eslint-disable-next-line es/no-object-getownpropertydescriptor -- safe
  var result = Object.getOwnPropertyDescriptor(RegExp.prototype, 'flags').get.call(O);

  return result !== expected || calls !== expected;
});

module.exports = { correct: FLAGS_GETTER_IS_CORRECT };


/***/ }),

/***/ "./node_modules/core-js/internals/regexp-flags.js":
/*!********************************************************!*\
  !*** ./node_modules/core-js/internals/regexp-flags.js ***!
  \********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var anObject = __webpack_require__(/*! ../internals/an-object */ "./node_modules/core-js/internals/an-object.js");

// `RegExp.prototype.flags` getter implementation
// https://tc39.es/ecma262/#sec-get-regexp.prototype.flags
module.exports = function () {
  var that = anObject(this);
  var result = '';
  if (that.hasIndices) result += 'd';
  if (that.global) result += 'g';
  if (that.ignoreCase) result += 'i';
  if (that.multiline) result += 'm';
  if (that.dotAll) result += 's';
  if (that.unicode) result += 'u';
  if (that.unicodeSets) result += 'v';
  if (that.sticky) result += 'y';
  return result;
};


/***/ }),

/***/ "./node_modules/core-js/internals/regexp-get-flags.js":
/*!************************************************************!*\
  !*** ./node_modules/core-js/internals/regexp-get-flags.js ***!
  \************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var call = __webpack_require__(/*! ../internals/function-call */ "./node_modules/core-js/internals/function-call.js");
var hasOwn = __webpack_require__(/*! ../internals/has-own-property */ "./node_modules/core-js/internals/has-own-property.js");
var isPrototypeOf = __webpack_require__(/*! ../internals/object-is-prototype-of */ "./node_modules/core-js/internals/object-is-prototype-of.js");
var regExpFlagsDetection = __webpack_require__(/*! ../internals/regexp-flags-detection */ "./node_modules/core-js/internals/regexp-flags-detection.js");
var regExpFlagsGetterImplementation = __webpack_require__(/*! ../internals/regexp-flags */ "./node_modules/core-js/internals/regexp-flags.js");

var RegExpPrototype = RegExp.prototype;

module.exports = regExpFlagsDetection.correct ? function (it) {
  return it.flags;
} : function (it) {
  return (!regExpFlagsDetection.correct && isPrototypeOf(RegExpPrototype, it) && !hasOwn(it, 'flags'))
    ? call(regExpFlagsGetterImplementation, it)
    : it.flags;
};


/***/ }),

/***/ "./node_modules/core-js/internals/regexp-sticky-helpers.js":
/*!*****************************************************************!*\
  !*** ./node_modules/core-js/internals/regexp-sticky-helpers.js ***!
  \*****************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");
var globalThis = __webpack_require__(/*! ../internals/global-this */ "./node_modules/core-js/internals/global-this.js");

// babel-minify and Closure Compiler transpiles RegExp('a', 'y') -> /a/y and it causes SyntaxError
var $RegExp = globalThis.RegExp;

var UNSUPPORTED_Y = fails(function () {
  var re = $RegExp('a', 'y');
  re.lastIndex = 2;
  return re.exec('abcd') !== null;
});

// UC Browser bug
// https://github.com/zloirock/core-js/issues/1008
var MISSED_STICKY = UNSUPPORTED_Y || fails(function () {
  return !$RegExp('a', 'y').sticky;
});

var BROKEN_CARET = UNSUPPORTED_Y || fails(function () {
  // https://bugzilla.mozilla.org/show_bug.cgi?id=773687
  var re = $RegExp('^r', 'gy');
  re.lastIndex = 2;
  return re.exec('str') !== null;
});

module.exports = {
  BROKEN_CARET: BROKEN_CARET,
  MISSED_STICKY: MISSED_STICKY,
  UNSUPPORTED_Y: UNSUPPORTED_Y
};


/***/ }),

/***/ "./node_modules/core-js/internals/regexp-unsupported-dot-all.js":
/*!**********************************************************************!*\
  !*** ./node_modules/core-js/internals/regexp-unsupported-dot-all.js ***!
  \**********************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");
var globalThis = __webpack_require__(/*! ../internals/global-this */ "./node_modules/core-js/internals/global-this.js");

// babel-minify and Closure Compiler transpiles RegExp('.', 's') -> /./s and it causes SyntaxError
var $RegExp = globalThis.RegExp;

module.exports = fails(function () {
  var re = $RegExp('.', 's');
  return !(re.dotAll && re.test('\n') && re.flags === 's');
});


/***/ }),

/***/ "./node_modules/core-js/internals/regexp-unsupported-ncg.js":
/*!******************************************************************!*\
  !*** ./node_modules/core-js/internals/regexp-unsupported-ncg.js ***!
  \******************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");
var globalThis = __webpack_require__(/*! ../internals/global-this */ "./node_modules/core-js/internals/global-this.js");

// babel-minify and Closure Compiler transpiles RegExp('(?<a>b)', 'g') -> /(?<a>b)/g and it causes SyntaxError
var $RegExp = globalThis.RegExp;

module.exports = fails(function () {
  var re = $RegExp('(?<a>b)', 'g');
  return re.exec('b').groups.a !== 'b' ||
    'b'.replace(re, '$<a>c') !== 'bc';
});


/***/ }),

/***/ "./node_modules/core-js/internals/require-object-coercible.js":
/*!********************************************************************!*\
  !*** ./node_modules/core-js/internals/require-object-coercible.js ***!
  \********************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var isNullOrUndefined = __webpack_require__(/*! ../internals/is-null-or-undefined */ "./node_modules/core-js/internals/is-null-or-undefined.js");

var $TypeError = TypeError;

// `RequireObjectCoercible` abstract operation
// https://tc39.es/ecma262/#sec-requireobjectcoercible
module.exports = function (it) {
  if (isNullOrUndefined(it)) throw new $TypeError("Can't call method on " + it);
  return it;
};


/***/ }),

/***/ "./node_modules/core-js/internals/safe-get-built-in.js":
/*!*************************************************************!*\
  !*** ./node_modules/core-js/internals/safe-get-built-in.js ***!
  \*************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var globalThis = __webpack_require__(/*! ../internals/global-this */ "./node_modules/core-js/internals/global-this.js");
var DESCRIPTORS = __webpack_require__(/*! ../internals/descriptors */ "./node_modules/core-js/internals/descriptors.js");

// eslint-disable-next-line es/no-object-getownpropertydescriptor -- safe
var getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;

// Avoid NodeJS experimental warning
module.exports = function (name) {
  if (!DESCRIPTORS) return globalThis[name];
  var descriptor = getOwnPropertyDescriptor(globalThis, name);
  return descriptor && descriptor.value;
};


/***/ }),

/***/ "./node_modules/core-js/internals/set-species.js":
/*!*******************************************************!*\
  !*** ./node_modules/core-js/internals/set-species.js ***!
  \*******************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var getBuiltIn = __webpack_require__(/*! ../internals/get-built-in */ "./node_modules/core-js/internals/get-built-in.js");
var defineBuiltInAccessor = __webpack_require__(/*! ../internals/define-built-in-accessor */ "./node_modules/core-js/internals/define-built-in-accessor.js");
var wellKnownSymbol = __webpack_require__(/*! ../internals/well-known-symbol */ "./node_modules/core-js/internals/well-known-symbol.js");
var DESCRIPTORS = __webpack_require__(/*! ../internals/descriptors */ "./node_modules/core-js/internals/descriptors.js");

var SPECIES = wellKnownSymbol('species');

module.exports = function (CONSTRUCTOR_NAME) {
  var Constructor = getBuiltIn(CONSTRUCTOR_NAME);

  if (DESCRIPTORS && Constructor && !Constructor[SPECIES]) {
    defineBuiltInAccessor(Constructor, SPECIES, {
      configurable: true,
      get: function () { return this; }
    });
  }
};


/***/ }),

/***/ "./node_modules/core-js/internals/set-to-string-tag.js":
/*!*************************************************************!*\
  !*** ./node_modules/core-js/internals/set-to-string-tag.js ***!
  \*************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var defineProperty = (__webpack_require__(/*! ../internals/object-define-property */ "./node_modules/core-js/internals/object-define-property.js").f);
var hasOwn = __webpack_require__(/*! ../internals/has-own-property */ "./node_modules/core-js/internals/has-own-property.js");
var wellKnownSymbol = __webpack_require__(/*! ../internals/well-known-symbol */ "./node_modules/core-js/internals/well-known-symbol.js");

var TO_STRING_TAG = wellKnownSymbol('toStringTag');

module.exports = function (target, TAG, STATIC) {
  if (target && !STATIC) target = target.prototype;
  if (target && !hasOwn(target, TO_STRING_TAG)) {
    defineProperty(target, TO_STRING_TAG, { configurable: true, value: TAG });
  }
};


/***/ }),

/***/ "./node_modules/core-js/internals/shared-key.js":
/*!******************************************************!*\
  !*** ./node_modules/core-js/internals/shared-key.js ***!
  \******************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var shared = __webpack_require__(/*! ../internals/shared */ "./node_modules/core-js/internals/shared.js");
var uid = __webpack_require__(/*! ../internals/uid */ "./node_modules/core-js/internals/uid.js");

var keys = shared('keys');

module.exports = function (key) {
  return keys[key] || (keys[key] = uid(key));
};


/***/ }),

/***/ "./node_modules/core-js/internals/shared-store.js":
/*!********************************************************!*\
  !*** ./node_modules/core-js/internals/shared-store.js ***!
  \********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var IS_PURE = __webpack_require__(/*! ../internals/is-pure */ "./node_modules/core-js/internals/is-pure.js");
var globalThis = __webpack_require__(/*! ../internals/global-this */ "./node_modules/core-js/internals/global-this.js");
var defineGlobalProperty = __webpack_require__(/*! ../internals/define-global-property */ "./node_modules/core-js/internals/define-global-property.js");

var SHARED = '__core-js_shared__';
var store = module.exports = globalThis[SHARED] || defineGlobalProperty(SHARED, {});

(store.versions || (store.versions = [])).push({
  version: '3.45.1',
  mode: IS_PURE ? 'pure' : 'global',
  copyright: '© 2014-2025 Denis Pushkarev (zloirock.ru)',
  license: 'https://github.com/zloirock/core-js/blob/v3.45.1/LICENSE',
  source: 'https://github.com/zloirock/core-js'
});


/***/ }),

/***/ "./node_modules/core-js/internals/shared.js":
/*!**************************************************!*\
  !*** ./node_modules/core-js/internals/shared.js ***!
  \**************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var store = __webpack_require__(/*! ../internals/shared-store */ "./node_modules/core-js/internals/shared-store.js");

module.exports = function (key, value) {
  return store[key] || (store[key] = value || {});
};


/***/ }),

/***/ "./node_modules/core-js/internals/species-constructor.js":
/*!***************************************************************!*\
  !*** ./node_modules/core-js/internals/species-constructor.js ***!
  \***************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var anObject = __webpack_require__(/*! ../internals/an-object */ "./node_modules/core-js/internals/an-object.js");
var aConstructor = __webpack_require__(/*! ../internals/a-constructor */ "./node_modules/core-js/internals/a-constructor.js");
var isNullOrUndefined = __webpack_require__(/*! ../internals/is-null-or-undefined */ "./node_modules/core-js/internals/is-null-or-undefined.js");
var wellKnownSymbol = __webpack_require__(/*! ../internals/well-known-symbol */ "./node_modules/core-js/internals/well-known-symbol.js");

var SPECIES = wellKnownSymbol('species');

// `SpeciesConstructor` abstract operation
// https://tc39.es/ecma262/#sec-speciesconstructor
module.exports = function (O, defaultConstructor) {
  var C = anObject(O).constructor;
  var S;
  return C === undefined || isNullOrUndefined(S = anObject(C)[SPECIES]) ? defaultConstructor : aConstructor(S);
};


/***/ }),

/***/ "./node_modules/core-js/internals/string-multibyte.js":
/*!************************************************************!*\
  !*** ./node_modules/core-js/internals/string-multibyte.js ***!
  \************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this */ "./node_modules/core-js/internals/function-uncurry-this.js");
var toIntegerOrInfinity = __webpack_require__(/*! ../internals/to-integer-or-infinity */ "./node_modules/core-js/internals/to-integer-or-infinity.js");
var toString = __webpack_require__(/*! ../internals/to-string */ "./node_modules/core-js/internals/to-string.js");
var requireObjectCoercible = __webpack_require__(/*! ../internals/require-object-coercible */ "./node_modules/core-js/internals/require-object-coercible.js");

var charAt = uncurryThis(''.charAt);
var charCodeAt = uncurryThis(''.charCodeAt);
var stringSlice = uncurryThis(''.slice);

var createMethod = function (CONVERT_TO_STRING) {
  return function ($this, pos) {
    var S = toString(requireObjectCoercible($this));
    var position = toIntegerOrInfinity(pos);
    var size = S.length;
    var first, second;
    if (position < 0 || position >= size) return CONVERT_TO_STRING ? '' : undefined;
    first = charCodeAt(S, position);
    return first < 0xD800 || first > 0xDBFF || position + 1 === size
      || (second = charCodeAt(S, position + 1)) < 0xDC00 || second > 0xDFFF
        ? CONVERT_TO_STRING
          ? charAt(S, position)
          : first
        : CONVERT_TO_STRING
          ? stringSlice(S, position, position + 2)
          : (first - 0xD800 << 10) + (second - 0xDC00) + 0x10000;
  };
};

module.exports = {
  // `String.prototype.codePointAt` method
  // https://tc39.es/ecma262/#sec-string.prototype.codepointat
  codeAt: createMethod(false),
  // `String.prototype.at` method
  // https://github.com/mathiasbynens/String.prototype.at
  charAt: createMethod(true)
};


/***/ }),

/***/ "./node_modules/core-js/internals/string-repeat.js":
/*!*********************************************************!*\
  !*** ./node_modules/core-js/internals/string-repeat.js ***!
  \*********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var toIntegerOrInfinity = __webpack_require__(/*! ../internals/to-integer-or-infinity */ "./node_modules/core-js/internals/to-integer-or-infinity.js");
var toString = __webpack_require__(/*! ../internals/to-string */ "./node_modules/core-js/internals/to-string.js");
var requireObjectCoercible = __webpack_require__(/*! ../internals/require-object-coercible */ "./node_modules/core-js/internals/require-object-coercible.js");

var $RangeError = RangeError;

// `String.prototype.repeat` method implementation
// https://tc39.es/ecma262/#sec-string.prototype.repeat
module.exports = function repeat(count) {
  var str = toString(requireObjectCoercible(this));
  var result = '';
  var n = toIntegerOrInfinity(count);
  if (n < 0 || n === Infinity) throw new $RangeError('Wrong number of repetitions');
  for (;n > 0; (n >>>= 1) && (str += str)) if (n & 1) result += str;
  return result;
};


/***/ }),

/***/ "./node_modules/core-js/internals/string-trim-forced.js":
/*!**************************************************************!*\
  !*** ./node_modules/core-js/internals/string-trim-forced.js ***!
  \**************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var PROPER_FUNCTION_NAME = (__webpack_require__(/*! ../internals/function-name */ "./node_modules/core-js/internals/function-name.js").PROPER);
var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");
var whitespaces = __webpack_require__(/*! ../internals/whitespaces */ "./node_modules/core-js/internals/whitespaces.js");

var non = '\u200B\u0085\u180E';

// check that a method works with the correct list
// of whitespaces and has a correct name
module.exports = function (METHOD_NAME) {
  return fails(function () {
    return !!whitespaces[METHOD_NAME]()
      || non[METHOD_NAME]() !== non
      || (PROPER_FUNCTION_NAME && whitespaces[METHOD_NAME].name !== METHOD_NAME);
  });
};


/***/ }),

/***/ "./node_modules/core-js/internals/string-trim.js":
/*!*******************************************************!*\
  !*** ./node_modules/core-js/internals/string-trim.js ***!
  \*******************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this */ "./node_modules/core-js/internals/function-uncurry-this.js");
var requireObjectCoercible = __webpack_require__(/*! ../internals/require-object-coercible */ "./node_modules/core-js/internals/require-object-coercible.js");
var toString = __webpack_require__(/*! ../internals/to-string */ "./node_modules/core-js/internals/to-string.js");
var whitespaces = __webpack_require__(/*! ../internals/whitespaces */ "./node_modules/core-js/internals/whitespaces.js");

var replace = uncurryThis(''.replace);
var ltrim = RegExp('^[' + whitespaces + ']+');
var rtrim = RegExp('(^|[^' + whitespaces + '])[' + whitespaces + ']+$');

// `String.prototype.{ trim, trimStart, trimEnd, trimLeft, trimRight }` methods implementation
var createMethod = function (TYPE) {
  return function ($this) {
    var string = toString(requireObjectCoercible($this));
    if (TYPE & 1) string = replace(string, ltrim, '');
    if (TYPE & 2) string = replace(string, rtrim, '$1');
    return string;
  };
};

module.exports = {
  // `String.prototype.{ trimLeft, trimStart }` methods
  // https://tc39.es/ecma262/#sec-string.prototype.trimstart
  start: createMethod(1),
  // `String.prototype.{ trimRight, trimEnd }` methods
  // https://tc39.es/ecma262/#sec-string.prototype.trimend
  end: createMethod(2),
  // `String.prototype.trim` method
  // https://tc39.es/ecma262/#sec-string.prototype.trim
  trim: createMethod(3)
};


/***/ }),

/***/ "./node_modules/core-js/internals/symbol-constructor-detection.js":
/*!************************************************************************!*\
  !*** ./node_modules/core-js/internals/symbol-constructor-detection.js ***!
  \************************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


/* eslint-disable es/no-symbol -- required for testing */
var V8_VERSION = __webpack_require__(/*! ../internals/environment-v8-version */ "./node_modules/core-js/internals/environment-v8-version.js");
var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");
var globalThis = __webpack_require__(/*! ../internals/global-this */ "./node_modules/core-js/internals/global-this.js");

var $String = globalThis.String;

// eslint-disable-next-line es/no-object-getownpropertysymbols -- required for testing
module.exports = !!Object.getOwnPropertySymbols && !fails(function () {
  var symbol = Symbol('symbol detection');
  // Chrome 38 Symbol has incorrect toString conversion
  // `get-own-property-symbols` polyfill symbols converted to object are not Symbol instances
  // nb: Do not call `String` directly to avoid this being optimized out to `symbol+''` which will,
  // of course, fail.
  return !$String(symbol) || !(Object(symbol) instanceof Symbol) ||
    // Chrome 38-40 symbols are not inherited from DOM collections prototypes to instances
    !Symbol.sham && V8_VERSION && V8_VERSION < 41;
});


/***/ }),

/***/ "./node_modules/core-js/internals/symbol-define-to-primitive.js":
/*!**********************************************************************!*\
  !*** ./node_modules/core-js/internals/symbol-define-to-primitive.js ***!
  \**********************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var call = __webpack_require__(/*! ../internals/function-call */ "./node_modules/core-js/internals/function-call.js");
var getBuiltIn = __webpack_require__(/*! ../internals/get-built-in */ "./node_modules/core-js/internals/get-built-in.js");
var wellKnownSymbol = __webpack_require__(/*! ../internals/well-known-symbol */ "./node_modules/core-js/internals/well-known-symbol.js");
var defineBuiltIn = __webpack_require__(/*! ../internals/define-built-in */ "./node_modules/core-js/internals/define-built-in.js");

module.exports = function () {
  var Symbol = getBuiltIn('Symbol');
  var SymbolPrototype = Symbol && Symbol.prototype;
  var valueOf = SymbolPrototype && SymbolPrototype.valueOf;
  var TO_PRIMITIVE = wellKnownSymbol('toPrimitive');

  if (SymbolPrototype && !SymbolPrototype[TO_PRIMITIVE]) {
    // `Symbol.prototype[@@toPrimitive]` method
    // https://tc39.es/ecma262/#sec-symbol.prototype-@@toprimitive
    // eslint-disable-next-line no-unused-vars -- required for .length
    defineBuiltIn(SymbolPrototype, TO_PRIMITIVE, function (hint) {
      return call(valueOf, this);
    }, { arity: 1 });
  }
};


/***/ }),

/***/ "./node_modules/core-js/internals/symbol-registry-detection.js":
/*!*********************************************************************!*\
  !*** ./node_modules/core-js/internals/symbol-registry-detection.js ***!
  \*********************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var NATIVE_SYMBOL = __webpack_require__(/*! ../internals/symbol-constructor-detection */ "./node_modules/core-js/internals/symbol-constructor-detection.js");

/* eslint-disable es/no-symbol -- safe */
module.exports = NATIVE_SYMBOL && !!Symbol['for'] && !!Symbol.keyFor;


/***/ }),

/***/ "./node_modules/core-js/internals/task.js":
/*!************************************************!*\
  !*** ./node_modules/core-js/internals/task.js ***!
  \************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var globalThis = __webpack_require__(/*! ../internals/global-this */ "./node_modules/core-js/internals/global-this.js");
var apply = __webpack_require__(/*! ../internals/function-apply */ "./node_modules/core-js/internals/function-apply.js");
var bind = __webpack_require__(/*! ../internals/function-bind-context */ "./node_modules/core-js/internals/function-bind-context.js");
var isCallable = __webpack_require__(/*! ../internals/is-callable */ "./node_modules/core-js/internals/is-callable.js");
var hasOwn = __webpack_require__(/*! ../internals/has-own-property */ "./node_modules/core-js/internals/has-own-property.js");
var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");
var html = __webpack_require__(/*! ../internals/html */ "./node_modules/core-js/internals/html.js");
var arraySlice = __webpack_require__(/*! ../internals/array-slice */ "./node_modules/core-js/internals/array-slice.js");
var createElement = __webpack_require__(/*! ../internals/document-create-element */ "./node_modules/core-js/internals/document-create-element.js");
var validateArgumentsLength = __webpack_require__(/*! ../internals/validate-arguments-length */ "./node_modules/core-js/internals/validate-arguments-length.js");
var IS_IOS = __webpack_require__(/*! ../internals/environment-is-ios */ "./node_modules/core-js/internals/environment-is-ios.js");
var IS_NODE = __webpack_require__(/*! ../internals/environment-is-node */ "./node_modules/core-js/internals/environment-is-node.js");

var set = globalThis.setImmediate;
var clear = globalThis.clearImmediate;
var process = globalThis.process;
var Dispatch = globalThis.Dispatch;
var Function = globalThis.Function;
var MessageChannel = globalThis.MessageChannel;
var String = globalThis.String;
var counter = 0;
var queue = {};
var ONREADYSTATECHANGE = 'onreadystatechange';
var $location, defer, channel, port;

fails(function () {
  // Deno throws a ReferenceError on `location` access without `--location` flag
  $location = globalThis.location;
});

var run = function (id) {
  if (hasOwn(queue, id)) {
    var fn = queue[id];
    delete queue[id];
    fn();
  }
};

var runner = function (id) {
  return function () {
    run(id);
  };
};

var eventListener = function (event) {
  run(event.data);
};

var globalPostMessageDefer = function (id) {
  // old engines have not location.origin
  globalThis.postMessage(String(id), $location.protocol + '//' + $location.host);
};

// Node.js 0.9+ & IE10+ has setImmediate, otherwise:
if (!set || !clear) {
  set = function setImmediate(handler) {
    validateArgumentsLength(arguments.length, 1);
    var fn = isCallable(handler) ? handler : Function(handler);
    var args = arraySlice(arguments, 1);
    queue[++counter] = function () {
      apply(fn, undefined, args);
    };
    defer(counter);
    return counter;
  };
  clear = function clearImmediate(id) {
    delete queue[id];
  };
  // Node.js 0.8-
  if (IS_NODE) {
    defer = function (id) {
      process.nextTick(runner(id));
    };
  // Sphere (JS game engine) Dispatch API
  } else if (Dispatch && Dispatch.now) {
    defer = function (id) {
      Dispatch.now(runner(id));
    };
  // Browsers with MessageChannel, includes WebWorkers
  // except iOS - https://github.com/zloirock/core-js/issues/624
  } else if (MessageChannel && !IS_IOS) {
    channel = new MessageChannel();
    port = channel.port2;
    channel.port1.onmessage = eventListener;
    defer = bind(port.postMessage, port);
  // Browsers with postMessage, skip WebWorkers
  // IE8 has postMessage, but it's sync & typeof its postMessage is 'object'
  } else if (
    globalThis.addEventListener &&
    isCallable(globalThis.postMessage) &&
    !globalThis.importScripts &&
    $location && $location.protocol !== 'file:' &&
    !fails(globalPostMessageDefer)
  ) {
    defer = globalPostMessageDefer;
    globalThis.addEventListener('message', eventListener, false);
  // IE8-
  } else if (ONREADYSTATECHANGE in createElement('script')) {
    defer = function (id) {
      html.appendChild(createElement('script'))[ONREADYSTATECHANGE] = function () {
        html.removeChild(this);
        run(id);
      };
    };
  // Rest old browsers
  } else {
    defer = function (id) {
      setTimeout(runner(id), 0);
    };
  }
}

module.exports = {
  set: set,
  clear: clear
};


/***/ }),

/***/ "./node_modules/core-js/internals/this-number-value.js":
/*!*************************************************************!*\
  !*** ./node_modules/core-js/internals/this-number-value.js ***!
  \*************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this */ "./node_modules/core-js/internals/function-uncurry-this.js");

// `thisNumberValue` abstract operation
// https://tc39.es/ecma262/#sec-thisnumbervalue
module.exports = uncurryThis(1.1.valueOf);


/***/ }),

/***/ "./node_modules/core-js/internals/to-absolute-index.js":
/*!*************************************************************!*\
  !*** ./node_modules/core-js/internals/to-absolute-index.js ***!
  \*************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var toIntegerOrInfinity = __webpack_require__(/*! ../internals/to-integer-or-infinity */ "./node_modules/core-js/internals/to-integer-or-infinity.js");

var max = Math.max;
var min = Math.min;

// Helper for a popular repeating case of the spec:
// Let integer be ? ToInteger(index).
// If integer < 0, let result be max((length + integer), 0); else let result be min(integer, length).
module.exports = function (index, length) {
  var integer = toIntegerOrInfinity(index);
  return integer < 0 ? max(integer + length, 0) : min(integer, length);
};


/***/ }),

/***/ "./node_modules/core-js/internals/to-indexed-object.js":
/*!*************************************************************!*\
  !*** ./node_modules/core-js/internals/to-indexed-object.js ***!
  \*************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


// toObject with fallback for non-array-like ES3 strings
var IndexedObject = __webpack_require__(/*! ../internals/indexed-object */ "./node_modules/core-js/internals/indexed-object.js");
var requireObjectCoercible = __webpack_require__(/*! ../internals/require-object-coercible */ "./node_modules/core-js/internals/require-object-coercible.js");

module.exports = function (it) {
  return IndexedObject(requireObjectCoercible(it));
};


/***/ }),

/***/ "./node_modules/core-js/internals/to-integer-or-infinity.js":
/*!******************************************************************!*\
  !*** ./node_modules/core-js/internals/to-integer-or-infinity.js ***!
  \******************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var trunc = __webpack_require__(/*! ../internals/math-trunc */ "./node_modules/core-js/internals/math-trunc.js");

// `ToIntegerOrInfinity` abstract operation
// https://tc39.es/ecma262/#sec-tointegerorinfinity
module.exports = function (argument) {
  var number = +argument;
  // eslint-disable-next-line no-self-compare -- NaN check
  return number !== number || number === 0 ? 0 : trunc(number);
};


/***/ }),

/***/ "./node_modules/core-js/internals/to-length.js":
/*!*****************************************************!*\
  !*** ./node_modules/core-js/internals/to-length.js ***!
  \*****************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var toIntegerOrInfinity = __webpack_require__(/*! ../internals/to-integer-or-infinity */ "./node_modules/core-js/internals/to-integer-or-infinity.js");

var min = Math.min;

// `ToLength` abstract operation
// https://tc39.es/ecma262/#sec-tolength
module.exports = function (argument) {
  var len = toIntegerOrInfinity(argument);
  return len > 0 ? min(len, 0x1FFFFFFFFFFFFF) : 0; // 2 ** 53 - 1 == 9007199254740991
};


/***/ }),

/***/ "./node_modules/core-js/internals/to-object.js":
/*!*****************************************************!*\
  !*** ./node_modules/core-js/internals/to-object.js ***!
  \*****************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var requireObjectCoercible = __webpack_require__(/*! ../internals/require-object-coercible */ "./node_modules/core-js/internals/require-object-coercible.js");

var $Object = Object;

// `ToObject` abstract operation
// https://tc39.es/ecma262/#sec-toobject
module.exports = function (argument) {
  return $Object(requireObjectCoercible(argument));
};


/***/ }),

/***/ "./node_modules/core-js/internals/to-primitive.js":
/*!********************************************************!*\
  !*** ./node_modules/core-js/internals/to-primitive.js ***!
  \********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var call = __webpack_require__(/*! ../internals/function-call */ "./node_modules/core-js/internals/function-call.js");
var isObject = __webpack_require__(/*! ../internals/is-object */ "./node_modules/core-js/internals/is-object.js");
var isSymbol = __webpack_require__(/*! ../internals/is-symbol */ "./node_modules/core-js/internals/is-symbol.js");
var getMethod = __webpack_require__(/*! ../internals/get-method */ "./node_modules/core-js/internals/get-method.js");
var ordinaryToPrimitive = __webpack_require__(/*! ../internals/ordinary-to-primitive */ "./node_modules/core-js/internals/ordinary-to-primitive.js");
var wellKnownSymbol = __webpack_require__(/*! ../internals/well-known-symbol */ "./node_modules/core-js/internals/well-known-symbol.js");

var $TypeError = TypeError;
var TO_PRIMITIVE = wellKnownSymbol('toPrimitive');

// `ToPrimitive` abstract operation
// https://tc39.es/ecma262/#sec-toprimitive
module.exports = function (input, pref) {
  if (!isObject(input) || isSymbol(input)) return input;
  var exoticToPrim = getMethod(input, TO_PRIMITIVE);
  var result;
  if (exoticToPrim) {
    if (pref === undefined) pref = 'default';
    result = call(exoticToPrim, input, pref);
    if (!isObject(result) || isSymbol(result)) return result;
    throw new $TypeError("Can't convert object to primitive value");
  }
  if (pref === undefined) pref = 'number';
  return ordinaryToPrimitive(input, pref);
};


/***/ }),

/***/ "./node_modules/core-js/internals/to-property-key.js":
/*!***********************************************************!*\
  !*** ./node_modules/core-js/internals/to-property-key.js ***!
  \***********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var toPrimitive = __webpack_require__(/*! ../internals/to-primitive */ "./node_modules/core-js/internals/to-primitive.js");
var isSymbol = __webpack_require__(/*! ../internals/is-symbol */ "./node_modules/core-js/internals/is-symbol.js");

// `ToPropertyKey` abstract operation
// https://tc39.es/ecma262/#sec-topropertykey
module.exports = function (argument) {
  var key = toPrimitive(argument, 'string');
  return isSymbol(key) ? key : key + '';
};


/***/ }),

/***/ "./node_modules/core-js/internals/to-string-tag-support.js":
/*!*****************************************************************!*\
  !*** ./node_modules/core-js/internals/to-string-tag-support.js ***!
  \*****************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var wellKnownSymbol = __webpack_require__(/*! ../internals/well-known-symbol */ "./node_modules/core-js/internals/well-known-symbol.js");

var TO_STRING_TAG = wellKnownSymbol('toStringTag');
var test = {};

test[TO_STRING_TAG] = 'z';

module.exports = String(test) === '[object z]';


/***/ }),

/***/ "./node_modules/core-js/internals/to-string.js":
/*!*****************************************************!*\
  !*** ./node_modules/core-js/internals/to-string.js ***!
  \*****************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var classof = __webpack_require__(/*! ../internals/classof */ "./node_modules/core-js/internals/classof.js");

var $String = String;

module.exports = function (argument) {
  if (classof(argument) === 'Symbol') throw new TypeError('Cannot convert a Symbol value to a string');
  return $String(argument);
};


/***/ }),

/***/ "./node_modules/core-js/internals/try-to-string.js":
/*!*********************************************************!*\
  !*** ./node_modules/core-js/internals/try-to-string.js ***!
  \*********************************************************/
/***/ ((module) => {


var $String = String;

module.exports = function (argument) {
  try {
    return $String(argument);
  } catch (error) {
    return 'Object';
  }
};


/***/ }),

/***/ "./node_modules/core-js/internals/uid.js":
/*!***********************************************!*\
  !*** ./node_modules/core-js/internals/uid.js ***!
  \***********************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this */ "./node_modules/core-js/internals/function-uncurry-this.js");

var id = 0;
var postfix = Math.random();
var toString = uncurryThis(1.1.toString);

module.exports = function (key) {
  return 'Symbol(' + (key === undefined ? '' : key) + ')_' + toString(++id + postfix, 36);
};


/***/ }),

/***/ "./node_modules/core-js/internals/use-symbol-as-uid.js":
/*!*************************************************************!*\
  !*** ./node_modules/core-js/internals/use-symbol-as-uid.js ***!
  \*************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


/* eslint-disable es/no-symbol -- required for testing */
var NATIVE_SYMBOL = __webpack_require__(/*! ../internals/symbol-constructor-detection */ "./node_modules/core-js/internals/symbol-constructor-detection.js");

module.exports = NATIVE_SYMBOL &&
  !Symbol.sham &&
  typeof Symbol.iterator == 'symbol';


/***/ }),

/***/ "./node_modules/core-js/internals/v8-prototype-define-bug.js":
/*!*******************************************************************!*\
  !*** ./node_modules/core-js/internals/v8-prototype-define-bug.js ***!
  \*******************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var DESCRIPTORS = __webpack_require__(/*! ../internals/descriptors */ "./node_modules/core-js/internals/descriptors.js");
var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");

// V8 ~ Chrome 36-
// https://bugs.chromium.org/p/v8/issues/detail?id=3334
module.exports = DESCRIPTORS && fails(function () {
  // eslint-disable-next-line es/no-object-defineproperty -- required for testing
  return Object.defineProperty(function () { /* empty */ }, 'prototype', {
    value: 42,
    writable: false
  }).prototype !== 42;
});


/***/ }),

/***/ "./node_modules/core-js/internals/validate-arguments-length.js":
/*!*********************************************************************!*\
  !*** ./node_modules/core-js/internals/validate-arguments-length.js ***!
  \*********************************************************************/
/***/ ((module) => {


var $TypeError = TypeError;

module.exports = function (passed, required) {
  if (passed < required) throw new $TypeError('Not enough arguments');
  return passed;
};


/***/ }),

/***/ "./node_modules/core-js/internals/weak-map-basic-detection.js":
/*!********************************************************************!*\
  !*** ./node_modules/core-js/internals/weak-map-basic-detection.js ***!
  \********************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var globalThis = __webpack_require__(/*! ../internals/global-this */ "./node_modules/core-js/internals/global-this.js");
var isCallable = __webpack_require__(/*! ../internals/is-callable */ "./node_modules/core-js/internals/is-callable.js");

var WeakMap = globalThis.WeakMap;

module.exports = isCallable(WeakMap) && /native code/.test(String(WeakMap));


/***/ }),

/***/ "./node_modules/core-js/internals/well-known-symbol-define.js":
/*!********************************************************************!*\
  !*** ./node_modules/core-js/internals/well-known-symbol-define.js ***!
  \********************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var path = __webpack_require__(/*! ../internals/path */ "./node_modules/core-js/internals/path.js");
var hasOwn = __webpack_require__(/*! ../internals/has-own-property */ "./node_modules/core-js/internals/has-own-property.js");
var wrappedWellKnownSymbolModule = __webpack_require__(/*! ../internals/well-known-symbol-wrapped */ "./node_modules/core-js/internals/well-known-symbol-wrapped.js");
var defineProperty = (__webpack_require__(/*! ../internals/object-define-property */ "./node_modules/core-js/internals/object-define-property.js").f);

module.exports = function (NAME) {
  var Symbol = path.Symbol || (path.Symbol = {});
  if (!hasOwn(Symbol, NAME)) defineProperty(Symbol, NAME, {
    value: wrappedWellKnownSymbolModule.f(NAME)
  });
};


/***/ }),

/***/ "./node_modules/core-js/internals/well-known-symbol-wrapped.js":
/*!*********************************************************************!*\
  !*** ./node_modules/core-js/internals/well-known-symbol-wrapped.js ***!
  \*********************************************************************/
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {


var wellKnownSymbol = __webpack_require__(/*! ../internals/well-known-symbol */ "./node_modules/core-js/internals/well-known-symbol.js");

exports.f = wellKnownSymbol;


/***/ }),

/***/ "./node_modules/core-js/internals/well-known-symbol.js":
/*!*************************************************************!*\
  !*** ./node_modules/core-js/internals/well-known-symbol.js ***!
  \*************************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var globalThis = __webpack_require__(/*! ../internals/global-this */ "./node_modules/core-js/internals/global-this.js");
var shared = __webpack_require__(/*! ../internals/shared */ "./node_modules/core-js/internals/shared.js");
var hasOwn = __webpack_require__(/*! ../internals/has-own-property */ "./node_modules/core-js/internals/has-own-property.js");
var uid = __webpack_require__(/*! ../internals/uid */ "./node_modules/core-js/internals/uid.js");
var NATIVE_SYMBOL = __webpack_require__(/*! ../internals/symbol-constructor-detection */ "./node_modules/core-js/internals/symbol-constructor-detection.js");
var USE_SYMBOL_AS_UID = __webpack_require__(/*! ../internals/use-symbol-as-uid */ "./node_modules/core-js/internals/use-symbol-as-uid.js");

var Symbol = globalThis.Symbol;
var WellKnownSymbolsStore = shared('wks');
var createWellKnownSymbol = USE_SYMBOL_AS_UID ? Symbol['for'] || Symbol : Symbol && Symbol.withoutSetter || uid;

module.exports = function (name) {
  if (!hasOwn(WellKnownSymbolsStore, name)) {
    WellKnownSymbolsStore[name] = NATIVE_SYMBOL && hasOwn(Symbol, name)
      ? Symbol[name]
      : createWellKnownSymbol('Symbol.' + name);
  } return WellKnownSymbolsStore[name];
};


/***/ }),

/***/ "./node_modules/core-js/internals/whitespaces.js":
/*!*******************************************************!*\
  !*** ./node_modules/core-js/internals/whitespaces.js ***!
  \*******************************************************/
/***/ ((module) => {


// a string of all valid unicode whitespaces
module.exports = '\u0009\u000A\u000B\u000C\u000D\u0020\u00A0\u1680\u2000\u2001\u2002' +
  '\u2003\u2004\u2005\u2006\u2007\u2008\u2009\u200A\u202F\u205F\u3000\u2028\u2029\uFEFF';


/***/ }),

/***/ "./node_modules/core-js/modules/es.array.concat.js":
/*!*********************************************************!*\
  !*** ./node_modules/core-js/modules/es.array.concat.js ***!
  \*********************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");
var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");
var isArray = __webpack_require__(/*! ../internals/is-array */ "./node_modules/core-js/internals/is-array.js");
var isObject = __webpack_require__(/*! ../internals/is-object */ "./node_modules/core-js/internals/is-object.js");
var toObject = __webpack_require__(/*! ../internals/to-object */ "./node_modules/core-js/internals/to-object.js");
var lengthOfArrayLike = __webpack_require__(/*! ../internals/length-of-array-like */ "./node_modules/core-js/internals/length-of-array-like.js");
var doesNotExceedSafeInteger = __webpack_require__(/*! ../internals/does-not-exceed-safe-integer */ "./node_modules/core-js/internals/does-not-exceed-safe-integer.js");
var createProperty = __webpack_require__(/*! ../internals/create-property */ "./node_modules/core-js/internals/create-property.js");
var arraySpeciesCreate = __webpack_require__(/*! ../internals/array-species-create */ "./node_modules/core-js/internals/array-species-create.js");
var arrayMethodHasSpeciesSupport = __webpack_require__(/*! ../internals/array-method-has-species-support */ "./node_modules/core-js/internals/array-method-has-species-support.js");
var wellKnownSymbol = __webpack_require__(/*! ../internals/well-known-symbol */ "./node_modules/core-js/internals/well-known-symbol.js");
var V8_VERSION = __webpack_require__(/*! ../internals/environment-v8-version */ "./node_modules/core-js/internals/environment-v8-version.js");

var IS_CONCAT_SPREADABLE = wellKnownSymbol('isConcatSpreadable');

// We can't use this feature detection in V8 since it causes
// deoptimization and serious performance degradation
// https://github.com/zloirock/core-js/issues/679
var IS_CONCAT_SPREADABLE_SUPPORT = V8_VERSION >= 51 || !fails(function () {
  var array = [];
  array[IS_CONCAT_SPREADABLE] = false;
  return array.concat()[0] !== array;
});

var isConcatSpreadable = function (O) {
  if (!isObject(O)) return false;
  var spreadable = O[IS_CONCAT_SPREADABLE];
  return spreadable !== undefined ? !!spreadable : isArray(O);
};

var FORCED = !IS_CONCAT_SPREADABLE_SUPPORT || !arrayMethodHasSpeciesSupport('concat');

// `Array.prototype.concat` method
// https://tc39.es/ecma262/#sec-array.prototype.concat
// with adding support of @@isConcatSpreadable and @@species
$({ target: 'Array', proto: true, arity: 1, forced: FORCED }, {
  // eslint-disable-next-line no-unused-vars -- required for `.length`
  concat: function concat(arg) {
    var O = toObject(this);
    var A = arraySpeciesCreate(O, 0);
    var n = 0;
    var i, k, length, len, E;
    for (i = -1, length = arguments.length; i < length; i++) {
      E = i === -1 ? O : arguments[i];
      if (isConcatSpreadable(E)) {
        len = lengthOfArrayLike(E);
        doesNotExceedSafeInteger(n + len);
        for (k = 0; k < len; k++, n++) if (k in E) createProperty(A, n, E[k]);
      } else {
        doesNotExceedSafeInteger(n + 1);
        createProperty(A, n++, E);
      }
    }
    A.length = n;
    return A;
  }
});


/***/ }),

/***/ "./node_modules/core-js/modules/es.array.filter.js":
/*!*********************************************************!*\
  !*** ./node_modules/core-js/modules/es.array.filter.js ***!
  \*********************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");
var $filter = (__webpack_require__(/*! ../internals/array-iteration */ "./node_modules/core-js/internals/array-iteration.js").filter);
var arrayMethodHasSpeciesSupport = __webpack_require__(/*! ../internals/array-method-has-species-support */ "./node_modules/core-js/internals/array-method-has-species-support.js");

var HAS_SPECIES_SUPPORT = arrayMethodHasSpeciesSupport('filter');

// `Array.prototype.filter` method
// https://tc39.es/ecma262/#sec-array.prototype.filter
// with adding support of @@species
$({ target: 'Array', proto: true, forced: !HAS_SPECIES_SUPPORT }, {
  filter: function filter(callbackfn /* , thisArg */) {
    return $filter(this, callbackfn, arguments.length > 1 ? arguments[1] : undefined);
  }
});


/***/ }),

/***/ "./node_modules/core-js/modules/es.array.find.js":
/*!*******************************************************!*\
  !*** ./node_modules/core-js/modules/es.array.find.js ***!
  \*******************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");
var $find = (__webpack_require__(/*! ../internals/array-iteration */ "./node_modules/core-js/internals/array-iteration.js").find);
var addToUnscopables = __webpack_require__(/*! ../internals/add-to-unscopables */ "./node_modules/core-js/internals/add-to-unscopables.js");

var FIND = 'find';
var SKIPS_HOLES = true;

// Shouldn't skip holes
// eslint-disable-next-line es/no-array-prototype-find -- testing
if (FIND in []) Array(1)[FIND](function () { SKIPS_HOLES = false; });

// `Array.prototype.find` method
// https://tc39.es/ecma262/#sec-array.prototype.find
$({ target: 'Array', proto: true, forced: SKIPS_HOLES }, {
  find: function find(callbackfn /* , that = undefined */) {
    return $find(this, callbackfn, arguments.length > 1 ? arguments[1] : undefined);
  }
});

// https://tc39.es/ecma262/#sec-array.prototype-@@unscopables
addToUnscopables(FIND);


/***/ }),

/***/ "./node_modules/core-js/modules/es.array.from.js":
/*!*******************************************************!*\
  !*** ./node_modules/core-js/modules/es.array.from.js ***!
  \*******************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");
var from = __webpack_require__(/*! ../internals/array-from */ "./node_modules/core-js/internals/array-from.js");
var checkCorrectnessOfIteration = __webpack_require__(/*! ../internals/check-correctness-of-iteration */ "./node_modules/core-js/internals/check-correctness-of-iteration.js");

var INCORRECT_ITERATION = !checkCorrectnessOfIteration(function (iterable) {
  // eslint-disable-next-line es/no-array-from -- required for testing
  Array.from(iterable);
});

// `Array.from` method
// https://tc39.es/ecma262/#sec-array.from
$({ target: 'Array', stat: true, forced: INCORRECT_ITERATION }, {
  from: from
});


/***/ }),

/***/ "./node_modules/core-js/modules/es.array.includes.js":
/*!***********************************************************!*\
  !*** ./node_modules/core-js/modules/es.array.includes.js ***!
  \***********************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");
var $includes = (__webpack_require__(/*! ../internals/array-includes */ "./node_modules/core-js/internals/array-includes.js").includes);
var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");
var addToUnscopables = __webpack_require__(/*! ../internals/add-to-unscopables */ "./node_modules/core-js/internals/add-to-unscopables.js");

// FF99+ bug
var BROKEN_ON_SPARSE = fails(function () {
  // eslint-disable-next-line es/no-array-prototype-includes -- detection
  return !Array(1).includes();
});

// `Array.prototype.includes` method
// https://tc39.es/ecma262/#sec-array.prototype.includes
$({ target: 'Array', proto: true, forced: BROKEN_ON_SPARSE }, {
  includes: function includes(el /* , fromIndex = 0 */) {
    return $includes(this, el, arguments.length > 1 ? arguments[1] : undefined);
  }
});

// https://tc39.es/ecma262/#sec-array.prototype-@@unscopables
addToUnscopables('includes');


/***/ }),

/***/ "./node_modules/core-js/modules/es.array.iterator.js":
/*!***********************************************************!*\
  !*** ./node_modules/core-js/modules/es.array.iterator.js ***!
  \***********************************************************/
/***/ ((module, __unused_webpack_exports, __webpack_require__) => {


var toIndexedObject = __webpack_require__(/*! ../internals/to-indexed-object */ "./node_modules/core-js/internals/to-indexed-object.js");
var addToUnscopables = __webpack_require__(/*! ../internals/add-to-unscopables */ "./node_modules/core-js/internals/add-to-unscopables.js");
var Iterators = __webpack_require__(/*! ../internals/iterators */ "./node_modules/core-js/internals/iterators.js");
var InternalStateModule = __webpack_require__(/*! ../internals/internal-state */ "./node_modules/core-js/internals/internal-state.js");
var defineProperty = (__webpack_require__(/*! ../internals/object-define-property */ "./node_modules/core-js/internals/object-define-property.js").f);
var defineIterator = __webpack_require__(/*! ../internals/iterator-define */ "./node_modules/core-js/internals/iterator-define.js");
var createIterResultObject = __webpack_require__(/*! ../internals/create-iter-result-object */ "./node_modules/core-js/internals/create-iter-result-object.js");
var IS_PURE = __webpack_require__(/*! ../internals/is-pure */ "./node_modules/core-js/internals/is-pure.js");
var DESCRIPTORS = __webpack_require__(/*! ../internals/descriptors */ "./node_modules/core-js/internals/descriptors.js");

var ARRAY_ITERATOR = 'Array Iterator';
var setInternalState = InternalStateModule.set;
var getInternalState = InternalStateModule.getterFor(ARRAY_ITERATOR);

// `Array.prototype.entries` method
// https://tc39.es/ecma262/#sec-array.prototype.entries
// `Array.prototype.keys` method
// https://tc39.es/ecma262/#sec-array.prototype.keys
// `Array.prototype.values` method
// https://tc39.es/ecma262/#sec-array.prototype.values
// `Array.prototype[@@iterator]` method
// https://tc39.es/ecma262/#sec-array.prototype-@@iterator
// `CreateArrayIterator` internal method
// https://tc39.es/ecma262/#sec-createarrayiterator
module.exports = defineIterator(Array, 'Array', function (iterated, kind) {
  setInternalState(this, {
    type: ARRAY_ITERATOR,
    target: toIndexedObject(iterated), // target
    index: 0,                          // next index
    kind: kind                         // kind
  });
// `%ArrayIteratorPrototype%.next` method
// https://tc39.es/ecma262/#sec-%arrayiteratorprototype%.next
}, function () {
  var state = getInternalState(this);
  var target = state.target;
  var index = state.index++;
  if (!target || index >= target.length) {
    state.target = null;
    return createIterResultObject(undefined, true);
  }
  switch (state.kind) {
    case 'keys': return createIterResultObject(index, false);
    case 'values': return createIterResultObject(target[index], false);
  } return createIterResultObject([index, target[index]], false);
}, 'values');

// argumentsList[@@iterator] is %ArrayProto_values%
// https://tc39.es/ecma262/#sec-createunmappedargumentsobject
// https://tc39.es/ecma262/#sec-createmappedargumentsobject
var values = Iterators.Arguments = Iterators.Array;

// https://tc39.es/ecma262/#sec-array.prototype-@@unscopables
addToUnscopables('keys');
addToUnscopables('values');
addToUnscopables('entries');

// V8 ~ Chrome 45- bug
if (!IS_PURE && DESCRIPTORS && values.name !== 'values') try {
  defineProperty(values, 'name', { value: 'values' });
} catch (error) { /* empty */ }


/***/ }),

/***/ "./node_modules/core-js/modules/es.array.join.js":
/*!*******************************************************!*\
  !*** ./node_modules/core-js/modules/es.array.join.js ***!
  \*******************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");
var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this */ "./node_modules/core-js/internals/function-uncurry-this.js");
var IndexedObject = __webpack_require__(/*! ../internals/indexed-object */ "./node_modules/core-js/internals/indexed-object.js");
var toIndexedObject = __webpack_require__(/*! ../internals/to-indexed-object */ "./node_modules/core-js/internals/to-indexed-object.js");
var arrayMethodIsStrict = __webpack_require__(/*! ../internals/array-method-is-strict */ "./node_modules/core-js/internals/array-method-is-strict.js");

var nativeJoin = uncurryThis([].join);

var ES3_STRINGS = IndexedObject !== Object;
var FORCED = ES3_STRINGS || !arrayMethodIsStrict('join', ',');

// `Array.prototype.join` method
// https://tc39.es/ecma262/#sec-array.prototype.join
$({ target: 'Array', proto: true, forced: FORCED }, {
  join: function join(separator) {
    return nativeJoin(toIndexedObject(this), separator === undefined ? ',' : separator);
  }
});


/***/ }),

/***/ "./node_modules/core-js/modules/es.array.map.js":
/*!******************************************************!*\
  !*** ./node_modules/core-js/modules/es.array.map.js ***!
  \******************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");
var $map = (__webpack_require__(/*! ../internals/array-iteration */ "./node_modules/core-js/internals/array-iteration.js").map);
var arrayMethodHasSpeciesSupport = __webpack_require__(/*! ../internals/array-method-has-species-support */ "./node_modules/core-js/internals/array-method-has-species-support.js");

var HAS_SPECIES_SUPPORT = arrayMethodHasSpeciesSupport('map');

// `Array.prototype.map` method
// https://tc39.es/ecma262/#sec-array.prototype.map
// with adding support of @@species
$({ target: 'Array', proto: true, forced: !HAS_SPECIES_SUPPORT }, {
  map: function map(callbackfn /* , thisArg */) {
    return $map(this, callbackfn, arguments.length > 1 ? arguments[1] : undefined);
  }
});


/***/ }),

/***/ "./node_modules/core-js/modules/es.array.slice.js":
/*!********************************************************!*\
  !*** ./node_modules/core-js/modules/es.array.slice.js ***!
  \********************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");
var isArray = __webpack_require__(/*! ../internals/is-array */ "./node_modules/core-js/internals/is-array.js");
var isConstructor = __webpack_require__(/*! ../internals/is-constructor */ "./node_modules/core-js/internals/is-constructor.js");
var isObject = __webpack_require__(/*! ../internals/is-object */ "./node_modules/core-js/internals/is-object.js");
var toAbsoluteIndex = __webpack_require__(/*! ../internals/to-absolute-index */ "./node_modules/core-js/internals/to-absolute-index.js");
var lengthOfArrayLike = __webpack_require__(/*! ../internals/length-of-array-like */ "./node_modules/core-js/internals/length-of-array-like.js");
var toIndexedObject = __webpack_require__(/*! ../internals/to-indexed-object */ "./node_modules/core-js/internals/to-indexed-object.js");
var createProperty = __webpack_require__(/*! ../internals/create-property */ "./node_modules/core-js/internals/create-property.js");
var wellKnownSymbol = __webpack_require__(/*! ../internals/well-known-symbol */ "./node_modules/core-js/internals/well-known-symbol.js");
var arrayMethodHasSpeciesSupport = __webpack_require__(/*! ../internals/array-method-has-species-support */ "./node_modules/core-js/internals/array-method-has-species-support.js");
var nativeSlice = __webpack_require__(/*! ../internals/array-slice */ "./node_modules/core-js/internals/array-slice.js");

var HAS_SPECIES_SUPPORT = arrayMethodHasSpeciesSupport('slice');

var SPECIES = wellKnownSymbol('species');
var $Array = Array;
var max = Math.max;

// `Array.prototype.slice` method
// https://tc39.es/ecma262/#sec-array.prototype.slice
// fallback for not array-like ES3 strings and DOM objects
$({ target: 'Array', proto: true, forced: !HAS_SPECIES_SUPPORT }, {
  slice: function slice(start, end) {
    var O = toIndexedObject(this);
    var length = lengthOfArrayLike(O);
    var k = toAbsoluteIndex(start, length);
    var fin = toAbsoluteIndex(end === undefined ? length : end, length);
    // inline `ArraySpeciesCreate` for usage native `Array#slice` where it's possible
    var Constructor, result, n;
    if (isArray(O)) {
      Constructor = O.constructor;
      // cross-realm fallback
      if (isConstructor(Constructor) && (Constructor === $Array || isArray(Constructor.prototype))) {
        Constructor = undefined;
      } else if (isObject(Constructor)) {
        Constructor = Constructor[SPECIES];
        if (Constructor === null) Constructor = undefined;
      }
      if (Constructor === $Array || Constructor === undefined) {
        return nativeSlice(O, k, fin);
      }
    }
    result = new (Constructor === undefined ? $Array : Constructor)(max(fin - k, 0));
    for (n = 0; k < fin; k++, n++) if (k in O) createProperty(result, n, O[k]);
    result.length = n;
    return result;
  }
});


/***/ }),

/***/ "./node_modules/core-js/modules/es.array.sort.js":
/*!*******************************************************!*\
  !*** ./node_modules/core-js/modules/es.array.sort.js ***!
  \*******************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");
var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this */ "./node_modules/core-js/internals/function-uncurry-this.js");
var aCallable = __webpack_require__(/*! ../internals/a-callable */ "./node_modules/core-js/internals/a-callable.js");
var toObject = __webpack_require__(/*! ../internals/to-object */ "./node_modules/core-js/internals/to-object.js");
var lengthOfArrayLike = __webpack_require__(/*! ../internals/length-of-array-like */ "./node_modules/core-js/internals/length-of-array-like.js");
var deletePropertyOrThrow = __webpack_require__(/*! ../internals/delete-property-or-throw */ "./node_modules/core-js/internals/delete-property-or-throw.js");
var toString = __webpack_require__(/*! ../internals/to-string */ "./node_modules/core-js/internals/to-string.js");
var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");
var internalSort = __webpack_require__(/*! ../internals/array-sort */ "./node_modules/core-js/internals/array-sort.js");
var arrayMethodIsStrict = __webpack_require__(/*! ../internals/array-method-is-strict */ "./node_modules/core-js/internals/array-method-is-strict.js");
var FF = __webpack_require__(/*! ../internals/environment-ff-version */ "./node_modules/core-js/internals/environment-ff-version.js");
var IE_OR_EDGE = __webpack_require__(/*! ../internals/environment-is-ie-or-edge */ "./node_modules/core-js/internals/environment-is-ie-or-edge.js");
var V8 = __webpack_require__(/*! ../internals/environment-v8-version */ "./node_modules/core-js/internals/environment-v8-version.js");
var WEBKIT = __webpack_require__(/*! ../internals/environment-webkit-version */ "./node_modules/core-js/internals/environment-webkit-version.js");

var test = [];
var nativeSort = uncurryThis(test.sort);
var push = uncurryThis(test.push);

// IE8-
var FAILS_ON_UNDEFINED = fails(function () {
  test.sort(undefined);
});
// V8 bug
var FAILS_ON_NULL = fails(function () {
  test.sort(null);
});
// Old WebKit
var STRICT_METHOD = arrayMethodIsStrict('sort');

var STABLE_SORT = !fails(function () {
  // feature detection can be too slow, so check engines versions
  if (V8) return V8 < 70;
  if (FF && FF > 3) return;
  if (IE_OR_EDGE) return true;
  if (WEBKIT) return WEBKIT < 603;

  var result = '';
  var code, chr, value, index;

  // generate an array with more 512 elements (Chakra and old V8 fails only in this case)
  for (code = 65; code < 76; code++) {
    chr = String.fromCharCode(code);

    switch (code) {
      case 66: case 69: case 70: case 72: value = 3; break;
      case 68: case 71: value = 4; break;
      default: value = 2;
    }

    for (index = 0; index < 47; index++) {
      test.push({ k: chr + index, v: value });
    }
  }

  test.sort(function (a, b) { return b.v - a.v; });

  for (index = 0; index < test.length; index++) {
    chr = test[index].k.charAt(0);
    if (result.charAt(result.length - 1) !== chr) result += chr;
  }

  return result !== 'DGBEFHACIJK';
});

var FORCED = FAILS_ON_UNDEFINED || !FAILS_ON_NULL || !STRICT_METHOD || !STABLE_SORT;

var getSortCompare = function (comparefn) {
  return function (x, y) {
    if (y === undefined) return -1;
    if (x === undefined) return 1;
    if (comparefn !== undefined) return +comparefn(x, y) || 0;
    return toString(x) > toString(y) ? 1 : -1;
  };
};

// `Array.prototype.sort` method
// https://tc39.es/ecma262/#sec-array.prototype.sort
$({ target: 'Array', proto: true, forced: FORCED }, {
  sort: function sort(comparefn) {
    if (comparefn !== undefined) aCallable(comparefn);

    var array = toObject(this);

    if (STABLE_SORT) return comparefn === undefined ? nativeSort(array) : nativeSort(array, comparefn);

    var items = [];
    var arrayLength = lengthOfArrayLike(array);
    var itemsLength, index;

    for (index = 0; index < arrayLength; index++) {
      if (index in array) push(items, array[index]);
    }

    internalSort(items, getSortCompare(comparefn));

    itemsLength = lengthOfArrayLike(items);
    index = 0;

    while (index < itemsLength) array[index] = items[index++];
    while (index < arrayLength) deletePropertyOrThrow(array, index++);

    return array;
  }
});


/***/ }),

/***/ "./node_modules/core-js/modules/es.date.to-primitive.js":
/*!**************************************************************!*\
  !*** ./node_modules/core-js/modules/es.date.to-primitive.js ***!
  \**************************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var hasOwn = __webpack_require__(/*! ../internals/has-own-property */ "./node_modules/core-js/internals/has-own-property.js");
var defineBuiltIn = __webpack_require__(/*! ../internals/define-built-in */ "./node_modules/core-js/internals/define-built-in.js");
var dateToPrimitive = __webpack_require__(/*! ../internals/date-to-primitive */ "./node_modules/core-js/internals/date-to-primitive.js");
var wellKnownSymbol = __webpack_require__(/*! ../internals/well-known-symbol */ "./node_modules/core-js/internals/well-known-symbol.js");

var TO_PRIMITIVE = wellKnownSymbol('toPrimitive');
var DatePrototype = Date.prototype;

// `Date.prototype[@@toPrimitive]` method
// https://tc39.es/ecma262/#sec-date.prototype-@@toprimitive
if (!hasOwn(DatePrototype, TO_PRIMITIVE)) {
  defineBuiltIn(DatePrototype, TO_PRIMITIVE, dateToPrimitive);
}


/***/ }),

/***/ "./node_modules/core-js/modules/es.function.name.js":
/*!**********************************************************!*\
  !*** ./node_modules/core-js/modules/es.function.name.js ***!
  \**********************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var DESCRIPTORS = __webpack_require__(/*! ../internals/descriptors */ "./node_modules/core-js/internals/descriptors.js");
var FUNCTION_NAME_EXISTS = (__webpack_require__(/*! ../internals/function-name */ "./node_modules/core-js/internals/function-name.js").EXISTS);
var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this */ "./node_modules/core-js/internals/function-uncurry-this.js");
var defineBuiltInAccessor = __webpack_require__(/*! ../internals/define-built-in-accessor */ "./node_modules/core-js/internals/define-built-in-accessor.js");

var FunctionPrototype = Function.prototype;
var functionToString = uncurryThis(FunctionPrototype.toString);
var nameRE = /function\b(?:\s|\/\*[\S\s]*?\*\/|\/\/[^\n\r]*[\n\r]+)*([^\s(/]*)/;
var regExpExec = uncurryThis(nameRE.exec);
var NAME = 'name';

// Function instances `.name` property
// https://tc39.es/ecma262/#sec-function-instances-name
if (DESCRIPTORS && !FUNCTION_NAME_EXISTS) {
  defineBuiltInAccessor(FunctionPrototype, NAME, {
    configurable: true,
    get: function () {
      try {
        return regExpExec(nameRE, functionToString(this))[1];
      } catch (error) {
        return '';
      }
    }
  });
}


/***/ }),

/***/ "./node_modules/core-js/modules/es.json.stringify.js":
/*!***********************************************************!*\
  !*** ./node_modules/core-js/modules/es.json.stringify.js ***!
  \***********************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");
var getBuiltIn = __webpack_require__(/*! ../internals/get-built-in */ "./node_modules/core-js/internals/get-built-in.js");
var apply = __webpack_require__(/*! ../internals/function-apply */ "./node_modules/core-js/internals/function-apply.js");
var call = __webpack_require__(/*! ../internals/function-call */ "./node_modules/core-js/internals/function-call.js");
var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this */ "./node_modules/core-js/internals/function-uncurry-this.js");
var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");
var isCallable = __webpack_require__(/*! ../internals/is-callable */ "./node_modules/core-js/internals/is-callable.js");
var isSymbol = __webpack_require__(/*! ../internals/is-symbol */ "./node_modules/core-js/internals/is-symbol.js");
var arraySlice = __webpack_require__(/*! ../internals/array-slice */ "./node_modules/core-js/internals/array-slice.js");
var getReplacerFunction = __webpack_require__(/*! ../internals/get-json-replacer-function */ "./node_modules/core-js/internals/get-json-replacer-function.js");
var NATIVE_SYMBOL = __webpack_require__(/*! ../internals/symbol-constructor-detection */ "./node_modules/core-js/internals/symbol-constructor-detection.js");

var $String = String;
var $stringify = getBuiltIn('JSON', 'stringify');
var exec = uncurryThis(/./.exec);
var charAt = uncurryThis(''.charAt);
var charCodeAt = uncurryThis(''.charCodeAt);
var replace = uncurryThis(''.replace);
var numberToString = uncurryThis(1.1.toString);

var tester = /[\uD800-\uDFFF]/g;
var low = /^[\uD800-\uDBFF]$/;
var hi = /^[\uDC00-\uDFFF]$/;

var WRONG_SYMBOLS_CONVERSION = !NATIVE_SYMBOL || fails(function () {
  var symbol = getBuiltIn('Symbol')('stringify detection');
  // MS Edge converts symbol values to JSON as {}
  return $stringify([symbol]) !== '[null]'
    // WebKit converts symbol values to JSON as null
    || $stringify({ a: symbol }) !== '{}'
    // V8 throws on boxed symbols
    || $stringify(Object(symbol)) !== '{}';
});

// https://github.com/tc39/proposal-well-formed-stringify
var ILL_FORMED_UNICODE = fails(function () {
  return $stringify('\uDF06\uD834') !== '"\\udf06\\ud834"'
    || $stringify('\uDEAD') !== '"\\udead"';
});

var stringifyWithSymbolsFix = function (it, replacer) {
  var args = arraySlice(arguments);
  var $replacer = getReplacerFunction(replacer);
  if (!isCallable($replacer) && (it === undefined || isSymbol(it))) return; // IE8 returns string on undefined
  args[1] = function (key, value) {
    // some old implementations (like WebKit) could pass numbers as keys
    if (isCallable($replacer)) value = call($replacer, this, $String(key), value);
    if (!isSymbol(value)) return value;
  };
  return apply($stringify, null, args);
};

var fixIllFormed = function (match, offset, string) {
  var prev = charAt(string, offset - 1);
  var next = charAt(string, offset + 1);
  if ((exec(low, match) && !exec(hi, next)) || (exec(hi, match) && !exec(low, prev))) {
    return '\\u' + numberToString(charCodeAt(match, 0), 16);
  } return match;
};

if ($stringify) {
  // `JSON.stringify` method
  // https://tc39.es/ecma262/#sec-json.stringify
  $({ target: 'JSON', stat: true, arity: 3, forced: WRONG_SYMBOLS_CONVERSION || ILL_FORMED_UNICODE }, {
    // eslint-disable-next-line no-unused-vars -- required for `.length`
    stringify: function stringify(it, replacer, space) {
      var args = arraySlice(arguments);
      var result = apply(WRONG_SYMBOLS_CONVERSION ? stringifyWithSymbolsFix : $stringify, null, args);
      return ILL_FORMED_UNICODE && typeof result == 'string' ? replace(result, tester, fixIllFormed) : result;
    }
  });
}


/***/ }),

/***/ "./node_modules/core-js/modules/es.number.constructor.js":
/*!***************************************************************!*\
  !*** ./node_modules/core-js/modules/es.number.constructor.js ***!
  \***************************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");
var IS_PURE = __webpack_require__(/*! ../internals/is-pure */ "./node_modules/core-js/internals/is-pure.js");
var DESCRIPTORS = __webpack_require__(/*! ../internals/descriptors */ "./node_modules/core-js/internals/descriptors.js");
var globalThis = __webpack_require__(/*! ../internals/global-this */ "./node_modules/core-js/internals/global-this.js");
var path = __webpack_require__(/*! ../internals/path */ "./node_modules/core-js/internals/path.js");
var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this */ "./node_modules/core-js/internals/function-uncurry-this.js");
var isForced = __webpack_require__(/*! ../internals/is-forced */ "./node_modules/core-js/internals/is-forced.js");
var hasOwn = __webpack_require__(/*! ../internals/has-own-property */ "./node_modules/core-js/internals/has-own-property.js");
var inheritIfRequired = __webpack_require__(/*! ../internals/inherit-if-required */ "./node_modules/core-js/internals/inherit-if-required.js");
var isPrototypeOf = __webpack_require__(/*! ../internals/object-is-prototype-of */ "./node_modules/core-js/internals/object-is-prototype-of.js");
var isSymbol = __webpack_require__(/*! ../internals/is-symbol */ "./node_modules/core-js/internals/is-symbol.js");
var toPrimitive = __webpack_require__(/*! ../internals/to-primitive */ "./node_modules/core-js/internals/to-primitive.js");
var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");
var getOwnPropertyNames = (__webpack_require__(/*! ../internals/object-get-own-property-names */ "./node_modules/core-js/internals/object-get-own-property-names.js").f);
var getOwnPropertyDescriptor = (__webpack_require__(/*! ../internals/object-get-own-property-descriptor */ "./node_modules/core-js/internals/object-get-own-property-descriptor.js").f);
var defineProperty = (__webpack_require__(/*! ../internals/object-define-property */ "./node_modules/core-js/internals/object-define-property.js").f);
var thisNumberValue = __webpack_require__(/*! ../internals/this-number-value */ "./node_modules/core-js/internals/this-number-value.js");
var trim = (__webpack_require__(/*! ../internals/string-trim */ "./node_modules/core-js/internals/string-trim.js").trim);

var NUMBER = 'Number';
var NativeNumber = globalThis[NUMBER];
var PureNumberNamespace = path[NUMBER];
var NumberPrototype = NativeNumber.prototype;
var TypeError = globalThis.TypeError;
var stringSlice = uncurryThis(''.slice);
var charCodeAt = uncurryThis(''.charCodeAt);

// `ToNumeric` abstract operation
// https://tc39.es/ecma262/#sec-tonumeric
var toNumeric = function (value) {
  var primValue = toPrimitive(value, 'number');
  return typeof primValue == 'bigint' ? primValue : toNumber(primValue);
};

// `ToNumber` abstract operation
// https://tc39.es/ecma262/#sec-tonumber
var toNumber = function (argument) {
  var it = toPrimitive(argument, 'number');
  var first, third, radix, maxCode, digits, length, index, code;
  if (isSymbol(it)) throw new TypeError('Cannot convert a Symbol value to a number');
  if (typeof it == 'string' && it.length > 2) {
    it = trim(it);
    first = charCodeAt(it, 0);
    if (first === 43 || first === 45) {
      third = charCodeAt(it, 2);
      if (third === 88 || third === 120) return NaN; // Number('+0x1') should be NaN, old V8 fix
    } else if (first === 48) {
      switch (charCodeAt(it, 1)) {
        // fast equal of /^0b[01]+$/i
        case 66:
        case 98:
          radix = 2;
          maxCode = 49;
          break;
        // fast equal of /^0o[0-7]+$/i
        case 79:
        case 111:
          radix = 8;
          maxCode = 55;
          break;
        default:
          return +it;
      }
      digits = stringSlice(it, 2);
      length = digits.length;
      for (index = 0; index < length; index++) {
        code = charCodeAt(digits, index);
        // parseInt parses a string to a first unavailable symbol
        // but ToNumber should return NaN if a string contains unavailable symbols
        if (code < 48 || code > maxCode) return NaN;
      } return parseInt(digits, radix);
    }
  } return +it;
};

var FORCED = isForced(NUMBER, !NativeNumber(' 0o1') || !NativeNumber('0b1') || NativeNumber('+0x1'));

var calledWithNew = function (dummy) {
  // includes check on 1..constructor(foo) case
  return isPrototypeOf(NumberPrototype, dummy) && fails(function () { thisNumberValue(dummy); });
};

// `Number` constructor
// https://tc39.es/ecma262/#sec-number-constructor
var NumberWrapper = function Number(value) {
  var n = arguments.length < 1 ? 0 : NativeNumber(toNumeric(value));
  return calledWithNew(this) ? inheritIfRequired(Object(n), this, NumberWrapper) : n;
};

NumberWrapper.prototype = NumberPrototype;
if (FORCED && !IS_PURE) NumberPrototype.constructor = NumberWrapper;

$({ global: true, constructor: true, wrap: true, forced: FORCED }, {
  Number: NumberWrapper
});

// Use `internal/copy-constructor-properties` helper in `core-js@4`
var copyConstructorProperties = function (target, source) {
  for (var keys = DESCRIPTORS ? getOwnPropertyNames(source) : (
    // ES3:
    'MAX_VALUE,MIN_VALUE,NaN,NEGATIVE_INFINITY,POSITIVE_INFINITY,' +
    // ES2015 (in case, if modules with ES2015 Number statics required before):
    'EPSILON,MAX_SAFE_INTEGER,MIN_SAFE_INTEGER,isFinite,isInteger,isNaN,isSafeInteger,parseFloat,parseInt,' +
    // ESNext
    'fromString,range'
  ).split(','), j = 0, key; keys.length > j; j++) {
    if (hasOwn(source, key = keys[j]) && !hasOwn(target, key)) {
      defineProperty(target, key, getOwnPropertyDescriptor(source, key));
    }
  }
};

if (IS_PURE && PureNumberNamespace) copyConstructorProperties(path[NUMBER], PureNumberNamespace);
if (FORCED || IS_PURE) copyConstructorProperties(path[NUMBER], NativeNumber);


/***/ }),

/***/ "./node_modules/core-js/modules/es.number.is-nan.js":
/*!**********************************************************!*\
  !*** ./node_modules/core-js/modules/es.number.is-nan.js ***!
  \**********************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");

// `Number.isNaN` method
// https://tc39.es/ecma262/#sec-number.isnan
$({ target: 'Number', stat: true }, {
  isNaN: function isNaN(number) {
    // eslint-disable-next-line no-self-compare -- NaN check
    return number !== number;
  }
});


/***/ }),

/***/ "./node_modules/core-js/modules/es.number.to-fixed.js":
/*!************************************************************!*\
  !*** ./node_modules/core-js/modules/es.number.to-fixed.js ***!
  \************************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");
var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this */ "./node_modules/core-js/internals/function-uncurry-this.js");
var toIntegerOrInfinity = __webpack_require__(/*! ../internals/to-integer-or-infinity */ "./node_modules/core-js/internals/to-integer-or-infinity.js");
var thisNumberValue = __webpack_require__(/*! ../internals/this-number-value */ "./node_modules/core-js/internals/this-number-value.js");
var $repeat = __webpack_require__(/*! ../internals/string-repeat */ "./node_modules/core-js/internals/string-repeat.js");
var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");

var $RangeError = RangeError;
var $String = String;
var floor = Math.floor;
var repeat = uncurryThis($repeat);
var stringSlice = uncurryThis(''.slice);
var nativeToFixed = uncurryThis(1.1.toFixed);

var pow = function (x, n, acc) {
  return n === 0 ? acc : n % 2 === 1 ? pow(x, n - 1, acc * x) : pow(x * x, n / 2, acc);
};

var log = function (x) {
  var n = 0;
  var x2 = x;
  while (x2 >= 4096) {
    n += 12;
    x2 /= 4096;
  }
  while (x2 >= 2) {
    n += 1;
    x2 /= 2;
  } return n;
};

var multiply = function (data, n, c) {
  var index = -1;
  var c2 = c;
  while (++index < 6) {
    c2 += n * data[index];
    data[index] = c2 % 1e7;
    c2 = floor(c2 / 1e7);
  }
};

var divide = function (data, n) {
  var index = 6;
  var c = 0;
  while (--index >= 0) {
    c += data[index];
    data[index] = floor(c / n);
    c = (c % n) * 1e7;
  }
};

var dataToString = function (data) {
  var index = 6;
  var s = '';
  while (--index >= 0) {
    if (s !== '' || index === 0 || data[index] !== 0) {
      var t = $String(data[index]);
      s = s === '' ? t : s + repeat('0', 7 - t.length) + t;
    }
  } return s;
};

var FORCED = fails(function () {
  return nativeToFixed(0.00008, 3) !== '0.000' ||
    nativeToFixed(0.9, 0) !== '1' ||
    nativeToFixed(1.255, 2) !== '1.25' ||
    nativeToFixed(1000000000000000128.0, 0) !== '1000000000000000128';
}) || !fails(function () {
  // V8 ~ Android 4.3-
  nativeToFixed({});
});

// `Number.prototype.toFixed` method
// https://tc39.es/ecma262/#sec-number.prototype.tofixed
$({ target: 'Number', proto: true, forced: FORCED }, {
  toFixed: function toFixed(fractionDigits) {
    var number = thisNumberValue(this);
    var fractDigits = toIntegerOrInfinity(fractionDigits);
    var data = [0, 0, 0, 0, 0, 0];
    var sign = '';
    var result = '0';
    var e, z, j, k;

    // TODO: ES2018 increased the maximum number of fraction digits to 100, need to improve the implementation
    if (fractDigits < 0 || fractDigits > 20) throw new $RangeError('Incorrect fraction digits');
    // eslint-disable-next-line no-self-compare -- NaN check
    if (number !== number) return 'NaN';
    if (number <= -1e21 || number >= 1e21) return $String(number);
    if (number < 0) {
      sign = '-';
      number = -number;
    }
    if (number > 1e-21) {
      e = log(number * pow(2, 69, 1)) - 69;
      z = e < 0 ? number * pow(2, -e, 1) : number / pow(2, e, 1);
      z *= 0x10000000000000;
      e = 52 - e;
      if (e > 0) {
        multiply(data, 0, z);
        j = fractDigits;
        while (j >= 7) {
          multiply(data, 1e7, 0);
          j -= 7;
        }
        multiply(data, pow(10, j, 1), 0);
        j = e - 1;
        while (j >= 23) {
          divide(data, 1 << 23);
          j -= 23;
        }
        divide(data, 1 << j);
        multiply(data, 1, 1);
        divide(data, 2);
        result = dataToString(data);
      } else {
        multiply(data, 0, z);
        multiply(data, 1 << -e, 0);
        result = dataToString(data) + repeat('0', fractDigits);
      }
    }
    if (fractDigits > 0) {
      k = result.length;
      result = sign + (k <= fractDigits
        ? '0.' + repeat('0', fractDigits - k) + result
        : stringSlice(result, 0, k - fractDigits) + '.' + stringSlice(result, k - fractDigits));
    } else {
      result = sign + result;
    } return result;
  }
});


/***/ }),

/***/ "./node_modules/core-js/modules/es.object.entries.js":
/*!***********************************************************!*\
  !*** ./node_modules/core-js/modules/es.object.entries.js ***!
  \***********************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");
var $entries = (__webpack_require__(/*! ../internals/object-to-array */ "./node_modules/core-js/internals/object-to-array.js").entries);

// `Object.entries` method
// https://tc39.es/ecma262/#sec-object.entries
$({ target: 'Object', stat: true }, {
  entries: function entries(O) {
    return $entries(O);
  }
});


/***/ }),

/***/ "./node_modules/core-js/modules/es.object.get-own-property-symbols.js":
/*!****************************************************************************!*\
  !*** ./node_modules/core-js/modules/es.object.get-own-property-symbols.js ***!
  \****************************************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");
var NATIVE_SYMBOL = __webpack_require__(/*! ../internals/symbol-constructor-detection */ "./node_modules/core-js/internals/symbol-constructor-detection.js");
var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");
var getOwnPropertySymbolsModule = __webpack_require__(/*! ../internals/object-get-own-property-symbols */ "./node_modules/core-js/internals/object-get-own-property-symbols.js");
var toObject = __webpack_require__(/*! ../internals/to-object */ "./node_modules/core-js/internals/to-object.js");

// V8 ~ Chrome 38 and 39 `Object.getOwnPropertySymbols` fails on primitives
// https://bugs.chromium.org/p/v8/issues/detail?id=3443
var FORCED = !NATIVE_SYMBOL || fails(function () { getOwnPropertySymbolsModule.f(1); });

// `Object.getOwnPropertySymbols` method
// https://tc39.es/ecma262/#sec-object.getownpropertysymbols
$({ target: 'Object', stat: true, forced: FORCED }, {
  getOwnPropertySymbols: function getOwnPropertySymbols(it) {
    var $getOwnPropertySymbols = getOwnPropertySymbolsModule.f;
    return $getOwnPropertySymbols ? $getOwnPropertySymbols(toObject(it)) : [];
  }
});


/***/ }),

/***/ "./node_modules/core-js/modules/es.object.get-prototype-of.js":
/*!********************************************************************!*\
  !*** ./node_modules/core-js/modules/es.object.get-prototype-of.js ***!
  \********************************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");
var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");
var toObject = __webpack_require__(/*! ../internals/to-object */ "./node_modules/core-js/internals/to-object.js");
var nativeGetPrototypeOf = __webpack_require__(/*! ../internals/object-get-prototype-of */ "./node_modules/core-js/internals/object-get-prototype-of.js");
var CORRECT_PROTOTYPE_GETTER = __webpack_require__(/*! ../internals/correct-prototype-getter */ "./node_modules/core-js/internals/correct-prototype-getter.js");

var FAILS_ON_PRIMITIVES = fails(function () { nativeGetPrototypeOf(1); });

// `Object.getPrototypeOf` method
// https://tc39.es/ecma262/#sec-object.getprototypeof
$({ target: 'Object', stat: true, forced: FAILS_ON_PRIMITIVES, sham: !CORRECT_PROTOTYPE_GETTER }, {
  getPrototypeOf: function getPrototypeOf(it) {
    return nativeGetPrototypeOf(toObject(it));
  }
});



/***/ }),

/***/ "./node_modules/core-js/modules/es.object.keys.js":
/*!********************************************************!*\
  !*** ./node_modules/core-js/modules/es.object.keys.js ***!
  \********************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");
var toObject = __webpack_require__(/*! ../internals/to-object */ "./node_modules/core-js/internals/to-object.js");
var nativeKeys = __webpack_require__(/*! ../internals/object-keys */ "./node_modules/core-js/internals/object-keys.js");
var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");

var FAILS_ON_PRIMITIVES = fails(function () { nativeKeys(1); });

// `Object.keys` method
// https://tc39.es/ecma262/#sec-object.keys
$({ target: 'Object', stat: true, forced: FAILS_ON_PRIMITIVES }, {
  keys: function keys(it) {
    return nativeKeys(toObject(it));
  }
});


/***/ }),

/***/ "./node_modules/core-js/modules/es.object.set-prototype-of.js":
/*!********************************************************************!*\
  !*** ./node_modules/core-js/modules/es.object.set-prototype-of.js ***!
  \********************************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");
var setPrototypeOf = __webpack_require__(/*! ../internals/object-set-prototype-of */ "./node_modules/core-js/internals/object-set-prototype-of.js");

// `Object.setPrototypeOf` method
// https://tc39.es/ecma262/#sec-object.setprototypeof
$({ target: 'Object', stat: true }, {
  setPrototypeOf: setPrototypeOf
});


/***/ }),

/***/ "./node_modules/core-js/modules/es.object.to-string.js":
/*!*************************************************************!*\
  !*** ./node_modules/core-js/modules/es.object.to-string.js ***!
  \*************************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var TO_STRING_TAG_SUPPORT = __webpack_require__(/*! ../internals/to-string-tag-support */ "./node_modules/core-js/internals/to-string-tag-support.js");
var defineBuiltIn = __webpack_require__(/*! ../internals/define-built-in */ "./node_modules/core-js/internals/define-built-in.js");
var toString = __webpack_require__(/*! ../internals/object-to-string */ "./node_modules/core-js/internals/object-to-string.js");

// `Object.prototype.toString` method
// https://tc39.es/ecma262/#sec-object.prototype.tostring
if (!TO_STRING_TAG_SUPPORT) {
  defineBuiltIn(Object.prototype, 'toString', toString, { unsafe: true });
}


/***/ }),

/***/ "./node_modules/core-js/modules/es.object.values.js":
/*!**********************************************************!*\
  !*** ./node_modules/core-js/modules/es.object.values.js ***!
  \**********************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");
var $values = (__webpack_require__(/*! ../internals/object-to-array */ "./node_modules/core-js/internals/object-to-array.js").values);

// `Object.values` method
// https://tc39.es/ecma262/#sec-object.values
$({ target: 'Object', stat: true }, {
  values: function values(O) {
    return $values(O);
  }
});


/***/ }),

/***/ "./node_modules/core-js/modules/es.promise.all.js":
/*!********************************************************!*\
  !*** ./node_modules/core-js/modules/es.promise.all.js ***!
  \********************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");
var call = __webpack_require__(/*! ../internals/function-call */ "./node_modules/core-js/internals/function-call.js");
var aCallable = __webpack_require__(/*! ../internals/a-callable */ "./node_modules/core-js/internals/a-callable.js");
var newPromiseCapabilityModule = __webpack_require__(/*! ../internals/new-promise-capability */ "./node_modules/core-js/internals/new-promise-capability.js");
var perform = __webpack_require__(/*! ../internals/perform */ "./node_modules/core-js/internals/perform.js");
var iterate = __webpack_require__(/*! ../internals/iterate */ "./node_modules/core-js/internals/iterate.js");
var PROMISE_STATICS_INCORRECT_ITERATION = __webpack_require__(/*! ../internals/promise-statics-incorrect-iteration */ "./node_modules/core-js/internals/promise-statics-incorrect-iteration.js");

// `Promise.all` method
// https://tc39.es/ecma262/#sec-promise.all
$({ target: 'Promise', stat: true, forced: PROMISE_STATICS_INCORRECT_ITERATION }, {
  all: function all(iterable) {
    var C = this;
    var capability = newPromiseCapabilityModule.f(C);
    var resolve = capability.resolve;
    var reject = capability.reject;
    var result = perform(function () {
      var $promiseResolve = aCallable(C.resolve);
      var values = [];
      var counter = 0;
      var remaining = 1;
      iterate(iterable, function (promise) {
        var index = counter++;
        var alreadyCalled = false;
        remaining++;
        call($promiseResolve, C, promise).then(function (value) {
          if (alreadyCalled) return;
          alreadyCalled = true;
          values[index] = value;
          --remaining || resolve(values);
        }, reject);
      });
      --remaining || resolve(values);
    });
    if (result.error) reject(result.value);
    return capability.promise;
  }
});


/***/ }),

/***/ "./node_modules/core-js/modules/es.promise.catch.js":
/*!**********************************************************!*\
  !*** ./node_modules/core-js/modules/es.promise.catch.js ***!
  \**********************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");
var IS_PURE = __webpack_require__(/*! ../internals/is-pure */ "./node_modules/core-js/internals/is-pure.js");
var FORCED_PROMISE_CONSTRUCTOR = (__webpack_require__(/*! ../internals/promise-constructor-detection */ "./node_modules/core-js/internals/promise-constructor-detection.js").CONSTRUCTOR);
var NativePromiseConstructor = __webpack_require__(/*! ../internals/promise-native-constructor */ "./node_modules/core-js/internals/promise-native-constructor.js");
var getBuiltIn = __webpack_require__(/*! ../internals/get-built-in */ "./node_modules/core-js/internals/get-built-in.js");
var isCallable = __webpack_require__(/*! ../internals/is-callable */ "./node_modules/core-js/internals/is-callable.js");
var defineBuiltIn = __webpack_require__(/*! ../internals/define-built-in */ "./node_modules/core-js/internals/define-built-in.js");

var NativePromisePrototype = NativePromiseConstructor && NativePromiseConstructor.prototype;

// `Promise.prototype.catch` method
// https://tc39.es/ecma262/#sec-promise.prototype.catch
$({ target: 'Promise', proto: true, forced: FORCED_PROMISE_CONSTRUCTOR, real: true }, {
  'catch': function (onRejected) {
    return this.then(undefined, onRejected);
  }
});

// makes sure that native promise-based APIs `Promise#catch` properly works with patched `Promise#then`
if (!IS_PURE && isCallable(NativePromiseConstructor)) {
  var method = getBuiltIn('Promise').prototype['catch'];
  if (NativePromisePrototype['catch'] !== method) {
    defineBuiltIn(NativePromisePrototype, 'catch', method, { unsafe: true });
  }
}


/***/ }),

/***/ "./node_modules/core-js/modules/es.promise.constructor.js":
/*!****************************************************************!*\
  !*** ./node_modules/core-js/modules/es.promise.constructor.js ***!
  \****************************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");
var IS_PURE = __webpack_require__(/*! ../internals/is-pure */ "./node_modules/core-js/internals/is-pure.js");
var IS_NODE = __webpack_require__(/*! ../internals/environment-is-node */ "./node_modules/core-js/internals/environment-is-node.js");
var globalThis = __webpack_require__(/*! ../internals/global-this */ "./node_modules/core-js/internals/global-this.js");
var path = __webpack_require__(/*! ../internals/path */ "./node_modules/core-js/internals/path.js");
var call = __webpack_require__(/*! ../internals/function-call */ "./node_modules/core-js/internals/function-call.js");
var defineBuiltIn = __webpack_require__(/*! ../internals/define-built-in */ "./node_modules/core-js/internals/define-built-in.js");
var setPrototypeOf = __webpack_require__(/*! ../internals/object-set-prototype-of */ "./node_modules/core-js/internals/object-set-prototype-of.js");
var setToStringTag = __webpack_require__(/*! ../internals/set-to-string-tag */ "./node_modules/core-js/internals/set-to-string-tag.js");
var setSpecies = __webpack_require__(/*! ../internals/set-species */ "./node_modules/core-js/internals/set-species.js");
var aCallable = __webpack_require__(/*! ../internals/a-callable */ "./node_modules/core-js/internals/a-callable.js");
var isCallable = __webpack_require__(/*! ../internals/is-callable */ "./node_modules/core-js/internals/is-callable.js");
var isObject = __webpack_require__(/*! ../internals/is-object */ "./node_modules/core-js/internals/is-object.js");
var anInstance = __webpack_require__(/*! ../internals/an-instance */ "./node_modules/core-js/internals/an-instance.js");
var speciesConstructor = __webpack_require__(/*! ../internals/species-constructor */ "./node_modules/core-js/internals/species-constructor.js");
var task = (__webpack_require__(/*! ../internals/task */ "./node_modules/core-js/internals/task.js").set);
var microtask = __webpack_require__(/*! ../internals/microtask */ "./node_modules/core-js/internals/microtask.js");
var hostReportErrors = __webpack_require__(/*! ../internals/host-report-errors */ "./node_modules/core-js/internals/host-report-errors.js");
var perform = __webpack_require__(/*! ../internals/perform */ "./node_modules/core-js/internals/perform.js");
var Queue = __webpack_require__(/*! ../internals/queue */ "./node_modules/core-js/internals/queue.js");
var InternalStateModule = __webpack_require__(/*! ../internals/internal-state */ "./node_modules/core-js/internals/internal-state.js");
var NativePromiseConstructor = __webpack_require__(/*! ../internals/promise-native-constructor */ "./node_modules/core-js/internals/promise-native-constructor.js");
var PromiseConstructorDetection = __webpack_require__(/*! ../internals/promise-constructor-detection */ "./node_modules/core-js/internals/promise-constructor-detection.js");
var newPromiseCapabilityModule = __webpack_require__(/*! ../internals/new-promise-capability */ "./node_modules/core-js/internals/new-promise-capability.js");

var PROMISE = 'Promise';
var FORCED_PROMISE_CONSTRUCTOR = PromiseConstructorDetection.CONSTRUCTOR;
var NATIVE_PROMISE_REJECTION_EVENT = PromiseConstructorDetection.REJECTION_EVENT;
var NATIVE_PROMISE_SUBCLASSING = PromiseConstructorDetection.SUBCLASSING;
var getInternalPromiseState = InternalStateModule.getterFor(PROMISE);
var setInternalState = InternalStateModule.set;
var NativePromisePrototype = NativePromiseConstructor && NativePromiseConstructor.prototype;
var PromiseConstructor = NativePromiseConstructor;
var PromisePrototype = NativePromisePrototype;
var TypeError = globalThis.TypeError;
var document = globalThis.document;
var process = globalThis.process;
var newPromiseCapability = newPromiseCapabilityModule.f;
var newGenericPromiseCapability = newPromiseCapability;

var DISPATCH_EVENT = !!(document && document.createEvent && globalThis.dispatchEvent);
var UNHANDLED_REJECTION = 'unhandledrejection';
var REJECTION_HANDLED = 'rejectionhandled';
var PENDING = 0;
var FULFILLED = 1;
var REJECTED = 2;
var HANDLED = 1;
var UNHANDLED = 2;

var Internal, OwnPromiseCapability, PromiseWrapper, nativeThen;

// helpers
var isThenable = function (it) {
  var then;
  return isObject(it) && isCallable(then = it.then) ? then : false;
};

var callReaction = function (reaction, state) {
  var value = state.value;
  var ok = state.state === FULFILLED;
  var handler = ok ? reaction.ok : reaction.fail;
  var resolve = reaction.resolve;
  var reject = reaction.reject;
  var domain = reaction.domain;
  var result, then, exited;
  try {
    if (handler) {
      if (!ok) {
        if (state.rejection === UNHANDLED) onHandleUnhandled(state);
        state.rejection = HANDLED;
      }
      if (handler === true) result = value;
      else {
        if (domain) domain.enter();
        result = handler(value); // can throw
        if (domain) {
          domain.exit();
          exited = true;
        }
      }
      if (result === reaction.promise) {
        reject(new TypeError('Promise-chain cycle'));
      } else if (then = isThenable(result)) {
        call(then, result, resolve, reject);
      } else resolve(result);
    } else reject(value);
  } catch (error) {
    if (domain && !exited) domain.exit();
    reject(error);
  }
};

var notify = function (state, isReject) {
  if (state.notified) return;
  state.notified = true;
  microtask(function () {
    var reactions = state.reactions;
    var reaction;
    while (reaction = reactions.get()) {
      callReaction(reaction, state);
    }
    state.notified = false;
    if (isReject && !state.rejection) onUnhandled(state);
  });
};

var dispatchEvent = function (name, promise, reason) {
  var event, handler;
  if (DISPATCH_EVENT) {
    event = document.createEvent('Event');
    event.promise = promise;
    event.reason = reason;
    event.initEvent(name, false, true);
    globalThis.dispatchEvent(event);
  } else event = { promise: promise, reason: reason };
  if (!NATIVE_PROMISE_REJECTION_EVENT && (handler = globalThis['on' + name])) handler(event);
  else if (name === UNHANDLED_REJECTION) hostReportErrors('Unhandled promise rejection', reason);
};

var onUnhandled = function (state) {
  call(task, globalThis, function () {
    var promise = state.facade;
    var value = state.value;
    var IS_UNHANDLED = isUnhandled(state);
    var result;
    if (IS_UNHANDLED) {
      result = perform(function () {
        if (IS_NODE) {
          process.emit('unhandledRejection', value, promise);
        } else dispatchEvent(UNHANDLED_REJECTION, promise, value);
      });
      // Browsers should not trigger `rejectionHandled` event if it was handled here, NodeJS - should
      state.rejection = IS_NODE || isUnhandled(state) ? UNHANDLED : HANDLED;
      if (result.error) throw result.value;
    }
  });
};

var isUnhandled = function (state) {
  return state.rejection !== HANDLED && !state.parent;
};

var onHandleUnhandled = function (state) {
  call(task, globalThis, function () {
    var promise = state.facade;
    if (IS_NODE) {
      process.emit('rejectionHandled', promise);
    } else dispatchEvent(REJECTION_HANDLED, promise, state.value);
  });
};

var bind = function (fn, state, unwrap) {
  return function (value) {
    fn(state, value, unwrap);
  };
};

var internalReject = function (state, value, unwrap) {
  if (state.done) return;
  state.done = true;
  if (unwrap) state = unwrap;
  state.value = value;
  state.state = REJECTED;
  notify(state, true);
};

var internalResolve = function (state, value, unwrap) {
  if (state.done) return;
  state.done = true;
  if (unwrap) state = unwrap;
  try {
    if (state.facade === value) throw new TypeError("Promise can't be resolved itself");
    var then = isThenable(value);
    if (then) {
      microtask(function () {
        var wrapper = { done: false };
        try {
          call(then, value,
            bind(internalResolve, wrapper, state),
            bind(internalReject, wrapper, state)
          );
        } catch (error) {
          internalReject(wrapper, error, state);
        }
      });
    } else {
      state.value = value;
      state.state = FULFILLED;
      notify(state, false);
    }
  } catch (error) {
    internalReject({ done: false }, error, state);
  }
};

// constructor polyfill
if (FORCED_PROMISE_CONSTRUCTOR) {
  // 25.4.3.1 Promise(executor)
  PromiseConstructor = function Promise(executor) {
    anInstance(this, PromisePrototype);
    aCallable(executor);
    call(Internal, this);
    var state = getInternalPromiseState(this);
    try {
      executor(bind(internalResolve, state), bind(internalReject, state));
    } catch (error) {
      internalReject(state, error);
    }
  };

  PromisePrototype = PromiseConstructor.prototype;

  // eslint-disable-next-line no-unused-vars -- required for `.length`
  Internal = function Promise(executor) {
    setInternalState(this, {
      type: PROMISE,
      done: false,
      notified: false,
      parent: false,
      reactions: new Queue(),
      rejection: false,
      state: PENDING,
      value: null
    });
  };

  // `Promise.prototype.then` method
  // https://tc39.es/ecma262/#sec-promise.prototype.then
  Internal.prototype = defineBuiltIn(PromisePrototype, 'then', function then(onFulfilled, onRejected) {
    var state = getInternalPromiseState(this);
    var reaction = newPromiseCapability(speciesConstructor(this, PromiseConstructor));
    state.parent = true;
    reaction.ok = isCallable(onFulfilled) ? onFulfilled : true;
    reaction.fail = isCallable(onRejected) && onRejected;
    reaction.domain = IS_NODE ? process.domain : undefined;
    if (state.state === PENDING) state.reactions.add(reaction);
    else microtask(function () {
      callReaction(reaction, state);
    });
    return reaction.promise;
  });

  OwnPromiseCapability = function () {
    var promise = new Internal();
    var state = getInternalPromiseState(promise);
    this.promise = promise;
    this.resolve = bind(internalResolve, state);
    this.reject = bind(internalReject, state);
  };

  newPromiseCapabilityModule.f = newPromiseCapability = function (C) {
    return C === PromiseConstructor || C === PromiseWrapper
      ? new OwnPromiseCapability(C)
      : newGenericPromiseCapability(C);
  };

  if (!IS_PURE && isCallable(NativePromiseConstructor) && NativePromisePrototype !== Object.prototype) {
    nativeThen = NativePromisePrototype.then;

    if (!NATIVE_PROMISE_SUBCLASSING) {
      // make `Promise#then` return a polyfilled `Promise` for native promise-based APIs
      defineBuiltIn(NativePromisePrototype, 'then', function then(onFulfilled, onRejected) {
        var that = this;
        return new PromiseConstructor(function (resolve, reject) {
          call(nativeThen, that, resolve, reject);
        }).then(onFulfilled, onRejected);
      // https://github.com/zloirock/core-js/issues/640
      }, { unsafe: true });
    }

    // make `.constructor === Promise` work for native promise-based APIs
    try {
      delete NativePromisePrototype.constructor;
    } catch (error) { /* empty */ }

    // make `instanceof Promise` work for native promise-based APIs
    if (setPrototypeOf) {
      setPrototypeOf(NativePromisePrototype, PromisePrototype);
    }
  }
}

// `Promise` constructor
// https://tc39.es/ecma262/#sec-promise-executor
$({ global: true, constructor: true, wrap: true, forced: FORCED_PROMISE_CONSTRUCTOR }, {
  Promise: PromiseConstructor
});

PromiseWrapper = path.Promise;

setToStringTag(PromiseConstructor, PROMISE, false, true);
setSpecies(PROMISE);


/***/ }),

/***/ "./node_modules/core-js/modules/es.promise.js":
/*!****************************************************!*\
  !*** ./node_modules/core-js/modules/es.promise.js ***!
  \****************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


// TODO: Remove this module from `core-js@4` since it's split to modules listed below
__webpack_require__(/*! ../modules/es.promise.constructor */ "./node_modules/core-js/modules/es.promise.constructor.js");
__webpack_require__(/*! ../modules/es.promise.all */ "./node_modules/core-js/modules/es.promise.all.js");
__webpack_require__(/*! ../modules/es.promise.catch */ "./node_modules/core-js/modules/es.promise.catch.js");
__webpack_require__(/*! ../modules/es.promise.race */ "./node_modules/core-js/modules/es.promise.race.js");
__webpack_require__(/*! ../modules/es.promise.reject */ "./node_modules/core-js/modules/es.promise.reject.js");
__webpack_require__(/*! ../modules/es.promise.resolve */ "./node_modules/core-js/modules/es.promise.resolve.js");


/***/ }),

/***/ "./node_modules/core-js/modules/es.promise.race.js":
/*!*********************************************************!*\
  !*** ./node_modules/core-js/modules/es.promise.race.js ***!
  \*********************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");
var call = __webpack_require__(/*! ../internals/function-call */ "./node_modules/core-js/internals/function-call.js");
var aCallable = __webpack_require__(/*! ../internals/a-callable */ "./node_modules/core-js/internals/a-callable.js");
var newPromiseCapabilityModule = __webpack_require__(/*! ../internals/new-promise-capability */ "./node_modules/core-js/internals/new-promise-capability.js");
var perform = __webpack_require__(/*! ../internals/perform */ "./node_modules/core-js/internals/perform.js");
var iterate = __webpack_require__(/*! ../internals/iterate */ "./node_modules/core-js/internals/iterate.js");
var PROMISE_STATICS_INCORRECT_ITERATION = __webpack_require__(/*! ../internals/promise-statics-incorrect-iteration */ "./node_modules/core-js/internals/promise-statics-incorrect-iteration.js");

// `Promise.race` method
// https://tc39.es/ecma262/#sec-promise.race
$({ target: 'Promise', stat: true, forced: PROMISE_STATICS_INCORRECT_ITERATION }, {
  race: function race(iterable) {
    var C = this;
    var capability = newPromiseCapabilityModule.f(C);
    var reject = capability.reject;
    var result = perform(function () {
      var $promiseResolve = aCallable(C.resolve);
      iterate(iterable, function (promise) {
        call($promiseResolve, C, promise).then(capability.resolve, reject);
      });
    });
    if (result.error) reject(result.value);
    return capability.promise;
  }
});


/***/ }),

/***/ "./node_modules/core-js/modules/es.promise.reject.js":
/*!***********************************************************!*\
  !*** ./node_modules/core-js/modules/es.promise.reject.js ***!
  \***********************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");
var newPromiseCapabilityModule = __webpack_require__(/*! ../internals/new-promise-capability */ "./node_modules/core-js/internals/new-promise-capability.js");
var FORCED_PROMISE_CONSTRUCTOR = (__webpack_require__(/*! ../internals/promise-constructor-detection */ "./node_modules/core-js/internals/promise-constructor-detection.js").CONSTRUCTOR);

// `Promise.reject` method
// https://tc39.es/ecma262/#sec-promise.reject
$({ target: 'Promise', stat: true, forced: FORCED_PROMISE_CONSTRUCTOR }, {
  reject: function reject(r) {
    var capability = newPromiseCapabilityModule.f(this);
    var capabilityReject = capability.reject;
    capabilityReject(r);
    return capability.promise;
  }
});


/***/ }),

/***/ "./node_modules/core-js/modules/es.promise.resolve.js":
/*!************************************************************!*\
  !*** ./node_modules/core-js/modules/es.promise.resolve.js ***!
  \************************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");
var getBuiltIn = __webpack_require__(/*! ../internals/get-built-in */ "./node_modules/core-js/internals/get-built-in.js");
var IS_PURE = __webpack_require__(/*! ../internals/is-pure */ "./node_modules/core-js/internals/is-pure.js");
var NativePromiseConstructor = __webpack_require__(/*! ../internals/promise-native-constructor */ "./node_modules/core-js/internals/promise-native-constructor.js");
var FORCED_PROMISE_CONSTRUCTOR = (__webpack_require__(/*! ../internals/promise-constructor-detection */ "./node_modules/core-js/internals/promise-constructor-detection.js").CONSTRUCTOR);
var promiseResolve = __webpack_require__(/*! ../internals/promise-resolve */ "./node_modules/core-js/internals/promise-resolve.js");

var PromiseConstructorWrapper = getBuiltIn('Promise');
var CHECK_WRAPPER = IS_PURE && !FORCED_PROMISE_CONSTRUCTOR;

// `Promise.resolve` method
// https://tc39.es/ecma262/#sec-promise.resolve
$({ target: 'Promise', stat: true, forced: IS_PURE || FORCED_PROMISE_CONSTRUCTOR }, {
  resolve: function resolve(x) {
    return promiseResolve(CHECK_WRAPPER && this === PromiseConstructorWrapper ? NativePromiseConstructor : this, x);
  }
});


/***/ }),

/***/ "./node_modules/core-js/modules/es.regexp.exec.js":
/*!********************************************************!*\
  !*** ./node_modules/core-js/modules/es.regexp.exec.js ***!
  \********************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");
var exec = __webpack_require__(/*! ../internals/regexp-exec */ "./node_modules/core-js/internals/regexp-exec.js");

// `RegExp.prototype.exec` method
// https://tc39.es/ecma262/#sec-regexp.prototype.exec
$({ target: 'RegExp', proto: true, forced: /./.exec !== exec }, {
  exec: exec
});


/***/ }),

/***/ "./node_modules/core-js/modules/es.regexp.to-string.js":
/*!*************************************************************!*\
  !*** ./node_modules/core-js/modules/es.regexp.to-string.js ***!
  \*************************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var PROPER_FUNCTION_NAME = (__webpack_require__(/*! ../internals/function-name */ "./node_modules/core-js/internals/function-name.js").PROPER);
var defineBuiltIn = __webpack_require__(/*! ../internals/define-built-in */ "./node_modules/core-js/internals/define-built-in.js");
var anObject = __webpack_require__(/*! ../internals/an-object */ "./node_modules/core-js/internals/an-object.js");
var $toString = __webpack_require__(/*! ../internals/to-string */ "./node_modules/core-js/internals/to-string.js");
var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");
var getRegExpFlags = __webpack_require__(/*! ../internals/regexp-get-flags */ "./node_modules/core-js/internals/regexp-get-flags.js");

var TO_STRING = 'toString';
var RegExpPrototype = RegExp.prototype;
var nativeToString = RegExpPrototype[TO_STRING];

var NOT_GENERIC = fails(function () { return nativeToString.call({ source: 'a', flags: 'b' }) !== '/a/b'; });
// FF44- RegExp#toString has a wrong name
var INCORRECT_NAME = PROPER_FUNCTION_NAME && nativeToString.name !== TO_STRING;

// `RegExp.prototype.toString` method
// https://tc39.es/ecma262/#sec-regexp.prototype.tostring
if (NOT_GENERIC || INCORRECT_NAME) {
  defineBuiltIn(RegExpPrototype, TO_STRING, function toString() {
    var R = anObject(this);
    var pattern = $toString(R.source);
    var flags = $toString(getRegExpFlags(R));
    return '/' + pattern + '/' + flags;
  }, { unsafe: true });
}


/***/ }),

/***/ "./node_modules/core-js/modules/es.string.includes.js":
/*!************************************************************!*\
  !*** ./node_modules/core-js/modules/es.string.includes.js ***!
  \************************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");
var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this */ "./node_modules/core-js/internals/function-uncurry-this.js");
var notARegExp = __webpack_require__(/*! ../internals/not-a-regexp */ "./node_modules/core-js/internals/not-a-regexp.js");
var requireObjectCoercible = __webpack_require__(/*! ../internals/require-object-coercible */ "./node_modules/core-js/internals/require-object-coercible.js");
var toString = __webpack_require__(/*! ../internals/to-string */ "./node_modules/core-js/internals/to-string.js");
var correctIsRegExpLogic = __webpack_require__(/*! ../internals/correct-is-regexp-logic */ "./node_modules/core-js/internals/correct-is-regexp-logic.js");

var stringIndexOf = uncurryThis(''.indexOf);

// `String.prototype.includes` method
// https://tc39.es/ecma262/#sec-string.prototype.includes
$({ target: 'String', proto: true, forced: !correctIsRegExpLogic('includes') }, {
  includes: function includes(searchString /* , position = 0 */) {
    return !!~stringIndexOf(
      toString(requireObjectCoercible(this)),
      toString(notARegExp(searchString)),
      arguments.length > 1 ? arguments[1] : undefined
    );
  }
});


/***/ }),

/***/ "./node_modules/core-js/modules/es.string.iterator.js":
/*!************************************************************!*\
  !*** ./node_modules/core-js/modules/es.string.iterator.js ***!
  \************************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var charAt = (__webpack_require__(/*! ../internals/string-multibyte */ "./node_modules/core-js/internals/string-multibyte.js").charAt);
var toString = __webpack_require__(/*! ../internals/to-string */ "./node_modules/core-js/internals/to-string.js");
var InternalStateModule = __webpack_require__(/*! ../internals/internal-state */ "./node_modules/core-js/internals/internal-state.js");
var defineIterator = __webpack_require__(/*! ../internals/iterator-define */ "./node_modules/core-js/internals/iterator-define.js");
var createIterResultObject = __webpack_require__(/*! ../internals/create-iter-result-object */ "./node_modules/core-js/internals/create-iter-result-object.js");

var STRING_ITERATOR = 'String Iterator';
var setInternalState = InternalStateModule.set;
var getInternalState = InternalStateModule.getterFor(STRING_ITERATOR);

// `String.prototype[@@iterator]` method
// https://tc39.es/ecma262/#sec-string.prototype-@@iterator
defineIterator(String, 'String', function (iterated) {
  setInternalState(this, {
    type: STRING_ITERATOR,
    string: toString(iterated),
    index: 0
  });
// `%StringIteratorPrototype%.next` method
// https://tc39.es/ecma262/#sec-%stringiteratorprototype%.next
}, function next() {
  var state = getInternalState(this);
  var string = state.string;
  var index = state.index;
  var point;
  if (index >= string.length) return createIterResultObject(undefined, true);
  point = charAt(string, index);
  state.index += point.length;
  return createIterResultObject(point, false);
});


/***/ }),

/***/ "./node_modules/core-js/modules/es.string.replace.js":
/*!***********************************************************!*\
  !*** ./node_modules/core-js/modules/es.string.replace.js ***!
  \***********************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var apply = __webpack_require__(/*! ../internals/function-apply */ "./node_modules/core-js/internals/function-apply.js");
var call = __webpack_require__(/*! ../internals/function-call */ "./node_modules/core-js/internals/function-call.js");
var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this */ "./node_modules/core-js/internals/function-uncurry-this.js");
var fixRegExpWellKnownSymbolLogic = __webpack_require__(/*! ../internals/fix-regexp-well-known-symbol-logic */ "./node_modules/core-js/internals/fix-regexp-well-known-symbol-logic.js");
var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");
var anObject = __webpack_require__(/*! ../internals/an-object */ "./node_modules/core-js/internals/an-object.js");
var isCallable = __webpack_require__(/*! ../internals/is-callable */ "./node_modules/core-js/internals/is-callable.js");
var isObject = __webpack_require__(/*! ../internals/is-object */ "./node_modules/core-js/internals/is-object.js");
var toIntegerOrInfinity = __webpack_require__(/*! ../internals/to-integer-or-infinity */ "./node_modules/core-js/internals/to-integer-or-infinity.js");
var toLength = __webpack_require__(/*! ../internals/to-length */ "./node_modules/core-js/internals/to-length.js");
var toString = __webpack_require__(/*! ../internals/to-string */ "./node_modules/core-js/internals/to-string.js");
var requireObjectCoercible = __webpack_require__(/*! ../internals/require-object-coercible */ "./node_modules/core-js/internals/require-object-coercible.js");
var advanceStringIndex = __webpack_require__(/*! ../internals/advance-string-index */ "./node_modules/core-js/internals/advance-string-index.js");
var getMethod = __webpack_require__(/*! ../internals/get-method */ "./node_modules/core-js/internals/get-method.js");
var getSubstitution = __webpack_require__(/*! ../internals/get-substitution */ "./node_modules/core-js/internals/get-substitution.js");
var getRegExpFlags = __webpack_require__(/*! ../internals/regexp-get-flags */ "./node_modules/core-js/internals/regexp-get-flags.js");
var regExpExec = __webpack_require__(/*! ../internals/regexp-exec-abstract */ "./node_modules/core-js/internals/regexp-exec-abstract.js");
var wellKnownSymbol = __webpack_require__(/*! ../internals/well-known-symbol */ "./node_modules/core-js/internals/well-known-symbol.js");

var REPLACE = wellKnownSymbol('replace');
var max = Math.max;
var min = Math.min;
var concat = uncurryThis([].concat);
var push = uncurryThis([].push);
var stringIndexOf = uncurryThis(''.indexOf);
var stringSlice = uncurryThis(''.slice);

var maybeToString = function (it) {
  return it === undefined ? it : String(it);
};

// IE <= 11 replaces $0 with the whole match, as if it was $&
// https://stackoverflow.com/questions/6024666/getting-ie-to-replace-a-regex-with-the-literal-string-0
var REPLACE_KEEPS_$0 = (function () {
  // eslint-disable-next-line regexp/prefer-escape-replacement-dollar-char -- required for testing
  return 'a'.replace(/./, '$0') === '$0';
})();

// Safari <= 13.0.3(?) substitutes nth capture where n>m with an empty string
var REGEXP_REPLACE_SUBSTITUTES_UNDEFINED_CAPTURE = (function () {
  if (/./[REPLACE]) {
    return /./[REPLACE]('a', '$0') === '';
  }
  return false;
})();

var REPLACE_SUPPORTS_NAMED_GROUPS = !fails(function () {
  var re = /./;
  re.exec = function () {
    var result = [];
    result.groups = { a: '7' };
    return result;
  };
  // eslint-disable-next-line regexp/no-useless-dollar-replacements -- false positive
  return ''.replace(re, '$<a>') !== '7';
});

// @@replace logic
fixRegExpWellKnownSymbolLogic('replace', function (_, nativeReplace, maybeCallNative) {
  var UNSAFE_SUBSTITUTE = REGEXP_REPLACE_SUBSTITUTES_UNDEFINED_CAPTURE ? '$' : '$0';

  return [
    // `String.prototype.replace` method
    // https://tc39.es/ecma262/#sec-string.prototype.replace
    function replace(searchValue, replaceValue) {
      var O = requireObjectCoercible(this);
      var replacer = isObject(searchValue) ? getMethod(searchValue, REPLACE) : undefined;
      return replacer
        ? call(replacer, searchValue, O, replaceValue)
        : call(nativeReplace, toString(O), searchValue, replaceValue);
    },
    // `RegExp.prototype[@@replace]` method
    // https://tc39.es/ecma262/#sec-regexp.prototype-@@replace
    function (string, replaceValue) {
      var rx = anObject(this);
      var S = toString(string);

      if (
        typeof replaceValue == 'string' &&
        stringIndexOf(replaceValue, UNSAFE_SUBSTITUTE) === -1 &&
        stringIndexOf(replaceValue, '$<') === -1
      ) {
        var res = maybeCallNative(nativeReplace, rx, S, replaceValue);
        if (res.done) return res.value;
      }

      var functionalReplace = isCallable(replaceValue);
      if (!functionalReplace) replaceValue = toString(replaceValue);

      var flags = toString(getRegExpFlags(rx));
      var global = stringIndexOf(flags, 'g') !== -1;
      var fullUnicode;
      if (global) {
        fullUnicode = stringIndexOf(flags, 'u') !== -1;
        rx.lastIndex = 0;
      }

      var results = [];
      var result;
      while (true) {
        result = regExpExec(rx, S);
        if (result === null) break;

        push(results, result);
        if (!global) break;

        var matchStr = toString(result[0]);
        if (matchStr === '') rx.lastIndex = advanceStringIndex(S, toLength(rx.lastIndex), fullUnicode);
      }

      var accumulatedResult = '';
      var nextSourcePosition = 0;
      for (var i = 0; i < results.length; i++) {
        result = results[i];

        var matched = toString(result[0]);
        var position = max(min(toIntegerOrInfinity(result.index), S.length), 0);
        var captures = [];
        var replacement;
        // NOTE: This is equivalent to
        //   captures = result.slice(1).map(maybeToString)
        // but for some reason `nativeSlice.call(result, 1, result.length)` (called in
        // the slice polyfill when slicing native arrays) "doesn't work" in safari 9 and
        // causes a crash (https://pastebin.com/N21QzeQA) when trying to debug it.
        for (var j = 1; j < result.length; j++) push(captures, maybeToString(result[j]));
        var namedCaptures = result.groups;
        if (functionalReplace) {
          var replacerArgs = concat([matched], captures, position, S);
          if (namedCaptures !== undefined) push(replacerArgs, namedCaptures);
          replacement = toString(apply(replaceValue, undefined, replacerArgs));
        } else {
          replacement = getSubstitution(matched, S, position, captures, namedCaptures, replaceValue);
        }
        if (position >= nextSourcePosition) {
          accumulatedResult += stringSlice(S, nextSourcePosition, position) + replacement;
          nextSourcePosition = position + matched.length;
        }
      }

      return accumulatedResult + stringSlice(S, nextSourcePosition);
    }
  ];
}, !REPLACE_SUPPORTS_NAMED_GROUPS || !REPLACE_KEEPS_$0 || REGEXP_REPLACE_SUBSTITUTES_UNDEFINED_CAPTURE);


/***/ }),

/***/ "./node_modules/core-js/modules/es.string.starts-with.js":
/*!***************************************************************!*\
  !*** ./node_modules/core-js/modules/es.string.starts-with.js ***!
  \***************************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");
var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this-clause */ "./node_modules/core-js/internals/function-uncurry-this-clause.js");
var getOwnPropertyDescriptor = (__webpack_require__(/*! ../internals/object-get-own-property-descriptor */ "./node_modules/core-js/internals/object-get-own-property-descriptor.js").f);
var toLength = __webpack_require__(/*! ../internals/to-length */ "./node_modules/core-js/internals/to-length.js");
var toString = __webpack_require__(/*! ../internals/to-string */ "./node_modules/core-js/internals/to-string.js");
var notARegExp = __webpack_require__(/*! ../internals/not-a-regexp */ "./node_modules/core-js/internals/not-a-regexp.js");
var requireObjectCoercible = __webpack_require__(/*! ../internals/require-object-coercible */ "./node_modules/core-js/internals/require-object-coercible.js");
var correctIsRegExpLogic = __webpack_require__(/*! ../internals/correct-is-regexp-logic */ "./node_modules/core-js/internals/correct-is-regexp-logic.js");
var IS_PURE = __webpack_require__(/*! ../internals/is-pure */ "./node_modules/core-js/internals/is-pure.js");

var stringSlice = uncurryThis(''.slice);
var min = Math.min;

var CORRECT_IS_REGEXP_LOGIC = correctIsRegExpLogic('startsWith');
// https://github.com/zloirock/core-js/pull/702
var MDN_POLYFILL_BUG = !IS_PURE && !CORRECT_IS_REGEXP_LOGIC && !!function () {
  var descriptor = getOwnPropertyDescriptor(String.prototype, 'startsWith');
  return descriptor && !descriptor.writable;
}();

// `String.prototype.startsWith` method
// https://tc39.es/ecma262/#sec-string.prototype.startswith
$({ target: 'String', proto: true, forced: !MDN_POLYFILL_BUG && !CORRECT_IS_REGEXP_LOGIC }, {
  startsWith: function startsWith(searchString /* , position = 0 */) {
    var that = toString(requireObjectCoercible(this));
    notARegExp(searchString);
    var index = toLength(min(arguments.length > 1 ? arguments[1] : undefined, that.length));
    var search = toString(searchString);
    return stringSlice(that, index, index + search.length) === search;
  }
});


/***/ }),

/***/ "./node_modules/core-js/modules/es.string.trim.js":
/*!********************************************************!*\
  !*** ./node_modules/core-js/modules/es.string.trim.js ***!
  \********************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");
var $trim = (__webpack_require__(/*! ../internals/string-trim */ "./node_modules/core-js/internals/string-trim.js").trim);
var forcedStringTrimMethod = __webpack_require__(/*! ../internals/string-trim-forced */ "./node_modules/core-js/internals/string-trim-forced.js");

// `String.prototype.trim` method
// https://tc39.es/ecma262/#sec-string.prototype.trim
$({ target: 'String', proto: true, forced: forcedStringTrimMethod('trim') }, {
  trim: function trim() {
    return $trim(this);
  }
});


/***/ }),

/***/ "./node_modules/core-js/modules/es.symbol.constructor.js":
/*!***************************************************************!*\
  !*** ./node_modules/core-js/modules/es.symbol.constructor.js ***!
  \***************************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");
var globalThis = __webpack_require__(/*! ../internals/global-this */ "./node_modules/core-js/internals/global-this.js");
var call = __webpack_require__(/*! ../internals/function-call */ "./node_modules/core-js/internals/function-call.js");
var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this */ "./node_modules/core-js/internals/function-uncurry-this.js");
var IS_PURE = __webpack_require__(/*! ../internals/is-pure */ "./node_modules/core-js/internals/is-pure.js");
var DESCRIPTORS = __webpack_require__(/*! ../internals/descriptors */ "./node_modules/core-js/internals/descriptors.js");
var NATIVE_SYMBOL = __webpack_require__(/*! ../internals/symbol-constructor-detection */ "./node_modules/core-js/internals/symbol-constructor-detection.js");
var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");
var hasOwn = __webpack_require__(/*! ../internals/has-own-property */ "./node_modules/core-js/internals/has-own-property.js");
var isPrototypeOf = __webpack_require__(/*! ../internals/object-is-prototype-of */ "./node_modules/core-js/internals/object-is-prototype-of.js");
var anObject = __webpack_require__(/*! ../internals/an-object */ "./node_modules/core-js/internals/an-object.js");
var toIndexedObject = __webpack_require__(/*! ../internals/to-indexed-object */ "./node_modules/core-js/internals/to-indexed-object.js");
var toPropertyKey = __webpack_require__(/*! ../internals/to-property-key */ "./node_modules/core-js/internals/to-property-key.js");
var $toString = __webpack_require__(/*! ../internals/to-string */ "./node_modules/core-js/internals/to-string.js");
var createPropertyDescriptor = __webpack_require__(/*! ../internals/create-property-descriptor */ "./node_modules/core-js/internals/create-property-descriptor.js");
var nativeObjectCreate = __webpack_require__(/*! ../internals/object-create */ "./node_modules/core-js/internals/object-create.js");
var objectKeys = __webpack_require__(/*! ../internals/object-keys */ "./node_modules/core-js/internals/object-keys.js");
var getOwnPropertyNamesModule = __webpack_require__(/*! ../internals/object-get-own-property-names */ "./node_modules/core-js/internals/object-get-own-property-names.js");
var getOwnPropertyNamesExternal = __webpack_require__(/*! ../internals/object-get-own-property-names-external */ "./node_modules/core-js/internals/object-get-own-property-names-external.js");
var getOwnPropertySymbolsModule = __webpack_require__(/*! ../internals/object-get-own-property-symbols */ "./node_modules/core-js/internals/object-get-own-property-symbols.js");
var getOwnPropertyDescriptorModule = __webpack_require__(/*! ../internals/object-get-own-property-descriptor */ "./node_modules/core-js/internals/object-get-own-property-descriptor.js");
var definePropertyModule = __webpack_require__(/*! ../internals/object-define-property */ "./node_modules/core-js/internals/object-define-property.js");
var definePropertiesModule = __webpack_require__(/*! ../internals/object-define-properties */ "./node_modules/core-js/internals/object-define-properties.js");
var propertyIsEnumerableModule = __webpack_require__(/*! ../internals/object-property-is-enumerable */ "./node_modules/core-js/internals/object-property-is-enumerable.js");
var defineBuiltIn = __webpack_require__(/*! ../internals/define-built-in */ "./node_modules/core-js/internals/define-built-in.js");
var defineBuiltInAccessor = __webpack_require__(/*! ../internals/define-built-in-accessor */ "./node_modules/core-js/internals/define-built-in-accessor.js");
var shared = __webpack_require__(/*! ../internals/shared */ "./node_modules/core-js/internals/shared.js");
var sharedKey = __webpack_require__(/*! ../internals/shared-key */ "./node_modules/core-js/internals/shared-key.js");
var hiddenKeys = __webpack_require__(/*! ../internals/hidden-keys */ "./node_modules/core-js/internals/hidden-keys.js");
var uid = __webpack_require__(/*! ../internals/uid */ "./node_modules/core-js/internals/uid.js");
var wellKnownSymbol = __webpack_require__(/*! ../internals/well-known-symbol */ "./node_modules/core-js/internals/well-known-symbol.js");
var wrappedWellKnownSymbolModule = __webpack_require__(/*! ../internals/well-known-symbol-wrapped */ "./node_modules/core-js/internals/well-known-symbol-wrapped.js");
var defineWellKnownSymbol = __webpack_require__(/*! ../internals/well-known-symbol-define */ "./node_modules/core-js/internals/well-known-symbol-define.js");
var defineSymbolToPrimitive = __webpack_require__(/*! ../internals/symbol-define-to-primitive */ "./node_modules/core-js/internals/symbol-define-to-primitive.js");
var setToStringTag = __webpack_require__(/*! ../internals/set-to-string-tag */ "./node_modules/core-js/internals/set-to-string-tag.js");
var InternalStateModule = __webpack_require__(/*! ../internals/internal-state */ "./node_modules/core-js/internals/internal-state.js");
var $forEach = (__webpack_require__(/*! ../internals/array-iteration */ "./node_modules/core-js/internals/array-iteration.js").forEach);

var HIDDEN = sharedKey('hidden');
var SYMBOL = 'Symbol';
var PROTOTYPE = 'prototype';

var setInternalState = InternalStateModule.set;
var getInternalState = InternalStateModule.getterFor(SYMBOL);

var ObjectPrototype = Object[PROTOTYPE];
var $Symbol = globalThis.Symbol;
var SymbolPrototype = $Symbol && $Symbol[PROTOTYPE];
var RangeError = globalThis.RangeError;
var TypeError = globalThis.TypeError;
var QObject = globalThis.QObject;
var nativeGetOwnPropertyDescriptor = getOwnPropertyDescriptorModule.f;
var nativeDefineProperty = definePropertyModule.f;
var nativeGetOwnPropertyNames = getOwnPropertyNamesExternal.f;
var nativePropertyIsEnumerable = propertyIsEnumerableModule.f;
var push = uncurryThis([].push);

var AllSymbols = shared('symbols');
var ObjectPrototypeSymbols = shared('op-symbols');
var WellKnownSymbolsStore = shared('wks');

// Don't use setters in Qt Script, https://github.com/zloirock/core-js/issues/173
var USE_SETTER = !QObject || !QObject[PROTOTYPE] || !QObject[PROTOTYPE].findChild;

// fallback for old Android, https://code.google.com/p/v8/issues/detail?id=687
var fallbackDefineProperty = function (O, P, Attributes) {
  var ObjectPrototypeDescriptor = nativeGetOwnPropertyDescriptor(ObjectPrototype, P);
  if (ObjectPrototypeDescriptor) delete ObjectPrototype[P];
  nativeDefineProperty(O, P, Attributes);
  if (ObjectPrototypeDescriptor && O !== ObjectPrototype) {
    nativeDefineProperty(ObjectPrototype, P, ObjectPrototypeDescriptor);
  }
};

var setSymbolDescriptor = DESCRIPTORS && fails(function () {
  return nativeObjectCreate(nativeDefineProperty({}, 'a', {
    get: function () { return nativeDefineProperty(this, 'a', { value: 7 }).a; }
  })).a !== 7;
}) ? fallbackDefineProperty : nativeDefineProperty;

var wrap = function (tag, description) {
  var symbol = AllSymbols[tag] = nativeObjectCreate(SymbolPrototype);
  setInternalState(symbol, {
    type: SYMBOL,
    tag: tag,
    description: description
  });
  if (!DESCRIPTORS) symbol.description = description;
  return symbol;
};

var $defineProperty = function defineProperty(O, P, Attributes) {
  if (O === ObjectPrototype) $defineProperty(ObjectPrototypeSymbols, P, Attributes);
  anObject(O);
  var key = toPropertyKey(P);
  anObject(Attributes);
  if (hasOwn(AllSymbols, key)) {
    if (!Attributes.enumerable) {
      if (!hasOwn(O, HIDDEN)) nativeDefineProperty(O, HIDDEN, createPropertyDescriptor(1, nativeObjectCreate(null)));
      O[HIDDEN][key] = true;
    } else {
      if (hasOwn(O, HIDDEN) && O[HIDDEN][key]) O[HIDDEN][key] = false;
      Attributes = nativeObjectCreate(Attributes, { enumerable: createPropertyDescriptor(0, false) });
    } return setSymbolDescriptor(O, key, Attributes);
  } return nativeDefineProperty(O, key, Attributes);
};

var $defineProperties = function defineProperties(O, Properties) {
  anObject(O);
  var properties = toIndexedObject(Properties);
  var keys = objectKeys(properties).concat($getOwnPropertySymbols(properties));
  $forEach(keys, function (key) {
    if (!DESCRIPTORS || call($propertyIsEnumerable, properties, key)) $defineProperty(O, key, properties[key]);
  });
  return O;
};

var $create = function create(O, Properties) {
  return Properties === undefined ? nativeObjectCreate(O) : $defineProperties(nativeObjectCreate(O), Properties);
};

var $propertyIsEnumerable = function propertyIsEnumerable(V) {
  var P = toPropertyKey(V);
  var enumerable = call(nativePropertyIsEnumerable, this, P);
  if (this === ObjectPrototype && hasOwn(AllSymbols, P) && !hasOwn(ObjectPrototypeSymbols, P)) return false;
  return enumerable || !hasOwn(this, P) || !hasOwn(AllSymbols, P) || hasOwn(this, HIDDEN) && this[HIDDEN][P]
    ? enumerable : true;
};

var $getOwnPropertyDescriptor = function getOwnPropertyDescriptor(O, P) {
  var it = toIndexedObject(O);
  var key = toPropertyKey(P);
  if (it === ObjectPrototype && hasOwn(AllSymbols, key) && !hasOwn(ObjectPrototypeSymbols, key)) return;
  var descriptor = nativeGetOwnPropertyDescriptor(it, key);
  if (descriptor && hasOwn(AllSymbols, key) && !(hasOwn(it, HIDDEN) && it[HIDDEN][key])) {
    descriptor.enumerable = true;
  }
  return descriptor;
};

var $getOwnPropertyNames = function getOwnPropertyNames(O) {
  var names = nativeGetOwnPropertyNames(toIndexedObject(O));
  var result = [];
  $forEach(names, function (key) {
    if (!hasOwn(AllSymbols, key) && !hasOwn(hiddenKeys, key)) push(result, key);
  });
  return result;
};

var $getOwnPropertySymbols = function (O) {
  var IS_OBJECT_PROTOTYPE = O === ObjectPrototype;
  var names = nativeGetOwnPropertyNames(IS_OBJECT_PROTOTYPE ? ObjectPrototypeSymbols : toIndexedObject(O));
  var result = [];
  $forEach(names, function (key) {
    if (hasOwn(AllSymbols, key) && (!IS_OBJECT_PROTOTYPE || hasOwn(ObjectPrototype, key))) {
      push(result, AllSymbols[key]);
    }
  });
  return result;
};

// `Symbol` constructor
// https://tc39.es/ecma262/#sec-symbol-constructor
if (!NATIVE_SYMBOL) {
  $Symbol = function Symbol() {
    if (isPrototypeOf(SymbolPrototype, this)) throw new TypeError('Symbol is not a constructor');
    var description = !arguments.length || arguments[0] === undefined ? undefined : $toString(arguments[0]);
    var tag = uid(description);
    var setter = function (value) {
      var $this = this === undefined ? globalThis : this;
      if ($this === ObjectPrototype) call(setter, ObjectPrototypeSymbols, value);
      if (hasOwn($this, HIDDEN) && hasOwn($this[HIDDEN], tag)) $this[HIDDEN][tag] = false;
      var descriptor = createPropertyDescriptor(1, value);
      try {
        setSymbolDescriptor($this, tag, descriptor);
      } catch (error) {
        if (!(error instanceof RangeError)) throw error;
        fallbackDefineProperty($this, tag, descriptor);
      }
    };
    if (DESCRIPTORS && USE_SETTER) setSymbolDescriptor(ObjectPrototype, tag, { configurable: true, set: setter });
    return wrap(tag, description);
  };

  SymbolPrototype = $Symbol[PROTOTYPE];

  defineBuiltIn(SymbolPrototype, 'toString', function toString() {
    return getInternalState(this).tag;
  });

  defineBuiltIn($Symbol, 'withoutSetter', function (description) {
    return wrap(uid(description), description);
  });

  propertyIsEnumerableModule.f = $propertyIsEnumerable;
  definePropertyModule.f = $defineProperty;
  definePropertiesModule.f = $defineProperties;
  getOwnPropertyDescriptorModule.f = $getOwnPropertyDescriptor;
  getOwnPropertyNamesModule.f = getOwnPropertyNamesExternal.f = $getOwnPropertyNames;
  getOwnPropertySymbolsModule.f = $getOwnPropertySymbols;

  wrappedWellKnownSymbolModule.f = function (name) {
    return wrap(wellKnownSymbol(name), name);
  };

  if (DESCRIPTORS) {
    // https://tc39.es/ecma262/#sec-symbol.prototype.description
    defineBuiltInAccessor(SymbolPrototype, 'description', {
      configurable: true,
      get: function description() {
        return getInternalState(this).description;
      }
    });
    if (!IS_PURE) {
      defineBuiltIn(ObjectPrototype, 'propertyIsEnumerable', $propertyIsEnumerable, { unsafe: true });
    }
  }
}

$({ global: true, constructor: true, wrap: true, forced: !NATIVE_SYMBOL, sham: !NATIVE_SYMBOL }, {
  Symbol: $Symbol
});

$forEach(objectKeys(WellKnownSymbolsStore), function (name) {
  defineWellKnownSymbol(name);
});

$({ target: SYMBOL, stat: true, forced: !NATIVE_SYMBOL }, {
  useSetter: function () { USE_SETTER = true; },
  useSimple: function () { USE_SETTER = false; }
});

$({ target: 'Object', stat: true, forced: !NATIVE_SYMBOL, sham: !DESCRIPTORS }, {
  // `Object.create` method
  // https://tc39.es/ecma262/#sec-object.create
  create: $create,
  // `Object.defineProperty` method
  // https://tc39.es/ecma262/#sec-object.defineproperty
  defineProperty: $defineProperty,
  // `Object.defineProperties` method
  // https://tc39.es/ecma262/#sec-object.defineproperties
  defineProperties: $defineProperties,
  // `Object.getOwnPropertyDescriptor` method
  // https://tc39.es/ecma262/#sec-object.getownpropertydescriptors
  getOwnPropertyDescriptor: $getOwnPropertyDescriptor
});

$({ target: 'Object', stat: true, forced: !NATIVE_SYMBOL }, {
  // `Object.getOwnPropertyNames` method
  // https://tc39.es/ecma262/#sec-object.getownpropertynames
  getOwnPropertyNames: $getOwnPropertyNames
});

// `Symbol.prototype[@@toPrimitive]` method
// https://tc39.es/ecma262/#sec-symbol.prototype-@@toprimitive
defineSymbolToPrimitive();

// `Symbol.prototype[@@toStringTag]` property
// https://tc39.es/ecma262/#sec-symbol.prototype-@@tostringtag
setToStringTag($Symbol, SYMBOL);

hiddenKeys[HIDDEN] = true;


/***/ }),

/***/ "./node_modules/core-js/modules/es.symbol.description.js":
/*!***************************************************************!*\
  !*** ./node_modules/core-js/modules/es.symbol.description.js ***!
  \***************************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {

// `Symbol.prototype.description` getter
// https://tc39.es/ecma262/#sec-symbol.prototype.description

var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");
var DESCRIPTORS = __webpack_require__(/*! ../internals/descriptors */ "./node_modules/core-js/internals/descriptors.js");
var globalThis = __webpack_require__(/*! ../internals/global-this */ "./node_modules/core-js/internals/global-this.js");
var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this */ "./node_modules/core-js/internals/function-uncurry-this.js");
var hasOwn = __webpack_require__(/*! ../internals/has-own-property */ "./node_modules/core-js/internals/has-own-property.js");
var isCallable = __webpack_require__(/*! ../internals/is-callable */ "./node_modules/core-js/internals/is-callable.js");
var isPrototypeOf = __webpack_require__(/*! ../internals/object-is-prototype-of */ "./node_modules/core-js/internals/object-is-prototype-of.js");
var toString = __webpack_require__(/*! ../internals/to-string */ "./node_modules/core-js/internals/to-string.js");
var defineBuiltInAccessor = __webpack_require__(/*! ../internals/define-built-in-accessor */ "./node_modules/core-js/internals/define-built-in-accessor.js");
var copyConstructorProperties = __webpack_require__(/*! ../internals/copy-constructor-properties */ "./node_modules/core-js/internals/copy-constructor-properties.js");

var NativeSymbol = globalThis.Symbol;
var SymbolPrototype = NativeSymbol && NativeSymbol.prototype;

if (DESCRIPTORS && isCallable(NativeSymbol) && (!('description' in SymbolPrototype) ||
  // Safari 12 bug
  NativeSymbol().description !== undefined
)) {
  var EmptyStringDescriptionStore = {};
  // wrap Symbol constructor for correct work with undefined description
  var SymbolWrapper = function Symbol() {
    var description = arguments.length < 1 || arguments[0] === undefined ? undefined : toString(arguments[0]);
    var result = isPrototypeOf(SymbolPrototype, this)
      // eslint-disable-next-line sonarjs/inconsistent-function-call -- ok
      ? new NativeSymbol(description)
      // in Edge 13, String(Symbol(undefined)) === 'Symbol(undefined)'
      : description === undefined ? NativeSymbol() : NativeSymbol(description);
    if (description === '') EmptyStringDescriptionStore[result] = true;
    return result;
  };

  copyConstructorProperties(SymbolWrapper, NativeSymbol);
  SymbolWrapper.prototype = SymbolPrototype;
  SymbolPrototype.constructor = SymbolWrapper;

  var NATIVE_SYMBOL = String(NativeSymbol('description detection')) === 'Symbol(description detection)';
  var thisSymbolValue = uncurryThis(SymbolPrototype.valueOf);
  var symbolDescriptiveString = uncurryThis(SymbolPrototype.toString);
  var regexp = /^Symbol\((.*)\)[^)]+$/;
  var replace = uncurryThis(''.replace);
  var stringSlice = uncurryThis(''.slice);

  defineBuiltInAccessor(SymbolPrototype, 'description', {
    configurable: true,
    get: function description() {
      var symbol = thisSymbolValue(this);
      if (hasOwn(EmptyStringDescriptionStore, symbol)) return '';
      var string = symbolDescriptiveString(symbol);
      var desc = NATIVE_SYMBOL ? stringSlice(string, 7, -1) : replace(string, regexp, '$1');
      return desc === '' ? undefined : desc;
    }
  });

  $({ global: true, constructor: true, forced: true }, {
    Symbol: SymbolWrapper
  });
}


/***/ }),

/***/ "./node_modules/core-js/modules/es.symbol.for.js":
/*!*******************************************************!*\
  !*** ./node_modules/core-js/modules/es.symbol.for.js ***!
  \*******************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");
var getBuiltIn = __webpack_require__(/*! ../internals/get-built-in */ "./node_modules/core-js/internals/get-built-in.js");
var hasOwn = __webpack_require__(/*! ../internals/has-own-property */ "./node_modules/core-js/internals/has-own-property.js");
var toString = __webpack_require__(/*! ../internals/to-string */ "./node_modules/core-js/internals/to-string.js");
var shared = __webpack_require__(/*! ../internals/shared */ "./node_modules/core-js/internals/shared.js");
var NATIVE_SYMBOL_REGISTRY = __webpack_require__(/*! ../internals/symbol-registry-detection */ "./node_modules/core-js/internals/symbol-registry-detection.js");

var StringToSymbolRegistry = shared('string-to-symbol-registry');
var SymbolToStringRegistry = shared('symbol-to-string-registry');

// `Symbol.for` method
// https://tc39.es/ecma262/#sec-symbol.for
$({ target: 'Symbol', stat: true, forced: !NATIVE_SYMBOL_REGISTRY }, {
  'for': function (key) {
    var string = toString(key);
    if (hasOwn(StringToSymbolRegistry, string)) return StringToSymbolRegistry[string];
    var symbol = getBuiltIn('Symbol')(string);
    StringToSymbolRegistry[string] = symbol;
    SymbolToStringRegistry[symbol] = string;
    return symbol;
  }
});


/***/ }),

/***/ "./node_modules/core-js/modules/es.symbol.iterator.js":
/*!************************************************************!*\
  !*** ./node_modules/core-js/modules/es.symbol.iterator.js ***!
  \************************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var defineWellKnownSymbol = __webpack_require__(/*! ../internals/well-known-symbol-define */ "./node_modules/core-js/internals/well-known-symbol-define.js");

// `Symbol.iterator` well-known symbol
// https://tc39.es/ecma262/#sec-symbol.iterator
defineWellKnownSymbol('iterator');


/***/ }),

/***/ "./node_modules/core-js/modules/es.symbol.js":
/*!***************************************************!*\
  !*** ./node_modules/core-js/modules/es.symbol.js ***!
  \***************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


// TODO: Remove this module from `core-js@4` since it's split to modules listed below
__webpack_require__(/*! ../modules/es.symbol.constructor */ "./node_modules/core-js/modules/es.symbol.constructor.js");
__webpack_require__(/*! ../modules/es.symbol.for */ "./node_modules/core-js/modules/es.symbol.for.js");
__webpack_require__(/*! ../modules/es.symbol.key-for */ "./node_modules/core-js/modules/es.symbol.key-for.js");
__webpack_require__(/*! ../modules/es.json.stringify */ "./node_modules/core-js/modules/es.json.stringify.js");
__webpack_require__(/*! ../modules/es.object.get-own-property-symbols */ "./node_modules/core-js/modules/es.object.get-own-property-symbols.js");


/***/ }),

/***/ "./node_modules/core-js/modules/es.symbol.key-for.js":
/*!***********************************************************!*\
  !*** ./node_modules/core-js/modules/es.symbol.key-for.js ***!
  \***********************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var $ = __webpack_require__(/*! ../internals/export */ "./node_modules/core-js/internals/export.js");
var hasOwn = __webpack_require__(/*! ../internals/has-own-property */ "./node_modules/core-js/internals/has-own-property.js");
var isSymbol = __webpack_require__(/*! ../internals/is-symbol */ "./node_modules/core-js/internals/is-symbol.js");
var tryToString = __webpack_require__(/*! ../internals/try-to-string */ "./node_modules/core-js/internals/try-to-string.js");
var shared = __webpack_require__(/*! ../internals/shared */ "./node_modules/core-js/internals/shared.js");
var NATIVE_SYMBOL_REGISTRY = __webpack_require__(/*! ../internals/symbol-registry-detection */ "./node_modules/core-js/internals/symbol-registry-detection.js");

var SymbolToStringRegistry = shared('symbol-to-string-registry');

// `Symbol.keyFor` method
// https://tc39.es/ecma262/#sec-symbol.keyfor
$({ target: 'Symbol', stat: true, forced: !NATIVE_SYMBOL_REGISTRY }, {
  keyFor: function keyFor(sym) {
    if (!isSymbol(sym)) throw new TypeError(tryToString(sym) + ' is not a symbol');
    if (hasOwn(SymbolToStringRegistry, sym)) return SymbolToStringRegistry[sym];
  }
});


/***/ }),

/***/ "./node_modules/core-js/modules/es.symbol.to-primitive.js":
/*!****************************************************************!*\
  !*** ./node_modules/core-js/modules/es.symbol.to-primitive.js ***!
  \****************************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var defineWellKnownSymbol = __webpack_require__(/*! ../internals/well-known-symbol-define */ "./node_modules/core-js/internals/well-known-symbol-define.js");
var defineSymbolToPrimitive = __webpack_require__(/*! ../internals/symbol-define-to-primitive */ "./node_modules/core-js/internals/symbol-define-to-primitive.js");

// `Symbol.toPrimitive` well-known symbol
// https://tc39.es/ecma262/#sec-symbol.toprimitive
defineWellKnownSymbol('toPrimitive');

// `Symbol.prototype[@@toPrimitive]` method
// https://tc39.es/ecma262/#sec-symbol.prototype-@@toprimitive
defineSymbolToPrimitive();


/***/ }),

/***/ "./node_modules/core-js/modules/es.weak-map.constructor.js":
/*!*****************************************************************!*\
  !*** ./node_modules/core-js/modules/es.weak-map.constructor.js ***!
  \*****************************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var FREEZING = __webpack_require__(/*! ../internals/freezing */ "./node_modules/core-js/internals/freezing.js");
var globalThis = __webpack_require__(/*! ../internals/global-this */ "./node_modules/core-js/internals/global-this.js");
var uncurryThis = __webpack_require__(/*! ../internals/function-uncurry-this */ "./node_modules/core-js/internals/function-uncurry-this.js");
var defineBuiltIns = __webpack_require__(/*! ../internals/define-built-ins */ "./node_modules/core-js/internals/define-built-ins.js");
var InternalMetadataModule = __webpack_require__(/*! ../internals/internal-metadata */ "./node_modules/core-js/internals/internal-metadata.js");
var collection = __webpack_require__(/*! ../internals/collection */ "./node_modules/core-js/internals/collection.js");
var collectionWeak = __webpack_require__(/*! ../internals/collection-weak */ "./node_modules/core-js/internals/collection-weak.js");
var isObject = __webpack_require__(/*! ../internals/is-object */ "./node_modules/core-js/internals/is-object.js");
var enforceInternalState = (__webpack_require__(/*! ../internals/internal-state */ "./node_modules/core-js/internals/internal-state.js").enforce);
var fails = __webpack_require__(/*! ../internals/fails */ "./node_modules/core-js/internals/fails.js");
var NATIVE_WEAK_MAP = __webpack_require__(/*! ../internals/weak-map-basic-detection */ "./node_modules/core-js/internals/weak-map-basic-detection.js");

var $Object = Object;
// eslint-disable-next-line es/no-array-isarray -- safe
var isArray = Array.isArray;
// eslint-disable-next-line es/no-object-isextensible -- safe
var isExtensible = $Object.isExtensible;
// eslint-disable-next-line es/no-object-isfrozen -- safe
var isFrozen = $Object.isFrozen;
// eslint-disable-next-line es/no-object-issealed -- safe
var isSealed = $Object.isSealed;
// eslint-disable-next-line es/no-object-freeze -- safe
var freeze = $Object.freeze;
// eslint-disable-next-line es/no-object-seal -- safe
var seal = $Object.seal;

var IS_IE11 = !globalThis.ActiveXObject && 'ActiveXObject' in globalThis;
var InternalWeakMap;

var wrapper = function (init) {
  return function WeakMap() {
    return init(this, arguments.length ? arguments[0] : undefined);
  };
};

// `WeakMap` constructor
// https://tc39.es/ecma262/#sec-weakmap-constructor
var $WeakMap = collection('WeakMap', wrapper, collectionWeak);
var WeakMapPrototype = $WeakMap.prototype;
var nativeSet = uncurryThis(WeakMapPrototype.set);

// Chakra Edge bug: adding frozen arrays to WeakMap unfreeze them
var hasMSEdgeFreezingBug = function () {
  return FREEZING && fails(function () {
    var frozenArray = freeze([]);
    nativeSet(new $WeakMap(), frozenArray, 1);
    return !isFrozen(frozenArray);
  });
};

// IE11 WeakMap frozen keys fix
// We can't use feature detection because it crash some old IE builds
// https://github.com/zloirock/core-js/issues/485
if (NATIVE_WEAK_MAP) if (IS_IE11) {
  InternalWeakMap = collectionWeak.getConstructor(wrapper, 'WeakMap', true);
  InternalMetadataModule.enable();
  var nativeDelete = uncurryThis(WeakMapPrototype['delete']);
  var nativeHas = uncurryThis(WeakMapPrototype.has);
  var nativeGet = uncurryThis(WeakMapPrototype.get);
  defineBuiltIns(WeakMapPrototype, {
    'delete': function (key) {
      if (isObject(key) && !isExtensible(key)) {
        var state = enforceInternalState(this);
        if (!state.frozen) state.frozen = new InternalWeakMap();
        return nativeDelete(this, key) || state.frozen['delete'](key);
      } return nativeDelete(this, key);
    },
    has: function has(key) {
      if (isObject(key) && !isExtensible(key)) {
        var state = enforceInternalState(this);
        if (!state.frozen) state.frozen = new InternalWeakMap();
        return nativeHas(this, key) || state.frozen.has(key);
      } return nativeHas(this, key);
    },
    get: function get(key) {
      if (isObject(key) && !isExtensible(key)) {
        var state = enforceInternalState(this);
        if (!state.frozen) state.frozen = new InternalWeakMap();
        return nativeHas(this, key) ? nativeGet(this, key) : state.frozen.get(key);
      } return nativeGet(this, key);
    },
    set: function set(key, value) {
      if (isObject(key) && !isExtensible(key)) {
        var state = enforceInternalState(this);
        if (!state.frozen) state.frozen = new InternalWeakMap();
        nativeHas(this, key) ? nativeSet(this, key, value) : state.frozen.set(key, value);
      } else nativeSet(this, key, value);
      return this;
    }
  });
// Chakra Edge frozen keys fix
} else if (hasMSEdgeFreezingBug()) {
  defineBuiltIns(WeakMapPrototype, {
    set: function set(key, value) {
      var arrayIntegrityLevel;
      if (isArray(key)) {
        if (isFrozen(key)) arrayIntegrityLevel = freeze;
        else if (isSealed(key)) arrayIntegrityLevel = seal;
      }
      nativeSet(this, key, value);
      if (arrayIntegrityLevel) arrayIntegrityLevel(key);
      return this;
    }
  });
}


/***/ }),

/***/ "./node_modules/core-js/modules/es.weak-map.js":
/*!*****************************************************!*\
  !*** ./node_modules/core-js/modules/es.weak-map.js ***!
  \*****************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


// TODO: Remove this module from `core-js@4` since it's replaced to module below
__webpack_require__(/*! ../modules/es.weak-map.constructor */ "./node_modules/core-js/modules/es.weak-map.constructor.js");


/***/ }),

/***/ "./node_modules/core-js/modules/web.dom-collections.for-each.js":
/*!**********************************************************************!*\
  !*** ./node_modules/core-js/modules/web.dom-collections.for-each.js ***!
  \**********************************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var globalThis = __webpack_require__(/*! ../internals/global-this */ "./node_modules/core-js/internals/global-this.js");
var DOMIterables = __webpack_require__(/*! ../internals/dom-iterables */ "./node_modules/core-js/internals/dom-iterables.js");
var DOMTokenListPrototype = __webpack_require__(/*! ../internals/dom-token-list-prototype */ "./node_modules/core-js/internals/dom-token-list-prototype.js");
var forEach = __webpack_require__(/*! ../internals/array-for-each */ "./node_modules/core-js/internals/array-for-each.js");
var createNonEnumerableProperty = __webpack_require__(/*! ../internals/create-non-enumerable-property */ "./node_modules/core-js/internals/create-non-enumerable-property.js");

var handlePrototype = function (CollectionPrototype) {
  // some Chrome versions have non-configurable methods on DOMTokenList
  if (CollectionPrototype && CollectionPrototype.forEach !== forEach) try {
    createNonEnumerableProperty(CollectionPrototype, 'forEach', forEach);
  } catch (error) {
    CollectionPrototype.forEach = forEach;
  }
};

for (var COLLECTION_NAME in DOMIterables) {
  if (DOMIterables[COLLECTION_NAME]) {
    handlePrototype(globalThis[COLLECTION_NAME] && globalThis[COLLECTION_NAME].prototype);
  }
}

handlePrototype(DOMTokenListPrototype);


/***/ }),

/***/ "./node_modules/core-js/modules/web.dom-collections.iterator.js":
/*!**********************************************************************!*\
  !*** ./node_modules/core-js/modules/web.dom-collections.iterator.js ***!
  \**********************************************************************/
/***/ ((__unused_webpack_module, __unused_webpack_exports, __webpack_require__) => {


var globalThis = __webpack_require__(/*! ../internals/global-this */ "./node_modules/core-js/internals/global-this.js");
var DOMIterables = __webpack_require__(/*! ../internals/dom-iterables */ "./node_modules/core-js/internals/dom-iterables.js");
var DOMTokenListPrototype = __webpack_require__(/*! ../internals/dom-token-list-prototype */ "./node_modules/core-js/internals/dom-token-list-prototype.js");
var ArrayIteratorMethods = __webpack_require__(/*! ../modules/es.array.iterator */ "./node_modules/core-js/modules/es.array.iterator.js");
var createNonEnumerableProperty = __webpack_require__(/*! ../internals/create-non-enumerable-property */ "./node_modules/core-js/internals/create-non-enumerable-property.js");
var setToStringTag = __webpack_require__(/*! ../internals/set-to-string-tag */ "./node_modules/core-js/internals/set-to-string-tag.js");
var wellKnownSymbol = __webpack_require__(/*! ../internals/well-known-symbol */ "./node_modules/core-js/internals/well-known-symbol.js");

var ITERATOR = wellKnownSymbol('iterator');
var ArrayValues = ArrayIteratorMethods.values;

var handlePrototype = function (CollectionPrototype, COLLECTION_NAME) {
  if (CollectionPrototype) {
    // some Chrome versions have non-configurable methods on DOMTokenList
    if (CollectionPrototype[ITERATOR] !== ArrayValues) try {
      createNonEnumerableProperty(CollectionPrototype, ITERATOR, ArrayValues);
    } catch (error) {
      CollectionPrototype[ITERATOR] = ArrayValues;
    }
    setToStringTag(CollectionPrototype, COLLECTION_NAME, true);
    if (DOMIterables[COLLECTION_NAME]) for (var METHOD_NAME in ArrayIteratorMethods) {
      // some Chrome versions have non-configurable methods on DOMTokenList
      if (CollectionPrototype[METHOD_NAME] !== ArrayIteratorMethods[METHOD_NAME]) try {
        createNonEnumerableProperty(CollectionPrototype, METHOD_NAME, ArrayIteratorMethods[METHOD_NAME]);
      } catch (error) {
        CollectionPrototype[METHOD_NAME] = ArrayIteratorMethods[METHOD_NAME];
      }
    }
  }
};

for (var COLLECTION_NAME in DOMIterables) {
  handlePrototype(globalThis[COLLECTION_NAME] && globalThis[COLLECTION_NAME].prototype, COLLECTION_NAME);
}

handlePrototype(DOMTokenListPrototype, 'DOMTokenList');


/***/ }),

/***/ "jquery":
/*!*************************!*\
  !*** external "jQuery" ***!
  \*************************/
/***/ ((module) => {

module.exports = jQuery;

/***/ })

/******/ 	});
/************************************************************************/
/******/ 	// The module cache
/******/ 	var __webpack_module_cache__ = {};
/******/ 	
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/ 		// Check if module is in cache
/******/ 		var cachedModule = __webpack_module_cache__[moduleId];
/******/ 		if (cachedModule !== undefined) {
/******/ 			return cachedModule.exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = __webpack_module_cache__[moduleId] = {
/******/ 			// no module.id needed
/******/ 			// no module.loaded needed
/******/ 			exports: {}
/******/ 		};
/******/ 	
/******/ 		// Execute the module function
/******/ 		__webpack_modules__[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/ 	
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/ 	
/************************************************************************/
/******/ 	/* webpack/runtime/compat get default export */
/******/ 	(() => {
/******/ 		// getDefaultExport function for compatibility with non-harmony modules
/******/ 		__webpack_require__.n = (module) => {
/******/ 			var getter = module && module.__esModule ?
/******/ 				() => (module['default']) :
/******/ 				() => (module);
/******/ 			__webpack_require__.d(getter, { a: getter });
/******/ 			return getter;
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/define property getters */
/******/ 	(() => {
/******/ 		// define getter functions for harmony exports
/******/ 		__webpack_require__.d = (exports, definition) => {
/******/ 			for(var key in definition) {
/******/ 				if(__webpack_require__.o(definition, key) && !__webpack_require__.o(exports, key)) {
/******/ 					Object.defineProperty(exports, key, { enumerable: true, get: definition[key] });
/******/ 				}
/******/ 			}
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/global */
/******/ 	(() => {
/******/ 		__webpack_require__.g = (function() {
/******/ 			if (typeof globalThis === 'object') return globalThis;
/******/ 			try {
/******/ 				return this || new Function('return this')();
/******/ 			} catch (e) {
/******/ 				if (typeof window === 'object') return window;
/******/ 			}
/******/ 		})();
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/hasOwnProperty shorthand */
/******/ 	(() => {
/******/ 		__webpack_require__.o = (obj, prop) => (Object.prototype.hasOwnProperty.call(obj, prop))
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/make namespace object */
/******/ 	(() => {
/******/ 		// define __esModule on exports
/******/ 		__webpack_require__.r = (exports) => {
/******/ 			if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 				Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 			}
/******/ 			Object.defineProperty(exports, '__esModule', { value: true });
/******/ 		};
/******/ 	})();
/******/ 	
/************************************************************************/
var __webpack_exports__ = {};
// This entry needs to be wrapped in an IIFE because it needs to be isolated against other modules in the chunk.
(() => {
/*!********************************!*\
  !*** ./assets/configurator.js ***!
  \********************************/
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! core-js/modules/es.object.to-string.js */ "./node_modules/core-js/modules/es.object.to-string.js");
/* harmony import */ var core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_es_object_to_string_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! core-js/modules/web.dom-collections.for-each.js */ "./node_modules/core-js/modules/web.dom-collections.for-each.js");
/* harmony import */ var core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(core_js_modules_web_dom_collections_for_each_js__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var _scss_configurator_scss__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./scss/configurator.scss */ "./assets/scss/configurator.scss");
/* harmony import */ var _js_configurator_configcode_save__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./js/configurator/configcode/save */ "./assets/js/configurator/configcode/save.js");
/* harmony import */ var _js_configurator_pricecalcs_pricelabels__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./js/configurator/pricecalcs/pricelabels */ "./assets/js/configurator/pricecalcs/pricelabels.js");
/* harmony import */ var _js_configurator_pricecalcs_pricematrices__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./js/configurator/pricecalcs/pricematrices */ "./assets/js/configurator/pricecalcs/pricematrices.js");
/* harmony import */ var _js_configurator_pricecalcs_sk1__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./js/configurator/pricecalcs/sk1 */ "./assets/js/configurator/pricecalcs/sk1.js");
/* harmony import */ var _js_configurator_pricecalcs_sk2__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./js/configurator/pricecalcs/sk2 */ "./assets/js/configurator/pricecalcs/sk2.js");
/* harmony import */ var _js_configurator_carousel__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ./js/configurator/carousel */ "./assets/js/configurator/carousel.js");
/* harmony import */ var _js_configurator_input__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ./js/configurator/input */ "./assets/js/configurator/input.js");
/* harmony import */ var _js_configurator_offdrops__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ./js/configurator/offdrops */ "./assets/js/configurator/offdrops.js");
/* harmony import */ var _js_configurator_summary__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ./js/configurator/summary */ "./assets/js/configurator/summary.js");
/* harmony import */ var _js_configurator_dependencies_ablage__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! ./js/configurator/dependencies/ablage */ "./assets/js/configurator/dependencies/ablage.js");
/* harmony import */ var _js_configurator_dependencies_ablagen__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! ./js/configurator/dependencies/ablagen */ "./assets/js/configurator/dependencies/ablagen.js");
/* harmony import */ var _js_configurator_dependencies_auflage__WEBPACK_IMPORTED_MODULE_14__ = __webpack_require__(/*! ./js/configurator/dependencies/auflage */ "./assets/js/configurator/dependencies/auflage.js");
/* harmony import */ var _js_configurator_dependencies_ambientelicht__WEBPACK_IMPORTED_MODULE_15__ = __webpack_require__(/*! ./js/configurator/dependencies/ambientelicht */ "./assets/js/configurator/dependencies/ambientelicht.js");
/* harmony import */ var _js_configurator_dependencies_ausschnitte__WEBPACK_IMPORTED_MODULE_16__ = __webpack_require__(/*! ./js/configurator/dependencies/ausschnitte */ "./assets/js/configurator/dependencies/ausschnitte.js");
/* harmony import */ var _js_configurator_dependencies_bedienung__WEBPACK_IMPORTED_MODULE_17__ = __webpack_require__(/*! ./js/configurator/dependencies/bedienung */ "./assets/js/configurator/dependencies/bedienung.js");
/* harmony import */ var _js_configurator_dependencies_digital_uhr__WEBPACK_IMPORTED_MODULE_18__ = __webpack_require__(/*! ./js/configurator/dependencies/digital-uhr */ "./assets/js/configurator/dependencies/digital-uhr.js");
/* harmony import */ var _js_configurator_dependencies_lichtfarbe__WEBPACK_IMPORTED_MODULE_19__ = __webpack_require__(/*! ./js/configurator/dependencies/lichtfarbe */ "./assets/js/configurator/dependencies/lichtfarbe.js");
/* harmony import */ var _js_configurator_dependencies_philips_hue__WEBPACK_IMPORTED_MODULE_20__ = __webpack_require__(/*! ./js/configurator/dependencies/philips-hue */ "./assets/js/configurator/dependencies/philips-hue.js");
/* harmony import */ var _js_configurator_dependencies_tv_geraet__WEBPACK_IMPORTED_MODULE_21__ = __webpack_require__(/*! ./js/configurator/dependencies/tv-geraet */ "./assets/js/configurator/dependencies/tv-geraet.js");
/* harmony import */ var _js_configurator_dependencies_tv_seitenanschluss__WEBPACK_IMPORTED_MODULE_22__ = __webpack_require__(/*! ./js/configurator/dependencies/tv-seitenanschluss */ "./assets/js/configurator/dependencies/tv-seitenanschluss.js");
/* harmony import */ var _js_configurator_dependencies_anschluesse__WEBPACK_IMPORTED_MODULE_23__ = __webpack_require__(/*! ./js/configurator/dependencies/anschluesse */ "./assets/js/configurator/dependencies/anschluesse.js");
/* harmony import */ var _js_configurator_dependencies_verblendung__WEBPACK_IMPORTED_MODULE_24__ = __webpack_require__(/*! ./js/configurator/dependencies/verblendung */ "./assets/js/configurator/dependencies/verblendung.js");
/* harmony import */ var _js_configurator_dependencies_schrankart__WEBPACK_IMPORTED_MODULE_25__ = __webpack_require__(/*! ./js/configurator/dependencies/schrankart */ "./assets/js/configurator/dependencies/schrankart.js");
/* harmony import */ var _js_configurator_dependencies_schminkspiegel__WEBPACK_IMPORTED_MODULE_26__ = __webpack_require__(/*! ./js/configurator/dependencies/schminkspiegel */ "./assets/js/configurator/dependencies/schminkspiegel.js");
/* harmony import */ var _js_configurator_dependencies_spiegelheizung__WEBPACK_IMPORTED_MODULE_27__ = __webpack_require__(/*! ./js/configurator/dependencies/spiegelheizung */ "./assets/js/configurator/dependencies/spiegelheizung.js");
/* harmony import */ var _js_configurator_dependencies_steckdose__WEBPACK_IMPORTED_MODULE_28__ = __webpack_require__(/*! ./js/configurator/dependencies/steckdose */ "./assets/js/configurator/dependencies/steckdose.js");
/* harmony import */ var _js_configurator_dependencies_tiefe__WEBPACK_IMPORTED_MODULE_29__ = __webpack_require__(/*! ./js/configurator/dependencies/tiefe */ "./assets/js/configurator/dependencies/tiefe.js");
/* harmony import */ var _js_configurator_dependencies_tueren__WEBPACK_IMPORTED_MODULE_30__ = __webpack_require__(/*! ./js/configurator/dependencies/tueren */ "./assets/js/configurator/dependencies/tueren.js");
/* harmony import */ var _js_configurator_dependencies_tuergriff__WEBPACK_IMPORTED_MODULE_31__ = __webpack_require__(/*! ./js/configurator/dependencies/tuergriff */ "./assets/js/configurator/dependencies/tuergriff.js");
/* harmony import */ var _js_configurator_events__WEBPACK_IMPORTED_MODULE_32__ = __webpack_require__(/*! ./js/configurator/events */ "./assets/js/configurator/events.js");


// configurator.js // noch nicht ready for update 06/2025

// Import Scss


// Import Js

// import "./js/configurator/configcode/load"; // Included in global.js










// Dependencies
// import "./js/configurator/dependencies";






 // needs to be tested
// import "./js/configurator/dependencies/facette";







 // needs to be tested
 // needs to be tested
 // needs to be tested


 // needs to be tested

// Events

document.addEventListener("DOMContentLoaded", function () {
  // Alle Radio-Inputs in dieser Option-Gruppe holen
  var radios = document.querySelectorAll('#option_montageset input[type="radio"]');
  function updateSelection() {
    radios.forEach(function (radio) {
      var label = document.querySelector("label[for=\"".concat(radio.id, "\"]"));
      if (!label) return;
      if (radio.checked) {
        label.classList.add("yes-selection");
        label.classList.remove("no-selection");
      } else {
        label.classList.add("no-selection");
        label.classList.remove("yes-selection");
      }
    });
  }

  // Auf Änderungen reagieren
  radios.forEach(function (radio) {
    radio.addEventListener("change", updateSelection);
  });

  // Initiale Markierung beim Laden
  updateSelection();
});
})();

/******/ })()
;
//# sourceMappingURL=configurator.js.map